# Rust RP1 验证中的空星球重复工作｜2026-09-09

Role: develop。承接读取缓冲及大工厂完整 UI 收益；本次修改共享 JS 精确模拟中的无用工作，保持时间断言、生产/存档规则及 Rust 准入不变。完整验证仍在进行，未发布。

## 已确认的失败与采样

第三轮云端 run `34280286855` / job `102243130872` 已结束，整组失败。正常 release Rust 1,363 通过 / 5 ignored / 0 失败，Native 全量、Host 构建、类型检查及带范围标注的候选上传成功；完整 Vitest 3,157 通过 / 2 失败 / 39 跳过。失败仍是有限矿脉宏结算 2,322.601 ms 超过原 2,000 ms 断言，以及递归量子建设对照超过默认 5 秒。完整日志保存在原工作区 `artifacts/rust-rp1-next/cloud-ci-6bf9f983-job.log`。此前失败保留，不能把大工厂 UI 通过称为全量检查全绿。

前两轮 Node CPU flags 没有取得执行游戏测试的工作进程采样：第一次无 profile，第二次仅调度进程；独立用例 2/31 跳过和两个完整文件 33/33 的本机通过不覆盖云端原失败。

第三轮改为测试 setup 中连接当前工作进程的 Node Inspector，在原测试开始前启用 CPU 采样、结束后主动保存；测试源文件、断言和超时均不变。实际取得 `game-timing-profile-v3/idleResourceSettlement.test.ts.cpuprofile` 和 `p2Batching.test.ts.cpuprofile` 及各自进程身份。前者约 3.866 秒采样中，有限矿脉案例约 2.602 秒，宏分支约 1.258 秒主要调用完整 Exact，热点包括电力计算、行星阶段和机器遍历；后者约 6.946 秒，建设规划、库存复制和结算占主要采样。采样时间仅用来定位函数，不算性能门槛验证。

## 两处有证据的空转修复

1. `runMachines` 把“已有完整索引，但该星球没有机器”误作“没有索引”，回退到 `state.entities.flatMap`，每个空星球每秒重新扫描所有星球。现在完整索引的缺席键代表空列表；空列表直接返回，无需创建科技集合和计算配方速度。没有索引时仍保留完整构建路径。
2. `runConstructionCenters` 在没有制造中心时仍枚举全部建设目标、检查科技与库存，并创建预算/缓存。现在确认该星球的中心列表为空后直接返回；有中心但关闭制造、缺料、缺电等情形仍经过原流程。

两处都只跳过没有可执行对象的工作，不对有机器/中心的生产、库存、进度或预算规则做修改，也不跳过任何模拟秒。

新增 `emptyPlanetSimulationWork.test.ts` 的两项回归在改动前实际失败：一个捕获到全实体 flatMap 一次，另一个捕获到无中心情况下读取三种建设目标。改动后这两项及原 `idleResourceSettlement.test.ts`、`p2Batching.test.ts` 共 **35 通过 / 0 失败 / 0 跳过**，并核对空机器场景实体/产量及无中心场景的完整施工状态未改变。证据 `empty-planet-before-v1.json`、`empty-planet-after-v1.json` 和 `empty-planet-after-v1-guard/`。

实际项目类型检查 `tsc -b --pretty false` 通过，约 43.41 秒，日志 `empty-planet-typecheck-v2/`。前一次误用根项目 `tsc --noEmit`，该配置只有 references、files 为空，不能计为覆盖代码的类型检查，故补跑实际命令。

轻量 Windows 诊断工作流增加引擎及这三个测试文件的触发条件，同一次运行保留原计时文件、增加空星球回归并记录四个源码摘要。原 2 秒性能断言、默认 5 秒期限、单 worker 均保持不变。

## 后续完整本机和轻量云端结果

源码提交 `2501f0d4c679f8df95ed356b25b1543bd9e796be` 的完整本机单元已结束：**3,160 通过 / 39 跳过 / 1 失败**。仅 `p2Batching.test.ts` 的 `keeps direct-quantum working capital exact across a recursive cycle` 失败，整个案例约 5,110.045 ms，超过原默认 5 秒。有限矿脉案例约 3,148.010 ms，案例内部只计宏计算的原 2 秒断言通过；整个案例含精确参考计算，不能把两种耗时混用。证据为开发工作树 `artifacts/rust-rp1-loop/empty-planet-full-unit-v1.json` 及 `empty-planet-full-unit-v1-guard/`。外部监控以 6 GiB 启动余量、2 GiB 停止线和单 worker 低优先级运行，399.070 秒退出码 1，最低可用内存 3,935,172 KiB，没有触发内存或期限终止，失败不得算作正常通过。

[轻量 Windows 云端 run 34285116573](https://github.com/snowsnow0926/DSPONLINE/actions/runs/34285116573) / job `102258780428` 同源码结果 **34 通过 / 0 跳过 / 1 失败**。仍仅递归量子建设案例失败，约 5,232.447 ms；有限矿脉原计时断言及两项新增空星球回归通过。制品 `10079168351` 的 ZIP 已下载并验证 SHA-256 `ae240bcd00788be76b8d5076dbfae3c8459aa5181a4cbce5728760fd0d366f4b`，本地原工作区 `artifacts/rust-rp1-next/cloud-game-timing-2501f0d4.zip`；只读取 `source.json`、`results.json`、`run.log`。两份原计时测试文件摘要均未改变，未提高超时或性能断言。

同源码完整 Windows 云端 run `34285116554` / job `102258780214` 已结束，结论为失败：格式、严格 release Clippy、Rust **1,363 通过 / 5 ignored / 0 失败**、Native **674 通过 / 1 跳过 / 0 失败**及类型检查通过；完整 Vitest **3,160 通过 / 39 跳过 / 1 失败**，仍仅递归量子案例超过 5 秒，日志显示约 5,050 ms。原 2 秒宏断言已通过。没有取消或重新启动该作业；其候选 Host 上传仅表示 Rust/Native 部分通过，不是完整资格。完整诊断 ZIP 制品 `10080215308` 已下载并校验 SHA-256 `df6e3abfc5f6cfa5ea6829430d9c42f1be76e8a1114d41fc9d18f2cbaa6783a5`，原工作区 `artifacts/rust-rp1-next/cloud-ci-2501f0d4-diagnostics.zip`；另保留终态摘要 `cloud-ci-2501f0d4-summary.log`。缓存命中后 cargo 仍花 19 分 7 秒重新编译测试，因此该等待主要是构建，并非已确认测试挂死。

接下来分解递归建设用例的旧逐项和批处理时间，定位规划/库存复制成本；生产构建和适用浏览器检查仍待完成。尚未测得该 JS 改动的独立新旧性能比率，不能把旧缓冲包 72.13% UI 收益重复算为这项改动的收益。

旧冻结 `67a2ffbe` 包的 UI 证据仍只属于该包。本次 JS 源码改变后不能直接继承为新包验收。原始终局 timeWarp 准入、历史堆异常及长离线/实时/跨端 Rust 边界保持此前状态。

## 材料可用性检查的后续开发

新增 `constructionMaterialAvailability.ts`，将只需要布尔结果的检查从实际扣料计划分开：只给配方涉及的物料建立临时余额，保留在制库存、行星托盘、量子直供的顺序和各自取整；重复物料条目共享检查内的剩余额度，不能重复使用同一份库存。实际预留、扣除、产出、施工计划与持久状态格式不变。旧检查每次复制三份完整库存；新增“不枚举或读取无关库存”反例对旧算法失败，修改后四项边界检查通过。

`construction-availability-before-v1.json` 为旧复制算法 3 通过 / 1 失败；`construction-availability-after-v1.json` 为 4/4；包括两个原计时文件和空星球回归的 `construction-availability-focused-v1.json` 为 **39 通过 / 0 跳过 / 0 失败**，递归量子案例约 4,889.013 ms。完整项目 `tsc -b --pretty false` 通过，守护日志 `construction-availability-typecheck-v1-guard/`；轻量类型检查使用 3 GiB 启动线，完整模拟/大档诊断仍使用原 6 GiB 启动线，所有任务串行、低优先级、外部监控 2 GiB 停止线。

为分离实际耗时，已将原测试的小型公开夹具冻结为独立诊断入口，源码两份原测试摘要不变。`quantum-construction-before-v2/` 的完整基线 bundle SHA-256 为 `3f14348f04dd0ad8629e29b3ae147f28f2517f2595f8260a5984d4ac487f912c`，只依赖 Node 内置模块。逐项参考路径约 3,408 ms、4,000 次计划构建，而批处理约 5.6 ms；采样显示重复稳定循环探测约占参考路径 1.84 秒。这说明当前计时问题主要在参考路径，不能把该案例超时直接解释为玩家批处理也要等 5 秒。

第一次新算法采样 `quantum-construction-after-v1/` 中，各自的逐项/批处理完整状态相等，但跨新旧运行的测试建档时间字段不同，因此不计同档完整状态或配对性能通过。具体差异只见 `orbitalStation.contractBoard.lastConfirmedWallClockMs`；后续直接读取基线冻结来源重验，不删除该字段或放宽完整比较。初次诊断因错误使用未安装的 esbuild 而在加载阶段退出，已改用项目实际 rolldown；原失败和内存启动拒绝保留，均不计测试通过。

随后 `quantum-construction-after-v2/` 直接读取基线原样来源，来源完整摘要均为 `385bec4b1d4100dcb54227e9f91d3901fa7a24c788423e9179b9a3d65e543412`；新旧两个算法模式四个终态完整摘要均为 `47552a5f8b4580ee8b79eae90fd8dd9ebc3372db0a0ea07ac70a79a63d9f0c1c`，递归比较来源和两个终态均无差异。每次都保留输入不变的断言、逐项/批处理完整状态相等、完成 1,000 台和批处理任务数断言，正常退出且无资源终止。诊断计时为参考路径 3,408→2,742 ms、批处理 5.6→6.9 ms；带采样且仅一次新旧运行，不声称稳定性能比率或玩家等待收益。

完整本机 `construction-availability-full-unit-v1.json` 已完成：**3,165 通过 / 39 跳过 / 0 失败**，约 420.613 秒，外部监控最低可用 4,475,348 KiB，退出 0，无内存/期限终止。原递归量子案例约 4,921.769 ms、有限矿脉案例约 3,197.218 ms（内部宏阶段仍须满足原 2 秒），两项均通过。新源码云端、构建和适用浏览器检查待完成；这次本机通过不覆盖 `2501f0d4` 的既有失败，也不授予新包资格。下一步仍需观察不同环境下递归参考路径的时间余量，并保留完整正确性对照。

## 参考路径不再探测批处理循环

材料检查提交 `38d069abc823d08208cc80ed08e48d9cceb63b27` 的轻量云端 run `34288352801` / job `102269037855` 已结束：38 通过 / 0 跳过 / 1 失败，递归量子案例约 5,022.659 ms，仍略超默认 5 秒。制品 `10080376074` 已下载并校验，原工作区 `cloud-game-timing-38d069ab.zip` SHA-256 `1fee5889b191b7e7eb6225764b5a2a639c42412490385b5eca5dd6068c140f4a`；有限矿脉和四项材料边界均通过。不得把上段本机全量通过概括成所有环境都通过。

进一步定位到 `batchConstructionAutomation:false` 的参考路径仍探测稳定批量循环，并把结果用于缓存。现在关闭批处理时，每个新作业只按当前托盘和量子直供材料独立规划，不执行或复用批量循环探测。生产默认批处理分支仍使用原有缓存、循环证明和预算。这使对照计算真正独立于它验证的批处理优化，保留逐步骤预留、扣料与产出。

新增 `constructionReferencePath.test.ts` 用 3 台递归制造构建反例：原来完整结果正确但计划构建 11 次，未满足“一作业一次参考规划”；修改后为 3 次，与批处理完整状态相同、参考批处理任务数及保护命中均为 0。原两个计时测试文件未改变。`construction-reference-focused-v1.json` 五文件 **40/40**，递归量子原案例约 **1,582.914 ms**，有限矿脉原内部 2 秒断言通过；实际项目类型检查通过。完整本机已启动，新源码云端和构建/浏览器门禁待完成。

同一冻结 1,000 台来源的 `quantum-construction-after-v3/` 再次与基线完整来源、逐项终态和批处理终态逐字段相同，终态仍为 `47552a5f8b4580ee8b79eae90fd8dd9ebc3372db0a0ea07ac70a79a63d9f0c1c`。参考计划构建从 4,000 降为 1,000，采样时约 946 ms；生产批处理仍为 5 次规划、1,000 个批处理作业，约 7.6 ms。只作为定位和正确性证据，不把参考算法加快当作玩家新的 Rust 收益。

这是对已确认云端计时失败的进一步修复。提交后由新源码替换前一个失败 JS 候选的完整云端任务，不因观察超时或临时日志不可用而重启作业。原 `38d069ab` 构建预检因本机可用内存低于 6 GiB 而未启动；没有生成或验收该源码的 Web 制品。
