# Windows 性能目标：连续开发记录

2026-09-10，Role: develop。用户明确要求“设为目标然后持续循环开发”，已创建 Windows 可交付性能优化 Goal，状态 active，不设 token 预算。完整 Rust 引擎替换继续暂停；本目标不包含线上部署。验收遵循[当前性能计划](../RUST_PERFORMANCE_ROLLOUT_PLAN_2026-09-07.md)。

## 第一轮：施工方案共享临时数据，已撤回

在 `c28a58a3` 上试验施工方案的写时复制，先让备选方案共用库存和步骤，只在修改时复制。调试施工专项 **24 pass / 0 fail / 0 ignored**；优化版 Host 构建通过。单次诊断的施工阶段合计约 2.771 秒，历史同工作量诊断约 3.127 秒，但局部差异没有变成稳定的整个请求收益。

相同授权终局档副本、固定 9 秒、两个 Rust 工作线程：各一次预热后，按 AB / BA / AB 跑三对独立进程。以下保留全部正式样本，顺序也保留：

| 对照 | 执行顺序 | 基线请求 ms | 候选请求 ms |
| --- | --- | ---: | ---: |
| 1 | 基线 → 候选 | 48,395.7098 | 48,666.5407 |
| 2 | 候选 → 基线 | 51,514.2573 | 48,367.3276 |
| 3 | 基线 → 候选 | 48,559.9567 | 48,688.2435 |

中位 **48,559.9567 → 48,666.5407 ms**，候选略慢约 **0.22%**；两对变慢，不能宣称提速。单个实际 Host 的 OS 峰值工作集中位 **1,638,907,904 → 1,643,294,720 bytes**，也没有改善；这不是完整 Electron 游戏进程树内存。

所有八份导出的完整摘要相同，最后一份与当前独立 JS 九步完整状态逐字段及摘要一致。JS 单独九步约 63.722 秒，范围不同，不能拿来计算界面提速。八次 Host 正常退出；原始玩家存档的字节、修改时间和摘要未变，隔离临时目录已清理。

结论：**没有证明整体收益，施工改动全部撤回**，不计新增性能成果。证据：`artifacts/rust-rp1-loop/planner-cow-private-ab-v1/report.json`；正常优化候选 Host SHA-256 `3be58b4a665ba9484f255e366429d46ad17540b4aafb8ab93d130331e48fc4a9`。基线 Host 为 `c28a58a3` 的 `df671ccc089122709b8baa954cdca5a05764c1ed5d2a913e46ea1592b2efe88d`。

守护均正常结束：施工调试 174.156 秒、最低可用内存 5,895,808 KiB；Host 构建 351.873 秒、8,653,496 KiB；诊断 60.861 秒、8,779,908 KiB；配对与完整 JS 对照 486.915 秒、2,558,964 KiB。保留原 6 GiB 启动 / 2 GiB 停止阈值，重型任务串行、BelowNormal，无游戏窗口、声音或焦点操作。

## 第二轮：科研校验复用现有索引，保留

`capture_research_proof_snapshot` 原来每次都会重新解码全厂实体，只保留使用 `matrix_research` 的实验室。授权档共 110,042 个实体，其中科研实体 1,354 个。此次复用已有科研目录，并以完整的紧凑配方列核对目录长度、成员和顺序；任何不一致回到原全厂扫描。仍逐项校验被选实验室的原始数据，不改变科研、产出或存档格式。

新增三个调试用例通过：有限/多输入/无限科研完整快照与旧全扫一致，实验室顺序、零堆叠、检查点重载一致，缺失/重复/乱序/越界目录和截短配方列回退，缺失输入字段仍报错。另三个既有科研消耗端用例通过。正常优化 Host 构建通过；一次可选诊断中，结算前后科研快照分别约 7.758 / 8.078 ms。该诊断单独保留，不混入正式性能样本。

随后仍以相同完整存档、固定 9 秒、两个 Rust 工作线程，各一次预热后执行三对 AB / BA / AB：

| 对照 | 执行顺序 | 基线请求 ms | 候选请求 ms |
| --- | --- | ---: | ---: |
| 1 | 基线 → 候选 | 50,396.8618 | 47,465.6729 |
| 2 | 候选 → 基线 | 51,098.6348 | 47,831.2630 |
| 3 | 基线 → 候选 | 51,296.0502 | 49,074.7817 |

中位 **51,098.6348 → 47,831.2630 ms**，减少 **3,267.3718 ms / 6.3942%**。三对均更快，本轮两组样本范围没有重叠。这是解析、证明、模拟和持久导出合计的完整 Native 请求收益，**不包含菜单、来源准备、最终入场保存和普通模拟恢复，不是整版或完整界面提速百分比**。

实际单个 Host 的 OS 峰值工作集中位 **1,644,433,408 → 1,639,952,384 bytes**，仅约 4.3 MiB 差异，不宣称稳定内存改善，也不替代整个 Electron 进程树测量。所有八份完整导出均为 208,891,374 bytes、同一 SHA-256；最后一份与本次独立 JS 九步的完整 JSON、canonical 和 domain 证明一致。八次 Host 正常退出，原始玩家存档大小、修改时间和摘要不变，隔离临时目录清理通过。

证据：`artifacts/rust-rp1-loop/research-index-private-ab-v1/report.json`。候选 Host SHA-256 `68d3e77adc45fae41a900bd3ae2c8a1ab08c88e461bbf8c13589a65494b3adf8`；核心改动文件 `pure_idle.rs` SHA-256 `61df273593b67907b1dcca3a306a82899adf3244420eee3fae81aa326a3ed05c`。基线仍为 `c28a58a3`，施工尝试已完全撤回后才构建本候选。配对守护正常 exit 0、490.418 秒，最低空闲 4,121,124 KiB；6/2 GiB 和后台约束不变。

科研/无限采矿实际 Host 与 JS 回归 **4 pass**；公开长离线矩阵 **14 pass / 2 可选性能基准 skip / 0 fail**，包含 8 小时完整状态、物资、非整数起始时间、热继续和检查点重载。分别见 `research-index-research-differential-v1` 与 `research-index-offline-public-v1`；守护正常退出，6.016 / 311.606 秒，最低空闲 6,944,444 / 5,274,656 KiB。长测结束后严格检查首次启动因不足 6 GiB 空闲被拒，未产生子进程或检查结果；空闲恢复到 8,057,312 KiB 后，保持原阈值单独启动正常 release、workspace/all-targets、`-D warnings` Clippy，**通过**。证据 `research-index-strict-v1`，守护正常退出，70.167 秒，最低空闲 5,582,924 KiB。

当前目标仍 active，尚未完成新包成功/取消/保存重开、完整界面等待三对及至少 20% 的完整等待目标，没有发布。

辅助统计首轮将不可持久化的 `undefined` 字段交给 `Buffer.byteLength`，脚本失败；已按 JSON 的省略规则修正并另跑 v2。v2 确认科研实体数量，全部实体/带以外的顶层值合计仅 408,834 bytes，因此暂不投入顶层状态重复编码优化。原失败日志保留在 `return-cost-shape-v1-guard`，成功记录为 `return-cost-shape-v2.json`。

此前基线 `c28a58a3` 的云端 Windows 验证已成功（run 34453052922）；CI 总体失败（34453052954），单元、构建和 Server/Ops/Native 成功，浏览器仍有失败。这是修改前源码的结果，不能作为本轮科研候选的全量验证。

## 第三轮：有界来源的内存解析，保留

第二轮已提交并推送 `aa20b1e901a2681bd6a5caad8bcad702759ecdd0`，PR #31 保持开发草稿，目标继续 active。科研候选的来源解析一次约 6.56 秒，第三轮针对这个成本。

已知长度且不超过原 256 MiB 上限的输入，先读取已声明的字节并确认真实 EOF，再由切片解析器解码；临时正文在构造游戏状态前释放。读入失败、长度变化、解析错误或尾随内容交回原有流式解码路径，保留错误发生次序、Unicode 兼容分类和文件身份检查。未知解压后长度的流式入口继续走原路径。没有取消 SHA、完整存档校验或扩大导入上限。

调试解析专项 **15 pass / 0 fail / 0 ignored**，含新旧完整记录与证明、提前语法错误与较晚 I/O 错误次序、尾随数据和长度变化对照，以及原有 v46 迁移、Unicode、校验和、读取边界和真实 EOF 用例。证据 `slice-parser-debug-v1`；守护正常退出，57.246 秒，最低空闲 4,635,944 KiB。正常优化 Host 构建通过；守护 358.690 秒，最低空闲 6,396,476 KiB。

一次单独诊断中，来源解析 **5,004.352 ms**，完整请求 **45,923.1499 ms**，导出与已验证的完整状态一致。这是分段选题证据，不参与正式配对。随后基于同档、固定 9 秒、两个 Rust 工作线程，各一次排除预热后运行三对：

| 对照 | 执行顺序 | aa20 科研基线请求 ms | 内存解析候选请求 ms |
| --- | --- | ---: | ---: |
| 1 | 基线 → 候选 | 47,229.5588 | 43,689.3247 |
| 2 | 候选 → 基线 | 46,452.8911 | 43,557.4547 |
| 3 | 基线 → 候选 | 47,842.8747 | 44,538.8961 |

中位 **47,229.5588 → 43,689.3247 ms**，减少 **3,540.2341 ms / 7.4958%**，三对均更快、两组范围不重叠。此轮基线已经含科研优化，仍只计完整 Native 请求；不同批次的百分比不直接相加或代替完整 UI 收益。单个 Host 的 OS 峰值工作集中位 **1,643,982,848 → 1,645,441,024 bytes**，约 1.4 MiB 差异，不宣称内存改善；它仍不是完整游戏进程树。

八份完整导出字节与摘要相同；本次独立 JS 九步的完整 JSON、canonical 和 domain 证明一致。八次 Host 正常退出、原始玩家存档大小/修改时间/摘要未变、隔离临时目录清理通过。证据 `slice-parser-private-ab-v1/report.json`，守护正常退出，**453.085 秒**，最低空闲 **5,480,116 KiB**；本轮全部重任务继续 6/2 GiB、串行、BelowNormal，无窗口、声音或焦点操作。

候选 Host SHA-256 `17a65c43b4bf24b7b1727c209b60a0e224a9edf6a664fa3a4d4efe6de28b572e`，`v47_import.rs` SHA-256 `aa69f154653deb84658d42c5a0796ef694baa49179aa2a8fb5a9e968f240932b`；科研文件维持第二轮的 `61df2735…`。实际 Host 导入/保存恢复边界 **67 pass / 0 fail / 0 skip**，正常 release workspace/all-targets 严格 Clippy 通过；证据 `slice-parser-import-closure-v1`，守护正常退出，146.581 秒，最低空闲 7,148,980 KiB。覆盖真实普通/Gzip 导入、Unicode 兼容回退、损坏输入保留旧检查点、实际来源导出与清理。源码和 Host 摘要在回归后保持与配对实测一致。

本轮已提交并推送 `776a9a8c5b178e93b0c397124569383cef227aa8`。以下为随后冻结包的真实入口验证；Goal 仍 active，没有发布。

## 当前冻结包：成功、取消、保存和重开

`1.2.7+776a9a8c5b17` 离线开发候选完成构建与冻结，清单 76 个制品、79 个文件；Host 与第三轮配对的 `17a65c43…` 一致，助手 SHA-256 `f05b1179a8f8084e5c12f4ef3bef45b24fbe6d450192d7ac49b54410c65f4bce`。先前 891 冻结包 79 个文件未变。记录为 `package-776a9a8c-receipt.json` 和 `package-776a9a8c-frozen/`。

首次构建检查将 beta 性能包环境错误地传给默认 stable 的完整 Native 测试，985 pass / 2 fail / 1 skip，尚未打包；两个失败均为默认更新目录断言。仅修正忽略目录内的构建驱动环境隔离后，完整 Native **987 pass / 0 fail / 1 条件 skip**，类型、生产构建、启动预算、平台、实际 Host/助手、源绑定及冻结校验通过。产品和测试断言没有为此修改。初次守护失败保留；成功构建守护 281.343 秒、最低空闲 6,554,852 KiB。

实际入口使用同一完整授权档副本、真实时钟、原始界面与原有限额。第一次完整场景实际结算 10 秒，约 **87.443 秒**进入可操作界面，随后暂停手动保存等待超时，报告仍为 **FAILED**。未发现崩溃或内存守护中止；正常关闭 2 次。此记录不能计为完整流程通过。源码的完成提示只保留 2 秒，测试在读整档后再检查该提示有漏看风险，但尚未由这份失败记录单独确认根因，未据此修改产品。

v2 仅增加诊断步骤及正确的错误栈文件名，保持全部等待限额、真实时钟、原档与断言不变：

| 场景 | 实际结算 | 结果 |
| --- | ---: | --- |
| 继续、确认结算、暂停保存、重进与两次重开 | 9 秒 | PASS；进入操作界面 82,159.817 ms，完整候选、落盘状态与独立 JS 一致 |
| 候选已算完后取消、关闭及两次重开 | 10 秒 | PASS；主档和 Native 检查点未变，未提交候选与独立 JS 完整状态一致 |

两项合计 8 次正常退出，实际窗口均不可见、不可聚焦、静音、离屏，零显示/焦点/系统弹窗；原始存档大小、修改时间、SHA-256 不变。成功守护 **315.150 秒 / 最低 3,220,456 KiB**，取消守护 **171.587 秒 / 最低 5,674,064 KiB**，均原 6/2 GiB、串行 BelowNormal。取消仅在候选返回前人为延迟 3 秒留出操作机会，不参与性能样本。

证据为 `private-packaged-complete-performance-return-v1/report.json`（失败保留）、`private-packaged-complete-performance-return-v2/report.json`、`private-packaged-cancel-performance-return-v2/report.json`。v2 驱动 SHA-256 `f77fd49542e6367d6a5fe3ad969d10999374883735ff5a52fd884e6f3fab3ccd`。v3 已准备记住手动保存从开始到完成的真实状态变化，避免读档后漏看短暂提示，仍共享原 90 秒总期限；尚未执行，不计验证。

以上是两项当前包的正确性记录，**不是三对完整 UI 性能对照**。9/10 秒工作量不混算；单次 82.16 秒也不与历史 d97 数据计算提速。完整 UI 配对、进程树峰值内存和 20% 目标仍待完成。

云端科研提交 aa20 的 Windows 全套通过（34460429419）；CI 的构建、单元、Server/Ops/Native 和浏览器第一分片通过，第二分片失败（34460429374）：冷菜单 p95 为 954 ms，重试 965 ms，超过 500 ms；多字号设置场景触发 30 秒用例限额。没有调高门槛。776 的新云端检查为 34464778194 / 34464778199，结果另行更新，旧源码通过不代替新源码资格。

## 第四轮：共享存档结构扫描，局部收益保留

先对同一授权原档执行一次排除预热、三次现有扫描，单次中位 **1,206.5283 ms**；这是扫描器选题诊断，没有同时运行其他重任务。之后把信封边界、state 属性边界和大集合数量合并为一次遍历，仍独立读取全部原始 state 字符计算校验和。原有字段、重复键、转义、标量和集合结构检查保留，未知嵌套字段沿用原扫描器，不构造第二份实体图。

冻结旧实现，同一进程中各一次排除预热后，对完整原档按 AB/BA/AB 执行三对：

| 配对 | 旧扫描 ms | 合并扫描 ms |
| --- | ---: | ---: |
| 1，旧版先 | 1,207.2878 | 573.3369 |
| 2，候选先 | 1,203.5695 | 573.4418 |
| 3，旧版先 | 1,190.2505 | 577.0737 |

中位 **1,203.5695 → 573.4418 ms**，减少 **630.1277 ms / 52.3549%**。三对检查对象、完整摘要相同，1,698 个转义/单字符破坏变体的新旧返回值逐项一致，原档字节/时间/摘要不变。此项仅为同进程扫描微测，**不是独立进程完整 Native 请求、完整保存或 UI 的性能验收**，也没有内存收益结论。记录 `envelope-inspection-profile-v1/report.json`、`envelope-inspection-ab-v1/report.json`；旧源码 SHA-256 `33a1fd107116b8dc1bacb9146a5abca23ad302d7f13ff6f8cb9a71b48bc562fb`，新源码 `ac95d94f78f4636b657857fb413c4feda86acc01d966066370c6fb1568cae5b7`。

相关单元 **17 pass / 0 fail / 0 skip**、TypeScript 通过；包含新增字段顺序、转义字段、空集合、重复键、缺失字段及损坏容器回归。实际 headless Chrome 的保存 Worker/持久化 Worker 原子主档+目录+revision/备份、错误种子/旧 fence 拒绝、Native 租约隔离、检查点覆盖字段绑定和 v46 兼容 **5 pass / 0 fail / 0 skip**。浏览器单 worker、静音、无界面，默认时间限额未改。记录 `envelope-scan-regression-v1`、`envelope-scan-browser-v1/report.json`。守护分别 **47.742 秒 / 6,223,568 KiB**、**22.541 秒 / 7,693,004 KiB**；微测 **8.633 秒 / 9,259,136 KiB**，均保持 6/2 GiB。

这项共享 TypeScript 优化尚未加入 776 冻结包，不能把该包验收冒充新扫描代码的实包验收。它可随共同源码用于网页和安卓，但没有接入跨端 Rust 或部署。下一循环聚焦完整等待和进程树内存，再决定下一主要成本；Goal 仍 active。
