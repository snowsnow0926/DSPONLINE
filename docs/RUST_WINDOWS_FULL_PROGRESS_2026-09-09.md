# 完整 Windows Rust 开发进度｜2026-09-09

**完整 Windows Rust Goal 已创建并启用。** 目标包含 Rust 实时运行、完整受支持玩法、可靠存档与恢复，以及可以交接发布的 Windows 包。沿用现有界面；网页/安卓 Rust 仍另行开发，本批未发布客户端。

## 本批推进

1. **Rust 请求再省约 1.75 秒。** 同一条工厂记录只编码一次，同时更新两个独立摘要。终局档三对真实 Host 测量，固定 9 秒结算请求的中位耗时 **53.55→51.80 秒，减少约 3.3%**；完整导出和所有摘要一致，8 次运行正常退出，原档未变。这是 Native 请求收益，尚不能当作完整进入游戏或帧率提升。
2. **修正堆叠设备连线偏离端口。** 隐藏的一端无法测量时，原代码会丢掉另一端的真实位置。现在分别保留两端的测量；在测试夹具中修正了约 38 个画布单位的纵向偏移，并保留命中、选择和隐藏节点行为。三种画布检查各跑三轮，**9/9 通过**。
3. 正常优化版 Rust 核心 **1,114 通过、5 条件忽略**，Host **256 通过、1 测试驱动忽略**；严格编译检查通过。Native 接口 **750 通过、1 条件跳过**，完整游戏 **3,176 通过、39 条件跳过、0 失败**，其中实际 Rust 对照 **50 通过、1 长测跳过**；类型、Web 构建和三项构建门禁全部通过。
4. **修复了实包启动时重复搬运整档的问题。** 新的 84 冻结包两次仍未在原 90 秒内进入工厂；诊断确认，启动恢复检查会停掉刚创建的模拟 Worker，随后重建并再传一次约 196 MiB 的状态。现在等待持久恢复检查通过后才启动 Worker；旧代码两项都失败，修复后各三轮 **6/6 通过**。这些使用真实界面和协议替身，完整实包效果仍待验证。
5. 新修复再次通过类型、Native **750/1 skip**、游戏 **3,176/39 skip**、实际 Native 对照 **50/1 长测 skip**。首次构建最后一步因覆盖清单行号过期失败，已保留失败记录；确认只更新源码行号后，Web 全部门禁及画布三轮 **9/9 通过**。没有放宽超时或模拟要求。
6. **终局完整入口仍未过关。** 84 包一次正常关闭失败、另一次成功；成功保存和两次重开尚未验收。它的 Linux 构建、单元、Server/Ops/Native 云端通过，浏览器仍 **31 项失败、22 项重试后通过**，Windows 云端全量通过。修复启动顺序后的源码还须新的冻结包和云端验证，不能按历史失败数波动判断问题已修复。

详见[启动顺序修复与 84 实包证据](./reviews/rust-rp1-startup-reconcile-order-2026-09-09.md)、[本批 Rust 收益](./reviews/rust-rp1-native-canonical-pair-2026-09-09.md)、[堆叠连线修复](./reviews/rust-rp1-partial-canvas-endpoints-2026-09-09.md)和[前批保存优化](./reviews/rust-rp1-empty-emergency-cleanup-2026-09-09.md)。不同环节的优化比例不能累加成整款游戏提速。

## 当前进度与剩余工作

| 工作 | 当前情况 |
| --- | --- |
| 短离线收益 | 一类公开大厂在旧冻结包测到完整等待缩短约 71.3%，保存和重开正确；不能泛化成所有工厂或本批成绩 |
| 超大终局档 | 84 包仍超原 90 秒；已定位并修正启动重复 Worker，待新包的完整进入、保存、正常关闭和重开验证 |
| 长离线 | 简单产线已有证明；复杂工厂待扩展，自动采用仍为 1–30 秒 |
| Rust 实时接管 | 八项底层测试及独立进程命令恢复通过；可信资格、实际桌面单所有者和其余恢复路径仍待完成 |
| 完整玩法兼容 | 普通/竞速、内容包、终局系统、云和跨端存档往返仍需逐项验收 |
| 可发布 Windows 包 | 同一包的完整回归、不同硬件、长测、异常恢复、签名及安装升级回退仍待完成 |

**完整 Windows 版本仍是数十人日级工程，尚无可信的总完成百分比。** 先前 5–10 人日只估当时 RP1 三组收尾；原计划累计 45–90 人日不能当作今天的精确剩余量。每批按实际通过项目更新。

接下来先用新冻结包验证启动修复能否让终局档完成原 90 秒入口、持久保存、正常关闭和两次重开；再继续精确模拟与开档校验的成本、实际程序接管、恢复和浏览器回归收口。本机重型测试一次一个，每批验证后提交并推送。测试继续后台、静音、不抢焦点；实包使用隐藏离屏入口、零可见游戏窗口，原内存门槛保持，不关闭你的应用腾内存。

[完整执行目标](./rust/windows-full-development.md) · [逐步计划](./RUST_PERFORMANCE_ROLLOUT_PLAN_2026-09-07.md) · [草稿 PR #31](https://github.com/snowsnow0926/DSPONLINE/pull/31)
