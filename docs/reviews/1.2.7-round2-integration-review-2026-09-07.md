# DSPONLINE 1.2.7 Round 2 审查报告

Role: develop
日期：2026-09-07
工作树：`D:\GameDev\DSPidle2-v127-windows-performance`
分支：`codex/1.2.7-integration-evidence`（本地，未 push）
上轮 source：`bede3e23940813daa3d0c218a8fc5e1ddbbb8648`
上轮文档：`b5808284dba7a98daca7f722aa4cb48bfedbf3f5`
`bede3e23..b5808284`：只新增 `docs/reviews/1.2.7-closeout-review-2026-09-07.md`，没有覆盖后续源码。
本轮源码改动：`desktop/pack.cjs` feed 脚本路径、`desktop/pack.test.cjs` 合成 feed 集成、隔离 Electron 旅程驱动与默认 Chromium `testIgnore`。
被测 Electron 包：上轮未签名性能目录包，Build ID `1.2.7+bede3e239408`，EXE SHA-256 `6b6a698185dca708fc4ba712c2b32f783cbcd8d38f78efcf90062c4d5ddd62b9`。
未开启 `authorityEligible`，未新增门禁旁路，未改 Skill，未签名，未 push，未发布，未操作生产。

## 1. 上轮“100 单位未做”是否准确？

**不准确。**

第 25 节是 2026-09-02 的剩余工作表。同一份计划第 26.2 节已把 14 包记为 **100/100 代码冻结候选**，并写明集中测试完成前发布仍为 No-Go，`authority_eligible` 仍为 false。第 27 节是集中验收；第 28 节把它合入 1.2.7。上一轮把第 25 节当成当前代码欠账，忽略了 26–28。

缺的是发布门禁、硬件/签名/云，以及普通包上的 Rust 玩家权威入口，不是整表业务代码缺失。对照见 `plan-implementation-matrix.md`。

## 2. 上轮 pack 修复是否通过独立复核？本轮新发现？

上轮身份/目录/workflow 修复：**成立**。本轮再次调用生产 `createDesktopUpdateFeedArguments`、真实 `scripts/create-native-update-manifests.mjs`、`selectCompleteDesktopReleaseOutput`。

新发现并已修：当 `repositoryRoot` 指向临时树时，feed **脚本路径**被拼到该临时 root，真实生成器找不到模块。红灯：`pack-feed-integration-red.log`。修复：脚本固定为 packer 的 `__dirname/../scripts/create-native-update-manifests.mjs`。绿灯：`pack.test.cjs` **9/9**。

没有把性能版混进普通更新源；普通 pack 仍不写 feed。该修复尚未打入被测 `bede3e23` 包（asar 内仍是上轮脚本）。

## 3. 多出来的 skip 是谁？长差分跑了吗？

精确用例：`src/game/nativeCoreDifferential.test.ts`
名称：`matches long mining boundaries and segmented offline settlement`
条件：`it.skipIf(process.env.DSP_RUN_NATIVE_CORE_LONG_DIFFERENTIAL !== "1")`
上轮 `npm test` 未设该变量 → 3052 pass / 29 skip。

本轮子进程设置 `DSP_RUN_NATIVE_CORE_LONG_DIFFERENTIAL=1`，Release Host 存在，结果 **51/51 pass**，该长窗口用例 **269291ms**。日志：`native-long-differential.log`。这是固定输入 JS/Rust 合成核心对照，不是玩家 Rust 端到端。

## 4. 真实 Electron 包玩家旅程

包：`release-performance-edition/win-unpacked/dsp-idle-performance-edition.exe`
隔离：`DSP_PERFORMANCE_SMOKE_ISOLATION=1` + `DSP_PERFORMANCE_SMOKE_APP_DATA_ROOT` 在 `%TEMP%\dspidle-performance-smoke-*`
加载包内 dist，无 Vite；API/更新环境变量已剥离。
驱动：`npm run test:desktop-package`。最终 **4 passed / 0 failed（1.7m）**。

| 旅程 | 结果 | 说明 |
| --- | --- | --- |
| 新建、暂停/继续、手动采矿、立即保存 | **PASS** | 真实产品入口；铁矿库存 `>=5`；`data-native-authority-mode=unbound` |
| 关闭后重开继续 | **PASS** | 进程确认退出后等待 `LOCAL_SAVE_LEASE_DURATION_MS + 2s`（15s 租约产品语义），再处理 pagehide 紧急镜像冲突条。截图 `desktop-journey/reopened.png` |
| 建造放置电弧熔炉/风力涡轮机 | **未作为通过条件** | 小地图/建造栏拦截点击；本轮改为真实采矿，未用 evaluate 作弊放置 |
| 导入合成 v47 | **PASS** | 预览“校验通过”；源文件 SHA-256 `0e0ce3bb964bc29e2f8470c08280c6cbb928b3e9cdc8ef1e220839064f00bbe0` 不变 |
| 导出压缩存档 | **PASS** | Electron 走原生保存/`will-download`，不是 Playwright `download` 事件。测试只把保存路径接到隔离证据文件。导出 `exported-save.json.gz` 9148 B，SHA-256 `57dfb033a133f0938da699e0991e84035306671dd7596501e1e332e3934eb6df` |
| 无效导入 | **PASS** | 截断 JSON 无“确认导入”，已加载状态字节不变 |
| 耐久保存后杀进程再开 | **PASS** | 确认保存后的工厂能继续。pagehide 紧急镜像/离线结算可能改写信封字节，不以信封字节全等冒充“无生命周期写入” |
| Rust 门禁可观察性 | **PASS** | `data-native-authority-mode=unbound`；未声称 Rust 玩家模式 |

不能用 Chromium E2E 代替上表。默认 `playwright.config.ts` 已 `testIgnore` 该规格，避免 Chromium 误跑 Electron 包测试。

## 5. Rust 玩家权威具体还缺什么？

见 `authority-blockers.md`。摘要：

- 实现：shadow、handoff、lease、薄 UI **存在**。
- `authority_eligible`：**硬编码 false**，不是证明计算结果。
- 发布证明：TS 有校验形状，**没有**生产生成器写入 Host。
- Release EXE：**无**完整接管证据；Host 测试靠 `#[cfg(test)]` override。
- 明天硬件齐了仍需开发激活管道，不能只改 boolean。
- 本轮没有把 `authorityEligible` 改成 true，没有环境变量旁路。

## 6. 下一轮最小任务

**不要**重做 100 单位，也不要开 `authorityEligible`。

最小任务：为 Electron **正常关窗**定义可测的写入租约释放（pagehide 之后把当前 writer 的 `expiresAt` 置为 now，崩溃路径仍保留 15s fail-closed）。本轮旅程用等待 15s 证明了产品语义，但玩家立刻重开仍会看到“已阻止跨标签页覆盖”。这是桌面生命周期，不是扩大权限。

Rust 晋升仍单独授权。

## 7. 验证摘要

| 项 | 结果 |
| --- | --- |
| pack 生产函数 + 真实 feed 生成器 | 9/9 PASS（修复后复跑） |
| 长差分 `DSP_RUN_NATIVE_CORE_LONG_DIFFERENTIAL=1` | 51/51 PASS，长窗口 269291ms |
| Electron 隔离包旅程 | 4/4 PASS（1.7m） |
| Chromium | **437 passed / 27 skipped / 0 failed**（7.6m，`testIgnore` 后复跑；Vite `/api/*` 代理 `ECONNREFUSED 127.0.0.1:65534` 为离线预期） |
| durable | 7/7 PASS（51.3s） |
| nightly Firefox+WebKit | 2/2 PASS（8.3s） |
| 性能 | 长差分证明合成核心一致；玩家 Rust 端到端 **NOT_MEASURED**；不引用旧 2.4–2.9× |

实现状态：14 包代码在树中；pack 脚本路径已修，尚未打入被测包。
验证状态：长差分、pack 集成、隔离 Electron 四段旅程、durable、nightly 已新跑。
制品状态：沿用上轮未签名性能目录包（EXE SHA 与 closeout 记录一致）。
发布状态：**未授权、未发布**。
