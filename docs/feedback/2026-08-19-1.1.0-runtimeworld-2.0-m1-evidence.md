# DSPidle2 1.1.0 RuntimeWorld 2.0 M-1 证据记录

> 日期：2026-08-19
>
> 角色：develop
>
> 状态：进行中；本文件只记录已经复核的证据，未完成项不得视为通过
>
> 限制：未连接生产服务器，未签名、部署或修改下载页

## 1. 隔离与源码边界

- 独立工作树：`DSPidle2-runtimeworld-2`；分支：`codex/1.1.0-runtimeworld-2`。
- 计划与基准文档固定提交：`da72c38`。
- 正式开发父提交：`093f2f1c75ce1a89b47b1e040157f9880ef594ff`；运行时代码父提交：`d64b9ef85f9dea1cf2d0617cb300fa492ca1f43c`。
- raw payload cache 收口提交：`bb29526112f08cabfa4a401a444c1632bb3330ae`。
- 物流独占阶段 profiler 提交：`3721670`。
- 冷进程夹具时钟固定提交：`eb3d87b`。
- GameState 仍为 v47；save envelope v2、cloud schema v8、SQLite layout v3 均未改变。
- 玩家真实存档只读载入；浏览器测试只写隔离 IndexedDB。文档不记录玩家身份、实体 ID、物品正文或原始文件路径。

## 2. 固定环境

| 项目 | 值 |
| --- | --- |
| OS | Windows 10 Pro 10.0.19045 x64 |
| CPU | Intel Core i7-14700KF，20 核 / 28 逻辑处理器 |
| 内存 | 34,163,752,960 bytes（约 31.82 GiB） |
| GPU | NVIDIA GeForce RTX 4070 SUPER，驱动 32.0.15.8115 |
| Chrome | 147.0.7727.138 |
| Node / npm | v24.14.0 / 11.9.0 |
| 电源计划 | Balanced，`381b4222-f694-41f0-9685-ff5bb260df2e` |
| root lock SHA-256 | `fc214a751d1830933d42a69bd591b81d30d8b220a048f59f7401014cea331cc5` |
| server lock SHA-256 | `112c3f8b9ba8465bb50a0e4e07c35630bdc113c7911c30378c105e4634c631fc` |

`npm ci` 已分别在 root 与 server 完成；两处 audit 均为 0 vulnerabilities。

## 3. 匿名真实夹具

| 样本 | bytes | SHA-256 | mtime（Asia/Shanghai） | GameState | 实体 / 线路 |
| --- | ---: | --- | --- | ---: | ---: |
| A：极端档 | 36,704,109 | `cd2356ea2b9a90a47cfa32ed9533e7056bfc4202f6af777fc4f3b98faa9a81b1` | 2026-08-14 15:42:32.990 | v47 | 27,153 / 48,917 |
| B：大型档 | 10,016,097 | `edf8dbb5444dd812d8a570f1c1003bfd8665166cc11d6242a8f1912dd2fb5c30` | 2026-08-18 22:48:05.986 | v47 | 8,687 / 14,634 |

所有已执行真实档测试均在结束时重新计算原件哈希；bytes、mtime 与 SHA-256 未改变。

## 4. raw payload cache P0

### 4.1 复现与根因

旧路径在真实 A 档稳定复现：等待 120 秒后 `getLocalSaveRawCacheSize()` 仍为 1。失败发生在产品 getter，不是 DOM 文案滞后。

根因是 React StrictMode mount 的 effect setup-cleanup-setup 触发同步 `saveGame()`；随后 primary、backup 与 automatic snapshot 通过协调式 IndexedDB 事务提交，但成功读回后没有释放 renderer 内的完整 raw candidate。

### 4.2 修复合同

- 只有 primary、backup、automatic snapshot 在 exact committed value 读回成功后释放 transient raw payload。
- 若同一 key 已排入更新值，不释放较新的值。
- 显式 slot/manual recovery 继续保留，兼容同页同步读取与 tombstone 合同。
- shell 的 raw-cache 属性改为订阅 storage status，避免把真实释放误报为旧 DOM 值。
- 未刷新页面、未清空 IndexedDB、未删除存档。

### 4.3 验证

| 门禁 | 结果 |
| --- | --- |
| 生命周期专项测试（提交后释放 transient raw） | 通过 |
| local-save catalog + coordination | 23/23 通过 |
| 真实 A 档 dev，独立浏览器连续 3 次 | 3/3；约 26.2 / 27.6 / 27.6 秒 |
| 真实 A 档 production-preview，独立浏览器连续 3 次 | 3/3；约 23.2 / 23.4 / 22.3 秒 |
| clean Web build startup budget | 通过；无 forbidden chunks |
| 原件复核 | 未改变 |

production-preview 不暴露源码模块，因此该路径通过订阅后的 `data-local-save-raw-cache-size` 验证真实产品边界。

## 5. 物流 profiler 非重叠归因

### 5.1 计时合同

以下字段是互斥独占阶段，其和与 `logisticsMs` 对账：runtime reset、buffer transfer、material delivery、orbital terminal、lookup maintenance、warper refill、dispatch、route advance、congestion、galactic export。

`peerMatchMs`、`routeEconomicsMs`、`dispatchPeerSortMs` 是嵌套诊断，明确不加入独占阶段之和。profiler 未提供时不调用阶段时钟、不编码 trace，也不改变玩法路径。

### 5.2 两档 10 秒只读实测

| 指标 | A | B |
| --- | ---: | ---: |
| engine wall | 4,563.474 ms | 1,400.537 ms |
| `logisticsMs` | 512.093 ms | 369.528 ms |
| 独占阶段之和 / `logisticsMs` | 100.0% | 100.0% |
| buffer transfer | 415.260 ms（81.1%） | 281.817 ms（76.3%） |
| congestion | 73.852 ms（14.4%） | 52.626 ms（14.2%） |
| dispatch | 0.333 ms（0.1%） | 20.406 ms（5.5%） |
| route advance | 5.370 ms（1.0%） | 2.149 ms（0.6%） |
| 其余独占阶段 | 17.279 ms（3.4%） | 12.529 ms（3.4%） |
| 已分类顶层 engine wall（含 lookup build） | 约 91.7% | 约 89.1% |
| 非法物资值 | 0 | 0 |

结论：原先 A 82.9%、B 79.3% 的“未归因物流时间”主要是 `transferLogisticsBuffers()`，不是路线调度。M4 的迁移顺序必须先处理 buffer 遍历/活跃集合，再按 B 档的 dispatch 热点处理候选调度；不能仅凭函数名重写 routes。

专项测试固定以下合同：独占 key 无重复、所有值有限且非负、独占阶段之和与 `logisticsMs` 在浮点容差内相等、开启 profiler 前后玩法哈希一致。

## 6. M-1 剩余门禁

### 6.1 正式 60 秒冷进程基线

A 原件为 v46。在启用空间站功能的代码上迁移为 v47 时，缺失的首个合同板按 `Date.now()` 生成，因此原先三次冷进程虽然各自的 60 秒整段/切片相等，跨进程 source hash 却不同。基准现只在夹具迁移期间把时钟固定为不可变 envelope `savedAt`；产品迁移语义未改。两个独立 1 秒进程先证明 source/final hash 相同，随后重跑正式矩阵。

| 样本 | 冷进程 | 60 秒整段 | 60×1 秒 P50 | P95 | max | source / final gameplay hash |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| A | 1 | 21,631.059 ms | 316.316 ms | 486.992 ms | 513.219 ms | `4546c9cc` / `cbb44f08` |
| A | 2 | 21,551.311 ms | 317.300 ms | 489.596 ms | 505.262 ms | 同上 |
| A | 3 | 23,093.463 ms | 321.213 ms | 506.067 ms | 535.279 ms | 同上 |
| B | 1 | 6,517.766 ms | 107.745 ms | 168.327 ms | 186.836 ms | `78e385ee` / `e1252787` |
| B | 2 | 6,432.416 ms | 99.312 ms | 157.670 ms | 169.554 ms | 同上 |
| B | 3 | 6,598.329 ms | 103.715 ms | 161.000 ms | 189.808 ms | 同上 |

- 每次整段与切片 gameplay hash 一致；完整 hash 的差异只来自允许不同切片密度的 `productionHistory`。
- 六次 `invalidAmounts=0`，物资、路线货物和量子库存汇总在同一样本内完全一致。
- 上表开启 opt-in profiler，不能与未来 profiler-off 发布数字直接相除；扰动对照仍是 M-1 剩余项。
- 相对开发前报告的旧提交数字更快，说明正式开发基线必须固定在本分支 clean SHA，不能沿用 `b1c91a8` 的绝对值作为新实现收益分母。

### 6.2 命令突变矩阵

测试把每条命令拆为 patch、apply、runtime lookup rebuild，并将首个样本与后续 7 个热样本分开。两档都强制为运行态；暂停态会合法跳过 lookup 构建，不能作为运行中命令基线。所有 apply 结果与目标状态 hash 一致。

| 热样本中位总耗时 | A | B |
| --- | ---: | ---: |
| 顶层非拓扑 1 项 | 151.162 ms | 49.563 ms |
| 实体非拓扑 1 项 | 158.596 ms | 48.257 ms |
| 实体非拓扑 100 项 | 152.165 ms | 51.231 ms |
| 实体非拓扑 1,000 项 | 154.484 ms | 55.064 ms |
| 线路拓扑 1 项 | 176.048 ms | 48.222 ms |
| 线路拓扑 100 项 | 168.289 ms | 51.120 ms |
| 线路拓扑 1,000 项 | 160.192 ms | 53.578 ms |

| 分阶段结论 | A | B |
| --- | ---: | ---: |
| 初次运行态 lookup compile | 208.824 ms | 86.122 ms |
| 单实体 patch/apply 热中位 | 4.764 / 0.865 ms | 1.664 / 0.273 ms |
| 单实体 rebuild 热中位 | 153.155 ms | 45.494 ms |
| 单线路 patch/apply 热中位 | 10.225 / 1.707 ms | 2.082 / 0.411 ms |
| 单线路 rebuild 热中位 | 161.356 ms | 45.498 ms |

结论：1～1,000 项之间的总耗时变化很小，因为当前 Worker 在每条命令后无条件执行完整 lookup rebuild。单记录命令的 rebuild 约占总时间 A 92%～97%、B 94%；M1 必须实现按 patch 领域局部失效，非拓扑命令的完整 rebuild 次数必须降为 0。热样本 P95 和浏览器端到端 painted acknowledgement 仍需单独报告，不能用这里的 Node 预处理耗时代替。

### 6.3 匿名运行形状矩阵

所有夹具都由公开目录和固定种子生成，明确标记为 synthetic；不包含玩家数据。快速矩阵执行 legacy/indexed/repeat 三路 hash，indexed 与 legacy 逐状态一致，repeat hash 一致，物资均为安全非负整数。

| 形状 | interval | 实体 / 线路 | indexed wall | 主要独占阶段 |
| --- | ---: | ---: | ---: | --- |
| 中期混合 | 4 s | 300 / 300 | 26.019 ms | logistics 13.407 ms |
| 生产重 | 4 s | 944 / 128 | 15.965 ms | production 5.498 ms、power 4.624 ms |
| 线路重 | 4 s | 2 / 2,500 | 10.642 ms | belts 3.099 ms；20,000 route checks |
| 物流重 | 4 s | 1,032 / 0 | 2,558.808 ms | logistics 2,384.550 ms；442,368 candidate checks |
| 完全堵塞 | 4 s | 600 / 1,250 | 52.523 ms | logistics 29.749 ms；10,000 route checks |
| 内容包 | 4 s | 380 / 500 | 25.790 ms | 自定义 recipe registry fingerprint |
| 速通 | 4 s | 380 / 500 | 25.373 ms | 固定 factory id / active clock |
| offline 快速边界 | 60 s | 300 / 300 | 108.988 ms | 五秒边界、36,000 route checks |

压力规模继续由 A/B、`terminal2x` 和 1,024 站物流形状承担。1h/24h/30d 长时门禁与快速矩阵分离：将 1 小时混合档放进每次三路快速测试会同步阻塞测试进程，不能用超时或强行缩短结果冒充长时通过。

### 6.4 profiler 开关扰动对照

两条持久 runtime 从同一规范化状态开始，先各自预热 2 秒，再按 off/on、on/off 交替顺序采集 7 个一秒样本。开启侧同时检查全部 profiler 字段为有限非负数；结束状态 gameplay hash 与关闭侧完全一致。

| 指标 | A | B |
| --- | ---: | ---: |
| profiler off 中位 | 304.431 ms | 105.195 ms |
| profiler on 中位 | 314.366 ms | 98.362 ms |
| 中位扰动 | +3.26% | -6.50% |
| on 侧已归因阶段中位 | 296.924 ms | 94.766 ms |

B 的负扰动是交替短样本中的调度噪声，不解释为 profiler 加速。较重的 A 档给出约 3.3% 的诊断成本；因此发布性能数字必须明确 profiler 是否开启，最终发布基线以关闭诊断为准。

### 6.5 1x / 4x / 11x pending debt 与不可让出段

每个倍率采集 3 次：当前持久 runtime 用一个完整请求执行；对照路径使用同一确定性 session、逐一秒边界推进。两路最终 hash 完全一致，`pendingSimulationSeconds` 与 `pendingWallSeconds` 均为 0。当前 Worker 请求在完成前不能接收后续命令，因此“最坏排队命令等待 P95”等于整请求 P95；确定性边界最大 P95 表示未来可安全让出的上界，不是当前 UI 已实现的响应时间。

| 样本 | 倍率 | 当前整请求 P95 | 确定性边界最大 P95 | 当前最坏命令等待 P95 |
| --- | ---: | ---: | ---: | ---: |
| A | 1x | 583.932 ms | 353.319 ms | 583.932 ms |
| A | 4x | 1,478.239 ms | 397.686 ms | 1,478.239 ms |
| A | 11x | 3,917.869 ms | 357.113 ms | 3,917.869 ms |
| B | 1x | 199.826 ms | 101.863 ms | 199.826 ms |
| B | 4x | 438.665 ms | 134.580 ms | 438.665 ms |
| B | 11x | 1,075.668 ms | 113.866 ms | 1,075.668 ms |

这组结果证明倍率不是“固定间隔的 1 秒 Worker 请求”：高倍率会把 4 或 11 个确定性秒边界放进同一个不可中断请求。M1 的 resumable session 必须只在完整边界提交权威状态并保留未完成 debt，才能降低命令等待且不改变结果。

### 6.6 production-preview 浏览器旅程与 painted acknowledgement

构建时显式设置 `VITE_RUNTIMEWORLD_DIAGNOSTICS=true`，普通开发/发布构建不包含可调用 bridge。bridge 还要求页面启动前已开启 runtime transition capture；它只把隔离 IndexedDB 副本的 1/100/1,000 条精确记录送入正式 `commitGame → command patch → Simulation Worker → Projection → React → 第二帧` 链，不改原件、不引入另一套模拟器。暂停、继续、结算确认和保存仍通过正式 UI。

两档使用同一个 production diagnostic build，均核对原件 bytes、mtime、SHA-256 未变；响应全为 Projection，full-state response 为 0，保存读回后 raw cache 为 0。

| 旅程指标 | A preview | B preview |
| --- | ---: | ---: |
| 进入工厂至 Worker active | 8,122 ms | 4,022 ms |
| 6 秒帧 P95 / P99 | 7.0 / 7.1 ms | 7.0 / 7.1 ms |
| 帧 max / Long Task max | 18.9 / 0 ms | 120.3 / 0 ms |
| 暂停第二帧 | 29.8 ms | 35.0 ms |
| 继续第二帧 | 12.5 ms | 源状态已运行，无单独继续动作 |
| 手动保存 UI 全程 | 5,587.8 ms | 2,488.5 ms |
| serialize / primary / backup | 755.0 / 1,802.6 / 1,949.4 ms | 287.0 / 601.4 / 602.2 ms |
| Projection / full-state response | 19 / 0 | 14 / 0 |

命令表中的“本地 mutation”只包含建立目标 GameState；“动作→post”包含当前最多约一秒的固定派发器对齐；“post→第二帧”包含 Worker、main merge、React commit 和两帧。运行中若恰逢另一个 slice，真实请求可合并该 slice 的 runtime 变化，因此 exact requested records 与线上 patch 的总 changed records 分开记录，不能把合并后的数量误报为玩家批量数。

| 命令 | A mutation | A 动作→post | A post→第二帧 | A 动作→第二帧 | B mutation | B 动作→post | B post→第二帧 | B 动作→第二帧 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| entity 1 | 0.7 ms | 424.5 ms | 628.4 ms | 1,052.9 ms | 0.5 ms | 381.1 ms | 235.1 ms | 616.2 ms |
| entity 100 | 1.0 ms | 444.1 ms | 511.9 ms | 956.0 ms | 0.4 ms | 427.7 ms | 162.1 ms | 589.8 ms |
| entity 1,000 | 1.8 ms | 496.6 ms | 516.7 ms | 1,013.3 ms | 0.9 ms | 460.4 ms | 277.4 ms | 737.8 ms |
| belt 1 | 2.0 ms | 509.5 ms | 826.9 ms | 1,336.4 ms | 0.3 ms | 471.8 ms | 164.6 ms | 636.4 ms |
| belt 100 | 1.6 ms | 544.9 ms | 825.7 ms | 1,370.6 ms | 0.6 ms | 509.7 ms | 247.9 ms | 757.6 ms |
| belt 1,000 | 1.7 ms | 560.2 ms | 762.3 ms | 1,322.5 ms | 0.7 ms | 514.0 ms | 199.5 ms | 713.5 ms |

首次探索若从“生产网络总览”逐行驱动 1,000 网络，B dev 会把全量网络 DOM 与重复 network setter 同时放大，renderer working set 观察到约 2.2 GiB 且数分钟不能返回。这不是正式 preview 数字，未混入上表；它作为 M5 大列表虚拟化和批量网络算法的独立缺陷保留。随后新增的显式 record batch primitive 用一个 ID set 和一次 belts 遍历，216 项 engine 测试通过，未选记录保持引用，重复设置返回原状态。

以下项目仍在进行中，当前不得给出 M-1 Go：

- 1 小时/24 小时/30 天确定性基线。
- Chrome CPU trace、自动保存 trace，以及 Worker/renderer/GPU/进程载入、运行、命令、保存、强制 GC 与 30 分钟趋势。
- 最终 Go/No-Go 与正式干净开发 SHA。
