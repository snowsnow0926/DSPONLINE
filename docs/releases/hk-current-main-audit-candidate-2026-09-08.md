# 香港当前主云档审计增量候选（未部署）

基于生产 API 源提交 `df828869e276e5d3a67513095a4d1c93d13c500f`，工作树 `D:/GameDev/DSPidle2-hk-presence-fix`，分支 `codex/hk-presence-audit-20260908`。这是香港玩家计数维护过程中发现的独立性能候选，不是已发布修复。

现役代码在每次云正文提交后重新检查所有用户的当前普通主档。候选保存每个账号的内存审计结果，启动时完整重建，提交后只检查 writes/fileWrites/deletes/userDeletes/replaceUserPayloads 涉及的账号，然后聚合原有公开计数。数据库、校验规则、云存档内容、API 响应字段、schema/layout 均不变；失败事务不发布缓存，审计异常清空缓存并暴露 unavailable，下次重新建立。

新增隔离测试证明：更改单个账号时不读取其他账号的 2 MiB legacy 正文；新增修订、缺失正文、修复正文、删除账号的汇总与原完整审计完全一致；事务故障保持旧审计；presence 不触发正文审计；重启重建一致且保留心跳。完整 server **386 通过 / 2 跳过 / 0 失败（388）**，station **4/4**，专项 recovery **7/7**。

未部署原因：现场有周期性磁盘等待和 API 超时，但短时、45 秒和 90 秒采样没有捕获足够的慢调用栈。已观测当前 883 个主档中 legacyDirectRows=0，不能把 legacy 扫描直接断言为本次生产延迟根因。本候选有独立的减少重复检查价值，尚须捕获实际慢上传并在完整生产备份的隔离副本复核，不应仅因存在代码优化就发布。

待发布前还需：完成变更审查、冻结 clean SHA、构建独立 API manifest、Linux 候选测试、完整备份副本和保护字段验收、稳定交接代理 dry-run 与明确的 API-only 发布交接。禁止热改现役文件。本次香港实际变更仅为恢复 Nginx `/api/presence` 路由，现役 API 继续 1.2.6，Web 继续 1.2.7。
