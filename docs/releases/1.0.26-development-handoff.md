# 1.0.26 亮色主题、设置与交互优化开发交接

```yaml
handoffSchema: dspidle-development-handoff/v1
role: develop
developmentStatus: complete
releaseTarget: 1.0.26
releaseReady: true
releaseStatus: authorized-for-release
releaseAuthorization: granted-by-user-2026-08-04
sourceCommit: 93abb7649bac752f4e793ba02ae30833feb062e6
sourceVersionAtCommit: 1.0.25
releaseCommit: resolve-from-final-clean-manifest
immutableArtifacts: pending-release-role
releaseManifest: pending-release-role
lastVerifiedDate: 2026-08-04
```

本交接确认开发内容已从原始 dirty 工作区机械隔离到独立 worktree。原工作区中的离线计算、Worker、存档及其他未提交改动没有被清理、覆盖或带入本候选。用户已明确授权 Release 角色发布香港、上海 Web/API、上海下载页及 Android/Windows 更新。

## 任务摘要

- **Task ID / title**：`UI-THEME-SETTINGS-20260804 / 亮色模式、设置页面、科技树滚轮与物品悬浮交互优化`
- **Priority**：主题与设置信息架构为 P1；科技树滚轮、版本历史和物品引用交互为 P2。
- **Target platforms**：香港 Web/API、上海 Web/API、Windows Electron、Android WebView、上海下载页。
- **Compatibility**：GameState v46、存档 envelope v2、云 schema v7、SQLite layout v2 均不得变化。
- **Data boundary**：主题、运行记录显示与设置分类使用设备级 localStorage，不进入 GameState、云存档、排行榜或确定性状态哈希。

## 实现范围

1. 统一亮色/深色语义主题，覆盖工作区、节点、线路、弹窗、表单、移动端以及禁用、选中、危险和焦点状态。
2. 设置页改为分类总览和二级页面；主题、运行记录显示和设置分类作为设备级偏好保存。
3. 版本更新记录补齐 1.0.0 至当前版本，支持离线分页、详情查看以及返回后的页码和滚动位置保留。
4. 科技树区域滚轮统一转换为横向移动并阻止页面纵向滚动穿透。
5. 物品悬浮卡缩小触发范围，支持鼠标、键盘焦点、移动端点击/长按以及“定位”和“打开图鉴”。

## 代码来源与隔离证明

- 功能提交：`93abb7649bac752f4e793ba02ae30833feb062e6`。
- 独立分支：`codex/release-1.0.26`；独立 worktree：`D:\GameDev\DSPidle2-release-1.0.26`。
- 12 个完整转入文件与原开发工作区 SHA-256 一致。
- `src/App.tsx`、`docs/ARCHITECTURE.md`、`docs/GAMEPLAY_SYSTEMS.md`、`docs/PROJECT_STATUS.md` 只接收本任务 hunks；明确排除 simulationBudget、offlineApproximation、多 Worker 和离线加速内容。
- 功能提交修改 16 个文件，新增 1,014 行、删除 45 行；没有修改 `src/game/types.ts`、`src/game/storage.ts`、`src/game/engine.ts`、`server/index.mjs` 或生产部署模板。

## 开发验证

| 命令 | 当前隔离候选结果 |
| --- | --- |
| `npm run typecheck` | 通过，0 个 TypeScript 错误 |
| `npm test -- --run` | 79 个测试文件通过、1 个跳过；698 项通过、9 项跳过、0 失败 |
| `npm run build` | 通过；Vite 转换 1,872 个模块 |
| `npx playwright test tests/e2e/v31-workspaces.spec.ts --workers=1` | 6/6 通过 |
| `git diff --check` | 通过 |

开发阶段的 Playwright 仅记录本地 `127.0.0.1:4320` 云服务未启动时的代理错误，不涉及生产服务。用户先前给出的 710 项通过、12 项跳过来自包含其他开发内容的 dirty 工作区，不能作为本隔离候选的发布门禁。

## Release 门禁

1. 从版本化后的最终 clean SHA 重新执行完整发布矩阵并生成不可变 manifest；不得复用开发阶段 `dist`。
2. Android 只能使用既有批准长期证书，versionCode 为 `1000026`；无法读取同一证书时立即停止 Android 发布。
3. Windows 沿用历史 `NotSigned` 测试包策略，不创建、替换或伪造证书。
4. 两节点切换前分别核对 live symlink、服务、Nginx、磁盘与回滚指针，并使用 Backup API 创建和验证 SQLite 快照。
5. Web/API 和下载页只上传到新 release 目录，先做依赖、manifest、备份副本隔离启动和签名/清单校验，再执行原子切换。
6. 发布后验证 Build ID、API health、systemd active/NRestarts、云存档只读兼容、hashed asset、完整下载哈希、Range、缓存和上海独立服务边界。

## 已知风险

- Windows 安装程序仍无 Authenticode 证书，必须在下载页明确显示未知发布者/未签名测试包提示。
- 当前没有物理 Android 或 iPhone 30 分钟温度、耗电和帧率证据；模拟器或 Chromium 触摸仿真不能替代该长期门禁。
- 亮色主题与交互修改跨越大型 `src/styles.css` 和 `src/App.tsx`，正式候选必须完成全量 Playwright，不能只用 6 个专项用例替代。
- 回滚只切换 Web/API 与下载目录，不恢复或替换数据库。
