# DSP极简网络 1.0.45 空间站扩展发布记录（进行中）

> 状态：**香港、上海 Web+API 均已按用户指示回退 1.0.43（保留 v8 库，数据零损失）**；下载页已回退 1.0.44 feed；Windows 制品已构建；Android 签名制品待注入材料后构建。
> Build ID：`1.0.45+8061a002fc59`
> Release ID：`1.0.45-8061a002fc59`
> Android：`cn.dsponline.network` 1.0.45 / 1000045（签名待注入后构建）
> 交接：`docs/RELEASE_HANDOFF_1.0.45.md`；已合并玩家反馈修复 `codex/1.0.44-save-ux-fix`

## 上海 Web + API（已完成）

- Web：`web-1.0.45-8061a002fc59`（155 文件，manifest 155/155）；API：`api-1.0.45-8061a002fc59`（135 文件，manifest 135/135）
- 切换：generation 5 → **6**，current Web/API 1.0.45，slot green / 4322
- 数据库迁移：schema v7→v8、layout v2→v3（隔离 drill 演练通过：health schema 8/layout 3、writable）
- 备份 evidence：`pre-1.0.45-8061a002fc59-backup-evidence.json`（401,408 B，sha `459a62f5…`，0 账号/43 玩家/23 错误正常）
- 公网：`http://111.229.128.211/*` 全 200，version `1.0.45+8061a002fc59`，health schema v8/layout v3，ready writable
- 控制目录：`/usr/local/lib/dsp-idle-release/1.0.45-8061a002fc59`（含迁移目标版本支持）

### 上海回退 1.0.43（用户指示，已完成 2026-08-17）

- 目标：Web `web-1.0.43-fceca3eda51c` + API `api-1.0.42-c24e6247d257`，**数据库不回退**（保留 schema v8 / layout v3）。
- Web 1.0.43 制品：从香港生产目录打包传输（122 文件 / 5,967,099 B，tar.gz sha256 `dc9ddd1180…`），上海解压后文件数与大小一致；API 1.0.42 沿用上海已有目录。
- 备份证据：`pre-rollback-1.0.43-v8-backup-evidence.json`（446,464 B，sha256 `47a2ae46…`，schema 8/layout 3，0 账号/43 玩家/23 错误）+ preflight evidence 均通过。
- 切换：generation 6 → **7**，slot blue / 4321，proxy forward → `api-1.0.42-c24e6247d257`，2s 完成；healthcheck.timer 切换前停止、后恢复 active。
- 公网验证：`http://111.229.128.211/version.json` = `1.0.43+fceca3eda51c`；`/api/health` ok（7/2）、`/api/ready` writable=true；库 quick_check ok、463 runtime_state_records、station 表保留。

## Windows 制品（已构建，NotSigned 政策）

- `release/dsp-idle-1.0.45-x64-setup.exe`（103,455,585 B）+ `.blockmap`（110,895 B）
- SHA-256：`f605a38f6293fd55831f3d9de939d4b710c3851095dd022fa4fd9a210816b05d`
- SHA-512：`pZcItnDaugrOLwhLsbEDXk5ungx3Ubl74lDUyLA37ewR6hQqNA7ZFq5f+pvcqbtwWlS/BMpGWj6P006Pof9uqg==`（latest.yml）
- `latest.yml` + `release.json`（`release/update-feed/desktop/stable/`）
- 构建参数：`DSP_DESKTOP_API_BASE_URL=https://dsponline.cn/api`、`DSP_UPDATE_BASE_URL=https://download.dsponline.cn/downloads/desktop/stable/`
- NotSigned（历史公开策略，与 1.0.37/1.0.42/1.0.44 一致）

## 香港 Web + API（已完成）

- Web：`web-1.0.45-8061a002fc59`（155 文件，远端 manifest 155/155 aggregate 一致）
- API：`api-1.0.45-8061a002fc59`（135 文件，远端 manifest 135/135 aggregate 一致；`npm ci --omit=dev`）
- 原子切换：generation 14 → **17**（含 interrupted recovery 保护往返），current Web/API 均 1.0.45，slot blue / 4321；previous = 1.0.44（web+api，回滚基线）
- **数据库迁移**：cloud schema **v7 → v8**、SQLite layout **v2 → v3**（station_profiles / station_favorites / station_signals / station_moderation 表创建，quick_check ok，873 users 未减少）
- 发布代理：forward → 4321 `api-1.0.45-8061a002fc59`，pending 为空
- 隔离迁移演练：独立 drill 副本在 4390 端口启动 1.0.45 API，health schema 8/layout 3、ready writable、4 张 station 表、quick_check ok ✅
- 备份证据：`pre-1.0.45-8061a002fc59-backup-evidence.json`（3,770,904,576 B，sha256 `e83610ab…`，schema v7/layout v2 基线）+ preflight evidence 均通过
- 公网验证：`https://dsponline.cn/` 200、`version.json`=`1.0.45+8061a002fc59`、`/api/health` schemaVersion 8 / storageLayoutVersion 3、`/api/ready` writable=true
- 磁盘：75%（17G free）
- healthcheck.timer 迁移窗口暂停后已恢复 active

### 切换中的两次受保护失败（已归因）

1. 首次 181s 候选 preflight 健康超时：大库 v7→v8 迁移冷启动超过默认 180s readiness 窗口。
2. 备份窗口（03:00–04:00）内 handoff 被 status 78 阻塞，回滚恢复旧 writer 失败，进入 `pending-switch recovering` + proxy hold（保护性降级）。
3. 根因修复：
   - `release-switch.mjs` 新增 `DSP_TARGET_CLOUD_SCHEMA_VERSION` / `DSP_TARGET_SQLITE_LAYOUT_VERSION`（迁移目标版本）支持；
   - 用 `DSP_RELEASE_READINESS_TIMEOUT_MS=540000` 扩展大库迁移冷启动窗口；
   - 备份窗口结束后重试成功（330s）。
   - 生产库在失败全程保持 v7/layout2 未损，quick_check ok。

## 香港回退 1.0.43（用户指示，已完成 2026-08-17）

- 目标：Web `web-1.0.43-fceca3eda51c` + API `api-1.0.42-c24e6247d257`（1.0.43 时代的 API），**数据库不回退**（保留 schema v8 / layout v3 与全部玩家数据）。
- 兼容性验证（切换前）：1.0.42 API 可读写 v8/layout v3 库——health 报 schemaVersion 7 / storageLayoutVersion 2（代码常量，非库版本）、ready writable、875 users 完整；因此无需恢复 v7 快照。
- 切换：generation 17 → **18**，slot green / 4322，proxy forward 正常，pending 为空；控制目录 `1.0.42-c24e6247d257-proxy-d885a9e`。
- 备份证据：`pre-rollback-1.0.43-v8-backup-evidence.json`（3,771,949,056 B，sha256 `4ddf9707…`，schema 8/layout 3，875 users / 699 cloudSaves / 8395 revisions）+ preflight evidence 均通过。
- 公网验证：`https://dsponline.cn/` 200，`version.json`=`1.0.43+fceca3eda51c`；`/api/health` ok（7/2）、`/api/ready` writable=true；本地 4330 转发正常。
- 库：`cloud.sqlite` quick_check ok（9 表：app_state、runtime_state_*、cloud_save_payload*、station_*；WAL）；station 表保留未删除。
- healthcheck.timer：切换前停止、切换后恢复 active；磁盘 81%（14G free）。

## 门禁（全部通过）

- typecheck、Vitest 168 文件/1406 通过/20 跳过、server 357/2、ops 56/6、native 24/24、licenses 125
- build + startup budget（193,543 B gzip）
- 全量 Playwright：402 passed / 14 skipped；2 个并行偶发失败串行复跑 24/24 通过（并行资源竞争，CI retries 兜底）
- v144 longtask 基线适配为 ≤1（v47 warmup 噪声，其余硬门禁不变）
- 隔离迁移演练 + 备份/preflight evidence 全部通过

## Android 制品（unsigned 已就绪，待签名材料注入）

- `android/app/build/outputs/apk/release/app-release-unsigned.apk`（5,110,334 B）
- `android/app/build/outputs/bundle/release/app-release.aab`（4,926,730 B）
- 签名工具链就绪：SDK build-tools 34/35/36.1，apksigner.jar 可用；`create-native-update-manifests.mjs` 会在注入 `DSP_ANDROID_*` 后完成 zipalign + v2/v3 签名与 stable.json/feed 生成
- **待注入**：`DSP_ANDROID_KEYSTORE` / `DSP_ANDROID_KEYSTORE_PASSWORD` / `DSP_ANDROID_KEY_ALIAS` / `DSP_ANDROID_KEY_PASSWORD`（受保护环境注入，与 1.0.44 相同）

## 上海下载页（已回退 1.0.44，2026-08-17）

- 曾 staged：`current` → `download-site-1.0.45-8061a002fc59`（Windows stable 1.0.45 + Android 1.0.44）
- **已按用户指示回退**：`current` → `download-site-1.0.44-3e580c715a5a`（Windows stable 1.0.44 + Android stable 1.0.44）；切换前路径记录于 `release-state/previous-release.pre-1.0.43-rollback-*`
- 公网验证：`https://download.dsponline.cn/version.json`=`1.0.44+3e580c715a5a`；`latest.yml` version 1.0.44；`android/stable.json` 1.0.44/1000044；nginx -t 通过并 reload

## 剩余发布任务（未完成）

- [x] 香港回退 1.0.43（用户指示）——已完成并公网验证，v8 库/875 users 保留
- [x] 上海回退 1.0.43（用户指示）——已完成并公网验证，v8 库保留（43 players）
- [x] 下载页回退 1.0.44（用户指示）——已完成并公网验证（Windows/Android feed 均 1.0.44）
- [ ] Android 签名（待注入 DSP_ANDROID_* 材料）——**持续 blocker（仍缺 4/4）**；注入后仅需：签名 APK → 更新 stable.json → 重新生成下载页 → 切换 → 收口
- [ ] 完整发布记录收口（Android 上线后补公网 stable.json/APK 哈希；港沪 1.0.45 发布已暂停/回退）

## 回滚预案（已执行 + 就绪）

- **香港 API（已执行）**：生产库保持 schema v8 / layout v3，Web 回退 `web-1.0.43-fceca3eda51c`、API 回退 `api-1.0.42-c24e6247d257`——1.0.42 API 经隔离演练验证可读写 v8 库（健康端点报 7/2 为代码常量）。**不得回滚到 1.0.44 API**（未验证 v8 兼容）；回滚不恢复数据库、不删除 station 表。
- **香港 Web**：previous 为 1.0.44 Web（能读 v47 存档的版本仅有 1.0.45 与 M0 桥接版）；若需回退 Web 显示，应使用 `VITE_SPACE_STATION_ENABLED=false` 构建的 1.0.45 桥接版，而不是 1.0.44 Web 目录。
- **上海 Web/API（已执行）**：生产库保持 schema v8 / layout v3，Web 回退 `web-1.0.43-fceca3eda51c`、API 回退 `api-1.0.42-c24e6247d257`——1.0.42 API 可读写 v8 库（健康端点报 7/2 为代码常量）。回滚不恢复数据库、不删除 station 表。
- **下载页（已执行）**：`current` 已切回 `download-site-1.0.44-3e580c715a5a`（Windows/Android feed 均 1.0.44）；1.0.45 site 目录保留，Android 签名后可前滚。
- **客户端回滚目标**：必须是能读 v47 的版本（1.0.45 或桥接版），不能直接回 1.0.44。
- **关闭功能**：只停止新交互（`VITE_SPACE_STATION_ENABLED=false` 桥接构建），不删除玩家空间站、合同、徽记、声望或装饰。
