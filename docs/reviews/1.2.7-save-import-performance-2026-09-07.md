# 1.2.7 保存、导入与 Rust 离线证明开发记录

Role: develop

日期：2026-09-07。候选运行源码：`2b1df8752082b509effffc8d75b00084ee6bb697`；Build ID：`1.2.7+2b1df8752082`。本记录不代表正式发布；当前线上基线仍为 1.2.6。

## 本轮交付

- `b6653a49`：保存时合并同一状态 JSON 的 UTF-16 校验和与 UTF-8 长度扫描，输出字节不变。
- `7639e0b9`：迁移时只在实体实际带有旧 `quantumTarget` 字段时建立删除字段的中间副本；最终实体、输入/输出库存与位置仍独立正规化，不借用可变原始实体。
- `9b38ca37`、`20b9452b`：Rust 将符合窄约束的二级量子上传塔识别为转运缓冲。仍须通过三个 exact 窗口、全域库存收支、无环配方、有限矿扣账和容量证明。新增 7 项参数化用例覆盖 600 秒材料账、预填库存、重复/分段哈希、回路与替代生产者拒绝。
- `64b61886`：本次主档通过持久读回后，自动快照复用同一份状态 JSON，只重建快照时间、类型、原因及整体校验；规范头尾或校验不符时回退原序列化。自动快照仍独立分配键、检查容量和配额、写入、读回；手动快照和失败时已确认主档的成功状态保留。
- `c635507b`：UTF-8 文本校验改用局部累加器，移除逐字节闭包调用。固定合成 ASCII、CJK、emoji、混合代理项各 7 对函数基准全部相等且更快；函数阶段中位下降 20%–52%，不将此比例外推为游戏收益。
- `2b1df875`：把重封装的原/新文本校验移到短时 `save-snapshot-rewrap` Worker，只传原文、证明和信封字段。返回可转移字节后，主线程仍校验完整字节及状态校验和、快照时间/模式/槽位/原因；30 秒超时、错误或不匹配回退原序列化。没有 Worker 的旧环境保留同步兼容路径。

保存与迁移改动属于共享 TypeScript 路径，Web/PWA、Android 和 Windows 构建都能使用；本轮实包收益只测 Windows。Rust 修复只推进算法资格，未打开超过 30 秒的自动采用，也未打开实时玩家权威。

## 实测口径

真实终局档由用户明确提供并授权本地测试，原件及只读登记副本不进入 Git、公开包、截图或日志正文。文件为 107,637,967 B，110,042 实体、233,300 线路；v47 / envelope v2。每个旅程使用独立临时 profile，禁用生产网络，正常退出后复核源文件大小、mtime 与 SHA-256。

### 保存扫描

固定 4 份公开合成状态，基线 `9948322e` 的实际源码与当前实现交错 AB/BA；每份每阶段 9 对，3 个独立 Node 进程，完整 envelope bytes、状态校验和、payload 校验和与字节长度逐次相等。约 7.5–8 MB 的 32k 实体合成状态，完整序列化函数中位耗时下降 5.1%–10.8%；小 Unicode 样本一次出现 3.1% 回退。该数字只描述函数阶段，不能表示全应用或所有尺寸的稳定提升。

### 真实档检查诊断

Node 的完整 `inspectSave` 函数墙钟初测约 1,542 → 1,505 ms，5 对同输入结果 JSON SHA-256 全相同。2.4% 幅度小且仅一个进程，本记录不把它判为稳定性能收益；CPU profile 只用于定位重复实体复制。它不包含 Worker 克隆、读盘、UI、离线或持久化。

### 实包导入

范围为真实 UI 选择文件至正常工厂可打开运营中心，另验证 IndexedDB 的主档/自动快照正文 SHA-256 和正常退出。每次全新 Electron/Host 进程与 profile，基线来自未改写的 `4b13948c8d6d` 包；所有样本使用同一源文件与测试 savedAt，A/B 顺序为 AB、BA、AB。采样时不并行构建或测试。结果是该机器上 3 对样本的描述性结果，不是跨硬件保证或 P95。

| 配对 | 基线导入 | 候选导入 | 基线最长主线程任务 | 候选最长主线程任务 |
| --- | ---: | ---: | ---: | ---: |
| 1（AB） | 22,916 ms | 16,681 ms | 1,828 ms | 1,027 ms |
| 2（BA） | 22,897 ms | 17,065 ms | 1,873 ms | 1,054 ms |
| 3（AB） | 21,525 ms | 16,406 ms | 1,800 ms | 1,042 ms |
| 中位数 | **22,897 ms** | **16,681 ms** | **1,828 ms** | **1,042 ms** |

完整导入等待缩短 **27.1%**，最长主线程任务中位缩短 **43.0%**；主线程长任务累计时长中位从 12,220 → 8,019 ms（下降 34.4%）。三对方向一致。6/6 完整主档 envelope SHA-256 相同，主档与自动快照状态正文 SHA-256 全相同，源档未变、renderer 错误 0、正常退出 6/6。旧包每次两次状态序列化；新包每次一次状态序列化加一次文本重封装 Worker，无回退重序列化。

资格：**VERIFIED_GAIN（该真实档的 Windows 实包导入范围）**。它不是完整启动、离线生产计算、实时模拟或 FPS 提升。Web/Android 复用的是同一份保存代码，本轮没有这两个平台的实机收益数字。

此前同步重封装包 `d462080e` 的 3 对虽然内容校验全部相同，导入等待中位仅下降 5.9% 且配对方向不稳定，最长主线程任务中位反而从 1,899 → 3,276 ms。因此该包被拒作性能交付并完整保留为回退证据；最终结果来自后续 Worker 修正版的新 6 次采样，未混用旧样本。

### 真实档返回

使用已正常保存退出的私有 profile 的副本，设置 600 秒返回边界；存档另有约 2 秒待恢复真实时间，实际 Worker 请求约 602 秒。现有复杂度保护进入 conservative 路径，约 9 秒 Worker 处理后返回 `decision-required`。驱动确认该决定并正常退出，未选择零收益或将候选写成结算结果。它不是 Rust 完成，也不是 RP1 完整收益 PASS。

## 新鲜验证

- 初次三项产品改动：完整 Vitest 3,066 通过 / 29 条件跳过；完整 Chromium 442 通过 / 27 条件跳过。
- 自动快照增量后：完整 Vitest 3,072 通过 / 29 条件跳过；随后补读回不符用例，所属文件 5/5；保存/导入/离线/检查点/持久化 Chromium 专项 94 通过 / 18 条件跳过。首次专项命令因 Windows `.cmd` 管道参数引用失败，未启动测试；使用 Node CLI 重跑通过。
- 最终 Worker 与文本校验增量后：完整 Vitest **3,092 通过 / 29 条件跳过（390 文件通过 / 13 文件条件跳过）**；上述 Chromium 专项再次 **94 通过 / 18 条件跳过**；Worker/保存定向 134/134，类型检查通过。
- Rust workspace：Core 1,064 通过 / 3 ignored、Host library 241 通过、Host binary 3 通过；fmt 与严格 Clippy 通过。首次 Clippy 提示布尔表达式可简化，等价改写后重跑通过。
- 原生启动传输/保护专项 6/6；typecheck、Web build、Desktop build、startup budget、native coverage、thin-UI boundary 通过。
- 最终 Windows 包旅程 **8/8**（保存中关闭、制造生产、立即重开、崩溃恢复、压缩导入导出、坏档拒绝、网络隔离）；实际 `NotSigned`，冻结 source/Build ID 与 ASAR/Host 文件清单一致。最终测试后包进程残留为 0；原始下载文件与只读登记副本均保持原 SHA-256。

真实档初次导入 driver 因异步启动公告遮住背景确认按钮超时；增加真实公告关闭操作后成功。首次返回 driver 错将初始决定对话框固定为 alertdialog；修正为两种实际 role 后，正确记录 decision-required。失败记录原样保留，均未计入收益配对。所有失败均正常退出、源档未变。

## 资格与下一步

GameState 47、存档 envelope 2、cloud schema 8、SQLite layout 3 不变。新 Windows 包为未签名、离线性能开发候选，不连接生产或更新源，未推送或部署。

候选 ZIP：`artifacts/1.2.7-save-import-final-20260907/DSPONLINE-1.2.7-performance-2b1df875-unsigned-offline.zip`，**157,302,474 B**，SHA-256：`93e2d2bf339122e569ae59a1a7c8c2c9302ac793ae8f23913b481725e214472b`。归档独立解包后，75 个文件按冻结构建证据逐个验证通过；不包含玩家档或测试 profile。同目录 `delivery-summary.json` 记录范围、身份、测试和结果，完整私有配对元数据仍留在本机 ignored 的 RP1 证据目录。

RP1 完整离线流程仍未完成：下一步应围绕此真实档触发的复杂度/内存保护，测量低内存单候选准备、结算和最终保存，并用公共目录与 JS 对账补齐 Rust 长时证明。当前量子上传正反例使用受控目录，不能外推到真实档全部玩法。只有完整返回路径、库存/进度一致和候选持久化都通过后，才考虑扩大 Rust 自动采用时长。

重封装 Worker 仍需扫描原、新字符串并可能展开字符串，返回端也保留字节校验与解码，不能称零拷贝、O(1)、零主线程长任务或 FPS 提升。若原主档序列化未提供 summary，既有完整解析后备路径仍存在。
