# 1.2.7 Rust 普通离线完整状态一致性

Role: develop

## 结果与身份

冻结运行源码 `e9db8de40fec` 已使原公共六例完整状态由 **3/6 提升到 6/6**；连同小数时钟、尾段余数和旧历史场景，**12/12 完整 canonical 相同**。其中九例使用新宏证明，三例仍为触界 Exact 回退。另两项拒绝保护通过，热继续与检查点重开后的继续推进也与 JS 相同。

公共铁链的三个独立配对中，相较 JS 逐秒计算加规范校验，十分钟和一小时结算的内核耗时中位分别缩短 **46.4% / 88.5%**。此为合格铁链的内核测量；RP1 真实大档完整等待尚未验收，超过 30 秒自动采用与实时权威继续关闭。本轮未推送、未发布。浏览器原失败项已全部复验通过，Windows ZIP 独立读回通过；详细来源见下方验证和制品记录。

本阶段接续[离线生产边界阶段](./1.2.7-rust-offline-boundaries-2026-09-07.md)，处理其普通宏路径剩余的累计传输、量子运行记录、生产历史和时钟差异。上一阶段完整状态相同的三例使用有预算的 Exact 回退；该结果不能替代本阶段宏路径的全状态验收。

| 身份 | 值 |
| --- | --- |
| 分支 | `codex/1.2.7-rust-offline-state-parity` |
| 接手源码 | `3501fa58e19d4452a16acd6fa79dc612d025ecb1` |
| 冻结基线运行源码 | `ab01bb781ddde2dcee199e72c918951d9087f12e` |
| 基线 Native Host SHA-256 | `7efb5802c1ab8dcd8f342203c3d5decf78c8cd0d813fbdca4360a6b0c5202cb6` |
| 本轮离线算法标识 | `native-offline-macro-v1-closed-ledger-one-shot-v3-state-parity` |
| 初次实现提交 | `8f026f808e7eba8abc41e9fb061bbd9caeb4daa5` |
| 最终运行 / 公共与桌面驱动源码 | `e9db8de40feca1319cd112d0e9174a9831fae474` |
| Web 复验驱动 | `268faa9abf5ed7819cb88f43250a127e89854297` 增加三个 E2E 文件的 17 行等待/报告处理；`0d9053b80b47bef76c2bbc411cabc36c2a1ee08a` 再固定一个精确预算夹具的 Date，共 19 行测试修改，产品源码不变 |
| Build ID / 发行属性 | `1.2.7+e9db8de40fec`，performance / beta / offline，实际签名检查为 `NotSigned` |
| 最终 Host SHA-256 | `b9ae637573737191e92938883427e5e80d765bfca4e61ac667022894bea3f77c` |
| 兼容边界 | GameState 47、envelope 2、cloud 8、SQLite 3 不变；`PureIdleMacroV10` 原路径和算法身份不变 |
| 自动采用 / 发布 | 超过 30 秒的自动 Rust 离线采用与实时 Rust 权威资格继续关闭；本报告不构成推送、部署或公开发布记录 |

证据上下文为 `artifacts/rust-performance/active-rp1-parity.json`，本轮证据根为 `artifacts/rust-performance/RP1-state-parity/2026-09-07T13-59-22/`。基线可执行文件位于上一阶段证据目录，接手源码、运行源码与二进制 SHA 分别保留。

## 普通尾段的严格资格

[父流程](../../native/dsp-native-core/src/pure_idle.rs) 保留真实 30 秒 Exact 校准和原普通物料证书。在物料宏写入前准备额外的 flow、history 和无出口时钟证明；任何证明失败都返回 unsupported，丢弃整个候选。不能先对外推状态补跑 Exact，再把重叠时间算作已正确完成。

[Flow 模块](../../native/dsp-native-core/src/pure_idle/offline_flow.rs) 只处理已获证、未触及容量或矿量边界的窄普通链：铁矿源、铁块冶炼、非燃料风力供电及纯上传的二级量子站。活动配方限定 `iron_ingot`，正生产项限定 `iron_ore` / `iron_ingot`；铁块生产要求 `smelt_iron` 同时存在于已完成和已发奖任务记录。已有但与该产线无关的未完成任务不能仅凭短探针被解释为新获支持的任务系统。

量子站须没有需求槽、传统运输工具、活动航线或模式切换，上传物品与实际缓存闭合。传送带端点须无歧义；共享输入、分流、循环或未知运行字段均不借用此资格。科研、手工、施工、戴森发射、外贸、轨道合同、探索任务、空间站自动工作、活动建设计时及启用的时间加速等独立写入者不进入本阶段的新证明范围。完整资格仍以最终源码和差分矩阵为准。

物料投影后，原 campaign 同步及轨道站资格同步还须在局部副本上完全不写入；出现新完成、奖励或解锁立即拒绝，副本不发布。限定范围内产量单调增加，其余任务指标和库存托盘固定，因此端点无写入检查排除尾段跨任务阈值。建设活动显式拒绝，戴森同时扫描实际各轨道的帆存量，不依靠单个聚合字段推断生命周期停止。

公共存档预建的八星系空间站目录可以保留：必须是已知结构、目录系统 ID 匹配、`not-started`、零进度、空物料/策略/路由及零模块的记录。整个目录仍参与物理签名，不能删除字段让样本通过。实际建设、库存、模块、未知结构以及星际舰队、未来返航队列均拒绝。

额外探针在私有状态上执行 **30 次真实的一秒 Exact**，与原校准分开计数。每一秒的物理实体、输入输出、机器进度和普通遥测均须与前缀保持相同；仅按原证书解释的生产累计、量子库存和有限矿扣账可以归一化。它不是只检查十秒末端相同的周期猜测。因此非整十秒尾段不能在错误的物理相位上套用平均产量。

传送带逐秒累计增量必须稳定且非负，最终计数先校验整数溢出。量子 `runtimeFlow` 的完整已知结构、上传/下载项、刷新边界和十秒内相位通过三个真实周期核对；不以最后一帧速率乘总时长代替累计传输，也不只改一个时间戳隐藏其余字段差异。证明绑定源 canonical、revision、检查点身份、不可变 catalog 实例和请求尾长，不能跨存档或跨目录复用。

## 历史与时钟

[History 模块](../../native/dsp-native-core/src/pure_idle/offline_history.rs) 复用同一组 30 次真实探针的末秒历史样本，不再独立模拟另一套工厂。首十秒保留实际样本中的旧诊断缓存；第二、第三个十秒周期必须逐相位完全一致，才允许重放后续序列。首周期未刷新时的旧库存或效率不能被稳定末值提前覆盖。

每个未来样本重新通过[原历史边界函数与 append/compact 实现](../../native/dsp-native-core/src/production_history.rs)，保留原有历史、采样时长、十秒诊断刷新、加权合并顺序，以及 1 / 10 / 60 秒分辨率和保留窗口。小数时钟造成的实际浮点采样时长也按原记录器产生，不能一律写成 `1`。未来刷新相位与证明不符时拒绝。历史先在局部数组完整构建，成功后只写候选的 `productionHistory`、`historyRecordedAt`，并使旧的私有历史投影缓存失效。

[共享时钟入口](../../native/dsp-native-core/src/simulation.rs) 使用真正 Exact 步骤调用的 `exact_elapsed_after_step`，避免另写四位小数规则。无新增出口证明在最多八小时范围内逐秒重放标量窗口，保留首个窗口已有 amount，之后才进入零新增出口窗口；返回经过证明的最终 elapsed 供父流程提交。循环不扫描实体、不在每步写入持久 JSON。

窗口证明只接受无物理出口建筑、明确的 `building` 模式，或 `legacy-network` 且显式关闭自动派发。未知模式或可能继续派发的 legacy 状态拒绝。一般宏的源时钟、量子边界和历史相位还必须同时通过各自更窄的证明；单独窗口 helper 接受某个大时钟不代表整个宏已经支持该输入。已有 Exact 回退继续保留其真实 elapsed，不再被父流程的一次浮点加法覆盖。

## 预算与隔离

| 边界 | 当前实现限制 |
| --- | --- |
| 新宏尾段 / 原请求 | 尾段为 1–28,800 个整数秒；组合后的无出口路径要求原请求为整数且不超过八小时 |
| 额外真实探针 | `30 × max(1, 实体数 + 传送带数) ≤ 60,000`，在复制探针状态前检查 |
| 探针内存 | 原始记录不超过 8 MiB、估算运行内存不超过 32 MiB；探针推进后继续复查 |
| 共享历史样本 | 30 个真实样本序列化累计不超过 2 MiB，不保留 30 份完整 CoreState |
| 新宏源时钟 | 四位小数规范起点，逐秒时钟与量子相位一致，且不超过 1,000,000,000 秒；异常或精度漂移拒绝 |
| 累计流量 | 非负整数，投影后不超过安全整数上限，所有乘加先检查 |
| 既有触界 Exact 回退 | 原请求不超过八小时，`ceil(请求秒数) × max(1, 实体数 + 传送带数) ≤ 4,000,000`；超预算拒绝 |

本阶段仍存在候选复制、探针状态复制、规范签名构建、历史样本复制及逐秒压缩。历史重建的目的在于避免每秒扫描整厂，**不声明零拷贝、O(1) 历史重建或所有边界保留宏速度**。窗口标量重放与历史压缩均计入端到端性能测量。

物料、元数据、历史及窗口全部在私有 candidate 上完成，最终总账校验通过后才安装源状态。预算、时钟、证明绑定、未知字段、缓冲漂移或溢出失败均须保留源 revision、完整状态及持久检查点。取消、失败和未获资格不能被统计为完成的离线结算。

本阶段输入只使用公共目录和合成夹具，**未执行私有玩家存档的读取、写入、导入或上传**。没有以真实终局档作本轮 Rust 收益样本。实现位于原生 Rust 路径，不能据此宣称网页版、安卓版或三端帧率提升。

## 验证

| 检查 | 最终结果 |
| --- | --- |
| Flow / history / clock 专项 | 最终 Flow 9/9、history 5/5、clock 11/11；Core 离线集成 38/38、Host 10/10；均随后纳入完整 Rust 回归 |
| 最终 Host 公共目录 | **14 通过 / 2 性能专项跳过**，459.29 秒；12 个完整状态场景、累计溢出原子拒绝、自动采用仍关闭；真实导出字节摘要核对通过 |
| 小数与后续推进 | `.0043`、`.25`、`.9999` 起点、非整十秒尾段、空历史、已压缩历史及八小时小数起点相同；`.0043` 例再热推进 11 秒及独立检查点重开推进 11 秒均相同 |
| WAL | 当前算法提交、冷重开与幂等重试通过；缺版本、v1、v2、未知算法长日志拒绝，源会话/检查点/WAL 保持不变；旧 ≤30 秒兼容 |
| 完整 Rust | Core **1,096 通过 / 3 ignored**，Host library **244**，bin **3**；共 **1,343 通过**；最终冻结源码等价工作树，Core 219.48 秒、Host 11.44 秒 |
| 类型 / fmt / 严格 Clippy | 通过；Clippy 使用 workspace、all-targets、locked、jobs 1 和 `-D warnings` |
| 完整 Vitest | **3,117 通过 / 35 跳过**，392 文件通过 / 13 跳过；82.48 秒 |
| Native/desktop Node | **628 通过 / 1 条件跳过**，22.35 秒 |
| Chromium | 首次 **360 通过 / 27 跳过 / 82 失败**；原样单 worker **75 通过 / 7 失败**，驱动修复后原 7 项 **7/7**（38 秒）。合计覆盖 442 项通过、27 条件跳过，不伪称一次全绿运行。受影响科技树文件再跑 **17 通过 / 1 失败**；精确预算夹具固定 Date 后该项 **1/1**（11.8 秒） |
| Web / Desktop build | 通过；启动预算、native coverage、thin-UI 边界和 127 项运行依赖许可证检查通过 |
| Windows 实包 | **8/8**，56.8 秒，运行与驱动均为 e9db8de4；启动、加工、保存关闭、崩溃租约、冷重开、导出导入、拒绝与隔离通过 |
| 目录包 / ZIP | 源身份、离线配置及 **75 件制品摘要**通过；ZIP 独立解压后再次核验同一完整文件集与 Host 摘要，通过；ZIP 共 76 个文件成员（含清单） |

完整 Rust 使用 `cargo test --manifest-path native/Cargo.toml --locked -- --test-threads=4`；完整单元使用四 worker，公共专项和性能专项各使用单 worker。最终公共测试与部分单元/打包工作有并行，计时仅作诊断；下述性能专项单独执行。本轮没有完整重跑 server、Ops、Android 真机、签名安装升级或多硬件长期门禁，不继承历史豁免。

## 同结果内核性能

同一冻结 Host、公共 catalog 和初始检查点，每次重新打开核心会话；每种时长三对 Native 顺序为 Exact→Macro、Macro→Exact、Exact→Macro。两条路径均先以 600 秒热身，各对另运行同源 JS 逐秒参考。12 次 Native 和 6 次 JS 的完整结果摘要均相同，源输入摘要不变。

| 离线时长 | JS 逐秒 + 规范校验中位 | Rust Exact RPC 中位 | Rust Macro RPC 中位 | 相较 JS 参考缩短 |
| --- | ---: | ---: | ---: | ---: |
| 600 秒 | 490.003 ms | 1,734.793 ms | **262.789 ms** | **46.4%** |
| 3,600 秒 | 3,275.937 ms | 15,410.543 ms | **377.906 ms** | **88.5%** |

只计 JS 模拟循环时，其中位为 481.873 / 3,265.026 ms。Rust Macro 相较同结果 Native Exact 分别缩短 84.9% / 97.5%。旧 v2 宏的完整结果不同，不能作为有效同输出基准。

Native 计时覆盖 `coreAdvance` RPC 及结果规范校验；JS 对照包括逐秒循环及规范校验。两者均不包含输入导入、会话打开、输出导出、玩家界面或持久提交，JS 参考也不是玩家当前保守离线策略。样本只有 110 实体、2 条带的公开铁链，不代表私有终局档、总体 P95、FPS 或跨端收益。性能日志为 `performance-ab.log`，逐样本记录为 `performance-ab/performance-600.json` 和 `performance-3600.json`；专项 2/2 通过，74.21 秒，本任务未并行编译或运行其他测试。

## 已保留的首轮问题

- `history-focused.log`：首次编译被生产路径调用 `#[cfg(test)]` 专用 `parse_belts_parallel`，以及父调用与 `offline_flow::apply` 参数数目不一致阻断。该原日志保留；后续结果由最终矩阵单独填写，不能把首轮记为通过。
- `flow-focused.log`：首次五项 Flow 筛选中四项失败，包括一处夹具缺失键，以及被证明器发现第一秒尚非物理稳态。`flow-signature-diagnostic.log` 保留了传送带 `congestion` / `lastFlow` 仍在收敛的具体差异。这是资格拒绝证据，不得删掉字段或放宽比较来制造一致。
- `flow-focused-v2.log`：两个 JSON 数字表示差异分别为累计传输 `61` / `61.0` 和量子边界 `75` / `75.0`；写回改用原 Exact 的 f64 表示，整数安全范围校验保留。完整规范哈希相同不等于原始字节相同。
- `rust-offline-integration.log`：四个旧正例只有周期平均物料相同，没有一秒物理稳态，严格证明拒绝。正例改用真正稳定的矿冶上传链，原场景保留为原子拒绝；没有豁免签名字段。Host 同类旧矿源也保留拒绝测试。
- `public-catalog-v1.log`：5 通过 / 9 失败 / 2 跳过，失败来自将公共默认空空间站目录误判为活动系统。保持公共夹具不变，新增严格默认目录证明及活动/损坏/未来返航反例；最终公共 14/14 通过。期间测试夹具遗漏 json 宏、缺星系 planet profile 的首次日志亦保留。
- 严格 Clippy 首轮报告范围写法、可合并 if 和测试字面量分组问题，等价调整后全目标复验通过；两次 release Host 构建分别耗时 4 分 11 秒、3 分 36 秒，均使用单编译任务，没有编译器异常。
- `chromium-full.log`、`chromium-first-report.json` 及 `chromium-results/` 保留首轮 82 项失败：64 项停在 App/Worker 启动，12 项时间或异步表现、6 项原因待定；不从分类推断环境是唯一原因。原样单 worker 75/7 后，剩余三项启动等待在新 worker 通过；另外四项由测试边界问题导致。账号测试在产品返回菜单的 200 ms 等待及持久保存开始前就读取旧基线并刷新，现等待真实主菜单出现。画布性能测试实际两帧仅 23.5 ms，失败来自报告导致 `inert` 后仍直接 evaluate 采点，现确认报告并等背景解锁。两项科技树点击也被报告遮挡，仅在科技树专用种档 helper 注册已有样式的报告处理，不影响离线报告测试。所有原存档字段、500 ms 性能上限和 5 秒就绪断言保留；复验日志为 `chromium-isolated-rerun.log`、`chromium-final-rerun.log`。
- 受影响科技树文件的补跑发现精确重试报告为 2 分 1 秒，旧测试却假定种档到点击必然不足一秒。仅该例在种档前用 Playwright `setFixedTime` 固定 Date，真实 timers 和模拟继续运行，保留“2 分钟 0 秒”、原状态不变及 elapsed `[162,163)` 的全部断言；没有修改生产时钟、桌面租约或放宽门槛。日志为 `chromium-technology-final.log`、`chromium-exact-retry-final.log`。

## 制品与后续

当前目录包为 `release-performance-edition/`，上一版完整移至本轮证据根的 `baseline-package-ab01bb78/`；旧 ZIP 未变。最终实包证据在 `artifacts/desktop-journey-2026-09-07T15-20-50-086Z-dabe5ca8/`，前后进程审计均为空。

- [Windows 未签名离线 ZIP](../../artifacts/1.2.7-rust-offline-state-parity-final-20260907/DSPONLINE-1.2.7-performance-e9db8de4-unsigned-offline.zip)：**157,531,080 字节**，SHA-256 `ee715e653d30f718482037ad6f8a620966dcacaa34799404343b0574991dbe0b`。
- 内部清单 SHA-256 `bcdf48ad1cec1e755e934415fce7746acac64711296634a456afd756177a1639`；独立解压验证见本轮证据根 `final-zip-readback-verification.json`。
- [源码审查 ZIP](../../artifacts/1.2.7-rust-offline-state-parity-final-20260907/DSPONLINE-1.2.7-state-parity-review.zip) 包含基线之后的变更源文件、文档和补丁，不含 EXE/Host、依赖、私有存档、测试 profile 或诊断日志。最终文档提交和外层摘要见 [交付清单](../../artifacts/1.2.7-rust-offline-state-parity-final-20260907/delivery-summary.json)。

下一阶段对真实大档按拒绝原因拆解复合产线资格，并行评估已验证有序历史的追加快路：省去逐秒重复排序，保留原桶合并、舍入和保留顺序，先逐次完整差分再做固定输入性能对照。不能用本轮小厂收益开放全部长离线采用。后续按[逐步计划](../RUST_PERFORMANCE_ROLLOUT_PLAN_2026-09-07.md)验收真实入口与上线资格；本文仍为开发交接。
