# DSPidle2 1.0.30 发布记录

## 发布摘要

- 角色：`release`
- 目标：香港 Web/API、上海 Web/API、上海下载页
- 版本：`1.0.30`
- GameState：v46；存档 envelope：v2；云 schema：v7；SQLite layout：v2（均未变化）
- 发布提交：`c6d896ae691147c671ab784eccaf704b23a6609b`
- Build ID：`1.0.30+c6d896ae6911`
- 发布清单：147 个文件，聚合 SHA-256 `437958ae8390b8d8ec5ec46dca921313af822bb54f6e49c391d89441df61a7d0`

本版上线 `fast-30s-v1` 快速离线结算实验和 `speedrun-v1 / season_01` 独立速通工厂与排行榜。快速离线只在 Worker 内存副本上默认尝试 30 秒精确校准后的批量外推，严格验证失败会从原始状态自动回到精确路径；复杂终局仍可能回退，不能把它解释为任意存档 30 秒硬保证。速通字段、设备级偏好、普通工厂和普通排行榜保持隔离，不改变存档或云协议。

## 不可变制品

| 制品 | 大小 | SHA-256 |
| --- | ---: | --- |
| Web `web-1.0.30-c6d896ae6911.tar.gz` | 1,165,837 | `9f5f46a60b2628655fcae514c171f5b386a9266c8b32f809d5ca9fa0c71cf20b` |
| API `api-1.0.30-c6d896ae6911-clean.tar.gz` | 79,564 | `fbab01c4afe1e1f58d456d14a54180fa74347198743ed48c1f0463e102dde011` |
| 下载页 `download-site-1.0.30-c6d896ae6911.tar.gz` | 115,814,767 | `6290ca5e83d5a7eca5ce73e8b86ed51d33cf5998f40d6ef4cf8f826d540cf408` |
| Android APK `1.0.30 / 1000030` | 4,685,456 | `f49f1ba5bb5cefa2058369863c50d9bc07c0ec0c635f26a1b28c762783700f8e` |
| Windows x64 EXE `1.0.30` | 112,283,091 | `7be64f6b6cd53717cb0dbebf128b594d2f8ec4802b7e6d3d5b3566fe041eaf4c` |
| Windows blockmap | 118,078 | `3ad09594283d07f07398464a2508ec88361ed0dd5ec97598029fb4497ef89b42` |

Android APK v2/v3 校验通过并保持批准长期证书连续性。Windows FileVersion/ProductVersion 均为 `1.0.30`，Authenticode 为 `NotSigned`；没有创建新证书。

## 开发与发布门禁

- 制品来自独立干净工作树 `codex/release-1.0.30` 的提交 `c6d896a`；共享主工作区的其他 Agent 改动未进入制品。
- Vitest：87 个文件通过、4 个跳过；758 项通过、15 项跳过。
- 服务端：49/49；运维：6/6；原生发布工具：8/8；运行时 licenses：128 个包一致。
- `npm run typecheck`、`npm run build` 和 `git diff --check` 通过；Vite 转换 1,878 个模块。
- 同一 clean 提交的 Playwright 全量结果为 236 个通过、3 个显式跳过、0 失败；本次发布收尾没有重复执行这组长测。
- `npm run release:verify -- artifacts/release-manifests/1.0.30-c6d896ae6911.json` 在发布收尾再次通过 147/147 文件复验。

## 备份与原子切换

发布前两地 Web/API 均为 `1.0.29-d9aa7d7921b0`。香港和上海分别通过 SQLite Backup API 生成发布前、发布后快照并执行 `quick_check`；只记录摘要，不复制、恢复或改写玩家存档。

| 节点 | 阶段 | 大小 | SHA-256 | 验证 |
| --- | --- | ---: | --- | --- |
| 香港 | 发布前 | 1,998,856,192 | `f9952805abadad620113be901ed1ef49a1571b6818be45a36cf550ab417b36b6` | Backup API、`quick_check` |
| 香港 | 发布后 | 2,000,416,768 | `a069df8c098ca69a14f98ef9b0aeaf57c6f90c1cacdd3a957033bd4a7b67fd73` | Backup API、`quick_check` |
| 上海 | 发布前 | 159,744 | `c92e1a5bd4495b88e3618728e7a572fe481095b7d812c643fda25b46e3842a8a` | Backup API、`quick_check` |
| 上海 | 发布后 | 159,744 | `36c0b5832471654826500e9447bd1258253b1791813f4753382e0abe02a9f504` | Backup API、`quick_check` |

两地均上传到新 release 目录后再原子切换，生产数据库保持原位：

- 香港 Web：`/var/www/dsp-idle/releases/web-1.0.30-c6d896ae6911`；API：`/opt/dsp-idle-cloud/releases/api-1.0.30-c6d896ae6911`。
- 上海 Web/API：同名独立目录，继续使用上海本机静态资源、本机 API 和独立数据库。
- 上海下载页：`/var/www/dsp-idle-downloads/releases/download-site-1.0.30-c6d896ae6911`。
- 当前 Web/API symlink 均为 `1.0.30-c6d896ae6911`；下载页显示 1.0.30 稳定清单和两个应用包。

## 健康与公网验收

| 检查 | 结果 |
| --- | --- |
| 香港/上海 `version.json` | 均 200，Build ID 为 `1.0.30+c6d896ae6911` |
| 香港/上海 `/api/health` | 均 200；`schemaVersion=7`、`storageLayoutVersion=2` |
| 服务 | 两地 `dsp-idle-cloud.service` active，`NRestarts=0`；香港只读复核同样确认当前 symlink 和服务状态 |
| Web 静态资源 | 新 hashed JS/CSS 与上一版资源均可取；二进制资源 `immutable`，HTML/service worker 和下载清单 `no-cache` |
| 云边界 | 未授权云读取 401；Android `https://localhost` CORS 预检 204；未知 Origin 403；没有使用生产账号写入云存档 |
| 下载页 | `https://download.dsponline.cn/` 200，显示 1.0.30、Android 长期证书说明和 Windows 未签名警告 |
| Android | 稳定清单为 `1.0.30 / 1000030`；完整下载大小和 SHA-256 与本地制品一致 |
| Windows | `latest.yml` 为 `1.0.30`；完整下载 SHA-256 与本地制品一致；Range 返回 206、`bytes 0-31/112283091`，缓存为 `immutable` |
| 香港下载入口 | `/downloads/*` 继续 302 到上海下载域，不代理上海游戏 API |

## 回滚指针

- 香港 Web/API：`sudo /usr/local/sbin/dsp-idle-switch-release --rollback-last`，目标为 `1.0.29-d9aa7d7921b0`。
- 上海 Web/API：同一固定切换工具，目标为上海本机 `1.0.29-d9aa7d7921b0`。
- 上海下载页：将 `current` 切回 `/var/www/dsp-idle-downloads/releases/download-site-1.0.29-d9aa7d7921b0-r2`；旧目录和稳定清单均保留。
- 回滚只切换代码或下载页 symlink，不恢复 SQLite；Windows/Android 回滚使用上海保留的 1.0.29 稳定包和清单。

## 未验证项目与剩余风险

- Windows 仍为 `NotSigned` 测试包，可能显示未知发布者或 SmartScreen 提示；没有创建新证书。
- 本次发布操作没有重新执行 Playwright 全量长测，使用的是同一 clean 提交已有的 236/3 结果；没有在真实 Android 设备、Electron 和低性能移动设备上执行长时间压力测试。
- 没有使用真实生产账号执行云登录、读取或上传写入 smoke；未授权边界、CORS、服务测试和备份副本隔离启动通过。
- 复杂真实存档的快速离线结算会严格回退精确路径，当前不能宣称 30 秒硬上限；大存档 Worker RSS 和后台切换恢复仍需后续设备矩阵观察。
