# 1.2.7 第三轮本地返修与交付

Role: develop

本轮 R1–R6 固定范围已完成。接手目录 D:\GameDev\DSPidle2，基线 b7ca5b8d2dcebffa095c2cdc547e1481e73e8a70，分支 codex/1.2.7-round3-validation。接手时另一 Windows 工作树同样位于 b7 且干净；本轮未改该工作树，没有未纳入的已有修改。

冻结运行源码与桌面驱动：27c4f15fd621800cf6000c1f156a0a401526f810。新包 Build ID：1.2.7+27c4f15fd621，edition windows-performance-development-v1，channel beta，默认 API/更新地址为空，Authenticode 为 NotSigned。最终报告提交只修改不参与打包的 Markdown，文档 SHA 在交付清单中单列。

## 实际修复

| 返修项 | 结果 |
| --- | --- |
| R1 内容收集 | 复现旧生产函数接受四类坏制品；现在从可信源码上下文校验 ASAR 身份、Build ID、Host、完整文件清单、安装器/feed 摘要与路径。坏候选、零份、多份均失败；正例运行实际生成器和收集函数。内部证据不替代签名。 |
| R2 保存恢复 | 实包复现正常关窗后立即重开的镜像/租约冲突。增加 20 秒关闭握手，停止新写入、等待真实持久 ACK，原子核对 owner/fencing/revision 后只释放自己的租约。取消、失败、超时或新 owner 保持安全，已确认删除模式也经先红后绿修复。 |
| R3 补丁 | 旧补丁分别在 148、683 行损坏，已从 Git 对象重导。b7 前态累计 15 文件、上一轮增量 8 文件通过实际应用与 blob 对账。最终增量从 b7、累计从 857947993538b88952a26feca620565bab1b6726 导出，在准确基线隔离副本检查、应用、逐文件核验，完整快照和结果随审查 ZIP 交付。 |
| R4 往返与拒绝 | 固定暂停合成厂经真实导入、持久保存、gzip 导出完成和独立 profile 再导入，比较全部持久游戏字段，仅排除窗口布局。无效输入明确拒绝且原始持久字节/revision/可见工厂不变；四类弱断言负例均检出。 |
| R5 UI 加工链 | 真实部署风机、熔炉和线路，三种施工库存各减 1；矿石源缓存 50→38，熔炉输入 11、铁块输出 1。供电按既有 300/360 kW 规则运行。定位失败属于控件、叠放及空白坐标搜索；最终等待视图稳定、正常缩小并使用整卡空白，无 force click、隐藏层或状态写入代替交互。 |
| R6 实包前提 | 缺包/错包非零 BLOCKED；绑定源码、ASAR/Host/75 文件清单及实际包内 dist。每轮独立目录与合成 profile；main/renderer 外部探测被进程内限制拒绝，Host 适用路径通过源码/锁定 normal Cargo 依赖审查。最终进程残留为空。 |

真实包还暴露两个前置产品缺陷：sandbox preload 使用不支持的 node:crypto/本地 require，导致 bridge 缺失；bridge 恢复后，启动等待原生资格期间查询空行星崩溃。现用锁文件已有版本的 rolldown 和 @noble/hashes 打包 preload，保留 sandbox/contextIsolation，并在资格明确前避免查询 JS 行星索引。没有开放 Rust 玩家资格或改变公开存档格式。

## 新鲜验证

| 验证 | 结果 |
| --- | --- |
| npm test | 385 文件通过 / 13 文件条件跳过；3,052 项通过 / 29 条件跳过 / 0 失败 |
| npm run test:native | 623 通过 / 1 Windows symlink 权限条件跳过 / 0 失败；Host 已构建并执行集成用例 |
| 关闭与跨标签页协调 | 23/23，含取消、新 owner、新 revision、已删除模式、镜像及冲突保护 |
| 定向 Web 保存与迁移 | 13/13，含原子提交、过期 fence、快照、容量失败和旧 localStorage 迁移 |
| npm run test:e2e:durable | 7/7，WAL、pagehide、失败修复及保存中编辑恢复 |
| npm run test:desktop-package | 8/8、0 重试，独立验证正常退出立即重开、强杀、在途保存退出、真实加工链、gzip 往返、无效导入与网络探测 |
| npm run desktop:performance:pack | typecheck、release Host、桌面构建、启动预算、thin UI、native coverage 均通过 |
| npm run licenses:check | 127 个运行时包声明有效 |

单元跳过项为 opt-in 基准、未提供的真实玩家夹具和 opt-in 原生长差分，没有算作通过。一次工具中断的 native 日志单列，随后完整重跑通过。此前失败日志均保留；2e4a5c66 的探索轮运行中编辑过驱动，不能作为最终验收。

最终桌面运行目录为 artifacts/desktop-journey-2026-09-07T07-17-37-360Z-6a507ca6。固定共用输入 91,563 字节，SHA-256 10b31d3c80526686f04d251e0c8e5f6499c28053d806af80ddaf5ecfebde90ba。正常退出不等待 TTL、不自动选择冲突；仅明确强杀场景等待原租约到期。补充生产画面由 Electron capturePage 捕获，避免缩放下 Playwright 截图裁切。

## 交付与限制

交付目录：artifacts/1.2.7-codex-round3-20260907-c7e39a12/。

- DSPONLINE-1.2.7-Round3-review-bundle.zip：R1–R6 详细记录、红/绿日志、结果 JSON、源码/制品清单、两份最终 patch、累计完整快照及 SHA256SUMS。
- DSPONLINE-1.2.7-performance-27c4f15fd621-unsigned-offline.zip：完整资源的独立性能开发目录包。
- delivery-summary.json：最终文档 SHA、外层 ZIP 字节数和 SHA-256、补丁基线与实际核验结果。

实现完成，验证限上述已执行范围，制品为本地开发候选，正式发布未授权、未执行。没有 push、远程 PR/tag、合并 main、生产访问、真实玩家数据或 Skill 修改。

JS 玩家权威、Rust shadow/受限实验路径和 authorityEligible=false 保持不变；GameState 47、envelope 2、cloud schema 8、SQLite layout 3 不变。受保护原生/纯挂机会话仍要求先完成自身安全停止。进程内网络限制和 Host 源码审查不等同于操作系统防火墙认证。

新性能和全进程内存测量为 NOT_MEASURED，不引用旧倍率。本轮未重跑完整 Chromium/nightly、服务器/运维或整套 Cargo 矩阵；未改相关 Rust 模拟/协议或服务器行为。签名安装器、覆盖升级、多硬件长时及生产发布不在本轮验收范围。

API 依据：[Electron sandbox](https://www.electronjs.org/docs/latest/tutorial/sandbox)、[BrowserWindow.close](https://www.electronjs.org/docs/latest/api/browser-window#winclose)、[WebRequest](https://www.electronjs.org/docs/latest/api/web-request)。
