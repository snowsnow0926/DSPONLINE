# 1.2.3 正式发布记录

> 发布日期：2026-08-28
> 运行时提交：`25aeeb34e501fdb68441b88cf305502b730c34d6`
> Release ID：`1.2.3-25aeeb34e501`
> Build ID：`1.2.3+25aeeb34e501`
> previous-stable：`1.2.2-8b9c93e13270`

## 结论

1.2.3 已完成香港 Web/API、上海 Web/API、上海下载页、Windows stable 和 Android stable 全量发布。两地 Web/API 均从新的不可变目录经 release-control 原子切换，上海下载页同步切换；香港 `/canary/previous/` 已更新为不可变 1.2.2。GameState v47、envelope v2、云 schema v8、SQLite layout v3 均未升级；没有恢复、覆盖、迁移或热改生产数据库，也没有修改玩家存档、账号、排行榜或业务数据。

用户只针对 `1.2.3-25aeeb34e501` 明确豁免 Android 实体设备、低配/主流/高配 Windows、Windows `1.2.2 → 1.2.3` 覆盖升级和长时运行门禁，并接受相关残余风险。Android API 36.1 模拟器覆盖升级已通过，但不能替代真机。Windows setup 按历史策略明确为 `NotSigned`；Android APK/AAB 使用批准长期证书，APK v2/v3、zipalign、包名、版本元数据和历史证书连续性通过。该豁免不得复用于后续候选。

## 候选与开发门禁

- 冻结候选 manifest：12 个不可变对象，aggregate SHA-256 `0ebcd1c79a3e8ba9990f6595ac597574c46123342d3f370b4dd423ee83c619da`；provenance 3/3、SHA256SUMS 20/20 通过。
- Web 171、API 182、native feed 6、download site 9、native archive 2、Windows unpacked 77 个文件均复验；Web aggregate 为 `9be0153cd6699a7927caa77f2fdeece9f3fe1860e7cf56d18ec5722f378b7f9d`，API aggregate 为 `8d7d079cff18d0324631c250a2602682befa6e7b39fd2c2c9d170a0efb07547f`，download aggregate 为 `3d1acf3e1ca0e0cca231a9dbec7a227e3db6b1716baaa811c4b4cca47b61611a`。
- Vitest 1,704/29、server 384/2、station 4/4、ops 56/6、release-switch 29/29、native JS/desktop 180/1、Rust 222/222、Chromium 432/27、durable 7/7、production preview 3/3，均 0 失败。
- 生产构建为 1,984 modules，startup gzip 180,059 B，forbidden startup modules 0；手机浅色 390×844 完整检查器专项通过。
- 远端冻结清单逐文件复验通过。归档内通用 `npm test` 入口仍引用一个未交付的 source-only 测试，并有一个测试依赖源码目录布局；这是打包测试入口缺陷，不是运行时文件差异。没有改写冻结制品，改为执行包内实际交付的 server 测试，并结合隔离 health/ready、组件 manifest 和公网验收完成发布判断。

主要正式制品：

| 制品 | 字节 | SHA-256 | 签名状态 |
| --- | ---: | --- | --- |
| Android APK | 5,378,289 | `8dfae60718f4543fbeb466245056c1fd634ef804253cd326ffa492c27d25cf60` | v2/v3、历史证书连续 |
| Android AAB | 5,167,303 | `00c0f7f1dac3c9d229ac683cea36bf2cbfbf5d625d99dee99bcc1ebbaa70c4ac` | 批准长期证书 |
| Windows setup | 105,314,618 | `7f118f230cba796dee99a07ea4b58373e6131a7cccfcc9d20b70cd407f400a93` | `NotSigned` |

## 备份与原子切换

两地在 API/data-affecting 操作前分别创建全新 SQLite Backup API 快照，并验证完整 SHA-256、`quick_check=ok`、schema v8、layout v3、文件身份和保护水位：

| 节点 | 备份字节 | SHA-256 | 发布后根盘 |
| --- | ---: | --- | ---: |
| 香港 | 4,054,929,408 | `de96d50c926c02e8ba366823b50a593936937e034ef1088384cd38b799a6772e` | 66% |
| 上海 | 462,848 | `020e6c65a58778182a961219ac38573263b16d30c79566d980569233b61ac458` | 75% |

香港第一次正式 drain 被 handoff proxy 的泄漏计数阻止：状态接口持续显示活动连接/写者，但只读 socket 审计确认实际 ESTABLISHED、监听队列和写入队列均为 0。前两次尝试均在 pending/current 改变前安全停止，并恢复旧 1.2.2 服务；上海也回切 1.2.2，未形成部分发布。第三次先进入 hold，在实际连接为 0 的前提下只重启 proxy 清零泄漏计数，再由 release-control 完成 writer lock、绿色槽预热和原子发布。没有中断真实 writer/上传，没有接触数据库、WAL/SHM 或玩家数据。proxy 计数泄漏应在后续版本作为运维缺陷修复，不能用本次现场处理替代代码修复。

最终状态：

| 目标 | current | previous | generation / slot |
| --- | --- | --- | --- |
| 香港 Web/API | `1.2.3-25aeeb34e501` | `1.2.2-8b9c93e13270` | 44 / green / 4322 |
| 上海 Web/API | `1.2.3-25aeeb34e501` | `1.2.2-8b9c93e13270` | 28 / green / 4322 |
| 上海下载页 | `download-site-1.2.3-25aeeb34e501` | `download-site-1.2.2-8b9c93e13270` | 原子软链 |

两地 pending 均为空，API 与 handoff proxy active、`NRestarts=0`，health/ready 200、`writable=true`、pending writes 0；香港 health/node-health/offsite timer 正常，上海 offsite timer 继续按既有策略 disabled。正式备份和 evidence 均保留并在发布后再次做身份验证。完成验收后只删除了冻结制品可重传、未被服务引用的临时上传目录：香港 2,696,806 B、上海 113,253,821 B；不可变发布、current/previous/canary、有效备份、数据库/WAL/SHM 和玩家数据均未删除。

## 公网、下载与 PWA 验收

- 香港和上海 Web 均返回 `1.2.3+25aeeb34e501`；根页、SPA fallback、Service Worker、hashed asset 缓存策略、health/ready 均通过。
- 香港 `/canary/previous/` 以 302 和 no-store 指向 `/canary/1.2.2-8b9c93e13270/`；上一版 `version.json` 为 `1.2.2+8b9c93e13270`，旧根 Worker 查询返回 404。
- 上海下载页 version、Android/Windows stable feed 均为 1.2.3；APK 和 setup Range 返回 206、`bytes 0-1023`，缓存策略通过。公网完整下载的字节与 SHA-256 和冻结制品一致。
- 公网 APK 再次通过 v2/v3 和证书连续性验证；Windows setup 再次确认 `NotSigned`。
- 全新 Chromium 通过受保护物理出口、严格主机与 TLS 校验完成当前根站 PWA 测试：Service Worker、current shell cache 和 Build ID 均为 1.2.3，断网重载可启动；测试阻断所有写请求，未登录生产账号，也未发送生产写请求。

## 回滚边界

- 香港与上海代码直接回滚目标均为 Web/API `1.2.2-8b9c93e13270`。仅在 current generation、pending、服务健康和新鲜 evidence 满足时通过 release-control 回滚；香港大库回滚前必须重新生成独立 preflight evidence。代码回滚不得恢复数据库。
- 上海下载、Windows 和 Android stable 一体回切到 `download-site-1.2.2-8b9c93e13270`，不得只改 feed 而保留不匹配制品。
- 香港 previous 路由变更前 Nginx snippet 备份为 `dsp-idle-app.conf.pre-previous-1.2.2-8b9c93e13270-20260828T114822Z`；恢复时必须再次执行配置检查、reload、current worker、health/ready 与公网 smoke。该入口只提供上一版 Web，不是 API、数据库或网络灾备。

开发细节与候选边界见 [Release Agent 交接](../RELEASE_HANDOFF_1.2.3.md) 和 [1.2.3 候选说明](./1.2.3-candidate.md)。
