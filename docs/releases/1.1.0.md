# DSP极简网络 1.1.0 正式发布记录

> 状态：**已正式发布并完成双节点、下载页、原生 stable、公网 PWA 与 previous-stable 验收**  
> 发布日期：2026-08-20  
> Runtime Git SHA：`9b2c579cbe0de8848f6a4573371fd1322cec0c45`  
> Release ID：`1.1.0-9b2c579cbe0d`  
> Build ID：`1.1.0+9b2c579cbe0d`

## 发布结果

- 香港 Web/API current：`1.1.0-9b2c579cbe0d`，generation 27，green / 4322；direct previous 为 `1.0.47-aab581cf0c78`。
- 上海 Web/API current：`1.1.0-9b2c579cbe0d`，generation 14，blue / 4321；direct previous 为 `1.0.47-aab581cf0c78`。
- 上海下载页 current：`download-site-1.1.0-9b2c579cbe0d`；previous 为 `download-site-1.0.47-aab581cf0c78`。
- Android stable：`1.1.0 / 1001000`，使用批准的长期证书；Windows stable：`1.1.0`，按既有策略明确为 `NotSigned`。
- 香港 `/canary/previous/` 以 `302` 指向不可变 `/canary/1.0.47-aab581cf0c78/`。它只提供上一稳定版 Web，继续使用当前 1.1.0 API，不是 API、数据库、主机或网络灾备。
- 两地继续使用 GameState v47、save envelope v2、cloud schema v8、SQLite layout v3 与既有 IndexedDB records；没有恢复、替换、复制或降级生产数据库，没有修改玩家云档、排行或账号数据。

## 发布前备份与容量

| 节点 | bytes | SHA-256 | 验证 |
| --- | ---: | --- | --- |
| 香港 | 3,772,833,792 | `59109b3c6698c48f5379b3ddc64b023dfca5bc59c779a0a1f429fc276f143225` | Backup API、`quick_check=ok`、schema 8、layout 3、独立 evidence |
| 上海 | 462,848 | `a96d61df44e610437a85a416698894dcfc4c85e632663439947619fe52914acf` | Backup API、`quick_check=ok`、schema 8、layout 3、独立 evidence |

香港发布前根盘容量不足时没有删除数据库、WAL/SHM、有效备份、current/previous/canary 或直接回滚目录。运维只清理标准可重建缓存，并把一个已经有完整 evidence 的旧历史快照迁移到既有受保护 COS raw archive：源为 3,234,443,264 bytes、SHA-256 `2763132d109a3ad209fbd1f57d0f3a9a8f43e3b16fa614ba90b3caf6b1ff15d2`，按 64 MiB 分为 49 个对象，逐片及完整串接哈希通过，manifest SHA-256 为 `ecd022b461ff147f0ac66f241903a9f5cc5aee8dac97e8b1f86a6e140a279310`；只有全部远端读回证明通过后才移除该冗余本地副本，仍保留六份本地大快照。

最终根盘约为香港 80%、上海 88%，均低于 90% 保护线。香港因维护窗口 SIGTERM 留下的 offsite service failed 状态已在确认无任务运行、API 健康和今日触发记录存在后清除；daily timer 恢复 active 并只安排下一次调度，没有手工启动大备份。上海 offsite timer 延续发布前已记录的 inactive/disabled 状态。

## 不可变制品

开发来源 manifest 为 254/254，aggregate SHA-256 `7356b832691c42d9bd5de8563f7101475b40a9f190171f0b16c6b25d75dbce4c`。正式发布组件如下：

| 组件 | 文件数 | aggregate SHA-256 | 归档 SHA-256 |
| --- | ---: | --- | --- |
| Web | 158 | `05c7bde93eddcec9da3790a8da9512393739191ca419d1a955e06d34f9313937` | `935bf2558f4a13918bac05e7f5ef7d7b978c9d95ccb84f32e3bc0572e5d7fd63` |
| API | 166 | `dee67751b272e567c840f1b75946c3f8cdceacb4a8c2ffd4a9c3da47676ff216` | `c237a7bd7ea68a217193cbdda02c2c086dd86aa5d9885a3915e8f201051a2686` |
| Native feed | 6 | `de0ce5476a14a77558afda6dd91ed4846e2ac0b00ffcac63d83d6cbec526109e` | `75138caa8ae24b81a96589a494b5f9e25c76fc3e53f0a44c67fe9fd490a3e4b2` |
| Download site | 9 | `90141afb2ddd2b71a51d6ba6e6c02600f218420bb0354a5b6998a91233c9d34f` | `7999d55f9b3b4ba40779c8905207f6613400968539bf718fe64cc85b5296b04e` |
| Native archive | 3 | `e5fc0d21ba8810ba792b4c807892d05a9c51cf6fc2a5b44a8bdb7f77db0f5a01` | `807343c5559fe8690b6a8b68cade01d9931814fae49459bcdab7e6aca139cdf5` |

正式原生包：

- Android APK：5,251,105 bytes，SHA-256 `6185968203e6cd3a6b81d0de3bb4c86e9425bc4c129f7f00df62ff682d1df285`；package `cn.dsponline.network`，versionName `1.1.0`，versionCode `1001000`，APK v2/v3、zipalign 与批准历史证书连续性通过。
- Android AAB：5,040,031 bytes，SHA-256 `8e2a72acb04eca316b5cd974eaa0c27601751c8db4193ff45b15ec355cdef0d1`；`jarsigner` 验证和签名条目通过，证书连续性通过。AAB 只作归档，不进入下载页。
- Windows setup：109,829,237 bytes，SHA-256 `281013bc80f57c6e62c4571175ebeb0a58b88eeafb0616cedbb580af70dbb7bd`；Authenticode `NotSigned`，未创建或替换 Windows 证书。

## 切换与公网验收

- 香港与上海均上传到新不可变目录，远端按组件 manifest 复验后，通过 release-control 原子切换；Web/API current、previous、proxy upstream、slot/port 与 symlink 一致，pending switch 为空。
- 两地 API、handoff proxy 与 Nginx active；health/ready 为 200，schema 8 / layout 3，API 与 proxy `NRestarts=0`。最终观察窗中两地 API/代理最近 15 分钟 error 为 0，DSP Web/API/下载专用 access log 最近 500 条 5xx 为 0。
- 香港公网根页、version、manifest、worker 和入口 hashed assets 均与冻结 Web 制品一致；入口/feeds 为 no-cache 或 no-store，hashed assets 与原生二进制为一年 immutable。
- 全新 Google Chrome profile 只存在当前 `1.1.0` Worker；访问 `/canary/previous/` 和不可变 1.0.47 后，无 waiting/installing worker，当前缓存根页 SHA-256 始终为 `84d4bca8d3afc094afffc66b64ef2e2a27654310341e068cec15e97cf92a8f44`，正式根页离线重载成功。浏览器层阻断所有 POST/PUT/PATCH/DELETE，没有登录生产账号或读取云档正文。
- previous-stable 的 302、`no-store`、`Vary: *`、noindex、HTML 字节一致、1.0.47 根 Worker 404 和当前 Worker 200 均通过。活动 Nginx snippet SHA-256 为 `26a2159912ca007450ceca0f5c375b26319620c1620c495b007a0d4f40c1ed10`；独立回滚备份 SHA-256 为 `88d5ebe1f5cf1f58a52897cd701f99a5ac733849eb18a562361db5ca1e8366e3`。
- Android/Windows stable feeds、`latest.yml`、APK/Setup 完整公网流 SHA-256、大小、Range 206 与 `Content-Range` 均通过。当前运维机日本 VPN 路径对下载域 TLS reset、实体网卡直连超时；未关闭 VPN、未改路由或降低 TLS。上海节点以正常公网域名和严格 TLS 完成完整下载验收，下载专用日志无 5xx。

## 发布过程中的受控停止与恢复

- 香港第一次切换在 API handoff proxy 的旧 `activeRequests/activeWriterRequests` 计数无法归零时安全停止；没有 generation 变化，服务恢复到 1.0.47。确认无实际连接后只刷新 proxy，不重启 API，计数归零；随后使用同一冻结制品完成正式切换。旧 proxy 对客户端中断后计数不回收是待开发修复的运维技术债，生产没有热改源码。
- 166 文件 API runtime 是交接定义的正式布局，并在两节点通过 manifest、临时 SQLite、health/ready 和隔离启动。上海 Node 20 上运行源码树完整 `npm test` 时会引用未打包的开发测试模块；发布没有把测试文件热塞进 runtime，也没有修改冻结制品。后续应让发布包的测试命令与 curated runtime 闭包一致。
- previous-stable 首次配置脚本因把 Nginx snippet 路径和绝对 `Location` 误判而多次在前置/验收门禁自动恢复旧配置；current Web/API 始终为 1.1.0。修正为精确 snippet、独立父站点配置测试及受信域绝对/相对跳转后完成原子 reload。

## 回滚边界

- 香港代码回滚：generation 27 的 direct previous Web/API `1.0.47-aab581cf0c78`；当前 slot green / 4322。
- 上海代码回滚：generation 14 的 direct previous Web/API `1.0.47-aab581cf0c78`；当前 slot blue / 4321。
- 上海下载/原生 stable 回滚：`download-site-1.0.47-aab581cf0c78`。
- 香港 previous-stable Nginx 独立回滚：恢复 `dsp-idle-app.conf.pre-previous-fallback-1.0.47-1.1.0`，执行 `nginx -t` 后 reload；不切换 Web/API 或数据库。
- 代码或下载回滚不得恢复、替换或降级生产数据库。备份仅用于明确授权的灾难恢复，不与普通版本回滚绑定。

## 残余风险

- 真实档 A 的 60 秒精确模拟较冻结基线下降 47.31%，通过 40% 最低线但未达到 55% 目标；B 为 58.28%。不得宣传为所有大档均提升 55%。
- 本轮没有由 Release Agent 新增正式 Android 实机、低配 Windows 或 30 分钟温度/内存趋势记录；用户已报告实际测试暂未遇到问题，但这不等同于自动化门禁通过。
- 上海根盘 88%，仍低于保护线但容量余量较小；后续发布必须继续在备份和上传前重新预算，禁止越过 90%。
- 上海既有 offsite timer 继续为 inactive/disabled；这不是 1.1.0 切换造成，仍需按独立备份运维计划处理。
