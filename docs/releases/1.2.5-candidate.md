# 1.2.5 发布候选记录

日期：2026-08-30

状态：发布前软件门禁与不可变制品已完成；未部署，生产仍为 No-Go

## 固定身份

- Runtime commit：`a347f1889044ccbdd1d6119f686eed6d2eb538c5`
- Release ID：`1.2.5-a347f1889044`
- Build ID：`1.2.5+a347f1889044`
- 分支：`codex/1.2.5-pure-idle-construction`
- 线上 1.2.4 父级：`3154f8cf4479b874362a2cf01510706fee60d16e`
- GameState v47、save envelope v2、cloud schema v8、SQLite layout v3 均不变，无数据迁移。

后续文档提交不属于 runtime，不得据其 SHA 重建或替换候选。发布时只能使用上述 runtime SHA 与本记录列出的不可变制品。

## 玩家修复范围

1. 守恒纯挂机允许同符号且持续缩小的旧戴森逐星系/全局账本差额；新增、扩大或跨零差额继续阻断。
2. 两种纯挂机都能以实测供给和有限燃料/储能时长持续推进建筑制造；产率复制模式先消耗真实库存施工，再复制允许的终局统计。
3. 模拟 Worker 运行故障与内容包校验错误分离；可恢复故障退还未提交时间、重建 Worker 并续跑。
4. 后台保护不再自动安装历史检查点；旧状态只允许玩家明确执行恢复时采用。
5. 蓝图详细卡片改用自然高度和有界溢出，修复密集卡片、大数量、长端口/参数和放大字号下的信息错位。

## 新鲜自动化门禁

- TypeScript、版本一致性、许可证、根/server 生产依赖审计、Rust fmt 与 clippy `-D warnings`：通过；审计漏洞 0，运行时许可证 125。
- Vitest：200 文件通过 / 16 条件跳过；1,673 通过 / 66 跳过 / 0 失败。
- Server：385/2；station：4/4；Ops：56/6；release-switch：29/29。
- Native JavaScript/desktop：176/7/0；Rust workspace：222/222。
- Chromium 最终完整复跑：434/27/0；durable：7/7；最终 Web production preview：3/3。
- Web：1,985 modules；startup gzip 180,157 B（JS 86,826 B、CSS 93,331 B）；最大启动 JS 58,974 B；menu 255,914 B；forbidden 0。
- API：182 文件布局、生产依赖安装和隔离 SQLite health 200 smoke 通过，schema 8 / layout 3。

第一轮完整 Chromium 在已作废的前置候选上有 1 条英文公告仍断言 1.2.4；修正测试基线后，聚焦项和最终 runtime SHA 的 434/27/0 全量复跑通过。Windows 首次原生编译遭遇本机 `rustc STATUS_ACCESS_VIOLATION`；同一 clean SHA 以 `CARGO_BUILD_JOBS=1` 有界重试成功，Rust 门禁保持 222/222。归档首轮读回遇到 Windows bsdtar 中文路径解码问题；显式 UTF-8 header 后完成全部内容复验。以上诊断均保留，不作为隐藏失败。

## 原生制品

- Android：`cn.dsponline.network`，`1.2.5 / 1002005`；APK 5,386,422 B，SHA-256 `d338add09bd8c9c6fc98311ecd0cc7186ce5b60a811ac483581730d4e0193874`；AAB 5,175,463 B，SHA-256 `3c221d274432fe2889ad369863e081cab34b0f2ab30bb30b705fd703e2e18883`。受保护加载器验证 APK v2/v3、zipalign 和 APK/AAB 长期证书连续性，没有输出或创建签名材料，也没有连接生产。
- Windows setup：105,319,618 B，SHA-256 `2c7fa69c5fe606a7b91ec552bccfbc5fda15ff99d553f2f17706623caf3aa33c`；setup/unpacked 均为 1.2.5，包内为正式 HTTPS API 与 stable 更新源；隔离用户目录启动 12 秒存活，停止后残留进程 0。Authenticode 按既有策略明确为 `NotSigned`。

## 不可变制品与读回

- Bundle：`artifacts/release-bundle/1.2.5-a347f1889044/`
- Candidate manifest：`artifacts/release-manifests/1.2.5-a347f1889044-candidate.json`
- Provenance：`artifacts/release-manifests/1.2.5-a347f1889044-provenance.json`
- SHA256SUMS：`artifacts/release-manifests/1.2.5-a347f1889044-SHA256SUMS.txt`
- 最终预发布报告：`artifacts/release-gate/1.2.5-final-report.json`

候选 manifest 包含 12 个对象，aggregate SHA-256 为 `d05d744ae91cc0c468a7144869351467c60dbd238b3f16b98e047a20d86b32e0`。组件清单为 source/Web+API 276、Web 171、API 182、原生 feed 6、下载页 9、原生归档 2、Windows unpacked 77；candidate 12/12、provenance 3/3、SHA256SUMS 20/20 均通过。六个 tar 从归档重新展开后又验证 source 1,256、Web 171、API 182、原生 feed 6、下载页 9、Windows unpacked 77 个文件；最终 Web 与下载页 `version.json` 均明确为 `platform=web`。

## 仍未满足的本 Release ID 门禁

- Android 实体设备验收；
- 低配、主流、高配 Windows 验收；
- Windows 1.2.4 → 1.2.5 原地覆盖升级；
- 长时运行；
- 真实终局玩家档的两种纯挂机、建筑制造和 Worker 故障注入验收。

这些门禁没有 1.2.5 专属豁免，旧版本豁免不得复用。因此当前结论是：开发和软件制品准备完成，但在上述门禁通过，或用户明确针对 `1.2.5-a347f1889044` 接受风险前，不得执行生产备份、上传或原子切换。本轮未连接香港/上海、未修改下载页、未改变生产数据或 current/previous 指针。
