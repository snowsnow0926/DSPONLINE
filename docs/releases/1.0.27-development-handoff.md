# 1.0.27 连接交互与批量建造开发交接

    handoffSchema: dspidle-development-handoff/v1
    role: develop
    developmentStatus: complete
    releaseTarget: 1.0.27
    releaseReady: true
    releaseStatus: authorized-for-release
    releaseAuthorization: granted-by-user-2026-08-04
    sourceCommit: 61303499f1806dd3752faeef881c0ac4395e58fd
    sourceVersionAtCommit: 1.0.26
    releaseCommit: resolve-from-final-clean-manifest
    immutableArtifacts: pending-release-role
    releaseManifest: pending-release-role
    lastVerifiedDate: 2026-08-04

本交接确认四项 1.0.27 开发内容已从原始 dirty 工作区隔离到独立 worktree。原工作区中的离线结算、Worker、存档及其他 Agent 未提交改动没有被清理、覆盖或带入候选。用户已明确授权发布香港、上海 Web/API、上海下载页以及 Android/Windows 更新。

## 实现范围

1. 连接点和连线圆圈提供默认、放大 25%、放大 50% 的设备级偏好，视觉尺寸、React Flow 吸附和空间索引命中半径同步。
2. 建筑制造中心可一次设置全部已解锁建筑目标；自定义目标为 1～100,000,000，继续受建筑仓储科技限制且不取消现有任务或 WIP。
3. 桌面与新版移动端混合选区支持 +1、+10、+100 和 1～1,000,000 自定义增加量；建筑与传送带材料先汇总，缺料时整批不变。
4. 移动端选择模式忽略 React Flow 瞬时空选择与画布冒泡清空，选择引用同步更新并持续显示选中状态。

## 兼容与数据边界

- GameState v46、存档 envelope v2、云 schema v7 和 SQLite layout v2 均不变化。
- 连接点尺寸使用 dsp-idle-network.ui.connection-point-size.v1 设备级 localStorage，不进入存档、云端、排行榜或状态哈希。
- 建筑堆叠继续限制为 100,000,000；传送带并联继续限制为 4096。历史超限值不会被静默压缩。
- 所有权威游戏状态修改继续通过纯命令和 commitGame 提交。

## 隔离与验证

- 功能提交：61303499f1806dd3752faeef881c0ac4395e58fd。
- 独立分支：codex/release-1.0.27；独立 worktree：D:/GameDev/DSPidle2-release-1.0.27。
- src/App.tsx 和 src/game/engine.ts 只接收本任务 hunks；明确排除 simulationBudget、offlineApproximation 和模拟索引优化。
- npm run typecheck：通过，0 个 TypeScript 错误。
- 版本公告、批量命令和设备偏好专项 Vitest：8/8 通过；设备偏好测试包含真实 v46 envelope 排除断言。
- tests/e2e/v127-selection-batch.spec.ts：7/7 通过，覆盖真实 tap 与 5/10/20/50 节点选择刷新。
- git diff --check：通过。

## Release 门禁

1. 从最终干净版本提交重新执行完整发布矩阵并生成不可变 manifest，不复用任何 dirty dist。
2. Android 使用既有长期证书，版本为 1.0.27 / 1000027；Windows 继续明确为 NotSigned，不创建证书。
3. 两地切换前分别验证 live symlink、服务、Nginx、磁盘和回滚点，并通过 Backup API 创建与验证 SQLite 快照。
4. Web/API 和下载页上传到新 release 目录，完成 manifest、生产依赖、备份副本隔离启动、原生签名和清单校验后再原子切换。
5. 发布后验证 Build ID、health、systemd、Origin、云接口边界、新旧 hashed assets、完整下载哈希、Range 和缓存。

## 剩余风险

- Windows 安装程序仍无 Authenticode 证书，下载页必须保留未知发布者/未签名测试包提示。
- 物理 Android/iPhone 的长期温度、耗电和帧率不在本发布窗口内；Chromium 触摸仿真不能替代设备长期测试。
- 代码回滚只切换 Web/API 与下载目录，不恢复或替换数据库。
