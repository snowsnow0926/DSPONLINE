# 2026-08-23 1.1.4 开发交接：制造巨构、蓝图拓扑与离线结算

## 状态与边界

- 角色：develop；本交接只包含本地源码、测试、构建和文档，不包含 VPS、生产 API、云存档、排行榜、下载页或玩家数据写入。
- 隔离工作树：`D:\GameDev\DSPidle2-v114-dev`。
- 分支：`codex/1.1.4-construction-blueprint-server`。
- 运行时实现提交：`56c6d2e5e7a5cd8cc52d7b506a9b6e4fd89da1e0`。
- 服务器运行时优化提交：`5cab4593d9db09e015f17d23e85efb5a143f9634`。
- 基线：`9ed57cdcc03640e9bb11e6622118c75c7b2c0359`。
- 目标版本：1.1.4（开发候选，未发布）；当前仓库 package version 仍为历史运行版本，尚未生成正式 release ID、制品清单或 previous-stable 指针。
- 未升级 GameState、存档 envelope、云 schema、SQLite layout 或 IndexedDB records；没有数据迁移和生产回滚操作。

## 玩家证据与只读调查

### 离线/挂机反馈

反馈截图显示时间扭曲面板处于“保守宏观结算中”，关键产线显示“未运行”，玩家因此判断挂机没有收益。提供的本地存档仅作内存副本检查，没有上传或写回生产：

- envelope v2、GameState v47、普通模式，完整性校验通过；约 47.7 MB。
- 约 36,771 个实体、71,220 条传送带、1 个建筑制造中心；属于复杂终局存档。
- 复杂度检查将其归入保守档；此前零校准回退会冻结整段不确定工厂，因此收益可能为零，但这不是存档损坏结论。
- 新路径的只读探针在同一规模存档上先完成 1 个模拟秒的隔离精确前缀，再冻结不确定尾段；60 秒结算仍推进完整墙钟时间，原始存档哈希不变。

### 制造巨构与蓝图调查

- 高堆叠递归制造的旧批处理每次按累计任务重新规划，且单一行星保护预算可被第一个制造中心耗尽，后续中心没有获得工作机会。
- 蓝图部署把设备级默认并联传送带数量写回模板，导致模板明确保存 1 条线时被覆盖成多条；这是拓扑/库存语义错误，不是玩家库存不足。
- 地热建筑的行星资源限制仍是规则门禁：无地热资源的行星必须拒绝部署，不能通过“一键补足”绕过。

## 本次实现

### 1. 建筑制造巨构批处理与多中心公平

- 对有限副产物相位进行最多 8 个作业的确定性探测；只有配方形状和副产物相位回到起点、没有副产物溢出/载具返还且聚合批次可重复时才建立多作业闭环。
- 聚合批次按真实前缀需求合并托盘消耗、返回、副产物、产物和工作时间；不闭环时继续使用原子任务路径，不丢弃 WIP、不凭空生成库存。
- 通过周期相位和起始库存复核保持缓存有效；外部新增材料会使缓存失效，避免新配方被旧计划吞掉。
- 同一行星的多个制造中心共享有界迭代/规划预算，按稳定实体顺序公平分配并返还未使用配额；一个高堆叠中心不能独占整颗行星的保护预算。
- 高堆叠、递归、多副产物与 1/2/3 相位回归均与未批处理 legacy oracle 做状态哈希比较。

### 2. 蓝图传送带数量守恒

- 蓝图需求、放置和排队均保留模板中的显式 `lanes`；`minimumBeltLanes` 仅为旧调用方保留的兼容参数，不再修改蓝图拓扑、库存需求或版本 ID。
- 新绘制传送带仍可使用设备默认并联数量；蓝图部署不再继承该偏好。

### 3. 离线与纯挂机的有界可验证前缀

- 普通快速离线路径在没有有效 30 秒校准候选时，先在隔离副本精确推进最多 1 个模拟秒，再对剩余不确定尾段使用保守结算；若前缀失败才回退零收益并报告具体原因。
- 纯挂机 Worker 连续失败后的保守会话采用同样的 1 秒精确前缀，并保存可恢复的 calibration checkpoint；原主线程状态保持事务性不变。
- UI 不再把保守模式下“尚未从短窗口测得”误标为“未运行”，会说明“短窗口未测得/不确定尾段已冻结”，避免把安全降级误报为存档无效。
- 该策略只发放可验证前缀收益，不外推运输、瞬时功率或其他不确定字段，不改变时间扭曲和速通规则。

### 4. 服务器运行时优化（不改数据协议）

- 已复核 `docs/PERFORMANCE_OPTIMIZATION_PLAN_2026-07.md`：PresenceIndex、LeaderboardSnapshotIndex 和上传检查队列等已有运行时索引/有界机制继续保留。
- 新增运行时 `UserLookupIndex`，在启动和显式 rebuild 时从权威 `store.data.users` 重建；注册、登录、找回密码、邮箱绑定、账号删除和外部 users mutation 都维护索引，`/api/metrics` 只暴露计数诊断。
- 索引命中仍复核权威用户对象；索引缺失/过期时执行一次冷回退扫描并修复索引，因此不会把索引失配变成认证失败。用户名/邮箱规范化与旧路径一致，重复历史邮箱仍保留原有“存在即冲突”语义。
- 不增加唯一约束、不改 SQLite 表、云协议、玩家数据或存档 schema；纯内存 10,000 账号热查找基准通过。完整 server/SQLite 集成门禁仍需在装有 `better-sqlite3` 的 release 环境补跑。

## 变更文件

- `src/game/engine.ts`
- `src/game/p2Batching.test.ts`
- `src/game/constructionAutomationGuard.test.ts`
- `src/game/v136-release.test.ts`
- `src/game/offlineApproximation.ts`
- `src/game/offlineApproximation.test.ts`
- `src/game/pureIdleMacro.ts`
- `src/game/pureIdleMacro.test.ts`
- `src/components/TimeWarpIdleOverlay.tsx`
- `src/App.tsx`
- `docs/ARCHITECTURE.md`
- `docs/GAMEPLAY_SYSTEMS.md`
- `server/index.mjs`
- `server/package.json`
- `server/user-lookup-index.mjs`
- `server/user-lookup-index.test.mjs`

## 已通过门禁

在隔离工作树执行：

```text
npm run typecheck -- --pretty false                         PASS
npm test -- --maxWorkers=4 --reporter=dot                  174 files / 1,443 passed / 7 skipped
npm run test:native                                          24/24 PASS
npx vitest run src/game/p2Batching.test.ts                  17/17 PASS
npx playwright test tests/e2e/construction-automation-stability.spec.ts --project=chromium
  1 PASS / 2 existing environment-gated skips
npm run build                                                PASS
  Vite 1,962 modules；startup total gzip 194,851 B；menu 282,337 B
node --check server/index.mjs                                PASS
node --test server/user-lookup-index.test.mjs                3/3 PASS
node --test server/runtime-indexes.test.mjs server/user-lookup-index.test.mjs
                                                              18/18 PASS
```

专项还覆盖：

- 建筑制造中心 44,311 堆叠保护、两个高堆叠中心公平推进、50,000/1,000,000 任务批处理、递归副产物相位和 legacy 状态哈希。
- 普通/速通蓝图 lane 需求、排队版本和放置守恒。
- 纯挂机保守前缀、取消/恢复事务性、普通离线保守前缀和非法校准候选回退。

## 未通过或未运行门禁

- `npm run test:server`：隔离工作树未安装 `better-sqlite3`，SQLite 集成/HTTP 服务测试无法加载；此外报告一个基线 `account-archive-import` 错误码断言不一致（实际 `ACCOUNT_ARCHIVE_IMPORT_INSPECTION_FAILED`，预期 `ACCOUNT_ARCHIVE_SAVE_FORMAT_INVALID`）。本候选的纯内存服务器索引专项已通过，但完整持久化/并发门禁仍未完成。
- `npm run test:ops`：同一 `better-sqlite3` 缺口阻断备份/发布控制相关测试；11 项通过、5 项失败，另有既有 `server/leaderboard-review-report.mjs` direct-invocation 列表断言失败。
- 未运行真实 Android/Windows 安装包、签名、真机、VPS、公网 API、下载页或生产观察窗口；不得把本候选直接发布。

## 兼容、回滚与发布交接

- 所有新增周期/前缀信息均为运行时临时结构，不进入 `GameState` 或云 payload；旧存档可按原迁移路径读取。
- 回滚边界是依次撤销运行时提交 `5cab4593d9db09e015f17d23e85efb5a143f9634` 和 `56c6d2e5e7a5cd8cc52d7b506a9b6e4fd89da1e0`；无数据库或玩家存档回滚动作。
- Release Agent 后续必须从 clean checkout 固定该提交，重新生成 Web/API/原生制品与 manifest，补齐 server/ops 依赖、真实设备、签名、备份、节点健康、原子切换和公网 smoke 门禁；本交接不授权任何生产操作。
