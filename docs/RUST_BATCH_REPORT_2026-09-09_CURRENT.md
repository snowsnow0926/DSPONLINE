# Rust 本批开发报告｜2026-09-09

这批已经测到了实际等待时间的改善：在 Windows 开发包中，一个包含 **9,107 个实体、20,000 条传送带**的公开测试工厂，完成同样的 5 秒离线收益，从点击继续到能打开设置，等待中位数由 **20.79 秒降到 5.96 秒，缩短约 71.3%**。存档结果逐字段一致，保存后两次重开也一致。

这是一项已验证的开发成果。当前整个版本的回归检查仍有失败，尚不具备发布资格；线上版本和下载没有在这批中更新。以下数字对应已冻结的 `2bb10b43` 开发包，不把后来只改测试和文档的提交号写成它的构建号。

## 你能感受到什么

| 场景 | 这批的实际结果 |
| --- | --- |
| 被测大工厂在 Windows 结算短离线后继续游戏 | 等待约 21 秒变为约 6 秒 |
| 结算出的物料、进度、统计和存档 | 与完整 JS 参考结果一致，没有通过少计算来提速 |
| 中途取消、保存后退出再进入 | 本批实包检查通过，取消时原档保留 |
| 日常运行帧率、数小时离线、所有终局工厂 | 本批没有证明这些场景获得同样提升 |
| 网页、安卓的 Rust 加速 | 尚未接入；已发布的共享优化与本批 Windows Rust 收益要分别看 |

## 这批具体做了什么

首先，把普通游戏存档真正连接到 Rust 离线计算入口。原来部分验证依赖提前准备好的原生检查点；现在普通保存来源也能上传到本地 Host，生成候选，经过完整检查后再保存，并通过实际游戏入口重开。

其次，减少大存档读取时大量细碎的底层读取，使用有界缓冲；仍核对完整数据和哈希。随后清理 JS 参考计算中的无用扫描、无关库存复制和重复建设规划，让跨语言结果校验更有效率。这几项收益不能互相相加；上面的 71.3% 是最终新包重新跑出的完整等待对照。

最后，落实你的后台测试要求：测试窗口隐藏、不抢焦点、静音，并使用实际离屏绘制。本批八个实包场景共 **32 次启动和正常关闭**，没有显示窗口、聚焦或原生弹窗。一次只跑一个本机测试任务，低优先级运行；完整模拟和私人存档测试至少保留 6 GiB 可用内存才启动，运行中触及 2 GiB 停止线则由独立监控终止本次测试。不会关闭你的其他程序来腾资源。

## 性能数字怎样测出来的

同一份公开工厂、同样 5 秒离线、同一后台驱动，交替运行新旧包三对。旧包为 `33d96597`，新包为 `2bb10b43`。

| 配对 | 旧包等待 | 新包等待 |
| --- | ---: | ---: |
| 第 1 对 | 20.864 秒 | 5.959 秒 |
| 第 2 对 | 20.789 秒 | 6.067 秒 |
| 第 3 对 | 20.582 秒 | 5.680 秒 |
| 中位数 | **20.789 秒** | **5.959 秒** |

六次都核对完整候选、真正保存的内容和两次正常重开。额外做了一次取消和一次由游戏实际保存产生来源的测试，这两次用于检查正确性，没有混进性能配对。此次测量不代表正式旧版 1.2.6 与新版的直接比较，也不代表你提供的 11 万实体终局档。

## 检查通过了哪些，还卡在哪里

| 检查 | 最新明确结果 |
| --- | --- |
| `2bb10b43` 本机完整游戏单元检查 | 3,166 通过 / 39 跳过 / 0 失败 |
| 同提交第一次 Windows 云端完整检查 | Rust 1,363 通过 / 5 忽略；Native 674 通过 / 1 跳过；游戏单元 3,166 通过 / 39 跳过，均零失败 |
| 新 Windows 开发包实际大工厂入口 | 八个场景通过；75 件包内制品、78 件冻结文件校验通过 |
| 本机完整浏览器检查 | 428 通过 / 33 跳过 / **27 失败** |
| 前一轮 c6 PR 云端完整浏览器检查 | 401 直接通过 / 33 跳过 / **37 失败** / 17 重试后通过；两项新测试修复尚未包含 |
| 新 0ce 云端浏览器第二分组 | 208 直接通过 / 22 跳过 / **6 失败** / 5 重试后通过；第一分组仍待结果，不是完整合计 |
| PR 第二次 Windows 云端检查 | 核心 1,110 通过 / 5 忽略 / **1 失败**；后续 Host、Native 和游戏单元步骤未执行 |
| 后续 c6 Windows 云端完整检查 | Rust 1,363 通过 / 5 忽略，Native 674 通过 / 1 跳过，游戏单元 3,166 通过 / 39 跳过，均零失败；尚不含两项最新测试修复 |
| 线程参与修复后的本机核心 | 1,113 通过 / 5 忽略 / 0 失败，严格静态检查通过；新源码云端待验 |
| 恢复入口启动前提修复后的浏览器专项 | 本机 13/13；新 0ce 云端同 13 项全部首次通过 |
| 授权终局档经过现有恢复流程后的短时检查 | 一秒真实 Rust 候选与 JS 完整状态一致，原档不变；时间预算组合专项 21/21、类型检查通过，尚非完整 UI 验收 |
| 运维测试同步修复后的检查 | 本机专项 4/4、完整 Ops 56 通过 / 6 平台跳过；Linux 云端 60 通过 / 2 跳过，均零失败 |

目前有四类收尾工作：

1. **浏览器回归仍有缺口，一组恢复检查已在本机及云端通过。** 前一轮 c6 有 15 份挂机失败现场（含重试）尚未完成初始化；测试现先等运行时就绪，再执行原停止/保存断言，相关 13 项在本机及新 0ce 云端全部首次通过。新第二分组仍有 6 失败 / 5 重试通过，涉及画布帧数、带端点、警报卡片选择、菜单耗时及设置；第一分组继续等待。截图和轨迹已经实际留存，不能统一归因于旧问题或机器慢；[本轮记录](./reviews/rust-rp1-recovery-test-startup-2026-09-09.md)保留原结果和修复范围。
2. **多线程验证已补正确前提，云端待验。** 已做出可重复反例：短任务可能在另一条线程参与前就由一条线程完成。专项现在等待实际第二条线程进入，保留原参与数量和完整结果断言；无法满足时仍明确失败。修正后的正常优化完整核心 **1,113 通过 / 5 忽略 / 0 失败**，严格静态检查通过；新源码云端仍待验，不能用本机结果覆盖旧云端失败。见[本轮记录](./reviews/rust-rp1-worker-participation-2026-09-09.md)。
3. **超大终局档的诊断前提已纠正，完整入口仍待验证。** 原先直接交给 Rust 的测试跳过了菜单已有的时间加速恢复步骤。这份原档的待提交时间其实为零；按实际恢复函数处理后，11 万实体工厂的一秒 Rust 候选与 JS 完整结果一致，原文件未动。无需为这项前提再开发一套恢复规则；下一步测真实菜单的完整等待、取消、保存和重开。该一秒检查使用明确的短时诊断条件，不能代表原档至今约十天的离线已结算，也没有证明其整体提速。见[复核记录](./reviews/rust-rp1-recovered-endgame-source-2026-09-09.md)。
4. **历史堆损坏根因尚未确定。** 已冻结对应程序、符号、转储和调用栈，原案例独立复验通过；这属于异常隔离证据，不等于根因修复。还需保留稳定性观察和专项验证。

## 当前进度与下一步

现在处于 **RP1“离线加速实际入口”已取得收益、完整阶段验收继续进行**。可以确认“一个代表性大工厂确实等得更短，而且结果能安全保存”，不能把它写成“Rust 已全面接管游戏”。网页/安卓 Rust、长离线自动采用和实时模拟接管仍是后续阶段。

下一批继续核对已修复回归的云端结果，并验证终局档现有恢复路径下的真实菜单完整流程，再依据测量结果推进有界长离线。保持原 1–30 秒采用范围、数据保护和性能门槛；测试继续后台运行。Windows 开发包目前为 `NotSigned`，合并、安装升级、正式签名与发布检查另行完成。

代码已在独立开发分支持续提交，并建立 [草稿 PR #31](https://github.com/snowsnow0926/DSPONLINE/pull/31)。详细构建身份、失败证据和复验路径见[本批技术记录](./reviews/rust-rp1-current-batch-validation-2026-09-09.md)；完整开发目标仍在进行中。
