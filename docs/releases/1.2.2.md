# 1.2.2 正式发布记录

> 发布日期：2026-08-27  
> 运行时提交：`8b9c93e13270dbc7046840364f7b8384dbc4b902`  
> Release ID：`1.2.2-8b9c93e13270`  
> Build ID：`1.2.2+8b9c93e13270`  
> previous-stable：`1.1.9-c3f4eff6cb5a`

## 结论

1.2.2 已完成香港 Web/API、上海 Web/API、上海下载页、Windows stable 和 Android stable 全量发布。两地均使用新建不可变目录和 release-control 原子切换；上海下载页同步原子切换，香港 `/canary/previous/` 已更新到不可变 1.1.9。GameState v47、envelope v2、云 schema v8 和 SQLite layout v3 均未升级；没有恢复、覆盖或迁移生产数据库，没有修改玩家存档、账号、排行榜或生产业务数据。

Windows 安装包继续按既有策略明确为 `NotSigned`。Android APK/AAB 使用已批准长期证书，证书连续性、APK v2/v3、zipalign、包名和版本元数据均通过。用户本轮明确要求直接全量发布，因此 Android 实体设备项记录为本候选专属豁免；API 36.1 模拟器从 1.2.1 使用 `install -r` 覆盖升级通过，不能把该豁免复用于后续版本。

## 候选与门禁

- 冻结候选 manifest：10/10，aggregate SHA-256 `b2e3a477a8b51ab5567c9e0be0e083e018e3f5273222abd5186913da4bcd6c48`。
- source provenance：276 文件，aggregate SHA-256 `06c7c7a3fc3f35f751c72139556e1374af38f8fe8ac6bc91585bcef792d750e4`；已在独立 clean worktree 复验。
- Web：171 文件；API：182 文件；native feed：6 文件；download site：9 文件；native archive：2 文件，全部在本机与远端逐文件复核。
- 完整门禁：Vitest 1,575 通过/62 跳过；server 384/2 加 station 4/4；ops 56/6；native JS 33/2；Rust 20/20；Chromium 431/27；durable WAL 7/7；production preview 3/3，均 0 失败。
- Windows 隔离启动：5 个进程均 responding，退出后无残留；setup 与 unpacked 均确认 `NotSigned`。
- Android 模拟器覆盖升级保持 `firstInstallTime`，进程正常，未发现 Fatal/ANR。

主要正式制品：

| 制品 | 字节 | SHA-256 |
| --- | ---: | --- |
| Android APK | 5,316,145 | `67f340c7d2585c21e196d59dcf049760cfc7f1a7975eb2f21561150211edec31` |
| Android AAB | 5,105,232 | `4fabf007426a9d4ba64d43edd406382173f6aa12c18740f4fdc1943e9f50f429` |
| Windows setup | 104,838,532 | `b2ff8dbb5cfb1b06470de15e719851af64f7e37d519eb9f2a5dc28ff2d017b4b` |
| Windows blockmap | 112,610 | `8cf411515bc281c6a11efc9af2ba1c7e3f8dc10f936257258e0b5e8e0414edf5` |

## 备份、安装与切换

两地在任何 API/data-affecting 切换前分别创建新鲜 SQLite Backup API 快照，并验证完整 SHA-256、`quick_check=ok`、schema v8、layout v3、文件身份与磁盘水位：

| 节点 | 备份字节 | SHA-256 | 发布后磁盘 |
| --- | ---: | --- | ---: |
| 香港 | 3,772,833,792 | `4cd39443e64962f9840b6792be9e53ca120cbde24a2a3ab3bfb32943342e7833` | 60% |
| 上海 | 462,848 | `7dbd36dfda658d338853bc19556b4b3ffeaba92ce49cbc7271bdbd6ed95416a2` | 75% |

上传归档在目标机再次核对字节和 SHA 后，分别解压到新的 Web/API/control/download 目录。Web 171/171、API 182/182、下载站 9/9 文件哈希通过；两地隔离 API 专项为 68 通过/2 跳过/0 失败。候选 control 中一个只读排行榜报告 unit 模板与节点既有已验证 unit 不同，本次没有覆盖节点 unit；发布所需 active/proxy/preflight/health unit 均与已安装版本匹配。

香港首次 handoff 的候选预检通过，但新 control 目录安装时遗漏了 `api-writer-lock.sh` 的执行位，正式启动返回 126。Web/API 指针始终保留 1.1.9，代理进入 hold；自动恢复也因同一权限失败。Release Agent 只把该已核验脚本修正为 `root:ubuntu 0750`，重新启动旧 writer，并通过 release-control 的 no-op recovery 清除 pending、恢复 proxy forward 和公网 health/ready。没有删除 pending journal、没有绕过 writer lock，也没有接触数据库/WAL/SHM。

随后从同一已验证正式备份重新生成独立预检副本，完整哈希仍为 `4cd39443…`，第二次香港切换成功。安装流程已补记三份必须可执行的 control shell：`api-active-entry.sh`、`api-writer-lock.sh`、`switch-release.sh`。上海在切换前应用同一权限修正，一次完成切换。

最终状态：

| 目标 | current | previous | generation / slot | proxy |
| --- | --- | --- | --- | --- |
| 香港 Web/API | `1.2.2-8b9c93e13270` | `1.1.9-c3f4eff6cb5a` | 43 / blue / 4321 | generation 168 / forward |
| 上海 Web/API | `1.2.2-8b9c93e13270` | `1.1.9-c3f4eff6cb5a` | 25 / blue / 4321 | generation 71 / forward |
| 上海下载页 | `download-site-1.2.2-8b9c93e13270` | `download-site-1.1.9-c3f4eff6cb5a` | 原子软链 | 不适用 |

两地 API、handoff proxy、health timer 均 active/result=success，`NRestarts=0`，pending 为空；health/ready 为 200，`writable=true`，backup state 为 idle，node-health 无失败。正式备份 evidence 保留；已被发布控制器采用的预检副本按设计清理，相关残留为 0。

## 公网、下载和 PWA 验收

- 香港根站、SPA fallback、`version.json`、manifest 和 Service Worker 均为 1.2.2；入口/no-cache、hashed asset/immutable、有效体积 JS/CSS gzip 和 iframe 白名单均通过。
- 上海下载站 9/9 公共文件逐文件完整下载，字节与 SHA-256 全匹配；APK 和 Windows setup 的 Range 均返回 206、`bytes 0-1023`。
- Android stable feed 为 `1.2.2 / 1002002` 且 APK 哈希一致；Windows stable feed 为 1.2.2，setup 继续显示 `NotSigned`。香港 `/downloads/*` 保持 302 到下载域名。
- 全新 Chromium 通过受保护香港 SOCKS 与严格 TLS 验证：当前 Build 1.2.2、previous Build 1.1.9；只有 1 个 current root Worker，0 waiting/installing；访问 previous 前后 current shell cache SHA 不变；断网后正式根站可重新打开。浏览器层拦截了 5 个写请求，没有登录生产账号或向生产发送写请求。
- 香港 `/canary/previous/` 现在以 302/no-store/`Vary: *`/noindex 指向 `/canary/1.1.9-c3f4eff6cb5a/`；旧 Build 对根 `/sw.js` 精确返回 404，当前 Build 返回 200。
- previous 路由活动 snippet SHA-256 为 `71ddd2c4b323f94bd82358384c04aa851d5ef6e148dc0070403e7c3eb2ccc5dd`；变更前备份为 `dsp-idle-app.conf.pre-previous-fallback-1.1.9-c3f4eff6cb5a-20260827T120510Z`，SHA-256 `6cbd19d0cf8f757ae5af75b62077f844e57381946d8c116bf02dfb6561cb4263`。

完成验收后，仅删除了可由本地冻结制品重传、且未被任何进程引用的上传暂存文件：香港 2,652,227 字节，上海 112,668,515 字节。当前/previous/canary、有效备份、release evidence、数据库/WAL/SHM 和玩家数据均未删除。

## 回滚边界

- 香港与上海代码回滚目标均为 Web/API 1.1.9。只在 current generation、pending、服务健康和新鲜 evidence 仍满足时通过 release-control 回滚；香港大库回滚前必须重新生成独立 preflight evidence。代码回滚不得恢复数据库。
- 上海下载/Windows/Android stable 一体回切到 `download-site-1.1.9-c3f4eff6cb5a`，不得只改 feed 而保留不匹配制品。
- 香港 previous 路由可原子恢复上述 Nginx snippet 备份，并重新执行 `nginx -t`、reload、current worker、health/ready 与公网 smoke；该入口不是 API、数据库、主机或网络灾备。

玩法实现与真实存档证据见 [1.2.2 纯挂机 30 秒轻量采样开发报告](./1.2.2-pure-idle-30s-lite-development-report-2026-08-27.md)。
