# DSP极简网络 1.1.5 完整稳定发布记录

> 发布日期：2026-08-24
> 运行时提交：`a92c0d3157f3658523d8d4abbbb0ae654dc4fc35`
> Release ID：`1.1.5-a92c0d3157f3`
> Build ID：`1.1.5+a92c0d3157f3`
> previous-stable：`1.1.4`

## 发布结论

1.1.5 已完成香港 Web/API、上海 Web/API、上海下载页、Windows stable 和 Android stable 全量发布。两地均使用不可变版本目录并完成受控原子切换；香港固定 previous-stable 入口已更新为 1.1.4。未恢复、覆盖或迁移生产数据库，未修改玩家存档、排行榜或账号数据。

| 目标 | 最终状态 |
| --- | --- |
| 香港 Web/API | `web-1.1.5-a92c0d3157f3` / `api-1.1.5-a92c0d3157f3` |
| 上海 Web/API | `web-1.1.5-a92c0d3157f3` / `api-1.1.5-a92c0d3157f3` |
| 上海下载页 | `download-site-1.1.5-a92c0d3157f3` |
| Android stable | `dsp-idle-1.1.5-1001005.apk`，批准历史证书签名 |
| Windows stable | `dsp-idle-1.1.5-x64-setup.exe`，`NotSigned` |
| 香港 previous-stable | `/canary/previous/` → `/canary/1.1.4-7dbc149a016c/` |

## 冻结候选与开发门禁

- 冻结候选目录、运行时 SHA、Release ID 和 Build ID 与交接一致；发布过程中未重建或覆盖 Web/API 冻结制品。受保护 Android 构建在候选工作树留下的单个生成配置改动未进入任何制品，已保留不覆盖。
- candidate manifest 8/8，aggregate SHA-256 `429b2f57608f96f77f47f4a8015dc94ef68e161ff4fa77c3b4fad227ce7cf8e1`；provenance 3/3；SHA256SUMS 10/10，逐项复核通过。
- source manifest 261/261，aggregate SHA-256 `1d4e0ae07c3ecafb04a87825ce8a3963b9ab7612a03217bfa045073f2f2563dc`。
- Web 归档 SHA-256 `f1f0958134cb875c3eb645ce51d9256bc6fff1e91aec5479284987efbea18b5d`；API 归档 SHA-256 `7328bb8b1cdee03fd3a1cda8405248a6b85d49d7a1be8b00d34bb1ea667843c2`。
- 开发门禁按交接记录通过：Vitest 1,457、server 376、station 3、native 25、ops 56、Chromium E2E 427、PWA 1、durable recovery 7、release switch 29；条件跳过均有原因记录。

## Android 与 Windows

- Android 包名 `cn.dsponline.network`，versionName `1.1.5`，versionCode `1001005`。
- 冻结 APK：5,211,333 B，SHA-256 `56aa74f0b5f72be320bbaffa0f3476119458137741e21745e2b4fc5281f8036e`；v2/v3、zipalign、包名/版本和历史证书连续性通过。
- 冻结 AAB：5,000,249 B，SHA-256 `92fe6f0759cddf8a99c740da8ee78336edf7e370186e0b5d3f1ac35285f5e6a1`；与 APK 证书连续性一致，保留在不可变原生归档中。
- 本轮没有可用 Android 实体设备；用户已明确豁免 1.1.5 Android 实体设备门禁并接受该风险。该豁免不改变签名、包元数据或静态验收结果。
- Windows setup：110,426,594 B，SHA-256 `8304d9bae267dcff4f480daf178bbb49b71e5c39148ca9c16065500a14a9b5cb`；blockmap SHA-256 `08f555b95dd7b91f54acab651c37e9396a295788ede10bbf4e8106078b525f78`。继续采用历史 `NotSigned` 策略，未创建新证书。

## 备份与数据门禁

两节点在 API/data-affecting mutation 前均创建并验证新的 SQLite Backup API evidence，quick_check、schema/layout 和哈希通过，磁盘低于 90% 保护线：

| 节点 | 快照字节 | SHA-256 | 检查 |
| --- | ---: | --- | --- |
| 香港 | 3,772,833,792 | `25aa4cd311585fefb60a9de60166249f96a5e96aa7c68c9319d0e695e5da5b38` | quick_check=ok、schema 8、layout 3；根盘约 78% |
| 上海 | 462,848 | `29a081addebfa818bc2448779f4f27d373be604008391956dd0b3b4d89eb6e05` | quick_check=ok、schema 8、layout 3；根盘约 87% |

正式备份 evidence 在切换后仍可独立验证。上海的临时 preflight 源文件按流程清理，因此收尾时不能再次对该临时源执行全量读回；这不影响已保留、可验证的正式 Backup API evidence。未删除 live DB、WAL/SHM、有效备份、current/previous/rollback/canary 或 release-control 状态。

## 香港切换结果

- generation **35**，current slot **blue / 4321**；proxy generation **135**，mode `forward`。
- current Web/API 均为 1.1.5；previous Web/API 均为 1.1.4（`web-1.1.4-7dbc149a016c` / `api-1.1.4-7dbc149a016c`）。
- `pending-switch.json` 不存在；API、handoff proxy 和健康定时器正常，`NRestarts=0`；本地/公网 `/api/health` 与 `/api/ready` 均为 200。
- `/version.json` 返回 1.1.5/build `1.1.5+a92c0d3157f3`；版本响应 no-cache；Range 请求返回 206（`bytes 0-15/92`）。

## 上海与下载页

- generation **21**，current slot **blue / 4321**；proxy generation **59**，mode `forward`；current Web/API 均为 1.1.5，previous 均为 1.1.4。
- API、handoff proxy、health/node-health timer 正常，`NRestarts=0`，pending absent，本地 health/ready 200；offsite-backup timer 按上海既有策略保持 disabled。
- 上海切换辅助脚本在收尾阶段返回非零，但独立的 current/previous 指针、release-control audit 的 `release_switch_completed` 事件、监听器、健康和版本检查全部通过；未重复执行切换。
- 下载页 current 为 `download-site-1.1.5-a92c0d3157f3`，previous 保留 1.1.4；9 文件站点清单和远端安装目录完整。
- `https://download.dsponline.cn` 的根页、version、Android stable feed、Windows latest/release feed 均 200/no-cache；从主域名 `/downloads/...` 的 302 重定向最终均为 200。
- 公开 Android APK Range 返回 206（`bytes 0-15/5211333`），完整下载 SHA-256 为 `56aa74f0b5f72be320bbaffa0f3476119458137741e21745e2b4fc5281f8036e`；Windows setup 完整下载 SHA-256 为 `8304d9bae267dcff4f480daf178bbb49b71e5c39148ca9c16065500a14a9b5cb`。

## 公网、PWA 与 previous-stable 验收

- 浏览器主站显示 v1.1.5；`/version.json` 与公开 API 健康/ready 均通过。
- 香港 `/canary/previous/` 返回 302 到 1.1.4 不可变目录，previous 页面显示 v1.1.4；响应保持 no-store/no-cache、`Vary: *` 与 noindex 约束。
- 当前根 `sw.js` 的路由感知缓存命名保持 `dsp-idle-pwa-v2`，按 root/previous/canary 隔离缓存；浏览器加载主站与 previous 页面未发现版本串线。
- 香港 Nginx active snippet 的新 SHA-256 为 `a0b1a0f89a02b17237b4204efb198b83d132ab7a070243b4ade286f2f199ca35`；原 snippet 已以哈希备份 `3f86c5c49326c31f420a6bc3dab2a3ac83beaa0af14653ea904125d3e325eb3f` 保留，可恢复 previous 路由。

## 回滚边界

- 香港：仅在 current 仍为 generation 35、1.1.5、pending 为空且 evidence 有效时执行受控 last rollback，目标为 Web/API 1.1.4；数据库不回滚。
- 上海：仅在 current 仍为 generation 21、1.1.5、pending 为空时执行受控 rollback，目标为 Web/API 1.1.4；数据库不回滚。
- 下载页：回切到 `download-site-1.1.4-7dbc149a016c` 时必须同时恢复 Android/Windows feed 与制品引用。
- 香港 previous 路由可用本次保留的 pre-previous Nginx 配置备份恢复；恢复后必须重新执行 `nginx -t`、reload、current worker、health/ready 和公网 smoke。

开发证据见 [Release Agent 交接](../RELEASE_HANDOFF_1.1.5.md)、开发报告和 [候选记录](./1.1.5-candidate.md)。
