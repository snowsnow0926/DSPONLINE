# 1.1.3 发布 No-Go（2026-08-22）

## 候选与制品

- Runtime commit：`e59885bee29b26e3281144ffca74d68003072a7e`
- Release ID：`1.1.3-e59885bee29b`
- Build ID：`1.1.3+e59885bee29b`
- Web/API/native/download-site 组件清单、归档哈希、完整测试矩阵和 Android 受保护签名门禁均已在隔离候选中通过；Windows 按既有策略为 `NotSigned`。
- 未切换任何 stable 指针，未更新下载页，未发布 Android/Windows feed。

## 已完成的生产门禁

- 香港和上海 staging 目录均为不可变新目录，远端 component manifest 与版本/build 标记通过。
- 香港 Backup API evidence：schema 8、SQLite layout 3、`quick_check=ok`，3,772,833,792 bytes，SHA-256 `3df2bc25a04c8ff467f4cc91e66e18cd0e437b8f4a0b9126bef13cf7b08a748c`。
- 上海独立备份 evidence 通过（schema 8/layout 3）。
- 香港大库独立预检副本在 83%→88% 磁盘占用范围内完成并通过同一 SHA/quick_check；失败切换后仅清理该预检副本及其 sidecar，权威备份、数据库、WAL/SHM、发布目录和回滚目录未修改。

## 阻断原因

文档化原子切换在香港候选 preflight 就绪后进入旧 writer 排水阶段。稳定公网流量下，代理持续报告约 65 个 active requests、约 12 个 active writer requests，至少五分钟没有收敛到零。默认和延长至 300 秒的有界窗口均未满足 `drain`/`hold` 门禁；继续等待或强制跳过会丢弃玩家云写入/导出请求，故禁止绕过。

第一次尝试在 180 秒冷启动窗口处停止；第二次候选预检成功，但在 drain 阶段安全回滚。两次均保持旧代码/数据指针。

## 本轮重试记录（2026-08-22）

- 按 1.0.42 运维流程完成香港代理受控刷新：临时分离 active API 对 handoff proxy 的 `Requires=`，仅重启 proxy，确认 API PID 未变后逐字恢复原 unit；原 unit SHA-256 `d8b46f4437dc1010031b4290d9e22671685b1ce8d4ea24bfad1a14fd384a9010`，proxy module SHA-256 `2f908b40b5a715bf290ee4b5aad55256eae25c1e642abd4bb2677ec90fc16dd0`。刷新曾将计数清到 0/0，API/proxy/timer 与公网 health/ready 恢复 200。
- 重新生成并验证香港独立 preflight：3,772,833,792 bytes，SHA-256 仍为 `3df2bc25a04c8ff467f4cc91e66e18cd0e437b8f4a0b9126bef13cf7b08a748c`，quick_check/schema8/layout3 通过；候选隔离 writer 启动通过。
- 后台切换随后在 `drain → hold` 阶段再次遇到单个长期活动的非写请求：`activeRequests=1`、`activeWriterRequests=0` 在整个有界窗口内不收敛，恢复等待同样超时。未强杀请求、未跳过门禁；current/previous/pending 均未改变，旧 writer 始终 active、`NRestarts=0`。
- 切换器退出后已将代理恢复为旧 API `forward`，并清理本次精确 preflight 临时副本、sidecar 和陈旧 evidence；权威备份、生产数据库/WAL/SHM、发布目录和回滚目录保留。

## 再次受控重试（2026-08-23）

- 在重新生成同一哈希的独立 preflight 后，再次按受控代理刷新流程启动候选；API PID 保持不变，候选 health/ready 通过。
- 将 `DSP_RELEASE_DRAIN_TIMEOUT_MS` 设为文档允许的最大 600 秒（未跳过 drain/hold）。代理进入 hold 后仍有一个持续读取响应无法结束，`activeRequests=1`、`activeWriterRequests=0`；排队请求按超时返回并未触碰数据库。
- 切换器在保护性恢复阶段退出，current/previous/pending 未变；随后通过固定 recovery helper 恢复旧 API forward（generation 114），本地/公网 health/ready 重新为 200，两个 health timer active，API/proxy `NRestarts=0`。
- 失败 preflight 副本、sidecar 和 evidence 已按精确路径清理，根盘回到约 83%。没有强杀连接、手工编辑 proxy 状态、停止旧 writer、修改数据库/WAL/SHM 或切换任何 stable 目标。

## 收口状态与回滚边界

- 香港 current Web/API 仍为 1.1.2，switch generation 31；proxy generation 114、mode `forward`；pending journal 不存在。
- 香港 API/proxy/health/node-health timers active，API/proxy `NRestarts=0`；代理已恢复 `forward`，本地和公网 `/api/health`、`/api/ready` 均 HTTP 200。
- 香港根盘约 83%；上海未执行任何切换，下载页和原生 stable 未改变。
- 代码回滚指针：香港现有 1.1.2 Web/API 目录；数据库保持原文件，不执行恢复。
- 本次没有修改玩家账号、云存档正文、排行榜或生产数据库。

## 继续发布的必要前提

在新的发布窗口重新验证同一 immutable candidate/manifest 和新鲜 Backup API evidence，并确保香港进入低流量维护窗口、在途请求（尤其长期非写请求）可归零；或由开发/运维角色先修复并测试代理在长请求负载下的排水策略。不得用手工改 proxy 状态、强杀请求、缩短 readiness/drain 门禁或部分切换来代替该前提。
