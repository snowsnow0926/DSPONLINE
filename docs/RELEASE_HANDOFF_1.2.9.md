# 1.2.9 开发交接

> **后续状态：已于 2026-09-11 发布。** 运行源码仍为本交接的 `0521eb63`；发布阶段完成最终 APK 直接升级/明确保存/重开、本机完整浏览器与原断言补验，以及双站、安卓下载和 PWA 回退隔离。首次失败和未验证范围保留，详见 [最终发布记录](./releases/1.2.9.md)。下文保持 9 月 10 日交接时点，不能将其“暂停”误作现在线上状态。

2026-09-10，Role: develop。**代码及候选制品已交付；发布验收未完成，未部署。**

## 范围与冻结来源

网页/安卓共享保存与模拟准备优化、WASD 平移、生产区域与节点联动均已实现。另修复部分折叠节点的连线端点、手机报告误弹退出提示、动画偏移及导航尚未提交时关闭报告的问题。GameState v47、envelope v2 不变；不包含跨端 Rust 核心或 Windows 实时接管。

- 分支：`codex/1.2.9-shared-performance`。
- [PR #33](https://github.com/snowsnow0926/DSPONLINE/pull/33)，保留 Draft。
- 制品源码：`0521eb63f179c78421b2dbed7216f46903d6a684`，已推送。
- 构建 ID：`1.2.9+0521eb63f179`。
- 原 main 的未提交工作、暂停的 Rust 工作树和玩家原存档未改动。

## 不可变候选

本地根目录：`D:/GameDev/DSPidle2-v129-shared-performance/artifacts/1.2.9-candidate-0521eb63/`，不在 Git 中。制品来自独立 clean checkout，构建后再次确认 clean。

| 文件 | 大小及 SHA-256 |
| --- | --- |
| `dsp-idle-1.2.9-1002009.apk` | 5,672,736 B；`06d0043a542faab0f7e3e2be2fad18b7d2da43c672e66da88ccf7255fe953bf7` |
| `dsp-idle-1.2.9-1002009.aab` | 5,461,984 B；`612b045047938306038133939f156651ee26994c102db6e1a75a202fcf609b2a` |
| `candidate-manifest.json` | `5fe673fbbaacb8d64ab2355e886673506abf289a85b7cd92b176399d2c34c9fd`，406 个文件，`BUILT_NOT_PUBLISHED` |

`web/` 与 `android-web/` 分别冻结，构建时间为 `2026-09-10T15:13:58.297Z` 和 `2026-09-10T15:12:47.743Z`。APK 包名 `cn.dsponline.network`，版本 `1.2.9 / 1002009`。受保护入口验证 APK v2/v3、zipalign、长期证书及 AAB 证书连续性；未新建或替换签名证书，未暴露凭据。

`cd76fa61` 构建因 App 行号变化导致覆盖清单过期而失败；只重新生成 44 处行号形成 `0521eb63`，能力与归属不变。最终类型、构建、启动预算、原生 UI 边界和覆盖清单全部通过。旧失败目录不能冒充最终制品。

## 已有验证

- 云端 `780ed5f3` 完整单元 **3,183 pass / 36 skip**，类型、许可证、生产构建及 Server/Ops/Native 任务成功。本机此前 **3,123 pass / 96 skip** 是 native host 不可用时的另一环境，不能重复相加。
- 最终运行代码的画布、触屏、手机报告、普通离线及挂机停止恢复三文件 **21/21**；Android 编译模式的启动租约 **6/6**、画布 **4/4**，新增报告动画/字体/中心命中检查通过。浏览器平台仿真不等于实体手机。
- 正式 1.2.7 的 196 个冻结文件逐个校验后升级到最终 `0521eb63`：在线和离线重开的主档字节完全相同；进入原工厂、构建版 WASD、再次保存和独立 checksum、库存、区域、施工托盘、游戏时间比对通过。证据：`artifacts/pwa-127-to-129-0521eb63/result.json`。
- 同机公开 4.1 MB 合成档，正式 1.2.8 源码与 `dba89809` 保存代码交替三组，主档 ACK 中位数 **694.1→469.5 ms，降低 32.4%**。完整状态、备份、快照一致，之后源变化不涉及保存实现。不能宣传为整版或安卓真机提速。
- 约 108 MB 授权终局原档预热校验通过，正式对比因内存保护停止，没有正式提速比例。原档哈希未变，未进入公开制品。

## 完整回归仍有未过项

已完成的完整浏览器结果绑定前一候选 `780ed5f3`：第一分片 **232 pass / 11 skip / 4 fail**，第二分片 **222 pass / 22 skip / 2 fail**。

- 冷菜单 P95 **627 ms > 500 ms**，暂停画布峰值帧 **849.9 ms > 800 ms**。门槛没有放宽；本机通过不能替代云端失败。
- 三个交互用例在云端受附带离线报告或自动恢复矿点的遮挡影响；本机逐项复验通过，云端仍记失败。
- 固定 Windows 字体高度的绕行线断言已改为实际测量，保留 64 世界单位间距与命中检查，本机复验通过。

最终源码 [CI 34495438147](https://github.com/snowsnow0926/DSPONLINE/actions/runs/34495438147) 已启动，交接时仍运行，未宣称全绿。

## 安卓设备验收未完成

旧匿名 AVD 曾从正式 1.2.8 直接覆盖到 `780ed5f3`，安装身份、旧目录时间及进入原工厂通过，并暴露了已补修的手机报告问题。

最终 `0521eb63` 复验时，WebView 日志报告 `Corruption: missing start of fragmented record`，随后报告 `IndexedDB recovering from a corrupted (and deleted) database`，界面显示空目录。**这不能算最终留档通过。** 原因未确定，不能归因于或排除游戏代码。WebView 在取证时已重建数据库；保留的是设备文件、截图与观察记录，不声称保住了已被其删除的数据库。

另建全新 `DSPidle129Final`，计划正常停止应用并同步磁盘后直接升级；其 `/data` 使用 15%、剩余约 4.8 GB，但在正式旧版 1.2.8 安装/启动阶段即触及主机内存底线（最低 2,090,580 KiB），尚未安装候选，测试被保护程序终止。当前本机缺少稳定的设备验证条件，已停止重试，所有自有模拟器均已关闭。个人 AVD、其他程序、生产账号及玩家存档未动。

尚缺稳定设备上的最终包直接升级、完整留档/保存/重开，以及实体手机和长时间后台、温度、耗电验证。不得复用历史豁免或把浏览器仿真冒充通过。

## 发布保持暂停

项目 [SKILL.md](../.codex/skills/develop-dspidle/SKILL.md) 要求：**“A production release requires the full matrix.”** 当前设备验收和完整浏览器未过项尚未收口，本交接不代表可发布结论。

本任务没有连接或修改生产服务器、云档、API、数据库、下载页、stable feed、版本指针及 Windows 下载。后续应先解决未过项，再固定发布 manifest/provenance，按获批站点执行独立发布与回滚流程；香港和上海保持独立。

操作说明见 [用户报告](./USER_REPORT_1.2.9.md)，过程证据见 [开发记录](./DEVELOPMENT_1.2.9.md)。
