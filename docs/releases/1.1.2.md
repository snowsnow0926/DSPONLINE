# 1.1.2 服务端发布记录

> 发布日期：2026-08-21
> 状态：香港、上海 API 与发布控制已完成；Web 继续使用已验证的 `web-1.1.2-48d70aa-cloudfix2`
> 固定源码：`ebb29977b1cdc8cc5de7770acafe239daa449452`
> Release ID：`1.1.2-ebb29977b1cd`
> Build ID：`1.1.2+ebb29977b1cd`

## 发布范围

本次只发布排行榜异常复核策略、只读复核报告与配套服务端控制，不发布新的 Android、Windows 或下载页制品。异常检测只进入人工复核队列；不会自动限制排行榜、自动移除提交、封禁账号、关闭登录或删除云档。账号与全部云档继续由既有 API 写入服务管理。

API 便携清单包含 178 个文件，聚合 SHA-256 为 `06a5c20708220fdcc0236c3c4d3a9c832a2191767c35cb61b58a8fe851f725db`。上传归档为 687,338 字节，SHA-256 `66a30c93a641f026fd2af8937f72e0d18c8b74436d40a06edb8da3257a0000c3`；便携清单为 29,831 字节，SHA-256 `39d5f25749fb0117442d1a27b38fb0fe6360d1a84de5b297be0a2437188fa7f0`。两节点均在不可变目录完成清单逐文件校验、`npm ci --omit=dev` 与 `better-sqlite3` 加载验证。

## 容量清理

- 香港仅删除两个已有权威证据替代的旧 release-preflight 克隆，共释放 7,545,675,776 字节。
- 上海仅删除 19 个过期或冗余的离站备份分片/清单文件，共释放 8,019,582,976 字节；保留两组最新且已验证的恢复点。
- 发布完成后，两节点各删除 717,169 字节的 `/var/tmp` 上传暂存副本；本机发布制品、远端不可变发布目录、current/previous 和权威备份均保留。
- 最终根卷使用率为香港 80%、上海 77%。没有删除生产 SQLite、WAL/SHM、玩家正文、账号数据、唯一有效备份或回滚版本。

## 备份、预检与切换

两节点在切换前分别通过 SQLite Backup API 创建并验证新的权威快照：

| 节点 | 字节 | SHA-256 | 校验 |
| --- | ---: | --- | --- |
| 香港 | 3,772,833,792 | `adb0c132fc0c2fa48146da37eb2d5cc314e63b3a2cf60694480b6205829144a3` | `quick_check=ok`、schema 8、layout 3；同哈希独立预检克隆 |
| 上海 | 462,848 | `dfa8476adadc88fcf28cba5774a246ad04e96f29f21552a17b1d934299ebfbc7` | `quick_check=ok`、schema 8、layout 3 |

最终切换状态：

| 节点 | current API / control | previous API | generation / slot / port |
| --- | --- | --- | --- |
| 香港 | `api-1.1.2-ebb29977b1cd` / `1.1.2-ebb29977b1cd` | `api-1.1.1-da53958b860c` | 31 / green / 4322 |
| 上海 | `api-1.1.2-ebb29977b1cd` / `1.1.2-ebb29977b1cd` | `api-1.1.2-48d70aa` | 17 / green / 4322 |

香港首次切换被交接代理的残留 active/writer 计数阻止，切换器在停止旧 writer 前主动失败并保持旧 API 可写；未出现数据库交接。复核 Nginx/API 日志确认当时存在大云档上传，后续不强制中断真实写入。最终在 drain 状态确认 4330 无 established socket 后，仅重置交接代理进程与残留计数，旧 API 的直接 health/ready 保持 200，切换器随后完成单写交接。最终代理 generation 100、forward、active/writer/queue 均为 0，API 与代理 `NRestarts=0`。

排行榜复核 oneshot 首次试跑因目标报告目录尚不存在，被 systemd namespace 门禁以 226 拒绝；API、数据库和排行榜数据均未触碰。创建精确的 `ubuntu:ubuntu 0750` 报告目录后，两节点试跑成功。

## 夜间人工复核报告

两节点均启用 `dsp-idle-leaderboard-review-report.timer`，下一次触发为 2026-08-22 22:00（Asia/Shanghai），之后每日 22:00 生成只读报告并保留最近 30 份。服务只依赖 `dsp-idle-api-active.service`，没有引用或启动旧 `dsp-idle-cloud.service`。

首次报告的复核队列为 0，策略字段为：

- `automaticRestriction=false`
- `automaticSubmissionRemoval=false`
- `manualActionRequired=true`

## 最终验收

- 两节点 `/api/health` 为 200，schema 8、SQLite layout 3；`/api/ready` 为 200、`writable=true`、`shuttingDown=false`、`pendingWrites=0`。
- `/api/ready` 按当前契约不返回 `ok/schema/layout` 字段，因此旧 preflight 文本中的 `ready_ok=false` 与 `ready_schema=unknown` 不是失败；health 与 ready 各自字段均通过。
- 两节点 pending switch 为空，活动 API、交接代理、健康 timer 与复核 timer 均 active；旧服务与 preflight unit 均 inactive。
- 香港最终为 945 个账号、754 个普通主云档、698 条普通榜提交、6 条速通提交和 49 条既有排行榜限制；这些计数相对发布前没有下降。上海保持空数据副本。
- 香港当前主云档检查 754 条，其中 752 条可解析、2 条沿用既有缺失 payload 行状态；missing blob、metadata mismatch、invalid metadata 均为 0。发布没有扩大该既有差异。

## 回滚边界

代码回滚使用当前切换器 `--rollback-last` 和本次权威 backup evidence；香港大库回滚前需重新生成同源预检 evidence。代码回滚不恢复数据库。两节点本次与 previous API、控制目录、权威备份和证据均继续保留。
