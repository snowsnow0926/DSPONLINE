# Windows Rust 开发进度｜2026-09-10

目标仍是完整 Windows Rust 版本，开发继续进行，尚未完成或发布。测试始终后台、静音、不显示窗口、不抢焦点；不关闭你的应用、不改玩家原档。

## 本批已经确认的进展

最新 `c9aa106a` Windows 开发包已构建并冻结，76 项制品、79 个文件校验一致。新增的安装身份读取已在真实 Electron 包内通过：版本、源码、Rust 程序和界面包摘要都与独立核对一致；整个探针没有创建窗口或抢焦点。首次包测试发现的 ASAR 文件接口问题已修复，失败记录保留。

约 108 MB、11 万设备和 23 万条带的终局存档，**取消结算流程此前在 1df 包中完整通过**：Rust 精确计算 10 秒后的完整状态与原 JavaScript 规则一致；取消后主存档没变，退出后两次重开仍保持原档。四次游戏启动均隐藏静音，全部正常关闭。新 c9aa 包尚未重做完整游戏入口，不能继承旧包的这项通过。

**成功结算后的完整进入、保存与重进还未过关。** 一轮 9 秒结算进入设置约需 90.5 秒，随后诊断工具的整档传输失败；另一轮 14 秒结算没有在原 90 秒门槛内进入可操作工厂。不同轮的离线秒数和负载不同，不能拿它们直接比较提速。

大存档取证工具已改为分片传输，完整读取、总大小和 SHA-256 校验通过。这个改动解决了当前诊断路径的传输问题，不是新的玩家存档格式，也不能替代游戏保存验收。

## 正在开发的性能改动

Rust 检查完整状态时会产生大量细碎的哈希输入。这一批增加固定 1 KiB 缓冲来合并这些输入，保留原来的全部字段、顺序和校验算法。最终代码已通过完整核心、实际 Host 和完整终局结果对照；公开合成场景的校验耗时少约 **14.6%**。

真实终局存档固定计算 9 秒，三组前后配对的完整 Native 请求中位从 **49.90 秒到 49.02 秒**，约少 **1.8%**。样本区间重叠，其中一对还慢了一些，因此目前只观察到小幅差异，不能宣传明显或稳定的整段提速；这也没有包含菜单、进入工厂和后续保存的全部等待。

开发同时推进到实时接管的前置环节：桌面程序现在能从自己的安装文件核对版本、源码标识和实际 Rust 程序摘要。相关专项 51 项通过、1 项权限限制跳过。内容与规则身份、Host 独立核对和正式准入还要继续接，当前玩家实时接管仍关闭。

已有的历史收益仍成立于各自冻结候选：公开大工厂的短离线完整等待曾从约 20.8 秒降到约 6.0 秒。这个结果不能推广成所有终局存档、实时游戏或最新候选都提速同样比例。

## 全部检查的真实状态

| 项目 | 当前结果 |
| --- | --- |
| 新冻结包的实际 Host/助手集成 | 36 项通过；类型、桌面构建和相关门禁通过 |
| 1df 的终局取消、完整候选与重开 | 通过；成功结算后的完整持久流程仍待收口，新 c9aa 实包入口未验 |
| 9e 本机完整优化核心 | 1,116 通过、6 项 opt-in 忽略；另行微测 1 项通过 |
| 9e 本机实际 Host / Windows 启动器 | 分别 36 / 16 项通过，无失败或跳过 |
| 9e 云端 Windows 全套 | 通过：Native 工具 851/1 条件跳过，游戏单元 3,201/39 条件跳过，均无失败 |
| 9e 云端构建与 Linux 类型/单元 | 通过：游戏 3,198/42 条件跳过，Server/Ops/Native 通过 |
| 9e 浏览器分片 1 | 241 通过、11 条件跳过、2 失败，无重试后通过 |
| 9e 浏览器分片 2 | Chrome 初始化超时，尚未执行游戏测试；不计为游戏通过 |
| 前一版 1df 完整浏览器 | 465 通过、33 条件跳过、4 失败、1 重试后通过，发布门槛未过 |

前一版 Windows 启动器失败的根因仍未确定，新版本机及云端复验已通过且增加了阶段诊断。9e 浏览器失败包括星图场景前的附带离线报告（测试固定期待 1 秒、实际 2 秒），以及暂停画布的峰值帧间隔。另一分片尚未执行，因此此前冷菜单和设置问题也没有完整复验结论。保留原门槛，不把重试、跳过或旧包通过当作新包通过。

## 距离完整版本还差什么

| 阶段 | 要完成的内容 |
| --- | --- |
| 离线与大存档 | 终局成功进入、持久保存、两次重进；进一步支持复杂长离线 |
| Rust 实时接管 | 可信资格与实际单一模拟所有者；目前玩家实时接管仍关闭 |
| 完整游戏与兼容 | 普通/竞速、内容包、所有玩法与命令、导入导出和云存档往返 |
| 可交付 Windows 版本 | 完整回归、全流程性能与内存、长期稳定性、不同硬件、签名和安装升级回退 |

当前没有可信的总完成百分比或总剩余天数。按这些阶段逐项验收、提交和汇报，全部交付条件满足后才结束 Goal；本目标不直接操作生产发布。

[本批准确技术证据](./reviews/rust-rp1-canonical-buffer-2026-09-10.md) · [实时接管前置进度](./reviews/rust-windows-installed-program-2026-09-10.md) · [完整执行目标](./rust/windows-full-development.md) · [此前输入修复](./reviews/rust-windows-native-limit-drafts-2026-09-10.md)
