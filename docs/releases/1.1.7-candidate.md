# 1.1.7 候选记录

状态：开发候选，尚未发布。本文只记录隔离工作树中的证据，不代表香港/上海线上 current、下载页或任何签名原生包已经切换。

## 范围

- 修复空间站合同在同一任务日重复生成后，`offers`、`accepted`、`history` 使用重复 ID，旧服务端因此返回 `SAVE_FORMAT_INVALID` 的加载/上传链路。
- 修复 v30 玩家存档迁移到 v47 后，持久稀疏投影省略默认 `quantumMode`、而服务端仍按缺失字段拒绝量子端点的兼容问题。
- 让已注册内容包中有有效制造成本的自定义建筑进入桌面和移动端部署托盘；用安全的通用 `kind` 分类，不开放脚本注入或新的模拟语义。

## 隔离与兼容边界

- 工作树：`D:\GameDev\DSPidle2-v117-development`
- 分支：`codex/1.1.7-contract-mod`
- 基线：1.1.6 固定提交 `4f6d24f`
- 1.1.6 工作树：`D:\GameDev\DSPidle2-1.1.6-pure-idle`（只读对照，未修改）
- 协议：GameState v47、save envelope v2、cloud schema v8、SQLite layout v3、Mod JSON v2（兼容 v1）均不升级。

## 玩家存档证据

新增反馈文件 `C:\Users\WINDOWS\Downloads\dsp-idle-save-2026-07-23 (1).json` 仅做只读检查：原始 envelope 格式和 checksum 有效，state 为 v30，迁移后为 v47。将迁移后的规范稀疏正文提交给 1.1.5/current 服务端时，缺失的 v47 端点默认字段触发旧 validator；显式补入 `quantumMode: "legacy"` 后接受。修复改在服务端 validator 的 v47 缺省值解释，不改写玩家文件、不生成伪造收益、不访问生产数据库。

此前反馈的空间站存档形状也已以合成等价 fixture 覆盖：历史奖励保留，`settledIds` 形成不可重复领取的 fence；冲突的活动 `offer`/`accepted` 只在其 ID 已结算时移除。

## 验收标准

1. 合法旧存档可以导入、迁移、规范序列化并上传；显式 `null`/未知 `quantumMode` 仍 fail closed。
2. 重复合同不会重复发放 marks/reputation，历史和 featured 合同不丢失；伪造的活动合同碰撞仍被拒绝。
3. 有成本的 Mod 建筑同时出现在桌面/手机部署与删除托盘，分类稳定；`belts` 条目继续走传送带语义，`kind: "splitter"` 不冒充 belt。
4. 1.1.6 保存、云修订、蓝图、核心建筑顺序和无 Mod 存档保持兼容。

## 发布前剩余门禁

候选还必须在 clean commit 上重新生成 Web/API/原生诊断制品、manifest/SBOM/provenance，并由 Release Agent 运行真实 Linux、两地预检、公网 smoke、Android 签名与 Windows 签名策略门禁。本分支不执行部署、签名、下载页更新或生产写入。
