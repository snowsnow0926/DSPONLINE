# 1.1.2 香港 Web-only 发布记录（2026-08-21）

## 收口状态

本记录只描述已实际观察到的香港 Web-only 稳定切换，不把未完成的跨节点发布写成全量成功。

- Runtime SHA：`48d70aa5de065704d7360683f5be8cabd5c7c2e6`
- Release ID：`1.1.2-48d70aa5de06`
- Build ID：`1.1.2+48d70aa5de06`
- 香港 Web current：`web-1.1.2-48d70aa`
- 香港 API current：仍为 `api-1.1.1-da53958b860c`（本候选服务端无代码变化，未做 API writer 切换）
- 香港 Web previous / 直接回滚：`web-1.1.1-da53958b860c`
- 香港 switch generation：`29`
- 香港 API、数据库、WAL/SHM 和玩家数据：未修改

## 本地冻结制品

候选来自独立 clean checkout，组件 manifest、provenance 和 SHA256SUMS 已复验。关键归档如下：

| 制品 | 大小 | SHA-256 |
| --- | ---: | --- |
| Web archive | 1,801,446 | `b3322d177a3eca7a7a349e01c1a5fb983516d416cc8dc9dd07b295568d3d8bbc` |
| API archive（未切换） | 670,258 | `1324e7fdb5f5362a07b7033611151efaab38b6e879c726ce4bb323c09d32fd5c` |
| Android APK（未发布） | 5,254,097 | `1a5c4e6b251b70f7126ffd5135b9bdfdc4f9aaddcde661f6ec9e45af73e6b34c` |
| Android AAB（未发布） | 5,043,029 | `7bdb73c43e639646da7db4ebd3c9a54c852e7089bab61f3ea5eeef09dbf2bf4e` |
| Windows setup（未发布） | 103,516,414 | `2dbaacb3af6d270c1f6fce992ddd63e0b99836fd8fb5325f31f9ef9414dd0d00` |

Windows 按历史策略为 `NotSigned`；Android 本地签名连续性、v2/v3 和包元数据已通过，但没有进入上海 stable 下载页。

## 香港切换与验收证据

1. 新 Web 归档上传到不可变目录 `web-1.1.2-48d70aa`，168/168 文件及 component aggregate `4f60d5930928bff330d89d2ac1c2987ef0e77610af562fc30110008b66e378d0` 通过；`current` 在安装阶段保持旧版本，随后由文档化 switch 控制器原子切换。
2. API 指针保持 1.1.1，因本版本不含服务端变化而没有创建新的大 SQLite snapshot，也没有绕过 API 备份门禁。
3. 切换后香港 API/proxy/health timer 均 active，`NRestarts=0`，本机和公网 health/ready 均 `200`，Nginx 语法测试通过。
4. 公网 `/version.json` 返回 `1.1.2` / `1.1.2+48d70aa5de06`；根页 SHA-256 为 `6e9d602234aaf84ba75059bb348dcf233e4f0c7655c53106c6be66c0a07e8561`，`sw.js` SHA-256 为 `ff0ec9d4458b10e0dad17ff453a46dfba72fd7cdb9c9b990c05d308d0faa5060`。
5. 一个 hashed asset 的 `Range: bytes=0-127` 返回 `206`、`Content-Range: bytes 0-127/128860`，完整静态响应可读。
6. `/canary/previous/` 以 `302` 指向不可变 `/canary/1.1.1-da53958b860c/`；回退页返回版本 `1.1.1`，响应含 `no-store`、`Vary: *` 和 `X-Robots-Tag: noindex, nofollow, noarchive`，6/6 引用资源可达。上一版本 worker 查询返回 `404`，当前 1.1.2 worker 返回 `200`。
7. 回退 Nginx 配置已备份，活动配置旧 SHA-256 为 `26a2159912ca007450ceca0f5c375b26319620c1620c495b007a0d4f40c1ed10`，新活动配置 SHA-256 为 `8918894fdba83edf5b32a134bb18d4ee02873e56777343a1ecbacc3a816f79c0`；备份文件名记录在受保护节点状态中，可用于只回退 fallback 配置。
8. 内置浏览器真实验收：根站显示 v1.1.2；访问上一稳定入口显示 v1.1.1；返回根站仍显示 v1.1.2；该页未产生前端 error 日志。

## 上海 / 下载页状态

上海保持 1.1.1 Web/API 和下载页。只读预检显示服务 active、`NRestarts=0`、公网 health `200`，但根盘仍处于保护显示值 `90%`（可用空间约 6.0 GiB）。按照 90% 硬门禁，没有上传下载归档、切换下载 `current`、发布 Android/Windows stable，也没有为形式上的发布删除备份、数据库、WAL/SHM 或回滚目录。

## 回滚指针

- Web-only 回滚：将香港 Web `current` 原子切回 `web-1.1.1-da53958b860c`，API 保持 `api-1.1.1-da53958b860c`；不恢复数据库。
- canary 回退：恢复受保护的 previous-fallback Nginx 备份并执行 `nginx -t`/reload；不切换 API、不触碰数据库。
- 上海、下载页和原生包没有本次切换，因此仍保持其 1.1.1 回滚边界。

## 未完成项

这是按用户“上海不行先完成香港”的授权收口的部分发布：上海 Web/API、上海下载页、Android stable、Windows stable 尚未切换。后续若要全量发布，必须先在上海恢复严格低于 90% 的磁盘余量，并分别重新完成目标节点的备份/哈希/健康/下载验收；不得把本记录当作全量发布成功。
