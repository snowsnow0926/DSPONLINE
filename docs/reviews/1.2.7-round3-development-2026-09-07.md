# 1.2.7 第三轮开发记录

Role: develop

接手基线：`b7ca5b8d2dcebffa095c2cdc547e1481e73e8a70`。任务分支：`codex/1.2.7-round3-validation`。主工作目录和 Windows 工作树在接手时都与该提交一致，均无未提交改动；交接包中关于旧主目录和未推送提交的描述已过时。

资料包 5/5 哈希匹配，独立 Markdown 与 ZIP 中对应文件字节相同。

## 前态与首批修复

- R3：从 `85794799` 原生导出累计前态，在准确基线的隔离副本中通过 `git apply --check` 和实际应用；15 个文件与 `b7ca5b8d` Git blob 全部一致。历史 Markdown 的 6 处硬换行空格保留。
- R1：先记录四类无效候选被旧选择器接受的红灯。收集入口现需可信源码上下文，并检查内部构建证据、实际 ASAR metadata/Build ID、Host/目录包文件、安装器 SHA-512、feed SHA-256/大小和路径边界。内部证据不替代签名，不改变公开更新协议。
- R6：显式验收缺包时 BLOCKED/退出 2。包验收绑定 clean source、ASAR、Host 与离线性能版身份，每次运行写独立目录。既有 smoke 模式增加进程内 Node 与 Chromium 出站限制。
- R2/R4：正在用固定、暂停且可识别的合成工厂替换弱断言；正常关窗与强杀分离，保存确认新的 revision 与 checksum，导出完成并校验后在独立 profile 再导入。
- R5、保存中退出及正常关窗的产品根因尚待真实新包验证，本记录不声称通过。

## 当前证据

目录：`artifacts/1.2.7-codex-round3-20260907-c7e39a12/`。

- R1 专项：42 pass / 1 权限条件 skip / 0 fail。
- R6 与 R1 联合：22 pass / 0 fail；显式缺包入口退出 2。
- TypeScript：通过。
- native Node：611 pass / 9 条件 skip / 0 fail；本目录当时未构建 Host，跳过项不算通过。

运行代码变化后须重新冻结、构建和复验。普通包仍保持 JS 权威，Rust 玩家资格未开启。未签名、未推送、未发布，没有使用真实玩家数据或生产账号。

API 行为依据：[Electron WebRequest](https://www.electronjs.org/docs/latest/api/web-request)、[BrowserWindow.close](https://www.electronjs.org/docs/latest/api/browser-window#winclose)。
