# DSP极简网络 1.0.47 正式发布记录

> 状态：**已正式发布并完成双节点、下载页、原生包、公网与 previous-stable 验收**  
> 发布日期：2026-08-20  
> Runtime Git SHA：`aab581cf0c78e480e1d10fe4c7f91e6d6d9311b7`  
> Release ID：`1.0.47-aab581cf0c78`  
> Build ID：`1.0.47+aab581cf0c78`

## 发布内容

- 新增设备级“经典”基础卡片，恢复 1.0.43 的 `224×76` 精简卡片结构。
- 修复画布拖动及后续几何重绘后 Canvas 传送带与建筑错位。
- 修复连接草稿活动期间建筑受既有聚焦状态影响而偶发半透明。
- GameState v47、save envelope v2、cloud schema v8、SQLite layout v3 与 IndexedDB records 均未升级。

## 正式目标

- 香港：Web/API current 为 `1.0.47-aab581cf0c78`，release-control generation 26，blue / 4321；previous 为 `1.0.46-d5b60dc3b7d6`，green / 4322。
- 上海：Web/API current 为 `1.0.47-aab581cf0c78`，release-control generation 13，green / 4322；previous 为 `1.0.46-d5b60dc3b7d6`，blue / 4321。
- 上海下载页：current 为 `download-site-1.0.47-aab581cf0c78`，previous 为 `download-site-1.0.46-d5b60dc3b7d6`；Windows 与 Android stable 均更新至 1.0.47。
- 香港 previous-stable Web：`/canary/previous/` 以 `302` 指向不可变 `/canary/1.0.46-d5b60dc3b7d6/`。备份站继续使用当前 1.0.47 API，不包含 API、数据库、原生 feed 或下载页回滚。

## 数据安全与容量证据

- 香港发布前 Backup API 快照为 3,772,833,792 bytes，SHA-256 `a31f68b15d02a48c569675fa1575597a4f3a8b1c496a38d4e43dbe2dfc307fd0`，`quick_check=ok`，schema v8 / layout v3；独立 rollback preflight 的大小、SHA、schema/layout 一致。
- 上海发布前快照为 462,848 bytes，SHA-256 `4715986006fc00bc3d5719f53a318a90a035cf0aee50eca75c95ee1104a43347`，`quick_check=ok`，schema v8 / layout v3。
- 香港容量治理只处理已证明可重建缓存、明确旧 Web 包和一个已经逐分片完整读回校验的历史快照副本；数据库、WAL/SHM、当前/previous/canary、有效发布备份和直接代码回滚目录均未删除或恢复。最终根盘 80%，约 14.5 GB 可用，低于 89%/90% 保护线。
- 香港大体积 auto-snapshot 与 offsite-backup timer 延续发布前的停用状态，避免约 3.77 GiB 快照和异地 staging 叠加触碰保护线；health 与 monitor timer 均 active。本次新鲜完整快照、旧有效恢复点和异地归档均保留。恢复这两个容量型 timer 前仍须先提供不会越过保护线的独立容量。

## 正式制品

- Source provenance：242 文件，aggregate SHA-256 `a32dffa10b2bc5706cc038f651d4ba373e38c6ac42a8a5cf099edd27617e527e`。
- Web：155 文件，aggregate SHA-256 `8b19dba19c3f11a6ba5362316c034099c521ef8b60df14781200268611fbd36f`；归档 SHA-256 `6bba68566ea378e7ad92c944d96e9500ccdccda6efaf4b190484982b0c82ca3b`。
- API：最终修正版 170 文件，aggregate SHA-256 `f18da825e9411d8698818d588ee85c07fd5eeaf56bdd3c74f53dc1757c20d073`；归档 SHA-256 `a77beb9800c41f00c6ed2ea56a301d2874f185823904c63c32d2068792562059`。初版归档因漏收发布测试引用的模块而未进入 current；修正版只补入固定源码中的精确文件，没有修改运行时代码。
- Download site：9 文件，aggregate SHA-256 `f8c6bdf56c7dd25cf841f1e7d986614542846cfd2c24c8b8a1391567a494232c`。
- Windows Setup：109,784,270 bytes，SHA-256 `241c5e515351af9ff6aa533c9f81788c5d1f106e216cc1753cd872296284e671`；按既有策略明确为 `NotSigned`。
- Android APK：5,195,361 bytes，SHA-256 `8d91382623faea1e45d498224cd799d5c5d0797945f50874a4db269c8a39afdc`；package `cn.dsponline.network`，versionCode `1000047`，zipalign、v2/v3 与历史证书连续性通过。
- Android AAB：4,984,256 bytes，SHA-256 `921e309b85e0cd4e45b674831ca1bf238be68f8feac36cb89de3167c76c243d2`；历史证书连续性通过。

## 门禁与公网验收

- 本机固定源码门禁：Vitest 1,436 passed / 20 skipped；server 363 passed / 2 skipped；station 3/3；ops、release-switch、native 均通过；完整 Chromium 428 passed / 26 skipped / 0 failed。
- API 修正版在香港和上海的新不可变目录均通过 365 total / 363 passed / 2 skipped，以及 station 3/3；远端 manifest 复核通过后才进入切换。
- 两地 `/version.json` 均为 1.0.47 / 正确 Build ID，health/ready 为 200 且 writable；香港 API、handoff proxy、Nginx active，`NRestarts=0`，代理处于 forward 且 active/writer/queued 均为 0。两地公网健康后续连续探测 3/3 通过。
- PWA 根页、`sw.js`、manifest 和 hashed asset 均可读；根页/version/worker 为 no-store 或 no-cache，静态 asset 使用长期缓存。previous-stable 返回 no-store、`Vary: *` 和 noindex；1.0.46 canary HTML 与保留 Web release 字节一致，canary worker 200，1.0.46 根作用域 worker 404，当前 1.0.47 根 worker 200。
- 下载页、Android/Windows feed 均为 200/no-cache；APK/Setup Range 为 206，二进制为 immutable。完整公网下载大小和 SHA-256 与冻结制品逐字一致。
- 第一次切换尝试在香港代理旧计数和上海备份维护窗口门禁处停止；香港恢复到既有 1.0.46 upstream 后，代理在不重启 API 的情况下按受控流程复位。维护窗口结束后使用同一冻结制品再次执行，双节点和下载页原子切换成功。

## 授权的单账号普通排行榜恢复

- 在上述香港完整发布备份保护下，对用户明确授权的唯一账号执行了普通排行恢复。
- 恢复前账号未受排行榜限制、登录正常、公开可见，普通主云档 revision 488 合法，但普通 submission 为 0，且普通复核阈值也为 488。
- 受保护工具只清除普通模式复核阈值，保留速通阈值与提交、账号、登录会话、云元数据、正文引用和 payload 表；1.0.47 启动回填随后把普通 submission 从 0 重建为 1。恢复后限制仍为 false、阈值为 null、健康/ready 200，并保留 root-only 可逆 guard。
- 没有直接写入排名分数，没有修改云存档、revision、玩家进度、密码、会话或速通数据。

## 回滚边界

- 香港直接代码回滚：generation 26 的 previous Web/API `1.0.46-d5b60dc3b7d6`；回滚只切换不可变代码目录，数据库保持当前 schema v8 / layout v3。
- 上海直接代码回滚：generation 13 的 previous Web/API `1.0.46-d5b60dc3b7d6`；上海下载页 previous 为 `download-site-1.0.46-d5b60dc3b7d6`。
- previous-stable Nginx 独立回滚：恢复 `nginx-dsp-idle-app.pre-previous-1.0.46.20260819T203621Z.conf` 后执行 `nginx -t` 与 reload；不影响 Web/API release-control。
- 任何代码回滚均不得恢复、替换或降级生产数据库；排行榜单账号恢复另有独立 root-only guard，不与版本回滚绑定。

> 本文件从 1.0.47 正式发布工作树纳入 1.1.0 开发线，用作兼容基线记录；不授权 1.1.0 连接生产、部署、签名或修改下载页。
