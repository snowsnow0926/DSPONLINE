# Windows 性能循环：完整包、内存与前端摘要

2026-09-10，Role: develop。Windows 性能 Goal 继续 active，完整 Rust 引擎替换暂停，没有部署。

## 玩家能理解的进度

包含共享扫描优化的 Windows 开发包已完成一次真实终局结算、暂停保存、重新进入和两次重开，结果与 JavaScript 独立计算一致。测试保持隐藏、静音、不抢焦点，原始玩家档未改动。

本轮首次记录了整套游戏进程的内存，而非只看 Rust：整段测试私有内存采样峰值约 **4.48 GiB**，最大部分在前端渲染进程。它包含测试读档等操作，不能当成日常游戏的固定占用，也没有证明内存已经下降。

随后减少了前端生成完整状态摘要时的重复字段排序、字段名编码及短字符串编码。两轮增量对照各自稳定节省约 **0.74 秒**和 **0.31 秒**，摘要完全相同。它们是前端局部测量，尚未包含到下述 Windows 包，不能相加成整版提速。

**完整进入游戏减少 20% 的目标尚未证明。** 下一步按阶段拆分进程树内存、完成同工作量完整入口配对，并继续处理主要耗时与浏览器交付检查。

## 8a5 冻结包与实际入口

包源码 `8a5bc89f45e6274c693f3e26c6871a1329e79712`，Build ID `1.2.7+8a5bc89f45e6`，离线 beta 开发包，76 个清单制品、79 个冻结文件。Rust、desktop、scripts、依赖清单与 776 未变，复用同 SHA 的已验证 Host/助手和 987/1 条件 skip Native 结果；本次运行全部正常前端构建、预算、平台与打包门禁，未重复编译未改的 Rust。构建守护 73.464 秒，最低空闲 7,698,864 KiB；旧 776 冻结包保持完整。

生成驱动首次因换行匹配失败而未创建构建脚本；标准化驱动文本换行后生成成功。实际构建 `build-desktop-performance-return-v3-guard` 与 `package-8a5bc89f-receipt.json` 通过，不把驱动生成失败当作产品失败。

v4 的实际 10 秒结算、手动保存、重进和两次重开已走完；内存采样脚本把字典交给 `Measure-Object`，采样进程退出，主报告因此 **FAILED**，未执行最终独立 JS 核算。该失败保留，守护 229.104 秒、最低空闲 3,968,920 KiB。它不是完整验收通过。

采样改为显式累加字典数值，自检确认单一已知 Node PID 的身份和两类内存总数一致，随后 v5 完整场景 **PASS**：

- 实际时钟结算 9 秒，Native 候选、最终落盘、暂停保存、重进及两次重开与独立 JS 完整状态相符。
- 手动保存从开始到完成的真实状态变化已记录；不在读完整大档后依赖只显示两秒的完成提示。仍共享原 90 秒保存期限，持久主档变化和完整状态检查保留。
- 完整进入操作界面 **89,516.1079 ms**；Native 请求 **42,341.9052 ms**。单次正确性记录不与旧 82.16 秒样本计算提速，也不计为 AB/BA。
- 4 次正常关闭，零显示/焦点/系统弹窗；全部实际窗口不可见、不可聚焦、静音、离屏。原始存档字节、修改时间、SHA-256 未变。
- 守护正常退出，324.079 秒，最低空闲 3,716,680 KiB；6 GiB 启动、2 GiB 停止约束不变。

证据：`private-packaged-complete-performance-return-v4/report.json`（失败保留）、`memory-sampler-self-check-v2/report.json`、`private-packaged-complete-performance-return-v5/report.json`。v5 驱动 SHA-256 `f73a4067fa126c2fc519b58cc4e70852352fc2ddb827db0c25c425668343bd80`。

## 内存记录的范围

只采集显式登记且可执行文件位于该冻结包内的 Electron/Native PID，并记录进程出生时间。测试脚本、采样器和其他应用不计入。四次启动累计 17 个进程身份、120 个有效采样点；其中实际 Rust Host 有 22 点。

私有内存采样峰值 **4,809,027,584 bytes**，同时段工作集 **4,652,994,560 bytes**。峰值样本中的前端进程私有内存为 **4,294,131,712 bytes**；该样本里 Rust 已退出。私有内存、工作集、单进程历史峰值含义不同，不混用或相加历史峰值。

采样循环目标间隔 500 ms，但读取 OS 进程信息存在开销，实际相邻有效点 **1,359–3,856 ms**，平均 **1,833.58 ms**；因此只称“采样峰值”，不声称精确捕获全部瞬时峰值。记录覆盖进入、诊断读取主档、保存及重开，下一轮应给控制记录增加阶段标签，分别统计真实进入与诊断读档。当前不把这份总体数字当成纯游戏运行内存或内存改善。

完整记录 `private-packaged-complete-performance-return-v5/game-memory.json`，SHA-256 `73485178df3bce22c91ec99832ecd02ea5c86d188dd8b34411e52f0d7bac9cf6`。

## 前端完整摘要优化

对同一完整授权档解除未提交 timeWarp 的来源状态测量：canonical 约 4.64–5.28 秒、domain 约 0.61–0.63 秒、来源编码约 2.30–3.94 秒。这只是 Node 分段选题，未同时运行其他重任务。状态 canonical 为 `2b8be04b…`，domain 为 `1d8545a2…`，来源编码 200,841,416 bytes，原档未变；见 `renderer-proof-profile-v1/report.json`。

第一项复用同一字段数下、完整字段名与输入次序均相同的排序结果和字段名编码。每个对象仍重新检查当前可持久化成员，并读取当前值；不缓存游戏状态或其摘要。缓存只保存字段名，单形状至多 256 字段、字段名至多 256 字符、总字段数至多 4,096，超限正常编码。

第二项缓存短字符串的原始 JSON 编码，至多 1,024 个、累计输入至多 65,536 字符；长字符串正常编码。原始字符串作为 Map 键，不使用可能碰撞的拼接签名。有限数使用等价的 ECMAScript 数字文本，非有限数仍写 null，负零仍写 0；布尔和 null 直接写对应字面值。完整 SHA-256 算法不变。

两轮各自排除一次双方预热、按 AB/BA/AB 测量同一来源的 canonical+domain 总时间：

| 配对 | 原版 → 字段复用 ms | 字段复用 → 字段及标量复用 ms |
| --- | ---: | ---: |
| 1，基线先 | 5,013.2857 → 4,376.6166 | 4,224.6376 → 3,877.4877 |
| 2，候选先 | 4,991.3019 → 4,257.1028 | 4,189.0238 → 3,899.3399 |
| 3，基线先 | 4,998.6422 → 4,257.8481 | 4,208.2199 → 3,999.2826 |
| 中位 | 4,998.6422 → 4,257.8481 | 4,208.2199 → 3,899.3399 |

各减少 **14.8199% / 740.7941 ms** 与 **7.3399% / 308.8800 ms**，每轮三对均更快，完整 canonical/domain 均与已知正确来源一致。两轮百分比不相加，不能作为完整 Native RPC、保存或 UI 的提速。原始玩家档始终只读且摘要/时间不变。

最终源码 SHA-256 `51b9a8d02cd1fcd623c22c5bca1b0310f0243ca5417202b7d9f598cf72ff2212`；中间字段候选 `0494c75b…`，原版 `e4167e68…`。记录 `proof-fields-ab-v1/report.json`、`proof-fields-ab-v2/report.json`；守护分别 42.949 秒 / 最低 8,133,960 KiB、38.337 秒 / 8,872,252 KiB，均正常退出。

最终相关摘要/Native 启动测试 **40 pass / 0 fail / 0 skip**、TypeScript 通过，包含字段变化/省略/次序、超出缓存容量、长字段名、字符串替换、UTF-8/代理字符、浮点边界、独立 Node crypto 和原有启动回退。记录 `proof-fields-final-regression-v3`，守护 44.801 秒、最低空闲 8,930,476 KiB。此前仅字段候选 39 项通过和其类型检查保留，不替代最终结果。

这组摘要改动尚未进入上述 8a5 包，必须在后续完整包中验证；不因局部验证就开启实时 Rust 权威或授予发布资格。

## 当前云端检查与后续重点

776 的 Native Windows 全套 **成功**（34464778194）。CI（34464778199）构建、单元、Server/Ops/Native 成功；浏览器 **465 pass / 33 skip / 4 fail / 1 flaky**：

- 冷菜单 p95 1,056 ms，重试 968 ms，超过 500 ms。
- 多字号设置超过 30 秒用例期限。
- 手动采矿供给熔炉场景缺少预期的“传送带 Mk.I”奖励元素。
- 暂停画布峰值帧 833.2 / 重试 850 ms，超过 800 ms。
- 精确数值提示与进度显示场景首次超时，重试通过，单独计 flaky。

以上未调高门槛或宣称已修复。下一阶段优先整包同工作量配对、按阶段记录进程树内存，以及这些实际交付缺口。已有 Rust 分段中九步模拟约 24.05 秒，供电/机器/矿机约 4.89 秒、施工约 3.17 秒、带输入/输出约 2.93/2.73 秒；它们包含嵌套计时，不能全部相加。施工写时复制已证明无整体收益并撤回，不重复该尝试。
