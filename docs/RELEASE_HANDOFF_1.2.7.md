# 1.2.7 网页与安卓发布交接

Role: develop。2026-09-08，候选已冻结，尚未发布；完整浏览器复验正在运行。

## 任务与授权

- 优先级 P1：停止挂机后保存失败、反复回到恢复界面；提供失败后重试保存及恢复数据导出。
- 用户已授权香港主站与上海独立站发布网页 1.2.7，以及安卓正式包和下载页；允许自主切换开发、发布角色并提交代码。
- 上海已完成另一个任务的全业务迁移。后续发布只使用受保护 `DSP_SH_NEW_*` 合同的一次性子进程映射，默认 `DSP_SH_*` 指向停写的旧机，不得使用。
- Windows 下载、两地 API 保持 1.2.6。Rust 跨端接入、长区间自动采用及实时权威不在本次推广范围。
- 原始反馈及私有存档只保留于本地支持目录；本交接、Git 与公开制品不含账号、玩家存档正文或其摘要。

## 用户可见结果与兼容

停止时启动恢复不再重复介入；已完成结算但主档写入失败时复用冻结结果，避免反复重算；离线报告不会遮挡挂机控件。挂机面板提供独立恢复数据导出，失败不清除原主档或日志。

网页与安卓共享导入、保存、离线准备优化。完整模拟状态的 transfer 分批编码以降低 JSON 中间对象峰值，所有字段和原生 JSON 字节语义保留。安卓初次启动有界等待已观测到的旧写入租约自然到期；不清缓存、不强抢活跃写入者。

GameState v47、envelope v2、cloud schema v8、SQLite layout v3 均不变。旧 API 的本地合成兼容验证 11 项通过，包括普通/gzip 上传精确读回及 409 冲突保留原档；没有使用生产账号或执行生产写入。

## 冻结身份

| 项目 | 值 |
| --- | --- |
| Runtime SHA | `dab2ff5066b7bc37bfbaa534628e1f7bc51bdceb` |
| Release ID / Build ID | `1.2.7-dab2ff5066b7` / `1.2.7+dab2ff5066b7` |
| Web manifest | `82e9ec9687f9b950630033ceab852ce9273d95e26fec6e708ae05f684e984e32` |
| Web aggregate | `1f07d6a0441404f04cb456e68a7dce41551f0affa7ebfdd37f33bb2e4a8a63ab` |
| Android APK | 5,665,693 B，`9e50cfe453cdc49d475bdfb23f6adb2204639bfae64820215b7c64835e4ff180` |
| Android AAB | 5,454,985 B，`27f89aa354ad7c2f3a10d7da7cd08a21fcc1fc67c83612898a29dd8cc16d068e` |
| Android identity | `cn.dsponline.network`，1.2.7 / 1002007，v2/v3、zipalign、历史证书连续性通过 |
| Download manifest | `1f3b72f109c36a5299afe7ea2cfcb4e9513708993181d1488029012ac589e927` |

本地证据根为 `artifacts/release-1.2.7-web-android/2026-09-08/candidates/1.2.7-dab2ff5066b7/`。三个组件、归档独立读回与 4 个原 Windows 下载文件保持检查通过。顶层 bundle 已准备 8 项输入；第 9 项最终 gate report、顶层 manifest 与 provenance 须在实际验收完成后生成。

冻结后的测试修订在 `8a1a4a0a06c99390905a2b2acef3ba0249d0c5ad`：CI 安装缺失服务端依赖及 Linux Node 入口，恢复两条运维脚本的执行位但正文不变，单元文件串行执行，2 MiB 字节相等改为完整 Buffer 字节/长度比较，原生测试使用平台绝对路径并为 PowerShell 冷启动留出夹具时间。这些提交没有改变游戏、API、包版本、依赖锁或冻结制品。

## 已完成的当前制品验证

| 检查 | 实际结果 |
| --- | --- |
| 类型 / 协议 / 全部单元 | 类型通过；协议 13/13；本地 Vitest 3,137 通过、35 条件跳过、0 失败 |
| Web 构建与静态门禁 | 构建、启动预算、原生边界、许可证 127 包通过；根/server 生产依赖审计 0 项 |
| Server / Ops / Rust | 云端 server 390/2 跳过加 station 4/4；Linux Ops 通过；Rust core 1,096、host 240、bin 3 通过，3 ignored |
| 原生 Node 工具 | 首轮 629 通过、1 跳过、1 个测试夹具失败；修正的两文件 Windows 48/48、Linux 47/1 跳过通过，原失败记录保留 |
| 挂机停止恢复专项 | 本地 9/9，连续失败重试、刷新、日志提交失败、取消、导出及报告重叠均覆盖 |
| 持久恢复 / 其他浏览器 | durable 7/7；Firefox/WebKit 2/2 |
| 生产构建预览 | PWA/启动 2/2，加连线/密集画布 2/2；密集场景耗时原样记录，不宣称所有帧率达标 |
| Chrome 与 Edge 长测 | 各 30 分钟、37 样本、3 次自然自动保存，以及最终完整手动保存/重载状态比对通过 |
| 大档停止失败恢复 | 派生副本构造约 51 小时、16× 场景，写失败→导出→复用结果重试→重载通过，75.212 秒；源档不变 |
| 安卓启动模型 | 平台配置下真实 IndexedDB 6/6，通过不等于实体设备 |
| 正式 APK 生命周期 | 原地安装身份和旧档保留通过，最终两次保存/退后台/重开通过，详见下方边界 |
| 发布工具 | release-switch 29/29；公网检查脚本本地 mock 8/8；尚未替代实际公网验收 |

## 失败记录与剩余门禁

云端完整 Chromium 两分片出现 91 个最终失败及 29 个重试通过，主要停在原 5 秒工厂启动等待；完整同断言本机回归仍在进行，不能将云端结果标为通过。原 2,000 ms 有限矿脉耗时门禁在同一云端 runner 上对照：线上 1.2.6 为 2,174.692 ms、候选为 2,194.073 ms，均仅该耗时断言失败；守恒与科研结果通过。保留原阈值和全部失败日志，不据此声称性能提升或云端 CI 全绿。

此前 ae5b 候选的 Edge 最终保存出现 Worker OOM，已经停止推广。新 dab2 的两浏览器完整长测通过是新证据，旧失败未覆盖或改名。详见[检查点内存补修](./reviews/1.2.7-checkpoint-memory-2026-09-08.md)和[停止恢复专项](./reviews/1.2.7-idle-stop-recovery-2026-09-08.md)。

安卓最终验证是匿名模拟器真实 APK，升级链经过多个 1.2.7 候选；不是最终包直接从 1.2.6 单步升级或实体设备结果。首轮安装后的短暂保存提示未被 UI dump 捕获，该轮仍记失败；随后用持久主目录时间推进及旧主档进入备份证明保存，且两次真实重开保留工厂身份。两次恢复可写为 11.914/12.913 秒；第二次 am-start 返回 wait-timeout/exit 0，但实际可写界面与工厂读回通过。Document 身份不明，未宣称重建方式；无记录到 Fatal/ANR。

尚须完成完整本机 Chromium、最终不可变 bundle、两节点实际发布及观察、公网 HTTP/PWA/下载读回、香港上版独立 fallback 验收、发布事实文档与 Git 收口。不把私有副本的构造场景描述为已找回玩家当前浏览器中那次挂机记录。

## 发布与回退交接

08:48 UTC 只读预检：香港 Web/API 1.2.6，generation 48/proxy 192；新上海 Web/API/下载 1.2.6，generation 32/proxy 92；均 green/4322，health/ready 200、服务无重启、Nginx 语法通过。新上海机器身份、下载文件和迁移恢复配置通过核对。没有执行本次上传、切换或服务重启。

发布角色须从冻结组件验证并使用维护的原子切换入口，仅传 Web 目标，保留 API 及 proxy。两站独立实施，上海保留迁移恢复 drop-in/validator。下载更新只替换安卓及页面/feed，Windows 4 文件从已核验版本保持。

直接 Web/API 当前基线为 `1.2.6-df828869e276`；这次只回退 Web 时应显式指定 1.2.6，不能使用同时回退 API 或数据的操作。成功观察后香港 previous-stable 才从 1.2.5 更新为刚替换的 1.2.6；先备份精确 Nginx snippet，验证根 service worker 与缓存隔离。后续如回退根 Web，先恢复该 snippet，再回退 Web。最终实际指针、哈希与回退记录由发布报告补录。
