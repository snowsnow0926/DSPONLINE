# 香港累计玩家历史展示补偿

用户指定将 2026-08-14 至 2026-09-07 的历史漏计补偿固定为 3,700，计入诊断反馈界面的累计游玩玩家。

展示公式为实时 API `players.total + 3700`。只在实际查询的 API 为 `https://dsponline.cn/api` 时启用；上海、本地独立 API 与离线缺失值不增加。页面显示“含历史补偿 3,700”，提示文字记录完整日期和估算属性。今日和在线玩家继续使用原始接口值。

这是一笔固定展示补偿，不向数据库创建匿名玩家、不按天摊入活跃人数、不改写服务端累计数。重复刷新、重新打开面板或后续发布不得再次累加或移动截止日期。用户知悉统计缺失和估算性质后指定固定值；未来回访可能与补偿产生身份重叠，无法从现有日志精确消除。

基线为香港当前 Web `1.2.7-dab2ff5066b7`，保持 API `api-1.2.6-df828869e276`。本提交只提供源代码与部署交接，生产状态需以运维验收记录为准。后续 Web/原生开发需要携带此补偿；若未来迁入 API，必须同时移除客户端加数，避免双计。

验收：香港页面以原始累计加 3,700 显示，今日/在线不变，刷新不累加；上海与本地独立接口不补偿；API 不可用保持 `--`。回滚仅切回之前 Web 目录，不恢复数据库。
