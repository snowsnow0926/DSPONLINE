# 1.0.46 发布前候选记录

> 状态：开发门禁完成，未发布；本记录不授权生产切换、正式签名或下载页更新。

## Scope

1.0.46 修复自动保存后模拟被暂停、主动暂停后模拟 Worker 无法恢复、纯挂机保存状态污染，以及手机连续拉线遮挡与候选状态机问题；同时把画布基础卡片、重叠处理和交互展开拆为三个独立本机偏好，并修复建筑消失、一行卡半截、空白视角无法恢复、过大的重叠数量标记，以及线路聚焦下选中/悬停卡片半透明、切换隐藏和无法平移画布。最终返修又修正周期进度测试的合法回绕误判、纯挂机终局功率的瞬态负数、普通空间站任务无法从量子库存交付，以及焦点上下文 hover 后偶发把画布平移变成节点拖动的真实竞态。

## Implemented

- 普通构建默认使用 1.0.43-compatible verified-primary 保存协调器；durable WAL 只在显式开发变量下启用。
- 自动保存前正在运行的模拟在保存期间和完成后保持运行，玩家主动暂停仍保持暂停；新 Worker 安装会清除旧 disabled latch。
- 默认保护模式拒绝保存窗口内编辑但不暂停模拟；实验模式保留已接受编辑、失败进度和立即导出能力。
- 纯挂机只有在主存档验证、Worker 接管和恢复日志提交全部成功后才清理终态。
- 纯挂机的累计计数只按非负速率插值；戴森功率与轨道帆等可升可降的瞬时量只展示每 30 秒提交的权威快照，因此不再把旧速率外推成短暂负增量。
- next-mobile shell 只挂载一个无 backdrop 的可折叠底栏；6/10/50/100 条候选可滚动、定位、任意移除，重复或非法点击不阻断有效候选。
- 最终连接仍由 `connectBeltsAtomically()` 严格原子复核；失败保持零创建、零扣料并定位候选原因。
- 普通运行时异常与真实 chunk/module 加载故障分流，诊断不记录错误正文、存档、云数据或玩家隐私。
- 画布提供 `auto/full/medium/minimal`、`marker/representative/all`、`selected/hover/base` 三组独立设置，只写 localStorage。
- 数量标记缩为 `88×44` 点击区和约 `80×30` 可见胶囊；一行卡及 wrapper 固定 `96×32`，变换 viewport 禁用 paint containment。
- 普通节点、重叠 halo、交互展开与选中节点使用递增层级；展开卡通过屏幕 hit-test 回归确认不会被后渲染邻居遮挡。
- 线路/任务/生产链聚焦不会再淡化选中或悬停目标；上下文节点即使因 hover 视觉展开也保持 `draggable=false`、`nopan=false`，不会把预期画布平移竞态地切成节点拖动。点击建筑或画布可退出聚焦，所有 LOD 都有确定初始几何。
- 一行生产卡显示“配方 · 产物”并保留特殊建筑名称回退；“完整 + 全部卡片”超过 480/1,000 个视口节点时启用明确标注的中等/一行密集保护，交互目标仍完整展开。
- fully-deferred 空白视角保留 Fit View 边界锚点；SVG MiniMap 与 CanvasMiniMap 均可恢复到建筑区。
- 普通平移、滚轮缩放和节点拖动触发的自动平移会在手势中发布带 overscan 的虚拟视口；小地图按 80 ms 上限实时重绘并在结束时补最终帧，拖动、框选和放置统一使用当前 transform。
- 空间站使用独立 `ReactFlowProvider` 和 flow id，不再覆盖或卸载工厂 store；8 次站内缩放往返后，工厂视口、拖动、框选、拉线和放置仍通过。
- 普通、指定来源和多行星空间站合同都允许玩家确认后从量子库存交付；来源行星限制仍严格约束自动轨道终端交付，量子扣除与任务进度在同一状态变更中原子完成。
- Web 使用根路径静态资源与 `%BASE_URL%` manifest，生产预览直接打开 `/station/<id>` 不再请求错误的 `/station/assets/*`；Desktop/Android 相对资源策略保持不变。
- 经典进度专项改为页面内 `requestAnimationFrame + MutationObserver + performance.now()` 连续采样。验证器按已知 `2/3 cycle/s` 和权威发布窗口展开模 1 进度，逐样本校验 aria、文字和 fill 一致，要求真实自然回绕，并拒绝时间范围内无法解释的非回绕倒退；没有增加 retry、skip 或下降阈值。

## Version and compatibility

- Product: `1.0.46`
- Android: `1.0.46 / 1000046`
- GameState: v47
- Save envelope: v2
- Cloud schema: v8
- SQLite layout: v3
- IndexedDB records: unchanged
- Runtime source: `d64b9ef85f9dea1cf2d0617cb300fa492ca1f43c`
- Build ID: `1.0.46+d64b9ef85f9d`
- Release ID: `1.0.46-d64b9ef85f9d`

## Validation

| Check | Result |
| --- | --- |
| TypeScript | passed |
| Progress blocker | target case single-worker 20/20; browser-timestamp validator unit coverage passed |
| Full Vitest | 1,434 passed / 20 skipped / 0 failed (173 files passed / 7 skipped) |
| Server + station | 363/2 + 3/3 |
| Ops / release switch / native | 56/6 + 29/29 + 24/24 |
| Durable WAL E2E | 7/7 |
| Chromium | exact final candidate 426 passed / 26 conditional skips / 0 failed (452 total, 4 workers) |
| Station canvas | production preview 7/7; Firefox/WebKit 14/14; quantum ordinary-contract E2E included |
| Firefox / WebKit general nightly | 2/2 |
| Production preview | feature gate 31/31; PWA 3/3; isolated density 1/1 and connection performance 1/1 |
| Real player saves (previous runtime compatibility baseline) | autosave + canvas/live pan + station round trip passed on `865f125e8624`; final recert did not reread attachments |
| Synthetic fixture / release contracts | 12/12 |
| Dependency / license | root and server 0 vulnerabilities; 125 runtime packages |
| Web build | 1,962 modules; startup 194,810 B gzip; menu 282,321 B gzip; forbidden 0; station direct route passed |

两份只读输入分别为 36,704,109 bytes / SHA-256 `cd2356ea2b9a90a47cfa32ed9533e7056bfc4202f6af777fc4f3b98faa9a81b1` 与 11,723,913 bytes / SHA-256 `f832f7fb909bad1981cd8476f28dcf0f1026c62955d822904218cee270a43d2a`。它们在上一 clean runtime 各触发两次自动保存后 Worker 仍 active、`paused=false`，源 bytes、mtime 与 SHA-256 完全一致；本轮未再次读取附件。运行时分发包未命中当前本机路径、截图名或私钥/token 模式；内部 source archive 不得作为公开下载制品。

## Residual release gates

- stable 构建不得设置 `VITE_DURABLE_RUNTIME_RECOVERY=true`。
- 合成 506 个重卡的固定完整/显式展开全部诊断仍可出现数百毫秒帧；自动档最终隔离门禁三轮 P95 为 7.1～20.9 ms、max 21.0 ms、0 个 >50 ms 帧。额外 `repeat-each=3` 压测为 2 pass / 1 fail，失败执行只有一轮 auto P95 量化到 27.8 ms、max 34.8 ms，前后隔离执行均通过；该诊断按原阈值保留，未增加 retry 或放宽预算。
- Android/Windows 正式签名、证书连续性、实体设备、生产备份、Linux/Nginx 切换、公开 smoke、下载页与观察窗口尚未执行。
- 代码回滚不得恢复生产数据库；线上版本与回退指针由 release agent 在授权后独立核对。

## Handoff

固定源码 SHA、10 文件候选归档、manifest、SBOM、3-subject provenance 和逐项 SHA-256 已写入 [Release Agent 交接](../RELEASE_HANDOFF_1.0.46.md)。候选 manifest aggregate 为 `09826ccc60e1c594979878cf6507405e913c180963f84a8d3e76405d5b587e52`。旧 `1.0.46-865f125e8624` 因发布门禁阻断已明确作废，且更早的 `1.0.46-c24a0f57efeb` 继续作废；两者均不得发布。release agent 必须从 `d64b9ef85f9d…` 独立复算，缺少授权、签名、备份或回滚证据时停止发布。
