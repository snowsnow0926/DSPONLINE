# DSP极简网络项目现状

> 基线日期：2026-07-23
> 产品阶段：首个公网版本已上线，当前更准确的定位是“公开测试版”。
> 事实来源：当前工作区代码、自动化测试、部署配置和线上只读检查。

> 香港正式版本为 `0.7.0` / `GameState` v31，上海独立备用版本仍为 `0.5.0` / v30；两地继续使用各自独立的 schema v6 数据库。

## 1. 一句话结论

项目已经从单页挂机原型发展为一套可持续游玩的 2D 无限画布工厂游戏，具备本地存档、云账号与云存档、排行榜、PWA、Electron 桌面壳和双节点部署。现在最重要的工作不再是继续堆玩法，而是建立可复现发布、生产数据保护、安全加固、性能优化和原创化边界。

## 2. 当前发布面

| 入口 | 定位 | 当前状态 | 云功能 |
| --- | --- | --- | --- |
| `https://dsponline.cn` | 香港正式入口 | 在线，HTTPS，指向 `43.129.249.102` | 开放 |
| `https://www.dsponline.cn` | 正式别名 | 301 跳转到根域名 | 开放 |
| `http://111.229.128.211` | 上海旧节点与备用试玩入口 | 在线，由上海本机直接提供服务 | 前端主动禁用账号密码传输 |
| Electron | Windows 桌面版 | 可打包，支持稳定版/Beta/Nightly 通道 | 仅在配置安全 API 地址后可用 |
| PWA | 可安装网页版 | manifest、离线壳和更新提示已接入 | 联网且使用 HTTPS 时可用 |
| `http://127.0.0.1:4318` | 本地开发 | Vite 开发服务器 | `/api` 代理到 `127.0.0.1:4320` |

2026-07-23 的 `0.7.0` 发布后检查结果：香港正式入口和上海入口均返回 `200`，两个 `/api/health` 均报告 SQLite 存储和 schema v6；DNS 的根域名与 `www` 均解析到香港节点。香港 Web/API 指向 `0.7.0-8bf16d91d82d`，上海继续指向 `0.5.0-5b3a468c94d0`，两地 JS/CSS 均启用 gzip。线上用户、存档与玩家统计是动态生产数据，不在文档中固化。

## 3. 已实现范围

### 核心玩法

- 不使用机甲角色，鼠标或触摸指针承担采矿、拿取、拖放、建造和跨星球托运。
- 以 React Flow 为无限画布，支持节点移动、框选、批量操作、蓝图、线路编辑、小地图、画布书签和持久化生产区域标记；区域可命名并设置背景色与边框色，不参与生产模拟。
- 采矿、熔炼、制造、化工、科研、电力、行星物流、星际物流、戴森云、戴森球和银河终局形成完整流程。
- 六色矩阵、科研队列、五条无限科研、四类银河出口项目和八章战役任务已经落地。
- 当前工作区包含 8 个恒星系、22 颗行星和 16 种行星生态模板。一次性新建种子确定恒星参数、星区坐标、生态、普通与稀有矿、海洋、潮汐锁定、矿储、风光地热、轨道产率、航程、设备专长和殖民成本；加载存档不会重新抽取。
- v29 固定为澜渊 II、赤砂 I、牧云 II 保留原油，其中赤砂为高储量、牧云为中等储量；风暴 I 仍由种子概率决定。v28→v29 只在对应资源不存在时补充确定性油井，不覆盖旧油井、矿储、建筑或线路。
- 恒星亮度真实进入太阳能、各恒星系独立戴森云/戴森球功率和射线接收；跨恒星物流支持直达、优先中转、强制中转、中转枢纽优先级、1-4 个/船翘曲预算及按二维星区坐标计算的多跳航程。
- 生产库存对玩家使用整数结算，设备内部保留不可见的周期进度。
- 内容目录当前包含 78 个物品、78 条配方、34 类建筑、65 项科技、13 项成就和 32 个战役任务。

### 操作与分析

- 线路采用直接传送带连接，支持多输入、多槽物流站、自动物品/配方匹配、连接预览、吞吐预测和成功/失败反馈。默认会锁定施工托盘中已解锁且有库存的最高等级，耗尽后自动降级；已有并行线优先复用原等级，也可手动指定 Mk.I/Mk.II/Mk.III。
- 行星与星际物流现在允许供给站主动派车和需求站主动取货；每条在途航线记录真实载具所属站，载具占用、返航、翘曲扣除/退款和诊断均按所属站计算。两端都有空闲载具时可以共同承担运量，但不会重复预留货物或载具。
- 物流站的后续传送带会复用已有同物品槽，否则依次占用剩余空槽；玩家手动配置的槽位不会被覆盖，空槽耗尽、物品冲突和非法方向会返回具体原因。
- 分拣器 Mk.I/Mk.II/Mk.III 已从施工、制造、科技文案、图鉴和升级入口移除；传送带等级直接决定吞吐。v30→v31 将旧分拣器库存按等级等量退为对应传送带，并把旧线路/蓝图的 `sorterTier` 归一为线路等级，不删除线路。
- 四向分流器的优先模式按高（2）→标准（1）→低（0）逐级分配，同级线路轮流均分，只有高等级线路堵塞或达到吞吐上限后才向下一等级回退。
- 生产统计、网络诊断、跨星球物流诊断、工业规划、效率曲线、挂机压力测试和自动性能报告均有界面入口。
- 统计中心新增统一的全星球生产管理视图：按行星、设备族、物品和运行状态筛选设备，汇总各行星节点、产能与效率；缺料可沿输入线追踪到上游设备和原矿来源，堵塞可定位输出线与下游缓存，问题行可以直接跳转到对应设备或连续网络。
- 生产管理结果集支持跨行星批量改配方和批量套用指定物流槽模板；只修改兼容设备或站点，保留未指定槽位，并将被替换的缓存退回设备所在行星。目标物品可以直接生成完整产量反推方案。
- 生产资料库包含物品与配方、建筑设施、物流运输、电力与能源、星球与资源、戴森工程、科研与机制七个分区。物品页保留搜索、正反查、聚焦配方、完整链和两层链；建筑页直接读取目录与引擎数据展示用途、等级、解锁、制造材料、功率、缓存、速度和适用配方，并与物品、配方和科技互相跳转。
- 施工托盘支持两行精简模式、批量扩建和 Ctrl 固定 `+1` 连续扩建；检查器可快速增加单台设备。
- 新工厂在母星物资托盘获得铁矿石、铜矿石和石矿各 100；以后每项科技首次完成时，其直接解锁的普通建筑各赠送 2 个，建筑制造中心等巨构明确排除。旧存档迁移不会补发开局矿物或重复科技奖励。
- 施工托盘小锤子可递归使用已解锁的手工配方补齐上游材料，完整规划成功后才原子扣料并短暂显示实际消耗；灰色锤子仍是可操作按钮，单击会定位第一个当前可手搓的上游材料，或明确报告缺科技、缺原矿和无手工配方，不再使用双击。
- 基础制造搜索工具固定置顶，建筑制造与物品手工分别保留最近 3 条搜索；建筑制造和物品手工中的可手搓缺料都能直达对应配方；自动整理支持独立的一步位置撤销。
- 紫糖阶段的建筑制造中心可按目标库存自动从所在行星托盘制造建筑，并递归加工缺少的可制造中间材料；原矿仍必须由玩家提供。基础材料加工为 0.1 秒/件、建筑成品为 5 秒/个，绿糖和白糖升级同时提高两阶段速度与库存上限；界面显示当前阶段、进度、预计时间和缺料原因。画布继续使用真实约 2× 宽高，端口坐标不依赖 CSS 缩放。黄糖阶段的三输入物资配送枢纽会把线路送达物品直接转入所在行星托盘。
- 矿脉、原油涌泉和海洋节点上的采集设备可按指定数量或全部回收；资源节点、剩余矿储、输出缓存、线路及线路货物保持不变，设备返还施工托盘。
- 闲置物流运输机和运输船使用全局随身载具库存，显示在底部施工托盘，可随玩家跨星球并从任意兼容物流站装卸；旧行星托盘中的闲置载具会无损迁移。
- 每颗行星独立保存单种物资托盘上限，可在 1,000 至 1,000,000 间设置。降低上限不会删除已有超额库存；设备、配送枢纽、手搓与任务奖励等自动写入仍受上限约束。玩家主动放下的一整组光标或手提载荷属于防阻塞例外，可完整写入并暂时超过上限，不会残留在光标、丢失或只存入一部分。
- 殖民前哨先一次性验证全部费用：普通材料从当前所在行星托盘统计，物流运输机和运输船只从全局随身载具库存统计；条件完整后才统一扣除。星图桌面与手机需求卡分别标明两类来源和缺失载具。
- 资源节点、桌面检查器、手机检查器和储量统计共用 `getResourceReserveSnapshot()`。有限资源显示剩余储量、初始总量、比例和枯竭状态；只有无限模式或真实海洋资源显示无限。
- 已堆叠普通建筑可逐台 `-1` 回收；生产设备输入/输出、燃料缓存、物流槽、运输机/运输船和翘曲器容量随堆叠数量增长并受统一安全上限约束。减堆后既有超额库存完整保留，只暂停继续输入。星际物流站仍可从所在行星托盘自动补充翘曲器；新建到物资配送枢纽的输入线默认低优先级；生产区域支持四边和四角缩放。
- 每颗行星保存独立画布平移与缩放；返回行星时恢复离开视角，书签和设备定位仍以显式目标为准。深色、亮色和跟随系统主题覆盖画布、节点、线路标签、工作区、科技树、统计、手机壳与原生表单；科技树新增标准/精简布局。顶栏主要工作区和新版手机固定导航再次点击当前入口会关闭并返回画布。
- 香港 `0.7.0` 公告（2026.07.23）首次进入时展示一次，并可从主菜单设置或游戏内设置重复查看；已读状态与游戏存档相互独立。上海 `0.5.0` 继续展示对应版本公告。
- 渐进教学包含从手动采矿到蓝糖、石油红糖、黄糖、星际物流、戴森云、临界光子和白糖的 13 个里程碑；已有目标设备或零流量输入线会显示停机原因并可直接定位。
- 桌面和手机横竖屏均有专用布局与抽屉；手机支持单指平移、双指缩放、56px 合法端口吸附、长按快捷菜单、逐点多选，并在横竖屏切换时保持世界中心、缩放、选择和面板状态。第二根手指可从建筑或线路上接管画布，并取消第一指尚未提交的拖动、连线、采矿、放置和长按。
- 所有粗指针设备默认关闭常驻节点/线路动画、粒子和重阴影；低内存、低核心或持续低帧率设备进一步降低模拟与画布发布频率、启用可见节点渲染和紧凑 LOD，并默认折叠小地图。模拟仍累计真实经过时间，不改变产量或存档规则。
- 内容包支持校验、依赖、启停、运行时注册和存档使用保护；蓝图支持 JSON 导入导出。

### 手机新版壳层（阶段 0-3，香港 opt-in 试用）

- 新版通过 `?mobileUi=next` 启用，也可在经典手机界面的现有入口切换；偏好写入独立的 `dsp-idle-network.mobile-ui.v1`，不写入 `GameState`、本地存档或云存档。`legacy` 可随时回退。
- `useCompactLayout` 按视口决定 compact portrait、compact landscape、medium 和 desktop；`pointer: coarse` 只影响触摸手势、端口吸附和命中范围，窄窗口鼠标不会被误判成另一种布局。
- 新版主场景提供当前行星、供电、暂停/继续和警报顶栏，以及 44/48px 触控目标的“工厂 / 建造 / 物资 / 科研 / 更多”导航。行星横条和常驻画布工具已改为抽屉入口。
- “更多工作区”覆盖生产、扩张、工具、设置、存档、账号、云存档、内容包、诊断反馈和返回主菜单；命令面板也登记为覆盖层。浏览器返回、Android 返回和界面返回共享同一移动历史栈。
- 阶段 2 提供 `peek / half / full` 三档抽屉；节点和线路单击默认只开摘要，完整检查器继续调用原有配方、物流、电网、升级和回收命令。建造抽屉包含部署/制造/载具、搜索、分类和最近使用；移动放置条提供 44px 数量步进器、显式连续扩建和取消。物资抽屉支持搜索、排序、点击拿取/放回、随身载具和戴森摘要。
- 移动画布显式区分浏览、放置、连接、多选、布局和区域模式。节点仅在布局模式可拖动；两次点击端口仍是主连接路径，并保留 56px 粗指针吸附、第二指接管、线路下层和真实 handle 坐标。教学卡在画布上压缩为单行任务芯片。
- 阶段 3 将科技、生产资料库、星图和蓝图改为列表/详情路由；科技与资料库返回时恢复筛选和滚动，星图支持恒星系→行星两级返回。统计/生产管理使用移动概览、分段导航和展开行卡；戴森、战役、银河、运营和建筑制造中心统一为不透明单纵向滚动页。
- 手机打开配方或物流槽物品目录时不会自动聚焦搜索框，只有主动点击输入框才请求软键盘；桌面仍自动聚焦。小型储物仓在画布、检查器和移动抽屉中完整显示名称、缓存物品、输入输出与数量，并通过重排适配 80%-200% 字体。
- 打开任一全屏工作区时，施工托盘、行星条、画布工具、教学卡和底层画布视觉隐藏，`canvasGame` 发布冻结；关闭后追上最新状态。工作区取消入场透明度，首帧也不会透出节点。
- Playwright `tests/e2e/mobile-shell.spec.ts` 覆盖 320×568、360×640、390×844、430×932、844×390、768×1024、80%-200% 字体、三档抽屉、建造/连续放置、显式布局模式、工作区列表/详情、滚动恢复、两级返回、不透明边界和所有现有工作区可达性。截图位于 `artifacts/qa/mobile-next-*.png` 与 `mobile-stage2/3-*.png`。

香港 `0.7.0` 继续提供该壳层的 opt-in 试用，上海 `0.5.0` 仍保留原发布构建。新版尚未切为手机默认，仍需完成阶段 4 真机门禁与真实玩家对照反馈。

### 存档与在线服务

- 当前工作区与香港 `0.7.0` 为 `GameState` v31，上海 `0.5.0` 仍为 v30；存档封装格式保持 v2。加载器可将 v1-v31 归一到 v31。v30→v31 增加逐行星画布视角、主题/科技布局、建筑制造中心任务和航线载具所属站，并无损转换旧分拣器库存与线路字段；旧库存、实体、线路、科研投入、蓝图、在途路线、行星档案、旧油井和戴森计划均保留。
- 自动保存选项为 30/60/120 秒，默认 30 秒；切后台、`pagehide`、卸载和返回主菜单会立即保存。手机端存档摘要扫描降至 30 秒，全屏工作区或网页后台期间冻结底层画布快照更新。
- 本地主存档、槽位和快照不再持久化 `productionHistory`，内存统计曲线仍照常运行并在刷新后重新积累。主存档先写入、读回并校验，成功后才通知界面；配额异常只会清理自动快照并重试一次，最终失败会持续提示玩家立即导出。
- 本地主存档有校验和和上一份有效备份；自动快照最多 2 份、至少相隔 5 分钟，手动快照和 3 个手动槽位不会被自动清理。手动槽位与快照删除均要求连续两次确认并显示目标摘要。
- 离线模拟默认最多 7 天，可由终局研究逐级提高到 30 天。
- 当前工作区和线上云服务均为 schema v6，提供邮箱验证、旧账号邮箱绑定、忘记/重置/修改密码、30 天设备会话及撤销、账号数据导出与注销；v3/v4 旧账号迁移后保持已验证状态。
- 新账号验证邮箱后才能写入云存档或排行榜；正式代码支持腾讯云 SES 审核模板 API，并保留通用 webhook 作为回退。前端从健康接口读取邮件能力：发送器未启用时，注册、邮箱绑定、重发验证和找回密码会标注“正在开发中”，现有账号登录、四槽云存档及已验证账号的自动同步不受影响。缺少完整 API 凭据或模板 ID 时不会伪装发送成功，线上邮件发送器仍未启用。
- 本地身份可以显式绑定或解除云账号，绑定元数据与 `GameState` 分离，不会改写本地工厂。
- 玩家进入工厂后发送匿名心跳；服务端只保存本机匿名 ID 的 SHA-256 哈希，并按节点统计累计、今日和最近 120 秒在线玩家。运营中心每 30 秒刷新这些聚合数据。
- 浏览器会按隐私分桶上报页面加载、LCP 和静态传输体积；运营后台显示 P75 档位，不保存原始时序、完整 URL 或玩家存档内容。
- 账号、改密、会话撤销、数据导出、注销和云修订恢复写入隐私最小化安全审计，只保留动作、时间、匿名化账号/IP 和客户端类别。
- 云端为每名玩家保存互不覆盖的主存档和手动槽位 1/2/3，各自维护修订、历史和冲突状态；手动云槽与本地手动槽一一对应，不会替换正在游玩的主工厂。邮箱验证后，主存档每 10 分钟检查并自动上传；失败只记录状态，不影响本地保存，检测到双端变化时会暂停并要求玩家选择。排行榜仍只校验主存档。
- 服务端生产存储为 SQLite，每 6 小时备份一次，部署配置保留最多 30 份。

### 发布与运行

- Vite 生产构建拆分 React、React Flow、游戏核心和按需工作区。
- 主菜单使用只读存档摘要，不再提前加载 React Flow、完整存档迁移器或模拟器；玩家进入工厂或执行存档操作时才下载对应 chunk。
- Electron 启用上下文隔离、sandbox、外链拦截和可选自动更新。
- GitHub Actions 可以按 stable、beta、nightly 构建桌面产物。
- Nginx 模板提供 gzip、静态资源长期缓存、SPA 回退、安全响应头、TLS 和 `/api` 反向代理；线上是否启用必须以节点响应头为准。
- PWA service worker 使用确定性 build ID 隔离缓存版本，避免代码发布后混用新旧应用壳。
- systemd 配置包含云服务自动重启和每两分钟健康检查。
- 认证加密的异地备份、隔离恢复演练、五分钟公网/API/磁盘/TLS 探针和可选告警 webhook 已落地；5 项运维工具测试通过。香港每日备份与上海每月恢复 timer 均已启用，首份真实生产备份已在上海隔离恢复成功，恢复过程未留下明文 SQLite。

## 4. 版本基线

项目中同时存在三种版本概念，后续发布必须分别管理：

| 概念 | 当前值 | 用途 |
| --- | --- | --- |
| npm 应用版本 | 香港 `0.7.0`；上海 `0.5.0` | Web/Desktop 发布版本与安装包版本 |
| 游戏状态版本 | 工作区与香港 `31`；上海 `30` | 加载器可迁移 v1-v31 |
| 存档封装版本 | `2` | 校验和、元数据和导入导出格式 |
| 云服务 schema | 工作区与线上 `6` | v6 增加旧账号邮箱绑定和三个独立手动云槽 |
| 本地身份 schema | `2` | 本地排行账本和可选云账号绑定，不属于 `GameState` |
| 内容包格式 | `1` | 外部内容包交换协议 |
| 蓝图交换格式 | `1` | 蓝图 JSON 协议 |

不要把“v31 存档状态”当作产品版本。每个正式发布都应先确定 SemVer，再生成可追溯构建 ID 和 Git 标签。

香港当前正式发布为 `0.7.0-8bf16d91d82d`，构建 ID 为 `0.7.0+8bf16d91d82d`，源 Git 提交为 `8bf16d91d82d780eabe6ad18655b75f1a58d14b9`，标签为 `v0.7.0`。发布清单包含 98 个文件且来源工作树干净；完整制品、备份、回滚与验收证据见 [releases/0.7.0.md](./releases/0.7.0.md)。

香港的安全回滚目标为 Web/API `0.6.0-ae779d297011`。上海继续运行 `0.5.0-5b3a468c94d0`，其安全回滚目标仍为 `0.4.0-c77d76223f67`。两地一键回滚只切换代码并保留各自 schema v6 数据库；固定工具路径为 `/usr/local/sbin/dsp-idle-switch-release`。

首个公网源码快照冻结为 Git 提交 `9acd4460868a4328a1b5e8bb3afbe736c7857e09` 并标记 `v0.1.0`。该标签继续用于追溯原始线上源码；从 `v0.2.0` 起，正式发布使用“产品版本 + Git SHA”的确定性构建 ID。首版证据见 [releases/0.1.0.md](./releases/0.1.0.md)。

## 5. 当前质量基线

| 检查 | 基线结果 |
| --- | --- |
| TypeScript 严格检查 | 通过 |
| Vitest | 香港 `0.7.0` 发布提交 282/282；上海 `0.5.0` 基线 268/268 |
| Playwright Chrome | 香港 `0.7.0` 发布提交 113/113；上海 `0.5.0` 基线 97/97 |
| Node 云服务测试 | `0.7.0` 本地与香港新 release 均为 22/22 |
| 运维备份/恢复/Nginx/探针 | 5 项通过 |
| Vite 生产构建 | 通过 |
| 桌面、手机竖屏、手机横屏 | 已进行视觉与交互检查 |
| 手机新版阶段 0-3 focused E2E | 9/9 通过，并纳入 108 项全量回归 |
| 正式 HTTPS 入口 | 已进行浏览器验收 |

详细命令和按风险选择测试的方法见 [TESTING_RELEASE.md](./TESTING_RELEASE.md)。

## 6. 已知风险与技术债

### P0：发布与数据安全

- 云服务仍是单进程、单 SQLite 文件架构；认证加密异地备份和恢复演练已经形成首个闭环，但仍需持续检查 timer、30 天密文保留期和每月恢复报告。
- 腾讯云发信域名与地址已经完成验证，正式代码已支持 SES API；正式环境仍缺少完整的审核模板 ID 和最小权限 CAM 凭据，因此新注册和邮件找回仍会返回 `503 EMAIL_SERVICE_UNAVAILABLE`。已有已验证账号的登录与四槽云存档不受影响。
- 账号系统已有设备会话撤销，但尚未实现异常地点检测、登录告警和后台人工账号处置。
- 排行榜校验属于“云存档关联 + 指标约束”，不是服务端权威模拟，不能抵御有针对性的作弊。
- 上海入口是 HTTP，代码已禁止云登录；不能为了兼容旧地址而放开明文凭据传输。

### P1：性能与运维

- 两个节点已对 JS/CSS 启用 gzip，hashed asset 保持 immutable；Brotli、真实用户一周趋势和更细的资源预算仍可继续补充。
- `src/styles.css`、`src/game/engine.ts`、`tests/e2e/game-flow.spec.ts` 和 `src/App.tsx` 已经成为超大文件，修改冲突和回归成本较高。
- Web/服务端已有可复现清单、原子切换、健康检查和固定路径回滚工具，但仍缺少从提交到双节点灰度发布的 CI 流水线。
- 节点探针已覆盖外部可用性、API、磁盘和 TLS，管理员后台能读取状态；可选告警 webhook 与集中日志仍未配置。上海磁盘在 `0.5.0` 验收时约剩 6.7 GiB（文件系统使用率约 89%），需要优先观察发布目录、日志和备份增长。
- 当前环境没有连接 Android 或 iPhone 真机，也没有 ADB 工具；香港 `0.7.0` 已完成 Chrome 触摸仿真与正式域名横竖屏烟测，但仍缺少 Android Chrome 与 iPhone Safari 各连续 30 分钟的耗电、温度和真机帧率记录，不能用桌面模拟结果冒充。
- 手机新版壳层目前仍是香港 opt-in 试用；阶段 0-3 已完成自动化与桌面 Chrome 触摸仿真，但阶段 4 的 Android Chrome、iPhone Safari、软键盘/PWA standalone、连续 30 分钟温度耗电和真实玩家对照数据尚未完成，因此不能切为默认。

### P1：产品与合规

- 当前物品、配方、科技和流程以《戴森球计划》高还原原型为目标。公开推广或商业化前，应做名称、美术、文案、数值和世界观的原创化审查，并取得专业法律意见。
- 当前是公开测试版，不应把“功能很多”误认为“生产运营成熟”。隐私政策、用户协议、数据删除流程和公开反馈机制仍需补齐。

### 明确不在当前范围

- 黑雾或战斗系统暂不开发。
- 原创玩法扩展应排在稳定性、数据保护和发布工程之后。

## 7. 文档入口

- [ARCHITECTURE.md](./ARCHITECTURE.md)：代码结构、状态流、模拟器和云服务架构。
- [GAMEPLAY_SYSTEMS.md](./GAMEPLAY_SYSTEMS.md)：玩法循环、内容边界和交互规则。
- [DEPLOYMENT_OPERATIONS.md](./DEPLOYMENT_OPERATIONS.md)：双节点拓扑、发布、备份和回滚。
- [TESTING_RELEASE.md](./TESTING_RELEASE.md)：测试矩阵、验收和版本发布清单。
- [ROADMAP.md](./ROADMAP.md)：从公开测试版走向长期运营的阶段计划。

本文件记录“当前是什么”；未来计划只在路线图中维护，避免把设想误写成已完成功能。
