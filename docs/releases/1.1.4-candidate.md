# 1.1.4 开发候选记录

## 状态

- 状态：`development-complete / release-not-authorized`
- 目标：制造巨构批处理与多中心公平、蓝图传送带拓扑守恒、挂机/离线结算有界前缀、服务器运行时账号查找优化、终局大存档压缩与低峰值保存、64～96 MiB 云容量扩展。
- 运行时提交：`56c6d2e5e7a5cd8cc52d7b506a9b6e4fd89da1e0`。
- 服务器优化提交：`5cab4593d9db09e015f17d23e85efb5a143f9634`。
- 终局大存档提交：`9187b4d970264b77f97bb56429dcc8db1035c8e1`。
- 开发工作树：`D:\GameDev\DSPidle2-v114-dev`，分支 `codex/1.1.4-construction-blueprint-server`。
- 当前没有正式 release ID、Build ID、冻结制品、SHA256SUMS、previous-stable 或 canary 指针；不得发布或更新下载页。

## 交付内容

1. 高堆叠建筑制造中心通过有限副产物相位探测建立安全聚合周期；多个中心共享有界预算，保持确定性和物资守恒。
2. 蓝图部署/排队保留模板显式传送带数量，设备默认只作用于新绘制线路。
3. 普通离线和纯挂机 Worker 失败回退先执行最多 1 秒隔离精确前缀，剩余不确定尾段明确保守冻结；UI 区分短窗口未测得和真实未运行。
4. 服务器优化新增运行时 UserLookupIndex：账号查找从全表扫描降为索引命中并权威复核，冷启动/外部 mutation/注册/删除/邮箱变更均可重建或增量维护；不改 server 数据协议、SQLite 表或云 schema。
5. v47 非活动默认字段稀疏化将玩家真实档从 67,254,143 字节降到 62,264,191 字节；大档自动/手动保存直接消费模拟 Worker 权威 transfer，避免第二份完整状态镜像，连续自动保存、手动保存、备份读回和重载均通过。
6. 云端保证档位提高到 64 MiB，单修订硬上限为 `96 MiB - 1024 B`；gzip、解压、并发、响应、超时、配额与 Nginx 112m 作为同一合同调整，30 MiB raw fallback 不变。

## 门禁摘要

| 门禁 | 结果 |
| --- | --- |
| TypeScript typecheck | PASS |
| Vitest | 174 files，1,446 passed，7 skipped |
| Construction/blueprint/offline focused | PASS |
| Native | 24/24 PASS |
| Chromium construction stability | 1 PASS，2 个既有环境跳过 |
| Vite build + startup budget | PASS，startup 194,862 B gzip，menu 282,357 B gzip |
| Server | PASS：376 passed / 2 skipped；station 3/3；UserLookupIndex 3/3、runtime-indexes 联合 18/18 |
| Ops | PASS：56 passed / 6 Linux-only skipped |
| Chromium 大存档/权威保存专项 | 10/10 PASS |
| 玩家真实 64.14 MiB 档 | PASS：压缩后 62.26 MB；自动保存 2 次、手动保存、备份读回、重载完整性通过 |
| Android/Windows/真机/公网/VPS | NOT RUN |

## 数据与回滚

- 不升级 GameState v47、envelope v2、cloud schema v8 或 SQLite layout v3；本候选没有生产数据写入。
- 回滚先回退 `9187b4d970264b77f97bb56429dcc8db1035c8e1`，再按需回退 `5cab4593d9db09e015f17d23e85efb5a143f9634` 与 `56c6d2e5e7a5cd8cc52d7b506a9b6e4fd89da1e0`；不需要数据库、云存档或玩家存档回滚。旧服务不能接收超过旧 64 MiB 上限的新修订，容量回滚需单独提示。
- 发布前须由 Release Agent 在独立 clean checkout 重新构建并生成不可变 manifest，完成备份、Nginx 112m 配置验证、64～96 MiB 公网传输、签名、设备和双节点发布门禁；失败即 No-Go，不得部分发布。

详细调查、测试命令和风险见 [1.1.4 开发交接](../feedback/2026-08-23-1.1.4-development-handoff.md)。
