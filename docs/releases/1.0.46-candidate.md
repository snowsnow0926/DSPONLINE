# 1.0.46 Durable 存档热修候选记录

> 状态：本地开发候选，未部署。

## Scope

本候选只修复 durable simulation recovery：保存/模拟 Worker 回执失败后的同页恢复、revision/head 安全重试，以及纯挂机终态保存时普通 Worker 的重建接管。未升级任何持久化协议或 GameState 版本。

## Acceptance evidence

- 第一次 durable finalize 被测试 Worker 注入失败后，页面保持暂停；点击“继续模拟”完成 recovery Worker 回放、T1 保存、recovery head 原子替换和普通 Worker 重建。
- 复原后的设置编辑、recovery sequence/revision 和 `pending=false` 通过浏览器断言。
- runtime protocol、WAL integration、纯挂机基础旅程和 existing unit suite 需在最终候选重新执行。

## Not a release record

此文件不证明香港/上海、下载页、Android stable、Windows feed、签名、生产备份或公网 smoke test 已完成。上述事项属于 release role，必须使用最终 clean development handoff 独立验证。
