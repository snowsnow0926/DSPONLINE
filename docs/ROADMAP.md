# 公开测试版后续路线图

> **2026-09-09 当前优先顺序**：新冻结 `2bb10b43` 已证明一类公开大工厂的 Windows 短离线完整等待收益和保档闭环；线程参与及恢复测试前提修复已通过本机专项，云端回归继续核对。终局档原有菜单恢复路径经一秒完整候选证明可用，下一步验证其真实菜单完整等待、取消、持久状态及重开，再依据成本设计有界长离线；不再把绕过前置恢复的 raw timeWarp 拒绝当作待新增的准入功能。保留历史堆异常隔离和根因缺口，不把一次全量成功等同于当前发布合格；原 1–30 秒采用范围、实时及跨端边界不变。测试继续隐藏、静音、不抢焦点，完整模拟需 6 GiB 启动余量。见[当前批次报告](./RUST_BATCH_REPORT_2026-09-09_CURRENT.md)和[终局来源复核](./reviews/rust-rp1-recovered-endgame-source-2026-09-09.md)。下方“下一循环”保留当时安排，以本段为当前顺序。

> **2026-09-09 本批完成**：Windows 普通存档到 Rust 的真实入口，包括自然保存、取消保档、完整结果持久保存和正常重开已验收。下一循环优先测授权终局档完整等待并独立配对，再依据成本扩展有界长离线证明；不把公开小工厂的正确性当作终局提速。长离线、实时及网页/安卓 Rust 保持未开放。用户要求后续测试后台运行，桌面实包验证须先具备不显示、不抢焦点的方式。见[本批小白版完整报告](./RUST_BATCH_REPORT_2026-09-09_ENTRY.md)。

> **2026-09-09 优先顺序调整**：自然保存入口已实际发现缺少匹配原生检查点；只读运行态来源 Host 已完成正确性与保档验证。下一步先接主进程有界上传/返回及继续游戏按钮，验证自然保存→Rust 候选→取消或持久保存→正常重开；随后对授权终局档做完整用户等待的独立配对，再扩展有界长离线证明。新 Host 接口没有自行开放长离线或实时权威。见[本批报告](./RUST_BATCH_REPORT_2026-09-09_RUNTIME_SOURCE.md)。

> Rust 下一批优先级（2026-09-09）：先测真实终局档“加载到能够继续操作”的完整等待和完整状态，做独立配对；再验证正常游戏检查点与无 revision/catalog 旧档的关闭兼容；按测量结果扩展有界长离线证明。继续保留 30 秒采用限制和 JS 实时权威。当前交付条件见[批次报告](./RUST_BATCH_REPORT_2026-09-09.md)。

> 2026-09-08 最新循环：冷开准入和精确步进的基础状态复用均取得配对收益，两项旧长离线超时已在原门槛内通过。下一步验证真实大厂多秒推进及完整入口/取消/提交/重开，再设计有界长证明；不直接提高原记录或内存预算。参见[精确计算阶段证据](./reviews/rust-rp1-exact-dyson-base-2026-09-08.md)。下方保留此前各阶段事实。

> 2026-09-08 持续开发：物理签名优化及完整 release 矩阵已通过；下一循环定位真实终局档开档约 2.33 GB 的采样峰值，再设计有界大厂证明和完整入口/取消/提交/重开验证。保留历史堆异常未定位的限制，不直接提高 2,000 记录上限，不把局部 RPC 收益当作 RP1 完成。见[本轮证据](./reviews/rust-rp1-streamed-physical-proof-2026-09-08.md)。

> **1.2.7 网页/安卓阶段已上线（2026-09-08）**：共享保存、导入、离线准备优化及挂机停止恢复修复已发布到香港主站、新上海独立站和 Android stable；Windows/API 保持 1.2.6。完整本机浏览器回归与两站观察通过，实际证据和验证限制见 [1.2.7 发布记录](./releases/1.2.7.md)。下一版本继续 RP1 真实大档完整离线收益与 Rust 跨端接入评估；本次共享 JS 优化上线不代表 RP1–RP5 或 Rust 实时权威已完成。下方阶段数字保留各自测量平台和范围，不能外推成网页/安卓性能百分比。

> 2026-09-08 运维进展：上海全业务及下载入口已从即将到期的旧机切至新机，版本保持 1.2.6；QQ 重新登录由用户安排稍后完成。数据、运行检查及备份恢复证据见 [上海迁移记录](./releases/ops-shanghai-vps-migration-2026-09-08.md)。下方旧上海 DNS 回切是 1.2.6 发布当日的历史记录。

## Rust 性能逐步开发与上线（2026-09-07）

2026-09-08 已从 1.2.7 发布后的 main 继续开发，阶段盘点见 [Rust 进度报告](./RUST_PROGRESS_REPORT_2026-09-08.md)。统计历史有序追加已提交 `5c619817`，30 对完整字节相同，局部耗时缩短 27.3%–27.8%；全量稳定性与测试夹具仍有待解决问题，见[阶段记录](./reviews/rust-rp1-history-append-2026-09-08.md)。真实大档完整闭环、实时资格与跨端 Rust 仍待后续验收。

执行入口：[Rust 核心性能提升逐步计划](./RUST_PERFORMANCE_ROLLOUT_PLAN_2026-09-07.md)。先核验现有 Windows Rust 离线路径和大档收益，再推进实时单所有者接管；Web/PWA 与 Android 的接入另行评估。以下是新阶段状态，不重置历史 100/100 代码候选记录。

- [x] 建立计划、源码/实包基线、累计工作量估算与验收口径。
- [x] 独立恢复阶段：42/42 实包样本有效，20k / 80k 过期原生侧车检查耗时下降 67.9% / 70.5%，匹配恢复基本持平；桌面旅程 8/8。准备未签名 Windows 离线性能开发版 candidate，未推送、未发布。见 [阶段记录](./reviews/1.2.7-rust-recovery-stage-2026-09-07.md)。
- [x] 独立保存/导入阶段：`2b1df8752082` 在 107.6 MB 真实终局档的 3 对 Windows 实包样本中，完整导入等待中位缩短 27.1%，最长主线程任务中位缩短 43.0%；主档/自动快照状态 SHA-256 全相同。共享保存优化可用于三端代码，实机数字只来自 Windows。见 [阶段记录](./reviews/1.2.7-save-import-performance-2026-09-07.md)。
- [x] 独立离线校验/准备阶段：`1c1b80157bdb` 的三对真实 Windows 实包决定等待中位缩短 16.1%，Worker 往返缩短 23.6%；六次取消后主档不变。完整单元 3,111/32、Chromium 442/27、实包 8/8；没有完成离线结算。见 [阶段记录](./reviews/1.2.7-offline-preparation-performance-2026-09-07.md)。
- [ ] RP0：已固定合成基线并阻止超过 30 秒的未证明尾段自动采用；总矩阵仍未完成。新真实档已完成迁移、实际导入/存盘/退出及同档对照；约 600 秒返回虽更快出现保守决定，仍需继续验证完整离线路径。
- [x] Rust 普通离线边界修复：`ab01bb78` 的公共六例物料与关键进度 6/6，三例受限精确回退完整状态一致；无证书/超预算/未知时钟明确拒绝，长日志算法版本绑定完成。此为正确性阶段，没有新增玩家加速结论，见 [阶段记录](./reviews/1.2.7-rust-offline-boundaries-2026-09-07.md)。
- [x] Rust 普通离线完整状态与内核收益：`e9db8de4` 的原六例完整状态 6/6，扩展后 12/12；传输、历史、量子窗口、小数时钟和后续继续运行均通过。公开铁链三对 600/3,600 秒测试相较 JS 逐秒加校验缩短 46.4%/88.5%，只计内核。见 [阶段记录](./reviews/1.2.7-rust-offline-state-parity-2026-09-07.md)。
- [ ] RP1：先收敛本轮核心并发异常及 Host 夹具失败，再核验大厂 2,000 记录证明门槛和复合产线资格，独立完成真实入口等待、取消、持久提交和恢复。有序历史追加已取得局部结果；整体资格未完成，超过 30 秒自动采用继续关闭。
- [ ] RP2：可信资格链与隔离 Rust 实时候选，验证单所有者、操作、保存退出和恢复。
- [ ] RP3：完整玩法/模式/内容兼容与性能、内存、确定性候选。
- [ ] RP4：硬件、系统故障、签名、测试账号及正式 Native 实包资格，随后邀请 Beta。
- [ ] RP5：按获准通道逐批发布、观察与回滚；当前未发布。

上述 1–2、3–7、15–30、45–90 人日分别对应不同累计里程碑，不能相加；是否收益及后续预算以 RP0/RP2 实测重新估算。

## 1.2.6 已发布（2026-08-31）

- [x] 将产率复制收口为白矩阵科研与逐恒星系戴森进度直结，不向量子仓库、行星/机器/施工库存或累计生产复制物品。
- [x] 为星图增加三次确认的一键重置星球工厂，精确保留天然资源当前储量和全部全局进度。
- [x] 完成香港/上海 Web/API、上海下载页、Windows 与 Android stable 全量原子发布；1.2.5 设为 previous-stable。
- [x] 完成双节点新鲜备份、6/6 稳定采样、PWA、下载 9/9 哈希、Range 206、Android 证书连续性和回滚边界；下载域名唯一 A 记录已按授权回切旧上海。完整证据见 [1.2.6 正式发布记录](./releases/1.2.6.md)。

Android 实体设备、三档 Windows、Windows 1.2.5→1.2.6 覆盖升级和长时运行由用户只针对本 Release ID 明确豁免；后续版本不得复用。Windows 仍为 `NotSigned`。

## 1.2.5 已发布（2026-08-30）

- [x] 放宽纯挂机跨终局域连坐，保留真实库存、供电与单写者边界；白矩阵、科研、火箭和建筑制造可独立继续结算。
- [x] 修复大档 Worker 启动/同步恢复、隐藏启动锁、同标签页 Web Lock 释放竞争和自动旧检查点回退风险。
- [x] 修复蓝图详细卡片高度、溢出与信息重叠。
- [x] 完成香港/上海 Web/API、上海下载页、Windows 与 Android stable 全量原子发布；1.2.4 设为 previous-stable。
- [x] 归档双节点新鲜备份、完整公网读回、PWA 离线、稳定性观察、原生哈希和回滚边界。完整证据见 [1.2.5 正式发布记录](./releases/1.2.5.md)。

Android 实体设备、三档 Windows、Windows 1.2.4→1.2.5 覆盖升级和长时运行由用户只针对本 Release ID 明确豁免；后续版本不得复用。Windows 仍为 `NotSigned`。

## Windows Performance Edition（独立开发工作树，未发布）

- [x] 2026-09-07 完成 1.2.7 Round 3 固定 R1–R6：可信制品收集、正常关闭持久握手、可应用补丁及完整快照、真实 gzip 往返、UI 加工链、离线实包与进程审计；新包 8/8。详见 [第三轮报告](./reviews/1.2.7-round3-development-2026-09-07.md)，不增加原生玩家资格或新性能结论。

- [x] 冻结 76.9 MB 极限档的规范哈希、阶段耗时和进程内存口径；基准测试支持绑定指定 Host 二进制 SHA，便于同机交错 A/B。
- [x] 记录并拒绝“保留第二份完整 JSON DOM”和“只换 Windows 分配器”两条高内存路线。
- [x] 落地第一批零语义壳层策略与有界运行诊断：默认 Chromium GPU、无隐式 V8/优先级参数、显式软件回退，以及脱敏 GPU/Electron 进程/内存快照。该快照不冒充完整进程树实测。
- [x] 为 1.2.3 性能开发版冻结独立 appId、AppUserModelID、产品/EXE/setup、安装输出、userData 与 sessionData；默认离线且不自动迁移稳定版数据，可与当前安装并存测试。
- [x] 完成 COW 原生静态索引/记录容器、确定性 power probe 多核和线路 scratch/稠密调度优化，并通过单步、长窗口与真实档差分。
- [x] 完成当前 v47 规则覆盖内可安全落地的紧凑线路/实体列、原始记录补丁、流式聚合、编译路由缓存和串行 barrier 固定合并；仍未把所有 Mod/未来规则从通用 JSON 迁出。
- [x] 完成 30 秒守恒纯挂机、同 revision WAL/检查点恢复、10,000 次故障注入和 1/2/4/8/auto 线程真实档哈希矩阵。
- [x] 为实验性 exact-realtime 增加 durable `normal-main` 单写者栅栏、启动恢复阻断和主进程内部固定 pending-tick 操作。
- [ ] 完成玩家可见的唯一原生权威与 renderer 薄投影接管；当前仍为影子/实验链，`authorityEligible=false`。
- [ ] 完成薄 Electron 壳层 A/B 实验、原生 GPU 画布评估、UI 功能对齐及三档硬件 24 小时门禁。
- [ ] 完成签名、覆盖升级、回滚、邀请 Beta 和分阶段灰度；这些外部门禁完成前不宣称 stable。

总体目标、工作包、性能预算和 Gate 见 [Windows 全原生高性能桌面版总体重构计划](./WINDOWS_DESKTOP_FULL_NATIVE_REARCHITECTURE_PLAN_2026-08.md)；架构边界、被否决方案和完成证据见 [ADR-008](./architecture/ADR-008-WINDOWS-PERFORMANCE-EDITION.md)。GameState v47、envelope v2、cloud schema v8、SQLite layout v3 保持不变。

## 1.2.3 Windows 原生增量候选（开发中，未部署）

- [x] 活动 revision 的 base/实体页/线路页/拓扑脏标记与 durable ACK 后清除。
- [x] 可证明唤醒的线路 active queue、稳定顺序和稠密自动全扫描；不降低结算频率或产量。
- [x] 有界视口/统计二进制投影与 Rust 流式 v47/envelope v2 导出。
- [x] 压缩 chunk 流式读取、变化线路编码和已解析记录规范证明复用。
- [ ] 原生唯一权威、完整纯挂机/时间扭曲覆盖和确定性多核模拟。
- [ ] Windows 10/11 三档硬件 24 小时、Defender/磁盘故障、签名、邀请 Beta 与灰度。

开发事实和 No-Go 边界见 [三层计划书第 20 节](./WINDOWS_NATIVE_PERFORMANCE_DEVELOPMENT_PLAN_2026-08.md#20-123-独立工作树实施结算2026-08-27)。

## 1.1.5 已发布（2026-08-24）

- [x] 大存档低峰值 Worker gzip、稀疏持久化、`.json.gz` 导入/导出和受限原生分享。
- [x] 超大终局档纯挂机保守宏观结算与可重载验证；不把不确定尾段伪装为精确产出。
- [x] 银河综合榜 `balanced-log-v2`：累计发电、累计白糖、白糖产量、戴森功率和实际结算吞吐五项等权对数计分，移除隐藏探索/殖民加分。
- [x] 香港/上海 Web/API、上海下载页、Windows 和 Android stable 全量发布；香港 Web previous-stable 固定为 1.1.4。
- [x] 发布证据、回滚边界和残余风险归档至 [1.1.5 正式发布记录](./releases/1.1.5.md)。Android 实体设备门禁为用户豁免，Windows 按历史策略保持 `NotSigned`。

后续版本必须以新的开发交接、不可变候选和独立发布证据为准；不得把 1.1.5 的 previous-stable 路由当作 API、数据库或原生制品回滚授权。

## 全星系空间站扩展（1.0.45 候选，开发合并完成）

- [x] M0：兼容桥接开关与新状态骨架；`VITE_SPACE_STATION_ENABLED=false` 可构建不升级 v46 的桥接版。
- [x] M1-M3：全局空间站状态、三阶段建设、轨道货运终端、量子手动交付、每日合同、双轨经济、装饰收藏与独立画布。
- [x] M4-M5：脱敏公开快照、独立隐私、排行榜访问入口、只读直达页、收藏、固定通讯信号、管理撤下和独立 SQLite 表。
- [x] 基于最新 1.0.44 release candidate 完成合并，解决 `types/storage/App/server/index/save-field-contract/package` 冲突并统一为 GameState v47 / cloud schema v8 / SQLite layout v3。
- [ ] Release Agent 完成跨端桥接 rollout、全量 Playwright、真实 Linux / 原生签名 / 覆盖升级和隔离 SQLite 迁移后发布。

实现交接见 [SPACE_STATION_DEVELOPMENT_HANDOFF_2026-08-14.md](./SPACE_STATION_DEVELOPMENT_HANDOFF_2026-08-14.md) 与 [RELEASE_HANDOFF_1.0.45.md](./RELEASE_HANDOFF_1.0.45.md)。星环仍不在本轮范围。

> **方向变更（2026-07-31，历史决策）**：终局物流性能工作改为量子物流网络；当时开发状态为 GameState v44。当前 1.0.40 候选继续使用 GameState v46，有效资产的 v43 空间站实验存档仍不迁入共享池，传统物流站升级入口继续作为兼容面维护。详见 [量子物流网络开发 Agent 提示词](./QUANTUM_LOGISTICS_NETWORK_AGENT_PROMPT.md)。

## 决策原则

首个版本已经上线。下一阶段应从“继续增加功能”切换到“让现有版本能被可靠发布、长期保存、诊断和恢复”。下面按依赖顺序安排，不把尚未实现的内容写入项目现状。

## 1.0.41 大版本（开发侧完成，发布侧 No-Go）

- [x] 48 MiB 保证云存档、上传前容量预检和统一错误诊断。
- [x] 云存档状态中心、冲突双向导出、安全重试/取消和脱敏诊断。
- [x] 快速/精确/放弃离线选择收口；纯挂机前台高倍率与五分钟后台宽限保持不变。
- [x] 排行榜服务器成绩、本地最佳、模式/槽位和下一窗口差额说明。
- [x] 自适应端口命中、桌面/手机连续拉线、10/50/100 条原子批量连接。
- [x] 中文输入法、移动键盘、横竖屏和父级刷新输入丢失修复。
- [x] 1.0.40 No-Go 代码修复：真实软链接、持久日志、单 writer、旧 readiness、启动备份、大库预检和 Linux 门禁测试。
- [ ] Release Agent 在隔离 Linux 完成真实 systemd/Nginx/UID/GID 故障矩阵；未通过前禁止生产切换。
- [ ] Android 正式签名/真机/覆盖升级、低配 Windows、多设备/网络中断和长时离线/纯挂机验收。

完整范围见 [1.0.41 大版本开发与发布前总纲](./1.0.41_MAJOR_DEVELOPMENT_PLAN.md)。

## 1.0.40 大版本总计划（开发与候选交接完成，未部署）

> 2026-08-13 已确认把 1.0.39 全项目复盘事项作为 1.0.40 大版本统一开发。权威范围、验收标准、实施顺序、兼容矩阵和回滚边界见 [1.0.40 大版本开发总纲](./1.0.40_MAJOR_DEVELOPMENT_PLAN.md)。

- [x] 银河排行榜当前账号真实名次与窗口状态修复；这是已完成并通过门禁的第 1 项，尚未部署。
- [x] P0：Windows/全端 30 MiB 内大存档云传输契约统一。
- [x] P0：浏览器多标签页本地存档 lease/fencing 防覆盖。IndexedDB v2 内部协调元数据、Web Lock 串行化、BroadcastChannel/storage 通知、逐键 revision/tombstone、冲突双副本与只读/接管 UI 已完成；GameState/envelope 和存档正文不变。
- [x] P0：Web/Windows/Android/API 的 30 MiB 云传输契约统一。直接正文与旧 JSON 包装边界分离，gzip、动态超时、Windows 分片、Worker SHA-256 和不确定提交只读确认已完成；固定候选完整复验通过，正式签名与实体设备仍为 Release Agent 门禁。
- [x] P1：SQLite 持久化原子可见性、云容量/流式导出、上传单次解析、API/会话安全、低中断发布、本地快照容量和速通云恢复入口。
- [x] P2：PWA 更新回退、首屏和内存、字体/弹窗可访问性、服务端规模化、共享存档字段契约、大型合成夹具、CI/供应链和文档/监控。
- [x] P3：仅对本版触达的大模块做有界拆分，新文案使用稳定 i18n key，不进行无边界重写；上传检查、归档、配额、会话、运行索引/持久化、本地协调、共享弹窗和工作区 CSS 均已有窄模块与测试。
- [x] 全量 Web/API/Windows/Android 候选验证完成；固定运行时 SHA `58d3e6f986ec…`、Build ID `1.0.40+58d3e6f986ec`、6 个制品 SHA-256、SBOM、provenance 和 Release Agent 交接已生成。Android/Windows 为未签名诊断制品，正式证书、实体设备、隔离 Linux 与生产备份副本仍是发布门禁；开发阶段未部署生产。

## 2026-08-02 紧急稳定性、云存档与蓝图界面专项（待开发）

> 完整需求、分阶段收益、风险、体验取舍和验收标准见 [2026-08-02 紧急稳定性、云存档与蓝图精简界面开发计划](./URGENT_STABILITY_CLOUD_AND_BLUEPRINT_PLAN_2026-08-02.md)。本节仅登记优先级，不代表已经实现。

- [ ] **今日必须修复 / P0：蓝图工作区默认使用精简版。** 当前完整卡片常驻展开方向、镜像、配方、接口和施工需求，受到多列布局及底部施工托盘挤压后，部署入口难以到达。新增“精简/详细”模式并默认精简；精简卡片必须固定展示部署或排队部署主操作，详细功能进入单项详情，覆盖 1366x768、2048x1080、手机横竖屏及 80%-200% 字体。
- [ ] **P1：巨构蓝图记忆启用状态。** 第一批处理微型黑洞连接装置：来源处于已确认运行状态时，新蓝图记录“部署后自动启用”，直接放置或灰模建成后立即生效；暂停来源和旧蓝图保持关闭。累计销毁量、端口历史、临时故障和运行进度不得复制。自动启用蓝图在每次放置会话前集中确认一次永久销毁风险，但落地后不再逐台手动启动。该项与既有“物流塔蓝图记忆量子目标状态”相关但相互独立。
- [ ] **P0：云存档突破现有 8 MiB 请求边界。** 短期统一应用服务器、Nginx 和原生客户端边界并保留事务校验；长期增加有解压后上限的压缩传输、账号配额和跨端兼容，不能只扩大固定数字后宣称根治。
- [ ] **P0-P6：极限存档 Edge/Chromium 崩溃治理。** 增加设备级“终局优化·极限模式”总开关，明确牺牲非关键实时刷新、线路/粒子/阴影动画和远景细节，但不改变模拟、产量、物流、保存或物资守恒。React Flow 更新环、Service Worker clone、后台保存、状态增量协议和其他无损优化必须对所有玩家生效，不能藏在体验开关后。P0/P1 与开关可作为今日止血候选；P2-P6 可以今日启动，但未经确定性和长时间压力门禁不得同日直接发布。
- [ ] 三项分别形成独立提交和回滚面；蓝图 UI 热修不得夹带 GameState、模拟或云 schema 变更，性能架构版本不得与玩法平衡同批发布。

## 下一批待合并开发需求（9 项，开发中）

> 记录日期：2026-08-01。以当前 `1.0.18 / GameState v45` 为兼容基线，等待用户确认开始后统一设计、实现和回归。记录阶段不修改实现或部署；整批完成全部门禁后，按最新授权发布香港、上海两个 VPS，并更新上海下载页的 Windows/Android 安装包、更新清单和校验信息。

### 总体约束与发布边界

- [ ] 保持旧存档、旧蓝图和玩家库存兼容；建筑、线路、载具、喷涂模块、在途物资、递归中间物和副产物全程守恒。
- [ ] 所有玩家可见数量使用正安全整数或现有大整数类型；禁止静默截断、溢出、负数、重复扣除、重复返还或免费建造。
- [ ] 新增持久化字段必须从 v45 建立保守迁移和往返夹具；不因迁移重建实体、线路、缓存、生产进度、物流槽或量子共享库存。
- [ ] 桌面网页、Windows 桌面版、Android WebView、Android/鸿蒙等移动浏览器均纳入验收；共享交互覆盖鼠标、触控板、触摸、横竖屏及 80%～200% 字体倍率。
- [ ] 发布前执行本批聚焦测试、完整 Vitest、类型检查、生产构建、完整 Playwright、原生构建/覆盖升级和发布清单校验，并报告本次准确通过、失败和跳过数量。
- [ ] 两个 VPS 发布必须使用同一可追溯制品，先完成生产备份与校验，再隔离复验、原子切换、公网健康检查和回滚点记录；Android 必须沿用现有长期签名证书。

### 0. 内容包注册表同步到模拟 Worker

当前 `1.0.18` 经代码核验仍未修复：主线程会应用完整内容注册表，但实时和离线模拟 Worker 只接收 GameState 与时间预算。

#### 问题与实现边界

- [ ] 为实时 `simulation.worker.ts` 和离线 `offlineSimulation.worker.ts` 定义明确、可序列化的内容注册表同步协议，优先传递规范化 `ContentPackRegistry` 或等价运行时快照，并在 Worker 内复用 `applyContentPackRegistry()`。
- [ ] 实时 Worker 在首次模拟前完成注册表握手；导入、启用、禁用、更新或删除内容包后，在下一次模拟前应用新快照。
- [ ] 离线 Worker 每次启动都取得与本次存档加载相同的注册表快照，并在 `createSimulationAdvanceSession()` 之前应用。
- [ ] 为快照增加明确的 revision/fingerprint。Worker 缺少注册表、版本不匹配或应用失败时必须请求刷新或安全失败，禁止静默使用核心目录继续模拟。
- [ ] 把注册表切换作为持久化 Worker 的模拟边界：旧代次响应不得覆盖新状态，刷新期间的 `simulationSeconds` 和 `wallSeconds` 不丢失、不重复；只重建运行时目录和必要索引，不重建或清空 GameState 实体、线路、缓存与生产进度。
- [ ] 主线程 fallback、实时 Worker 和离线 Worker 对相同初始状态、注册表和时间预算保持确定性等价。
- [ ] 保持 `GameState.contentPacks` 的 `{ id, version }` 语义，不把完整定义写入 GameState，不升级 GameState 或存档 envelope。
- [ ] 在现有持久化 Worker、纯挂机和离线关键事件改动上增量实现；禁止 reset、回退或覆盖当前工作区其他未提交改动。

#### 固定回归夹具

- 使用合法 v2 内容包 `worker_registry_regression@1.0.0`，新增配方 ID `worker_gravity_matrix_to_warpers`：`assembling_machine_mk1` 每 10 秒消耗 1 个 `gravity_matrix`，产出 8 个 `space_warper`。
- 不得用已有核心配方 ID `space_warper_from_gravity_matrix` 代替复现；该核心定义会掩盖 Worker 未同步问题。
- 再增加一个“自定义物品 + 自定义建筑 + 自定义配方”的完整注册表夹具，并增加建筑 `speed/inputCapacity/outputCapacity` 覆盖或自定义传送带代表用例。

#### 必须通过的回归

- [ ] 手工放入 1 个引力矩阵时，主线程、实时 Worker、离线 Worker 均只消耗 1 个并产出 8 个空间翘曲器；传送带输入也能被目标建筑接收并完成生产。
- [ ] 原版 `space_warper_from_gravity_matrix` 对照组保持正常。
- [ ] Worker 创建后再导入或启用内容包，无需刷新页面即可运行新配方；更新版本后使用新定义，合法禁用或删除后不再沿用旧定义。
- [ ] 比较三条模拟路径的关键库存、生产进度、产出、线路转移量和状态哈希；断电、缺料、输出堵塞与分段模拟保持原行为。
- [ ] 构造注册表更新期间存在在途模拟请求的场景，验证旧响应被隔离，时间预算既不丢失也不重复。
- [ ] 完成 `npm run typecheck`、`npm test`、`npm run build` 和相关内容包/Worker E2E；因改动共享实时与离线模拟，条件允许时运行完整 `npm run test:e2e`，并记录本次每条命令的准确通过、失败和跳过数量。
- [ ] 若实现改变稳定 Worker/内容包架构边界，同步更新 `docs/ARCHITECTURE.md`、`docs/GAMEPLAY_SYSTEMS.md` 和 `docs/PROJECT_STATUS.md`；在开发完成前不得将其写成当前能力。

### 1. 蓝图导入数量上限不一致（P1）

固定附件 `蓝图 20.dspblueprint.json` 为合法 v2 蓝图，包含 71 个建筑和 181 条传送带；23 个建筑的 `machineCount` 大于 10,000，最大为 400,000。当前导出保留真实堆叠，但导入端仍按 1～10,000 校验，首个 10,240 堆叠被拒绝后又产生大量线路端点级联错误。

- [ ] 建立并复用唯一的蓝图建筑数量上限常量，与游戏实际建筑堆叠范围一致；至少接受 400,000，且只接受正安全整数。
- [ ] 禁止以截断到 10,000 的方式“修复”；非法值错误必须包含设备、`machineCount` 字段、实际值和允许范围。
- [ ] 建筑校验失败后，抑制仅由该建筑缺失引发的重复线路端点错误，同时保留真正独立的线路问题。
- [ ] 增加“创建蓝图 → 导出 → 原文件重新导入”往返测试，精确保留 71 个建筑、181 条线路和每个原始堆叠数量。
- [ ] 部署时按真实堆叠和线路汇总并原子扣除施工库存；材料不足返回原状态，不得部分放置或免费建造。

### 2. 拖动建筑时对齐辅助线消失（P1）

- [ ] 单选和多选拖动接近其他建筑的中心线、水平边缘或垂直边缘时显示辅助线；辅助线覆盖当前可视画布，但位于节点文字和操作按钮下方。
- [ ] 正确换算缩放和平移坐标，并覆盖暂停模拟、性能模式、鼠标、触控板和移动触摸拖动。
- [ ] 松开、取消拖动、失去页面或切换工作区时立即清除；辅助线只属于瞬时 UI，不修改建筑坐标或进入存档。
- [ ] 复用当前行星、视口和拖动开始时冻结的空间索引，禁止每帧扫描全部建筑；增加大型画布性能回归。

### 3. 蓝图记忆物流塔量子网络目标状态（P1）

- [ ] 蓝图模板、v2 导出和导入校验增加 `quantumMode` 或等价的“计划接入量子网络”字段；创建蓝图时记录来源物流塔的目标状态。
- [ ] 旧蓝图缺少字段时保持传统模式。满足量子科技和 Mk.II 条件的新塔在下一次五秒量子边界批量完成接入，无需逐塔确认。
- [ ] 不满足科技或等级时保留计划接入状态并显示具体阻塞原因；条件恢复后自动接入，禁止绕过前置。
- [ ] 批量放置使用一次规划、一次遍历和一次状态更新，避免逐塔全局扫描。
- [ ] 本地运输机、起送比例和本地槽位继续工作；量子模式只替代星际运输船调度。接入不得丢失物流槽设置、载具、本地缓存、在途物资或量子共享库存。

### 8. 量子网络全局吞吐基础值提高（P1）

- [x] 全局上传和下载继续使用两个独立额度，所有已接入量子塔按堆叠数共同贡献。
- [x] 公式统一为 `5000 件/分钟 × 无限物流科技倍率² × 全部量子塔堆叠总数`，替换原 `400 件/分钟` 基础值。
- [x] 量子采集器只使用共享上传额度，不额外生成独立带宽；同步更新星图库存面板、诊断统计和确定性回归。

### 4. 手搓建筑递归制造被托盘上限阻塞（P1）

固定验收场景为递归制造轨道采集器时，行星氢库存已达到约 1.01 亿软上限，但基础材料足够。

- [ ] 手搓与建筑制造中心共用同一套递归事务和副产物溢出规则；必要中间物进入事务级 WIP 并直接供后续步骤消耗。
- [ ] 不可避免的氢等副产物允许临时突破托盘软上限，按真实数量进入超限库存；不得删除、覆盖或丢弃。
- [ ] 超限物品暂停普通自动入库，但仍允许生产消费；数量回落后自动恢复。副产物满仓不能永久阻断当前递归任务。
- [ ] 整个制造计划先完整规划再原子结算；缺基础原料时不得留下半成品、重复产出或不可见的无限缓存。
- [ ] 保留绝对安全整数保护并提供真实阻塞原因。验收必须证明氢满仓时轨道采集器仍完成，库存增量与配方副产物完全一致。

### 5. 原生确认弹窗后数字输入永久失效（P1）

当前至少存在多处同步阻塞式 `window.confirm()`；已知复现为拆卸喷涂模块确认或取消后，传送带并联数量输入失效，直至重启。

- [ ] 建立统一的游戏内非阻塞对话框服务，替换仓库内全部 `window.alert()`、`window.confirm()` 和 `window.prompt()`；保留确认、取消和文本输入语义。
- [ ] 关闭后恢复到合理的触发控件或输入框，并清除残留指针捕获、画布拖动、多选、连线草稿、键盘修饰键和中文输入法组合状态。
- [ ] 确认和取消路径均验证传送带并联数、建筑数量、物流塔载具数及所有文本框的输入、删除、组合输入和提交。
- [ ] 覆盖网页、Windows、Android WebView 和手机浏览器；连续打开多个对话框后无需刷新或重启仍可输入。

### 6. 生产统计排序、时间窗口与大数显示（P1）

- [ ] 默认按固定物品目录顺序展示；“生产”和“消耗”列分别支持升序/降序切换并显示方向图标，数值相同时以目录顺序稳定兜底。
- [ ] 搜索、筛选和切换时间范围不意外重置用户当前排序。
- [ ] 右上角提供“每秒、每分钟、每十分钟、每小时”：分别使用最近 1 秒平均 `/s`、60 秒滚动平均 `/min`（默认）、10 分钟滚动统计 `/10min` 和 60 分钟滚动统计 `/h`。
- [ ] 默认每分钟视图使用稳定采样周期，顶部生产/消耗汇总与表格严格共享同一窗口；统计基于模拟时间，暂停时不产生虚假产量。
- [ ] 统一大数格式：例如 `97200/min` 显示为 `9.72万/min`，支持万、亿、兆、京及后续单位，超范围使用科学计数法并删除无意义 `.00`。
- [ ] 桌面悬停、手机点击可查看精确值；使用增量滚动时间桶，禁止每次渲染重新扫描全部建筑。

### 7. 蓝图缺料预建设、灰色待建与多次补足（P1）

#### 放置与模拟边界

- [ ] 建筑托盘为零或不足时仍允许创建蓝图施工订单；画布显示灰色半透明建筑和虚线线路，待建对象不参与生产、耗电、供电、物流或线路运输。
- [ ] 缺料放置后保持蓝图工具，可连续拍摄；Esc 或右键退出。库存不足不阻止下单，但行星不兼容、唯一建筑冲突、位置重叠和矿脉不匹配继续阻止。
- [ ] 第一版在主体材料全部齐备后整张蓝图原子建成，禁止部分节点提前参与模拟。

#### 蓝图工作区与补足

- [ ] 蓝图工作区拆为“蓝图库”和“待建与补足”两个标签；后者显示红色待处理数量。
- [ ] 每个订单显示蓝图名称、行星、创建时间、坐标、方向，以及建筑、喷涂模块、各级线路、运输机和运输船的已投入/总需求/剩余数量。
- [ ] 提供“补足建筑与线路”“补足物流载具”“一键补足本订单”“取消并返还”，以及按订单创建顺序稳定分配库存的“一键补足全部订单”。
- [ ] 每次补足只扣当前建筑托盘和便携载具池中实际存在的数量，并写入订单专用预留库存；允许多次补足。取消时完整返还所有预留物资。
- [ ] Mk.II、量子目标、喷涂模块和线路计入完整成本及科技门槛，禁止通过预建设绕过升级材料或前置科技。

#### 载具补足

- [ ] 运输机和运输船是非阻塞附加目标：主体材料齐备后建筑可完成，载具不足则转为“建筑完成，等待载具补足”。
- [ ] 后续补足按蓝图模板顺序在多座塔间稳定分配，不得反复只补第一座，不得超过单塔容量，也不得从其他已建塔拆取载具。
- [ ] 量子塔中停用的运输船继续保存，切回传统模式后可用；目标物流塔被拆除时自动取消对应载具补足任务并守恒返还预留。

#### 存档与性能

- [ ] 待建订单只在库存变化、玩家补足、订单增删或状态变化时重算缺口，禁止每个模拟步骤全量扫描。
- [ ] 订单引用不可变、可追溯的蓝图版本以及位置、方向和预留数量；后续修改或删除蓝图库条目不能改变已放置灰模，也避免每个订单重复保存整张蓝图。
- [ ] 只渲染视口附近待建节点和线路，低缩放使用简化轮廓；设置明确的订单、节点和线路安全上限，并在达到上限时准确提示。
- [ ] 保存、读取、本地/云端往返和跨端同步后，订单位置、方向、灰模、版本绑定和已投入数量完整恢复。
- [ ] 回归多次点击补足、多订单竞争、取消、完成、塔拆除和重载，验证库存不为负、不重复扣除、不重复返还且状态哈希确定。

### 整批完成定义

- [ ] 8 项实现、迁移、文档和全部回归完成后才调整版本号并生成候选制品；任何 P1 未通过都不得发布。
- [ ] Windows 覆盖安装必须保留本地存档、账号状态和设置；Android 使用连续签名和递增 `versionCode` 验证旧版原地升级。
- [ ] 同一候选版本依次完成香港、上海 Web/API 原子发布，再切换上海下载页及 Windows/Android 稳定更新清单；记录 Git SHA、build ID、文件大小和 SHA-256，并保留上一版回滚目录。

## 本地 v44 量子网络与传统站兼容面

- [x] GameState v42→v44 迁移、有效 v43 实验资产拒绝规则、量子共享库存和传统站 legacy 默认值。
- [x] 阶段施工、Mk.I→Mk.II 原地升级、legacy/elevator 安全切换、输出口唯一性和蓝图字段。
- [x] 五秒 hub 结算原型接入确定性模拟；传统站点保持原伙伴匹配路径。
- [x] 施工平台真实输入缓存、电力因子门控和按星系距离计算的聚合舰队返回桶。
- [x] 星图全图/恒星系批量升级入口、桌面/新版手机工作区和本地零材料测试模式。
- [ ] Worker/离线专用 hub profiler、真实终局样本 80%/50% 性能门槛和 30 分钟内存趋势。
- [ ] 施工发射平台实体输入、能源核心虚拟负载、完整跨星系路径/距离冷却和经典手机专用列表 QA。
- [ ] 全量 Playwright、云端 v43 验证门禁和候选版本发布；本轮不部署、不推送。

## 2026-07-31 `1.0.15` 纯挂机与零基础教程（双节点已上线）

- [x] 时间扭曲统一为纯挂机覆盖层；状态发布降频不改变模拟结果，停止时安全结算并验证主存档。
- [x] 暂停成为模拟硬边界，暂停期间不提交 Worker、不累计补算债务，恢复从暂停快照继续。
- [x] 设置增加 15 节中文自然语言教程，覆盖桌面/手机操作、生产、物流、戴森、巨构、蓝图、性能和存档排障；阅读进度独立于 GameState。
- [x] 香港/上海 Web/API 已原子切换到 1.0.15；上海下载页已切换到 Windows 1.0.15 未签名测试包，Android 正式签名包暂保留 1.0.14，旧目录和数据库回滚点保留。

## 2026-07-31 `1.0.16` 运行时账本与桌面云连接候选

- [x] 物流路线只在动态账本脏标记时重建；新路线即时入账，legacy 全扫描保留为状态哈希 oracle。
- [x] 画布沿用稳定拓扑/运行数据分离，离线 Worker 使用保守关键事件边界并保持精确回退；告警面板关闭时延迟构造详情。
- [x] 正式桌面构建缺少云 API 或更新地址时硬失败；成功生成的 Windows 包已复验 `app.asar` 元数据，覆盖升级先等待本地保存确认。
- [ ] 香港当前 SSH 公钥未授权，Android 长期签名 Secrets 未配置；在授权和签名恢复前不切换香港或公开 Android 1.0.16。
- [ ] 百亿流水线的稳定运输合同、Worker 增量状态、分块存档和生产历史计数器列为后续高风险专项。

## 2026-07-31 `1.0.14` 星区资料与大型工厂物流（双节点与原生客户端已发布）

- [x] 增加星球/恒星系自定义名称、备注、标签、搜索和恢复默认；元数据进入 GameState v42、本地/云存档并保持内部 ID 不变。
- [x] 无限采集科技同步作用于固体、原油、液体、硫酸和轨道采集器，固体矿脉消耗降低规则保持独立。
- [x] 蓝图部署先规划全部实体、矿机锚点、线路和施工库存，十万级需求采用安全整数和原子扣料；失败不改变原状态。
- [x] 物流诊断明确供应端送货/需求端取货，只从实际派遣站检查翘曲器；同恒星系线路不消耗翘曲器。
- [x] 修复索引无匹配时退回全实体扫描，并保留存档传送带顺序；真实样本候选检查 6,105，legacy/indexed 状态哈希一致。
- [x] 双节点原子发布、上海下载页和 1.0.14 原生包公网验收；发布标识 `1.0.14-d30daeae3a9f-dirty`，公网健康、哈希、Range 和缓存头通过。

## 2026-07-29 `1.0.10` 性能基础版（双节点与客户端已发布）

- [x] 建立不含玩家存档的 P50/P95/Max 合成终局夹具，覆盖 300/380/569 实体、300/500/1,160 线路和 45/80/128 物流站。
- [x] 在模拟会话内建立实体、行星、电网和线路端点运行时索引；供电、采矿、生产、施工、射线接收、线路转运和容量预留复用索引。
- [x] 保留 legacy 全扫描路径，并在三档夹具中逐状态比较；4 个模拟秒的本机基准显示 indexed 路径快约 60%～68%，状态字节数和状态哈希一致。
- [x] 画布端先派生当前行星实体/线路视图，端口占用、线路分组、任务高亮、节点和边避免扫描其他行星。
- [ ] 尚未实现 Worker 权威状态、保存 Worker、云端 SQLite 拆分、完整视口级边裁剪或真实 Android/Chrome/360 30 分钟内存与温度测试。
- [x] 用户已审阅并批准将当前成果作为 `1.0.10` 发布；版本、Android `1000010`、双语公告和完整本地发布矩阵已同步。
- [x] 从可追溯提交构建并验证 Windows/Android 制品，完成双节点备份、未激活目录复验、原子切换和下载清单更新。

详细实测、牺牲项、用户体验影响和回滚方式见 [PERFORMANCE_OPTIMIZATION_REPORT_2026-07.md](./PERFORMANCE_OPTIMIZATION_REPORT_2026-07.md)。

## 2026-07-30 `1.0.11` 性能与排行榜完整性（双节点与客户端已发布）

- [x] 缓存会话内稳定物流伙伴、路线环境、活动载具负载和路线经济，并让拥堵统计复用本步派遣摘要；10/50/100/128/300/500 站与 legacy 状态哈希一致。
- [x] 将燃料和能量枢纽充放电改为闭式批量结算，保留部分余量、整数库存和 legacy oracle。
- [x] 将完整递归制造计划编译为确定性事务；复杂链、副产物、多目标、多制造中心和已有部分 WIP 保持轮询、物资守恒与 legacy 状态一致。
- [x] 增加玩家同形与 2 倍终局匿名夹具，覆盖 600/1,200 实体、1,250/2,500 线路、100/256 物流站和 150/300 万并联；1x/4x/11x 哈希一致。
- [x] 增加服务器内部排行榜资格限制和幂等 dry-run/事务处置工具；五榜、上传、恢复、可见性和启动回填统一防止受限提交重建，账号和云档不受影响。
- [x] 正式版 546 项 Vitest、166 项 Playwright、42 项服务端、6 项运维、6 项原生工具、类型检查、许可证门禁和生产构建通过；未改变 GameState、envelope、云 schema 或 SQLite layout。
- [x] 完成原生覆盖升级、双节点备份/隔离复验、香港唯一目标处置、上海下载页和公网验收；账号、主云档、历史正文和其他同名账号保持不变。

完整制品、备份、排行榜事务、回滚和公网证据见 [releases/1.0.11.md](./releases/1.0.11.md)。

## 2026-07-30 `1.0.12` 轨道与物流交互（双节点与客户端已发布）

- [x] 重排物资配送枢纽卡片的三个端口、物资和状态分区，80%～200% 字号及手机/平板下保持紧凑并保留真实 handle 几何。
- [x] 将第一个逐项点选的运输网络固定为同步模板；全选/框选要求显式指定模板，并增加同步摘要与成功/跳过/失败报告。
- [x] 为电磁轨道弹射器增加单台、批量、蓝图和存档级目标太阳帆轨道；失效或跨恒星系目标暂停发射且不丢物资与进度。
- [x] 补齐亮色物流槽、舰队诊断和设置项的默认、悬停、按下、选中、焦点、配置与禁用状态。
- [x] 完成 v40→v41 守恒迁移、548 项 Vitest、176 项 Playwright、42 项服务端、6 项运维、6 项原生工具、类型检查、许可证和生产构建。
- [x] 从提交 `4f149409f433` 构建 Windows/Android，完成长期 Android 证书验证、覆盖升级保档、双节点 Backup API 备份、未激活目录复验、隔离启动、原子切换与上海下载站更新。

完整实现、制品哈希、覆盖升级、双节点备份、回滚和公网证据见 [releases/1.0.12.md](./releases/1.0.12.md)。

## 2026-07-30 `1.0.13` 大型工厂画布与大数显示（香港已发布，上海待授权）

- [x] 拆分并缓存稳定画布拓扑、端口占用、线路束和避让几何；节点与边只在视觉签名变化时创建新对象。
- [x] 为 300 个以上当前行星实体启用视口裁剪，小工厂保持全部节点可达；线路继续位于节点下层且真实 handle 几何不变。
- [x] 修复性能模式把建筑永久锁成灰色缩略态的问题；低细节现在只由真实缩放决定，放大后恢复完整卡片。
- [x] 预热并缓存跨星系行星对路径，提前稳定排序物流候选；10/50/100/128/300/500 站与 legacy 状态哈希一致。
- [x] 移除排行榜 `10^15` 人为上限，并把共享数量和功率显示扩展到中文大数单位与 SI 的 QW；极端值使用科学计数法。
- [x] 完成 556 项 Vitest、176/176 Playwright、44/44 服务端、6/6 运维、6/6 原生工具、类型检查、许可证和生产构建。
- [x] 从最终提交构建 Web/API，完成香港发布前后 Backup API 备份、未激活目录复验、原子切换和公网验收。
- [ ] 取得上海部署 SSH 授权后，复用同一 release ID 完成上海备份、未激活目录复验、原子切换和下载站更新。
- [ ] 配置既有 Android 长期证书与 Windows 发布签名 Secrets，完成覆盖升级、稳定清单和上海下载包验收；在此之前保留 1.0.12 原生包。

## 2026-07-29 `1.0.9` 跨端存档与高吞吐稳定性（双节点与客户端已发布）

- [x] 将主档、备份、快照和三个手动槽迁入 IndexedDB，补齐写入读回校验、旧副本验证迁移、急救镜像、容量明细和快照批量管理。
- [x] 为动态模块增加自动重试、版本识别和重新加载最新版入口，并让发布切换保留旧页面需要的 hashed assets。
- [x] 修复普通来源多线路固定顺序饥饿与一秒步长吞吐损失，统一高/标准/低优先级、同级确定性轮询和 1 亿线路转运额度。
- [x] 完成声明式内容包 v2、精确版本存档依赖、缺包阻止载入、动态传送带目录和 Mod 排行榜隔离。
- [x] 修复移动注册组合输入与文字清晰度问题，补齐亮色主页和新增英文无障碍文本。
- [x] 将空间站巨构收集任务改为长期开放，保留本地贡献并让旧 72 小时只冻结全服模拟曲线。
- [x] 在主菜单第一屏常驻中文/English 切换，并完成 320×568、200% 字体与 44px 触控回归。
- [x] 完成 v39→v40 守恒迁移、523 项 Vitest、166 项 Playwright、37 项服务端、6 项运维和 6 项原生工具回归。
- [x] 从最终可追溯提交构建并验证 Windows/Android，完成双节点备份、未激活目录复验、原子切换、上海下载页与稳定清单更新。

完整实现、制品哈希、覆盖升级、双节点备份、回滚和公网证据见 [releases/1.0.9.md](./releases/1.0.9.md)。

## 2026-07-28 `1.0.8` 存档完整性与性能诊断（双节点与客户端已发布）

- [x] 保存、导出和云上传前立即复核 envelope 内部校验，服务端独立重算并拒绝异常 payload。
- [x] 校验失败仍展示真实运行、实体、线路和科技摘要；结构完整存档提供原件备份、两次确认和当前工厂回滚保护的受控救援。
- [x] 物资配送枢纽三个端口支持独立指定、恢复自动识别和清空，并对线路施工件、缓存和在途物资做守恒返还。
- [x] 亮色制造栏覆盖正常、选中、可制造、缺料和禁用状态的可读对比度。
- [x] 运营中心增加默认关闭的按需性能监控，记录 60 秒 FPS、主线程、Worker、积压、内存、存档与模拟阶段耗时，并导出匿名报告。
- [x] 完成 v38→v39 确定性迁移、服务端 v39 校验、桌面/经典手机/新版手机和中英文回归。
- [x] 通过 510 项 Vitest、162 项 Playwright、36 项服务端、6 项运维、6 项原生工具、类型检查、许可证检查和生产构建。
- [x] 从最终提交构建并验证 Windows `1.0.8` 与 Android `1.0.8 / 1000008`，完成同签名覆盖升级，再备份并原子切换两个 Web/API 节点和上海下载清单。

完整实现、制品哈希、双节点备份、回滚和公网证据见 [releases/1.0.8.md](./releases/1.0.8.md)。

## 2026-07-28 `1.0.7` 建筑制造中心 WIP 急救补丁

- [x] 精确复现必要中间材料 `1,080,000/1,000,000` 时旧固定阈值永久阻断结算。
- [x] 移除建筑制造中心必要 WIP 的固定总量限制，继续只按后续步骤真实净需求保留任务库存。
- [x] 保持非必要产物“优先入行星托盘，满仓后销毁并累计”的守恒结算，不扩大到即时施工快制路径。
- [x] 暂停、断电和缺料保留原任务、步骤、进度与库存，恢复后自动继续；界面列出 WIP 明细和真实等待原因。
- [x] 加载器不再用普通建筑 1 亿缓存上限裁剪制造中心任务库存，GameState 继续为 v38。
- [x] 通过完整发布矩阵，为香港和上海分别创建验证备份并原子切换 Web/API；原生安装包继续为 1.0.6。

完整范围、制品哈希、备份与公网证据见 [releases/1.0.7.md](./releases/1.0.7.md)。

## 2026-07-28 `1.0.6` 高容量产线与采矿蓝图（双节点与客户端已发布）

- [x] 修正紫色矩阵粒子宽带为每周期 1 个，并用单周期回归固定统一配方数据源。
- [x] 将传送带并联上限提高到 4,096，同步检查器输入、蓝图、整网设置、存档边界、服务端校验和守恒退款。
- [x] 修复建筑制造中心被氢等副产物卡死：必要 WIP 保留、可存副产物入托盘、无空间且不再需要的新增副产物销毁并累计显示。
- [x] 增加批量减堆目标输入和 `-10 / -100 / 减至 1`，复用逐台减堆并保护物流载具、缓存、进度、线路和在途货物。
- [x] 让采矿蓝图非破坏性收录资源锚点和矿机相对布局，只在同类型现有矿脉上部署，不复制矿脉、储量或无限状态。
- [x] 完成 v37→v38 守恒迁移、旧蓝图兼容、服务端 v38 校验、双语公告与桌面/经典手机/新版手机回归。
- [x] 通过 503 项 Vitest、157 项 Playwright、35 项服务端、6 项运维、6 项原生工具、类型检查、许可证检查和生产构建。
- [x] 从最终提交构建并验证 Windows `1.0.6` 与 Android `1.0.6 / 1000006`，完成同签名覆盖升级和应用数据保留检查。
- [x] 为香港与上海分别创建并验证 SQLite 备份，在未激活目录复验后原子切换 Web/API，并更新上海下载页和双应用稳定清单。

完整范围、制品哈希、备份与公网证据见 [releases/1.0.6.md](./releases/1.0.6.md)。

## 2026-07-27 `1.0.5` 星际物流、戴森扩容与矿脉科技（双节点与客户端已发布）

- [x] 增加 `1` 引力矩阵制造 `8` 空间翘曲器的高级配方，保留基础路线，并让递归规划优先完整可行的精简路线。
- [x] 为物流塔增加载具目标输入、一键填满、忙碌载具保护，以及蓝图载具目标保存、部分装载和缺口反馈。
- [x] 修复配送枢纽 900～1000 停机、线路配置遗漏 `lanes`、灰锤缺料导航、同恒星系错误消耗翘曲器和建筑刷新漂移。
- [x] 显示玻璃产量中的合法行星工业加成；把人造恒星燃料缓存调整为每台 30 个，并把标准戴森球壳帆容量提高到 10,000。
- [x] 升级“矿脉极限利用”：每级固体采矿速度 `+10%`、矿脉消耗 `-10%`，Lv.10 起无损开采；使用持久整数余数保持确定性。
- [x] 验算默认有限星区可完成 Lv.1～Lv.10 的 49,620 个宇宙矩阵，限制资源煤矿仍剩本次消耗的约 356.7%。
- [x] 完成 v36→v37 守恒迁移、v37 云存档校验、双语公告、单元/服务/运维/原生工具和完整 Playwright 回归。
- [x] 从最终提交生成 Windows/Android `1.0.5 / 1000005` 制品，验证长期签名、覆盖升级、更新清单和本地存档保留。
- [x] 为香港与上海分别创建并验证 SQLite 备份，在未激活目录复验后原子切换 Web/API，并更新上海下载站。

完整范围、制品哈希、备份和公网发布证据见 [releases/1.0.5.md](./releases/1.0.5.md)。

## 2026-07-26 `1.0.4` 物流并联与终局管理（双节点与客户端已发布）

- [x] 为已建传送带增加 `-1`、直接输入和 `+1` 并联数量命令，原子扣返同级施工库存并保持全部线路状态。
- [x] 将线路吞吐与并联数量统一到既有 `getBeltCapacity()`，增加 64 条边界、缺料、旧超额存档与在途进度测试。
- [x] 将五条无限科技并入新版手机科技目录，补齐真实前置、空筛选恢复和列表/详情路由。
- [x] 将建筑仓储扩容 II 的最终目标上限提升为 100,000，保留 100/500 前置阶段、速度规则、直接输入和常用预设。
- [x] 保持 GameState v36、存档 envelope v2、云 schema v7 与 SQLite layout v2 不变，旧存档不重建线路或施工库存。
- [x] 同步 Web、Windows 和 Android 产品元数据到 `1.0.4 / 1000004`，更新双语公告和自动化契约。
- [x] 生成并校验 Windows/Android 安装包、更新清单和覆盖升级结果，上传完成后原子切换上海下载页。
- [x] 完成最终 152 项 Playwright、上海生产前后备份、未激活目录复验、原子切换和公网烟测。
- [x] 使用恢复的香港 SSH 授权独立完成备份、35/35 远端测试、127 文件复验、备份副本启动、Android Origin 白名单和原子切换。

## 2026-07-26 稳定性复盘（已随 1.0.4 双节点发布）

- [x] 修复浏览器持久存储拒绝写入或临时不可读时，登录成功后下一次认证请求立即丢失 token 的问题。
- [x] 保证退出或撤销当前会话时，即使 `localStorage.removeItem()` 失败，也不会重新使用残留旧 token；健康存储下继续支持跨标签凭据变更。
- [x] 在服务启动和每分钟持久化前清理过期/孤立会话、邮箱验证与密码重置 token，并按时间窗口回收认证和注册限流桶。
- [x] 收敛 PWA 更新的 `controllerchange` 生命周期，连续点击更新不再累积刷新监听器。
- [x] 保持 GameState v36、存档 envelope v2、云 schema v7、SQLite layout v2 和全部模拟规则不变。
- [x] 通过 49 个文件 472 项 Vitest（1 项可选基准跳过）、35 项服务端测试、149 项 Playwright、6 项原生工具、6 项运维工具、严格类型检查、许可证检查和生产构建。
- [ ] 在部署前使用生产备份副本做隔离启动，并完成两节点备份、清单复验、原子切换和公网烟测；本轮未获发布指令，因此没有操作 VPS。

审计范围、证据和保留风险见 [STABILITY_REVIEW_2026-07-26.md](./STABILITY_REVIEW_2026-07-26.md)。

## 2026-07-26 Android `1.0.3` 云存档兼容（上海下载节点已发布）

- [x] 确认 1.0.2 报错来自本地加载器拒绝 v36，而不是云服务拒绝；禁止有损降级新版存档。
- [x] 服务端测试固定接受 1.0.2/v35 和 1.0.3/v36，并覆盖 Android `https://localhost` 预检与未知来源拒绝。
- [x] 构建同签名 `1.0.3 / 1000003` APK，验证官方 API/更新地址、v2/v3、证书连续性和 1.0.2 覆盖升级保留应用数据。
- [x] 原子发布上海稳定清单、APK 和下载页；保留 1.0.2 APK 与完整回滚目录，验证公网 SHA-256、Range 和缓存边界。
- [x] 取得香港实例有效 SSH 授权后，先创建并验证 SQLite 备份，再部署 `https://localhost` origin 白名单并完成 GET/PUT 生产烟测。
- [ ] 下一次原生发布前把本次工作树整理为可重建的干净提交；当前 APK 以源码 HEAD、构建时 diff blob 和制品 SHA-256 追溯。

完整证据见 [releases/1.0.3-android.md](./releases/1.0.3-android.md)。

## 2026-07-26 `1.0.3` 递归制造与生产定位（双节点 Web/API 已发布）

- [x] 建立手工、施工快制和建筑制造中心共用的原子递归规划器，优先高级/稀有精简配方并在整链不可完成时回退。
- [x] 打通物流运输船递归快制及运输机/运输船建筑制造中心目标，成品统一进入全局随身载具栏。
- [x] 为物品图鉴增加当前行星产线定位、完整上游网络高亮、多目标切换、跨行星跳转和独立清除。
- [x] 修正轨道采集器自供能与高运输量舰队瓶颈诊断；补齐时间扭曲请求/实际倍率和功率证据。
- [x] 增加枯竭矿脉提醒、资源模式确认、喷涂模块无损拆卸，并修复显式零储量重载恢复。
- [x] 修复鸿蒙组合输入、80%～200% 仓储端口、手机托盘删除操作区和折叠物资侧栏残留。
- [x] 完成 v35→v36 迁移、服务端 v36 接受测试、双语公告与文档更新。
- [x] 通过 467 项 Vitest、149 项 Playwright、32 项服务端、6 项原生工具、5 项运维工具、类型检查、许可证和生产构建。
- [x] 从可追溯提交生成 release manifest，并完成香港/上海独立备份、远端测试、原子切换和公网验收。

完整范围与证据见 [releases/1.0.3.md](./releases/1.0.3.md)。本轮只发布 Web/API，不生成或更新 Windows/Android 公共安装包。

## 2026-07-25 `1.0.2` 英文版与亮色模式补全（双节点与原生应用已发布）

- [x] 增加设备级简体中文/English 切换与 `?lang=en` 入口，不升级 GameState 或云存档。
- [x] 为核心目录、科技效果、行星生态、恒星系、战役与主要桌面/手机工作区补齐英文文案。
- [x] 补齐开始菜单、账号、云存档、排行榜、全屏工作区、模态与两套手机界面的亮色主题。
- [x] 增加中英文 README、1.0.2 更新公告、目录翻译单元测试和亮色/英文 Playwright 回归。
- [x] 完成本地类型检查、451 项 Vitest、143 项 Playwright、32 项服务端测试、5 项运维测试、6 项原生工具测试、许可证校验和生产构建。
- [x] 从可追溯 Git 提交生成 Windows/Android `1.0.2` 制品，校验签名、哈希、覆盖升级与更新清单。
- [x] 为两台 VPS 创建并验证独立 SQLite 备份，原子发布 Web/API，并由上海下载节点发布原生应用更新。

## 2026-07-25 `1.0.1` 界面精修与物流稳定性（双节点已发布）

- [x] 统一功率/大数量精确值 Tooltip，并修复桌面悬停、键盘和触摸关闭生命周期。
- [x] 完成深色/亮色语义变量审计，覆盖异步工作区、画布节点、表单和两套手机界面。
- [x] 重做储物仓/储液罐共用节点布局，保持真实端口 ID、几何和命中范围。
- [x] 增加 v35 建筑交互锁与领域命令守卫，批量操作、蓝图和旧存档迁移保持无损。
- [x] 增加检查器分区重排、折叠、键盘排序和恢复默认的设备级偏好。
- [x] 完成塔内输入/未预留输出/行星托盘三级翘曲补仓，并复检 50 艘、多塔公平、故障切换和航线诊断。
- [x] 将行星托盘单种物资上限提高到 1 亿，并同步客户端、任务奖励、迁移与服务端校验。
- [x] 移除建筑制造中心单步 128 次截断并增加阶段追踪；长时、高堆叠和高倍率工时完整结算。
- [x] 统一生产进度填充、文本与 ARIA，视觉时钟独立于权威状态发布档并跟随真实模拟倍率。
- [x] 以近 5 模拟秒累计运输窗口统一线路 `/s` 吞吐与限制原因，消除短尾步假抖动。
- [x] 完成 123 文件最终制品清单、双节点独立备份、远端 32/32 服务测试、原子切换、公网烟测和发布后备份。
- [x] 发布 Android `1.0.1` 同签名覆盖升级 APK 和 Windows x64 未签名测试安装包，稳定更新清单与二进制由上海下载节点承载。

完整设计、算法和验收要求见 [POST_1.0_UI_LOGISTICS_DEVELOPMENT_SPEC.md](./POST_1.0_UI_LOGISTICS_DEVELOPMENT_SPEC.md)。

## 2026-07-24 `1.0.0` 戴森工程、终局巨构与物流调度（双节点已发布）

- [x] 戴森壳层复制使用分配起点隔离目标系统历史工程量，副本从零开工且后续建设正常推进。
- [x] 打通超大型物资出口四类专用输入，分离活动墙钟与模拟时间，活动窗口外保留缓存且不消费。
- [x] 增加三端口微型黑洞、默认暂停、首次双确认和 BigInt 精确销毁统计。
- [x] 增加全存档时间扭曲主控、指数功耗、剩余电力最低优先级和供电不足自动降档。
- [x] 星际物流允许同一需求由多个供应源部分补足，并用持久公平游标避免固定命中首塔；索引路径与旧全扫描状态哈希一致。
- [x] 功率显示统一为 `kW → MW → GW → TW → PW`，所有入口保留精确 kW 提示。
- [x] 戴森规划的复制、粘贴、保存和关闭命令固定可达，并覆盖 960～1920px、100%～200% 字体。
- [x] `GameState` v33→v34、服务端云存档校验、1.0 公告、Web/Windows/Android 版本和自动化测试已经完成。
- [x] 将云存档正文从单行 `app_state` 拆到独立 SQLite 修订行；真实香港备份的全部正文键、大小和 SHA-256 一致，普通指标写入不再重写全部历史。
- [x] 完成最终干净提交、115 文件发布清单、双节点一致性备份、原子切换、公网烟测和上线后备份。

完整规则、迁移和验收矩阵见 [1.0.0_DEVELOPMENT_SPEC.md](./1.0.0_DEVELOPMENT_SPEC.md)。

## 2026-07-25 `1.0.1` 原生应用测试版（上海下载节点已发布）

- [x] 完成 Electron 单实例、窗口恢复、受限云 API、外链隔离和 Stable/Beta/Nightly 更新通道。
- [x] 建立 Capacitor 8 Android 工程、API 24-36 配置、图标/启动画面、系统返回、网络、后台保存和新版手机 UI 默认值。
- [x] 让 Android 存档、蓝图、模板、诊断和账号数据通过系统分享面板导出。
- [x] 建立统一版本同步、更新清单生成、同源 HTTPS 校验、Android 证书指纹校验和 Windows/Android CI。
- [x] 在 Windows 目录包与 Android API 36.1 模拟器完成冷启动、横竖屏、覆盖安装保留数据和云健康接口烟测。
- [x] 配置 Android 长期 keystore，生成签名 `1.0.1` APK，并验证从同签名 `1.0.0` 原地升级保留应用数据。
- [x] 生成 Windows `1.0.1` x64 安装程序、blockmap 和稳定更新清单；在下载页明确标注 Authenticode 未签名风险。
- [ ] 物理 Android 与 iPhone 各完成 30 分钟温度、耗电、FPS、软键盘和文件导入导出测试。
- [x] 将 APK、Windows 测试安装包和更新清单原子发布到上海下载节点，并让香港 `/downloads/*` 重定向到上海。
- [ ] 取得 Windows 可信代码签名证书并发布签名安装包；当前测试包仍会触发未知发布者或 SmartScreen 提示。
- [ ] 解决 AAB 严格 JAR 校验兼容警告后，再进入 Google Play 或其他应用商店发布流程。

构建、签名、通道和更新目录规范见 [NATIVE_APPLICATIONS.md](./NATIVE_APPLICATIONS.md)。

## 2026-07-24 `0.9.1` 联合空间站活动

- [x] 修复超大型物资出口遗漏在桌面与新版手机施工托盘部署目录之外的问题。
- [x] 点击已放置巨构可直达银河活动任务页，并可暂停、恢复或定位每座出口。
- [x] 明确个人四项各 100 万、全服模拟四项各 10 亿和精确 72 小时规则。
- [x] 所有玩家无需账号即可把真实投入记录到本地存档；界面明确说明没有服务器上传和本版奖励。
- [x] 上海 HTTP 仅放开匿名只读活动状态，账号、云存档和密码传输仍保持禁用。
- [x] 完成完整发布矩阵、双节点备份、104 文件清单复验、28 项远端服务测试、隔离数据库启动、原子切换与统一活动配置启用。

完整边界和验收见 [0.9.1_DEVELOPMENT_SPEC.md](./0.9.1_DEVELOPMENT_SPEC.md)。真实贡献提交、服务器累计、跨存档去重、活动奖励和宇宙联合空间站本体继续留到后续版本。

## 2026-07-24 `0.9.0` 十三项需求（双节点已发布）

- [x] 完成当前代码、`GameState` v32、存档封装 v2、云 schema v7 和相关实现的只读审计。
- [x] 确认增产剂缓存默认 600、在轨/壳面太阳帆均为 88 kW、四条长期无限科技 Lv.1000 与约 5% 差异化成本增长、连续体演算 Lv.23 封顶、托盘全选范围和球壳直接新增层。
- [x] 确认活动使用真实本地个人交付、服务器时间模拟全服进度；服务器确认、奖励发放和宇宙联合空间站本体延期。
- [x] 按 P1 正确性、v33 迁移、物流性能、平衡与终局活动的依赖顺序完成实现。
- [x] 完成 TypeScript、Vitest、服务端、运维、离线 1 小时至 30 天等价、10/50/100/500 塔性能对比、Chrome/360 30 分钟内存趋势及六类视口/五档字体自动化。
- [x] 增加设备级生产画面刷新偏好，模拟、状态发布和视觉动画相互独立；固定档不会被自动策略覆盖，各档一小时状态哈希一致。
- [ ] Android Chrome 与 iPhone Safari 真机的全屏、PWA standalone、软键盘、方向切换及 30 分钟温度/耗电仍是阶段 4 门禁，桌面 Chrome 触摸仿真不能替代。
- [x] 香港与上海分别完成 schema v7 一致性备份、105 文件清单复验、27 项远端服务测试、隔离数据库启动、原子切换和独立公网烟测；活动配置保持关闭。

完整状态字段、算法、平衡表、活动曲线、迁移和验收规格见 [0.9.0_DEVELOPMENT_SPEC.md](./0.9.0_DEVELOPMENT_SPEC.md)。实现状态以本路线图、项目现状和 `0.9.0` 发布记录为准。

## 2026-07-23 v32 模拟、离线与缓存治理 Goal（0.8.0 双节点已发布）

- [x] 新建传送带增加存档级默认货物堆叠与线路形状，保持蓝图、既有线路和并行线参数不变。
- [x] 戴森界面统一 kW，并拆分理论接收、接收站实际利用和戴森功率利用口径。
- [x] 完成喷涂机安装原因诊断与新版手机制造搜索，模块仍不进入画布部署目录。
- [x] 统一储物仓/储液罐布局，并在原 13 步教学前加入五步真实命令基础导览。
- [x] 新版手机离线收益改为安全区模态，物流塔增加一次性补满载具，自动整理避开固定资源节点与安装空间。
- [x] 建筑堆叠、施工制造和手搓增加数量步进、最大值、材料预览及递归原子规划。
- [x] 长时间离线模拟移入 Worker；取消、失败和刷新不写半成品，1 小时至 30 天与同步路径等价。
- [x] 复杂工厂降低完整状态和画布发布频率；新增 Chrome/360 生产构建强制 GC 内存趋势脚本与证据。
- [x] v31→v32 增加独立的生产建筑、仓储物流建筑缓存安全上限，默认 100 万、合法范围 1,000～100,000,000；按每个物品缓存和每个物流槽分别生效。
- [x] 降低上限、减少堆叠及在途到货均保留既有物品；新输入、生产和派遣在超额期间暂停，调高后立即恢复。
- [x] 本地保存、三槽、导入导出、云同步、离线模拟、状态校验和排行榜云存档验证均识别 v32 两项设置。
- [x] 通过 TypeScript、31 个文件 320 项 Vitest、23 项服务端、5 项运维、生产构建和 118 项 Playwright；完成三种界面五档字号与自定义输入截图检查，以及 Chrome/360 各 30 分钟生产内存门禁。

本 Goal 已由干净提交 `2af7dc15eebb` 形成 `0.8.0`，并发布到香港与上海独立节点。两地均先创建并验证 schema v6 一致性备份，再在新目录完成 100 文件哈希与服务端测试后原子切换；香港保留 `0.7.0`、上海保留 `0.5.0` 作为代码回滚点。完整证据见 [releases/0.8.0.md](./releases/0.8.0.md)。

## 2026-07-23 v31 物流与工作区体验 Goal（香港 0.7.0 已发布）

- [x] 行星与星际物流站同时支持供应端主动送货和需求端主动取货；航线记录载具所属站，并按所属站处理占用、供电、翘曲器和返航。
- [x] 每颗行星独立保存并恢复 React Flow 平移与缩放，显式书签和设备定位仍可覆盖恢复视角。
- [x] 增加深色、亮色和跟随系统主题，覆盖画布、节点、线路标签、工作区、科技树、表单与新版手机壳。
- [x] 建筑制造中心递归加工可制造中间材料，按材料 0.1 秒/件、建筑成品 5 秒/个推进并显示阶段、预计时间和缺料。
- [x] 从施工、制造、图鉴和科技展示移除分拣器 Mk.I/Mk.II/Mk.III；v30→v31 将旧库存退款为传送带并归一旧线路字段。
- [x] 科技树增加标准/精简布局；经典手机在设置中切换，避免内联切换控件挤压科技关闭按钮。
- [x] 设置、银河、任务、建筑制造中心、星图、统计、生产资料库、科技，以及画布中的蓝图、戴森和网络入口在当前页面已打开时可再次点击关闭；新版手机固定导航与抽屉使用同一规则。
- [x] 生产、仓储和物流站的输入、输出、燃料、槽位、载具及翘曲器容量随堆叠增长并统一封顶；减堆保留超额库存。
- [x] 行星/星际物流站后续传送带依次复用同物品槽或第一个空槽，不覆盖手动槽；空槽耗尽、物品冲突和方向非法均返回具体反馈。
- [x] 本地通过 TypeScript、27 个文件 282 项 Vitest、生产构建和 113 项 Playwright；完成 1440px 亮色设置/画布/精简科技树及 390×844 新版手机壳截图检查。

本 Goal 已由干净提交 `8bf16d91d82d` 作为 `0.7.0` 发布到香港，GameState 升至 v31；发布前创建并验证 SQLite 一致性备份，Web/API 采用新目录原子切换，`0.6.0` 保留为一键回滚点。上海仍运行 `0.5.0` / v30，没有上传、切换或代理香港。完整证据见 [releases/0.7.0.md](./releases/0.7.0.md)。

## 2026-07-23 手机竖屏重设计 Goal（阶段 0-3 已在香港 0.6.0 opt-in 上线）

真实玩家反馈表明现有手机第三轮虽然解决了双指缩放、触摸吸附、长按防误触和方向切换，但桌面信息架构压缩到竖屏后仍存在字号过小、控件位置分散、常驻施工托盘遮挡工作区、节点点击立即覆盖半屏和横向工作区难以阅读等系统性问题。

- [x] 基于当前代码、390x844 自动化截图和实际 DOM 完成现状审计。
- [x] 确定“画布主场景 + 上下文抽屉 + 全屏工作区”的手机信息架构。
- [x] 完成顶部状态栏、底部五项导航、建造/检查器/物资、画布手势和大型工作区的目标规格。
- [x] 阶段 0：建立新版试用开关、移动布局判定、尺寸门禁和截图基线。
- [x] 阶段 1：实现移动壳层、工作区总览、行星抽屉和全屏工作区边界。
- [x] 阶段 2：实现三段抽屉、建造/放置、节点摘要、连线、多选和显式布局模式。
- [x] 阶段 3：依次重做科技、生产资料库、统计、星图、蓝图及其他移动工作区。
- [x] 阶段 4 自动化部分：80%-200% 字体、目标视口、方向切换、软键盘焦点策略、工作区边界和完整 Chrome 回归。
- [ ] 阶段 4 真机部分：Android Chrome 与 iPhone Safari/PWA standalone 连续 30 分钟温度、耗电、FPS、软键盘和低端设备验证。
- [ ] 阶段 4 产品部分：收集经典/新版任务成功率与玩家反馈，决定是否切为手机默认并保留经典回退一个版本周期。

阶段 0-3 最初随香港 `0.6.0` opt-in 上线，当前已随 `1.0.0` 同步到香港与上海，并纳入 131 项完整 Playwright：`?mobileUi=next` 为独立偏好，经典手机壳仍可回退；建造、物资和节点摘要已经替换桥接面板，完整检查器继续复用原命令；科技、生产资料库、星图和蓝图采用列表/详情路由，统计及其余工作区使用不透明单滚动移动布局。完整规格见 [MOBILE_PORTRAIT_REDESIGN.md](./MOBILE_PORTRAIT_REDESIGN.md)。该 Goal 的 UI 偏好不进入 `GameState` 或云存档。

## 2026-07-23 第七批与生产资料库 Goal（香港 0.6.0 已发布）

- [x] 殖民前哨把普通材料与全局随身运输机/运输船分源统计，全部验证后原子扣除。
- [x] 玩家主动放下手持载荷可完整越过单种物资上限，自动入库继续遵守上限。
- [x] 手机配方与物流槽物品选择器不再自动弹出软键盘，桌面自动聚焦保持不变。
- [x] 有限资源在节点、检查器和统计中共用储量快照并显示剩余、总量、比例与枯竭。
- [x] 小型储物仓在画布、检查器、手机抽屉及 80%-200% 字体下完整显示名称和输入输出。
- [x] 将配方图鉴扩展为七分区生产资料库，补齐建筑实际数据、传送带堆叠吞吐和物品/建筑/配方/科技互链。
- [x] 完成 `0.6.0` 公告、272 项单元测试、22 项服务测试、5 项运维测试、108 项 Playwright 和生产构建。

该 Goal 没有改变 GameState v30、存档 envelope v2 或云 schema v6。正式提交、干净发布清单、香港一致性备份、上传、原子切换与生产烟测均已完成；上海按本轮范围保持 `0.5.0`。

## 2026-07-22 公开测试版强化 Goal（已完成）

- [x] 冻结首个公网源码基线、版本标签、可复现构建清单、原子切换与一键回滚。
- [x] 建立隐私友好的运营统计和受保护管理后台。
- [x] 完成 schema v5 账号安全、设备会话、本地账号绑定和云存档冲突选择。
- [x] 完成认证加密异地备份、隔离恢复演练和节点探针的代码与自动化测试；生产密钥、timer 和真实演练报告留到发布窗口安装。
- [x] 完成手机端第三轮：双指缩放、触摸吸附、长按防误触、逐点多选、横竖屏状态保持和自动低性能模式。
- [x] 加入可迁移、可确定性模拟的行星资源丰度、能源环境、运输距离和工业加成，并在星图公开倍率与推荐分工。
- [x] 将渐进教学延伸至红糖、石油链、黄糖、跨星球物流、戴森云和白糖，并支持直接定位停机设备或零流量线路。
- [x] 建立全星球产能总览、缺料/堵塞追踪、设备跳转、目标产量反推和批量配方/物流槽操作。
- [x] 启用 gzip、补充加载与 API 性能采集，完成全量测试、版本发布、生产备份、双节点灰度部署和首轮发布后观察。

完成基线：`v0.2.0` 指向已部署提交 `e6e7daf113dc`；香港与上海运行 `0.2.0-e6e7daf113dc`，云服务 schema v5。九个阶段的发布证据、哈希、备份和恢复记录见 [releases/0.2.0.md](./releases/0.2.0.md)。Brotli 属于后续可选优化，不作为本 Goal 的阻塞条件。

## 2026-07-22 `0.3.0` 行星生态与自动建造 Goal（本地完成，待人工试玩）

- [x] 修复小屏、横竖屏、安全区和 200% 字体下版本公告关闭按钮不可见的阻断问题。
- [x] 建立 16 种行星生态模板、8 种恒星类型以及由持久化种子确定的 8 个恒星系和 22 颗行星。
- [x] 让普通/稀有矿、海洋、潮汐锁定、风光地热、轨道资源、矿储、工业专长、航程和殖民成本进入真实规则。
- [x] 让恒星亮度进入太阳能、独立戴森云/戴森球和本系射线接收收益。
- [x] 完成直达/优先中转/强制中转、枢纽优先级、多跳距离、每船翘曲预算、枢纽供电诊断和退款保护。
- [x] 完成建筑制造中心、物资配送枢纽、全行星供电、科研预接线、线路框选升级、行星阶段解锁与存档两次删除确认。
- [x] 加入科技解锁建筑各赠送 2 个、铁铜石矿各 100 的新档物资，以及可原子递归合成上游的施工小锤子。
- [x] 扩展星图、星际工业控制台、物流站检查器和戴森规划，并修复 200% 字体、移动载荷提示和公告关闭兼容性。
- [x] 将状态升级到 v26，补齐 v24 真实工厂迁移、v25→v26 不补发资源、蓝图/在途路由往返和非默认种子确定性测试。
- [x] 本地通过 TypeScript、241 项 Vitest、生产构建和 86 项 Playwright；覆盖桌面、手机竖屏、手机横屏及 200% 字体。

发布状态：该 Goal 已在 1.0.14 中完成双节点 Web/API 与上海下载页发布；后续仍需按发布门禁补做真实设备长时间温度、耗电和更多全量视觉回归，不将未完成门禁写成已完成。

## 阶段 A：冻结并标记首个线上基线

目标：任何人都能从一个 Git 提交重建当前线上版本。

- 整理当前脏工作树，按功能审阅后提交，不删除现有用户或代理留下的有效改动。
- 设定首个公开测试 SemVer，统一 `package.json`、桌面安装包、构建 ID 和 Git 标签。
- 保存前端/后端发布清单、依赖锁文件和部署时间。
- 将本文档与 `develop-dspidle` Skill 作为后续任务的强制入口。
- 建立简短发布说明和已知问题列表。

完成标准：在空目录 `git clone` 后可用锁文件通过测试并生成与发布清单一致的产物。

## 阶段 B：生产数据与安全加固

目标：即使错误发布、磁盘故障或账号攻击发生，也不会丢失玩家存档。

- `/api/metrics` 与 `/api/admin/metrics` 已受管理员 bearer token 保护，公开入口只保留隐私聚合的 `/api/public-status`。
- 双 VPS 密钥、每日异地备份 timer、每月隔离恢复 timer 和首份真实恢复报告已落地；后续按月复查并演练新主机恢复。
- 账号与云恢复安全审计、磁盘/TLS/端点探针已落地；剩余生产告警接收端与日志集中化。
- 邮箱验证、忘记/重置/修改密码、会话撤销、数据导出和账号删除已部署为 schema v5；剩余邮件供应商配置、异常登录告警和后台处置。
- 仓库已补充隐私政策、服务条款、数据导出/删除说明和安全报告规则；正式入口仍需提供可达链接，并完成运营主体与适用法律审查。
- 对反馈和错误上报加入垃圾内容限制与管理查看工具。

完成标准：有书面恢复记录；一次全新主机恢复演练可以还原账号和云存档，并验证修订历史。

## 阶段 C：Web 性能与可观测性

目标：大陆玩家稳定快速进入主菜单，问题发生时能够定位。

- JS/CSS 已配置 gzip 并在香港、上海验证 `Content-Encoding`；Brotli 留作真实收益可衡量时的可选优化。
- 主菜单已不再提前下载 React Flow、模拟器和完整存档迁移器；后续继续审计 300 KB 以上资源。
- 外部 HTTPS、TLS、API、磁盘和静态入口探针已通过 systemd timer 运行。
- 前端错误、服务端错误率、P95 延迟、429、云冲突和备份状态已进入受保护后台。
- service worker 已使用确定性 build ID 隔离缓存版本，避免新旧应用壳混用。
- 基于真实访问量评估优选流量包，而不是一次性购买大额流量。

完成标准：记录压缩前后传输量；连续一周可用性和错误率有趋势数据与告警。

## 阶段 D：账号、云存档与排行榜正式化

目标：把当前可用原型升级为可运营系统。

- 本地身份已可显式绑定、换绑或解除云账号，绑定元数据不进入 `GameState`；后续补充首次绑定引导。
- 云冲突界面已支持比较本地/云端时间、运行进度、设备和科技摘要，并明确选择保留方向；后续补跨设备推送提醒。
- 设计赛季结算、封榜、历史榜、昵称治理和管理员撤榜。
- 将关键成绩验证逐步迁移到服务端可重放摘要或权威事件，而非只检查客户端总量。
- 云存档正文已经迁移为独立修订行；用户规模继续增长后，再将紧凑 `app_state` 中的用户、会话、榜单和事件元数据按领域拆表。

完成标准：多设备冲突和历史恢复有端到端测试；排行榜提交可以追溯到明确云存档和验证版本。

## 阶段 E：稳定性与架构治理

目标：降低改一处坏多处的成本。

- 拆分 `App.tsx` 的模拟会话、画布命令、存档、工作区和输入控制。
- 拆分 `engine.ts` 的电力、物流、科研、戴森和实体命令，保留一个确定性推进入口。
- 将 `styles.css` 拆为有顺序约束的 shell、canvas、workspace 和 responsive 层。
- 按领域分拆 E2E，建立存档 fixture 和少量视觉回归。
- 给内容目录增加生成式 ID/引用检查，减少手工联合类型和目录不一致。

完成标准：核心大文件显著缩小；每个领域有明确所有权和针对性测试，完整回归时长可控。

## 阶段 F：体验与平衡第二轮

目标：让玩家理解瓶颈并愿意长期挂机，而不是只看到系统数量。

- 使用真实玩家存档运行 2/8/24/72 小时平衡审计。
- 调整手搓过渡、科技卡点、电力曲线、物流吞吐和离线回归奖励。
- 13 步渐进教学已覆盖红糖、跨星球、戴森云和白糖；后续只根据真实新手流失数据调整节奏与文案。
- 加强缺料警报到设备、线路和聚焦配方的直接导航。
- 手机第三轮已经建立双指缩放、触摸吸附和方向保持等交互基线；真实玩家反馈触发了新的竖屏信息架构重设计，按本文顶部 Goal 分阶段实施，不与玩法规则混改。

完成标准：新存档从开局到白糖有可重复的通关时间报告；关键卡点来自设计而不是 UI 不可发现。

## 阶段 G：原创化与正式公测

目标：形成可以独立推广的产品身份。

- 审查物品名称、配方、科技树、文案、图标、世界观和产品名称。
- 设计原创资源生态、行星特性和生产策略差异，但暂不引入黑雾战斗。
- 建立原创视觉资产与声音规范，替换仅用于高还原验证的内容。
- 在商业化或大规模推广前获取知识产权与商标方面的专业意见。
- 完成反馈入口、公开问题追踪、版本更新说明和社区运营规则。

完成标准：产品不依赖玩家对另一款游戏的名称记忆也能理解和传播，内容权属与发布材料清晰。

## 当前推荐的下一迭代

香港与上海 Web/API 当前均为 `1.0.15` / v42，并继续使用相互独立的 schema v7、SQLite layout v2 数据库；共同代码回滚点为 `1.0.14`，Windows 测试包为 1.0.15，Android 正式安装包暂为 1.0.14。首轮活动的三天假全服曲线已经冻结，但玩家参与和本地贡献长期开放；下一轮按以下顺序推进：

1. 观察 `1.0.15` 在真实工厂中的 Worker 延迟、主线程长帧、内存和保存停顿，并收集纯挂机、暂停和教程反馈；代码回滚不恢复数据库，也不撤销香港内部排行榜限制。
2. 根据 1.0.15 真实监控再进入 P3，优先拆分画布拓扑/遥测快照、缓存线路几何，并按视口更新节点与线路；暂停状态专项按 [暂停画布性能优化计划](./PAUSED_CANVAS_PERFORMANCE_OPTIMIZATION_PLAN_2026-07-31.md) 先处理输入隔离和静态快照，避免为低收益架构改造提前承担确定性风险。
3. 通过 opt-in 入口收集新版阶段 0-3 的真实试玩反馈，补齐 Android Chrome 与 iPhone Safari 各 30 分钟真机门禁。
4. 新版通过真机、低端手机和反馈试用后，再在后续独立 SemVer 中切换为手机默认；经典版至少保留一个版本周期。
5. 继续执行不删除当前版、回滚版和有效备份的磁盘保留策略；1.0.15 发布后上海约剩 4.3 GiB、香港约剩 21 GiB，仍需按阈值监控上海节点并保持香港备份异地归档。
6. 继续接入正式邮件供应商，把仓库隐私政策、服务条款和数据导出/注销说明接入正式入口，并完成公测反馈治理与专业合规审查。
7. 借移动壳层拆分 `App.tsx`、`styles.css` 和移动 E2E 的所有权边界；`engine.ts` 仍按独立确定性治理计划推进。
