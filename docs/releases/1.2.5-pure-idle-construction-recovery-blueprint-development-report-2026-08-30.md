# 1.2.5 纯挂机、建筑制造连续运行与蓝图界面修复开发报告

日期：2026-08-30  
状态：开发完成；最终候选 `1.2.5-0a1c6629ced1` 已封装，未发布

## 1. 范围与结果

本批在独立工作树 `D:\GameDev\DSPidle2-v125-pure-idle-construction` 中完成；最终 runtime 分支为 `codex/1.2.5-real-save-release-fix`，固定父级为线上 1.2.4 runtime `3154f8cf4479b874362a2cf01510706fee60d16e`。没有修改主工作树、线上流量、服务器或玩家原存档。

完成的玩家问题包括：

- 守恒纯挂机被历史戴森聚合差额误阻断；
- 两种纯挂机中的建筑制造不能持续工作；
- 模拟 Worker 异常被误报为内容包错误并让纯挂机退出；
- 自动保护路径安装旧检查点，造成玩家看到进度回退；
- 蓝图库详细卡片在高密度、大数量和大字体下错位、重叠。

最终收口在保留真实物料、供电、内容包兼容和并发写入规则的前提下，进一步拆除了过严的跨终局域连坐：已独立证明的白矩阵、科研和火箭不再因无关普通产线的尾段边界到期而整体归零；施工仍只使用真实库存和同电网供给证明。

## 2. 根因与修复

### 2.1 历史戴森差额被当成本段新造物

旧门禁要求逐恒星系合计与全局合计在每个候选边界绝对相等。部分合法旧档本来就带有历史差额，即使新结算让差额向零收敛，也会被当作新的守恒失败。

新门禁区分干净基线和历史债务：

- 起点差额为零时，终点仍必须为零；
- 起点已有差额时，只允许保持同一方向并缩小绝对值；
- 新增差额、扩大差额或越过零点均拒绝。

回归测试覆盖精确 580,144 的历史差额：收敛候选通过，额外制造 1 单位的候选拒绝。

### 2.2 建筑制造的供电证书过窄且失效过度

旧施工证书只把可再生余电作为持续额度；燃料电站或储能已经由主供电证书证明的有限运行时长没有正确传递给施工。同时科研完成会无条件让施工证书失效，即使中心需求和电网配置没有变化。

现在三个精确窗口继续保留制造中心的真实负载，并为每个中心记录最低实测供电比例。可再生供电可持续；燃料和储能只能使用主供电证书已计量的有限时长，耗尽后仅停止施工，不向其他产线或施工创造无限电力。证书重新绑定实际实体/线路数组、控制器、倍率、难度、中心需求、数量与优先级；科研完成本身不再构成失效条件。

产率复制挂机新增施工运行时证书：先让施工用真实库存递归制造，再验证施工领域守恒，最后才复制产品定义允许的终局统计。施工不会复制原料或中间件。目标已满足且没有施工任务时会直接返回，不遍历终局大实体图。

### 2.3 Worker 异常被误报并导致会话退出

模拟 Worker 原来把通用运行异常与内容包注册表异常混在一起，主线程只能按“内容包同步失败”停止。现在协议分别返回：

- `registryError`：真实内容包注册表缺失、指纹或目录校验失败，继续失败关闭并显示明确原因；
- `runtimeError`：可恢复的引擎运行异常，要求重新提供权威状态。

运行异常、超时、durable stage/finalize/checkpoint 失败或 Worker 临时不可用时，主线程先退还尚未完成的模拟秒和墙钟预算，终止故障 Worker，再从 durable recovery 或已确认 authority 重建；纯挂机在恢复成功后自动继续。

### 2.4 自动保护安装旧检查点造成视觉或真实回退

内存保护与多条 Worker 失败路径曾直接用最近检查点替换当前画面。该检查点可能早于已经显示的进度或玩家编辑，因此看起来像回档，后续保存还可能把旧状态固化。

所有后台安全路径现在只能暂停“当前可见状态 + 尚未提交的模拟债务”，不能安装历史检查点。历史状态只保留给玩家明确点击的恢复操作。durable recovery 重建期间画面保持不变，但新 Worker 只接收已验证的恢复 authority，以及已有协议明确记录的受支持 UI 编辑；整份画面快照不会被命令重放，避免累计量、时钟或引擎内部字段被重复应用。

### 2.5 蓝图详细卡片固定布局互相覆盖

旧详细视图的紧凑网格与内容高度组合，在卡片包含大量配方参数、外部端口、超大数量或字体放大时会让内容穿出卡片并覆盖下一行。

详细视图现在使用独立 class 和行流式布局，卡片按内容自然增高，列最小宽度为 420 px；长内容在卡片内换行或省略，选择框、施工需求和组合标签不再突破卡片边界。紧凑视图保持既有行为。

## 3. 主要代码

- `src/game/offlineApproximation.ts`：历史聚合差额、施工供电证书和运行时施工证书；
- `src/game/pureIdleMacro.ts`：产率复制施工顺序和运行时证书接入；
- `src/game/simulation.worker.ts`：运行异常与内容包异常分型；
- `src/App.tsx`：无自动回退、预算退还、Worker 自动恢复和纯挂机续跑；
- `src/components/BlueprintWorkspace.tsx`、`src/styles.css`：蓝图视图模式边界和详细卡片布局；
- 对应 Vitest 与 Chromium E2E：守恒、有限供电、真实库存施工、Worker 错误分型、恢复和密集蓝图库。

## 4. 验证结果

- `npm run typecheck`：通过；
- 完整 Vitest：202 个文件通过、14 个跳过；1,711 项通过、29 项跳过；0 失败；
- 完整 Chromium：434 通过、27 条件跳过、0 失败；durable 7/7；最终 production preview 3/3；
- server 385/2 + station 4/4、Ops 56/6、release-switch 29/29、native JavaScript 182/1、Rust 222/222，均 0 失败；
- `npm run build`：Vite 1,985 modules；startup 总 gzip 180,156 B，JS 86,825 B，CSS 93,331 B，最大 JS 58,974 B，menu 256,162 B，forbidden modules 0；
- 蓝图几何测试覆盖 16 张卡片、每卡 10 个配方参数和 12 个端口，并在普通字体与 150% 字体下断言卡片互不重叠、子元素不越界；
- 真实档 Chromium 专项通过两次纯挂机启动、44.5 MB 停止保存/读回，以及 Worker 故障恢复后不退出挂机页。

## 5. 残余边界

- Android 实体设备、低配/主流/高配 Windows、Windows 1.2.4→1.2.5 覆盖升级和长时运行仍未执行；
- GameState v47、envelope v2、cloud schema v8 和 SQLite layout v3 均未变化；
- 已完成签名、打包和不可变候选，但开发阶段没有连接生产或发布线上版本；
- “放宽边界”不等于允许负库存、伪造供电、覆盖其他写入者或跳过保存读回。

## 6. 后续候选封装说明

上述“未修改 package 版本”描述的是功能开发提交当时的边界。随后同日开始独立的发布前封装：产品版本升级为 `1.2.5`，Android 升为 `1.2.5 / 1002005`，新增双语当前公告并把 1.2.4 完整移入历史记录；GameState、存档 envelope、云 schema 和 SQLite layout 仍未变化。最终 clean runtime SHA、完整门禁、制品哈希和未完成硬件边界以单独的 1.2.5 Release Agent 交接为准，不复用本报告的开发态计数冒充候选门禁。

## 7. 最终发布前封装结果

最终 runtime 固定为 `0a1c6629ced1c99ba5ec55c4e4947de7300da4bd`，Release ID 为 `1.2.5-0a1c6629ced1`。旧 `1.2.5-a347f1889044` 候选已作废并冻结，不得混入当前候选。

44,566,548 B、46,168 实体、93,200 线路的只读真实终局档在 15×、10 分钟测试中增加白矩阵 300,540,099,810、火箭 2,239,930,721，并完成施工 622,656；校准 64,144 ms、宏观尾段 2,740 ms、序列化/重载 3,485 ms，结束 heap 约 778 MiB，原档大小、mtime 和 SHA-256 均未变化。

受保护加载器生成并验证 Android `1.2.5 / 1002005` APK/AAB，APK v2/v3、zipalign 与长期证书连续性通过。Windows setup/unpacked 的正式 HTTPS 元数据、版本、Build ID 和隔离 12 秒启动通过，Authenticode 明确为 `NotSigned`。12 文件 candidate aggregate SHA-256 为 `9181a35fa9587bb900915a3fb5d40cc44baa80c3971d3c3c7cf000595b927049`；组件 manifest、六归档读回、provenance 3/3 和 SHA256SUMS 20/20 均已复验。

本 Release ID 仍缺 Android 实体设备、三档 Windows、1.2.4→1.2.5 Windows 覆盖升级和长时运行；未通过或未获针对 `1.2.5-0a1c6629ced1` 的明确风险豁免前仍为生产 No-Go。完整候选与发布边界见 [1.2.5 候选记录](./1.2.5-candidate.md) 和 [Release Agent 交接](../RELEASE_HANDOFF_1.2.5.md)。
