# 香港 1.1.8 → 1.1.6 回滚记录（2026-08-25）

- 操作范围：仅香港 Web/API；上海、下载页、Windows、Android 未修改。
- 用户请求：将香港当前回滚到 1.1.6。
- 回滚前：web-1.1.8-0826fa0819b0 / api-1.1.8-0826fa0819b0，generation 37。
- 回滚后：web-1.1.6-4f6d24f8c709 / api-1.1.6-4f6d24f8c709，generation 38，slot green / port 4322。
- 直接回滚目标：web-1.1.8-0826fa0819b0 / api-1.1.8-0826fa0819b0，保留在 previous。

## 备份与 preflight

- 复用的已验证 SQLite Backup API evidence：1.1.8 evidence，3,772,833,792 bytes，SHA-256 7bac4de5f762b9a7d66d38a9c8323bae00ab9157d84bfa71c4f3d74c6241630a，quick_check=ok，schema 8，layout 3。
- 第一次尝试在 preflight 阶段安全拒绝：目标所需的独立 preflight 副本不存在；指针和服务保持原状。
- 为释放 staging 空间，仅删除了已证明未被引用、未被打开且有对应源备份保留的旧 1.1.7 disposable preflight 副本；没有删除数据库、WAL/SHM、有效备份或发布目录。
- 按有界 32 MiB/s 生成了与上述 evidence 字节数和 SHA-256 一致的 1.1.6 独立 preflight，并在回滚器采用后清理其数据库副本。preflight 过程中磁盘保护线未触发。

## 切换与验收

- dry-run 通过；正式原子切换返回成功，pending switch 为空。
- API、handoff proxy、Nginx、healthcheck、node-health、offsite-backup 均 active；API 和 Nginx NRestarts=0。
- 本机 active API（4322）与 proxy（4330）的 /api/health、/api/ready 均 HTTP 200。
- 公网首页、version.json、sw.js、manifest.webmanifest、/api/health、/api/ready 均 HTTP 200；公网版本为 1.1.6+4f6d24f8c709。
- 回滚后磁盘约 74%（findmnt 读数）；数据库未恢复、替换或改写。

## 回滚边界

再次回滚到 1.1.8 只允许沿用受保护的 release-switch 流程、重新验证当前 evidence、preflight、健康和指针；不得恢复数据库。本文不记录任何凭据、玩家数据或服务器连接细节。
