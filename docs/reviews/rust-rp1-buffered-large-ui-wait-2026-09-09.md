# Rust RP1 大工厂完整入口等待｜2026-09-09

Role: develop。读取缓冲已通过公开文件和运行态请求对照，本批验证真正的 Windows 游戏入口。所有进程后台运行，未发布，未改玩家数据。

## 制品和测量边界

- 基线包：`33d96597d0ba05930bf6e663bb65b6aeaaa4d587`，冻结目录 `artifacts/rust-rp1-loop/package-33d96597-frozen`，Host `a901961117d1e9aa0495403ac61663765fabfa0dac81bf71f54dfa17ef702489`。
- 候选包：干净提交 `67a2ffbecc2ef52eb26ab3abc508696a9ccb3d22`，beta `1.2.7+67a2ffbecc2e`，冻结目录 `artifacts/rust-rp1-loop/package-67a2ffbe-frozen`。Host `1f19d28a92702096492293b8cde667252dc44859790b4a3055230122851ef80a` 来自已记录的正常 release `6bf9f983` 构建，67a2 与其运行源码相同，仅文档不同。
- 两包 `src/`、`desktop/` 源码完全相同；原生实现差异为读取缓冲，另有覆盖清单变化。旧 Host 为历史本机构建，未重新编译基线。
- 候选通过正式平台构建脚本与 pack 验证，75 件清单制品通过，78 件冻结文件逐件读回相同，`NotSigned`。构建约 65.38 秒正常退出，最低空闲 5,950,780 KiB。未重复编译已校验的 Host。回执为 `package-67a2ffbe-receipt.json` 和 `package-67a2ffbe-build-v1/`。

驱动 `artifacts/rust-rp1-loop/probe-packaged-background-entry-v5.mjs` SHA-256 `03ce915de8399be5c133a5ff043b198e29639e7f4da03e8cac3834c743b73401`。启动前验证包身份、完整清单和 hidden-no-focus-offscreen-v2，旧可见入口不会被启动。外部监控 6 GiB 启动余量、2 GiB 停止线、600 秒期限；Node、Electron、Host 低优先级，Host 两线程，顺序运行，无 GPU 渲染方式切换。

公开固定来源文件 SHA `7767eff12ef110a4dd7bccc9d385c9a7551d49f1561489055d2fa2d73a266d1a`，9,107 实体 / 20,000 带。每次用同一游戏状态，自动保存偏好固定 30 秒，只有外层 savedAt 刷新，确保测试实际经过 5 秒离线；不替换 main、renderer 或 Host 的时钟。六例完整来源 canonical 均为 `63e5393922ec2f511391489d6a510762ee296b74c96b3338fe5e10ebbed907bd`，候选完整 canonical 均为 `f97a419552fdca84977395353b5a90db3090a06b7e421ff2a468d407fe8d2127`，实际结算全部是 5 秒 Exact、0 秒近似。

## 实际完整等待三对

顺序为候选/基线、基线/候选、候选/基线，每例独立进程和独立临时资料目录。计时从点击“继续游戏”之前，到实际结算确认后打开运营中心；包含自动点击确认的操作时间，不包含真人思考时间。源档无需预先存在原生检查点，真实 UI 采用候选并写入持久主档。

| 配对 | 旧包完整等待 ms | 新包完整等待 ms | 旧 Host 请求 ms | 新 Host 请求 ms |
| --- | --- | --- | --- | --- |
| 1 | 21330.376 | 5990.654 | 17307.348 | 1996.858 |
| 2 | 21003.003 | 5853.602 | 17298.613 | 2081.920 |
| 3 | 20756.781 | 5709.969 | 17092.962 | 2011.892 |
| 中位 | **21003.003** | **5853.602** | **17298.613** | **2011.892** |

完整等待中位缩短 **72.13%**，Host 请求中位缩短 88.37%。完整等待是本公开大工厂、短离线和同一离屏模式下的实测收益，不能推广为真实终局、几小时离线、前台图形性能、Web 或 Android 收益。辅助 launchToInteractive 计数包含夹具等待到真实离线满 5 秒的时间，不作为纯冷启动性能结论。

六例均验证：真实来源完整状态与 JS 对照相同；来源临时数据清理后才采用；完整持久投影与候选相同；通过真实界面暂停并保存；正常关闭后的原始磁盘状态不变；点击继续后的状态与正常 JS 加载器相同；第二次重开一致。不是只比较资源总数。

## 取消和自然保存来源

候选包额外两例通过：

- **取消**：真实 Host 准备好 5 秒候选后，仅在测试传输返回点加入 3 秒延迟，以稳定命中取消窗口。点击“取消计算并返回”后原主档字节、savedAt 和原生检查点不变，两次重开有效。该人为延迟例不计入性能三对。
- **自然来源**：先通过实际游戏运行、保存按钮和正常关闭产生主档，确认没有原生检查点，再实际重开、继续和采用 5 秒候选。完整 JS 状态、持久提交及两次重开通过，仍为 9,107 实体 / 20,000 带；来源是游戏刚生成的状态，与固定配对样本不同，不混入性能中位。

八例共 **32 次启动及正常退出 0**。每次启动/关闭均验证不可见、不可聚焦、静音、实际离屏绘制、无原生对话框；菜单三帧仍满足 500 ms 原门槛。外部监控最低空闲 4,397,544 KiB，未触发停止或强制清理，未操作用户其他进程。完成这些实际 UI 流程后，再次核对候选的 75 件清单制品和 78 件冻结文件，全部相同。

逐例报告为 `artifacts/rust-rp1-loop/large-background-{baseline,candidate}-complete-p{1,2,3}-v1/report.json`，另有 `large-background-candidate-{cancel,natural}-p1-v1/report.json`，每例有对应 `-guard/guard.json`。汇总 `large-background-v1-summary.json` 逐件重新核对完整来源/候选摘要、结算秒数、持久/重开、32 次正常隐藏关闭和内存监控，结果 PASS。

## 仍未完成的资格

授权原始终局档此前被 `offline-macro-time-warp-active` 拒绝，未生成候选；原 300 秒超时与新拒绝均保留，不能由公开大工厂通过覆盖。历史堆异常根因仍未确定，其归档程序、转储和独立复验保持独立证据。

云端第三轮 `34280286855` 已确认正常优化 Rust、Host 构建、Native 全量及候选制品上传成功；完整游戏单元仍在执行。候选 artifact `10078383367` 明确只证明 Rust/Native 检查通过，不代表最终全绿。此前两项 JS 计时失败尚未解决；本机仅选中两例的诊断为 2 通过 / 31 跳过，首轮 CPU flags 未产出工作进程 profile。第二轮原两个完整文件 33/33 通过，但仅生成调度进程 profile，没有游戏调用栈；不能把本轮 CPU 采样记为成功，也不能把本机一次通过覆盖原云端失败。证据为 `game-timing-profile-v{1,2}/` 及各自 `-guard/`，原测试文件与时间断言均未修改；后续需要正确采集执行测试的工作进程。

当前只授予此包公开普通工厂 5 秒入口的开发验证结论。超过 30 秒自动采用、实时权威和跨端 Rust 保持关闭，无生产部署。整体目标继续推进，见[易读报告](../RUST_BATCH_REPORT_2026-09-09_UI_WAIT.md)。
