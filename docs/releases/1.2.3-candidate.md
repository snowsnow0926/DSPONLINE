# 1.2.3 候选说明

> 状态：最终开发候选，等待 clean 运行时提交与不可变 manifest 冻结；尚未签名、上传或部署。

## 候选内容

1. 建筑制造、普通离线结算和时间扭曲继承逐恒星系事件账本、闭合稳态供需证书和 Windows 原生增量热路径，JavaScript 继续是玩家可见权威。
2. 新增与守恒模式并列的玩家可选“产率复制挂机”。五项无限科技有效等级总和大于 200 后，可直接锁定最近 60 个模拟秒（不足时回退 30 秒）的正向材料、科研、逐恒星系火箭和壳面帆统计，并按实际供电倍率复制；该模式按产品定义不扣原料或执行守恒门。
3. 移除产率复制 Worker 在检查点之后重复扫描全工厂的路径，终局大档直接复用检查点锁定供电倍率；专项测试禁止会话访问实体/线路 getter。
4. Windows 正式包恢复稳定版 appId、产品名、历史数据目录和 stable 更新通道；性能开发版继续使用隔离身份，不能污染稳定版存档。
5. 修复手机新版界面浅色主题下“完整检查器”正文被白色移动桥接层覆盖；覆盖层现在透明且不接管指针，深色主题与检查器拖拽行为不变。
6. 当前/历史中英文发布说明更新为 1.2.3 的真实交付内容。

## 兼容与数据保护

- GameState v47、envelope v2、cloud schema v8、SQLite layout v3 不变。
- `productionHistory` 的复制遥测只存在运行态，持久投影仍删除整段历史，不增加主存档体积。
- Rust 生产历史遥测与 JavaScript 差分精确一致；原生提交失败不取得权威资格，`authorityEligible=false`。
- 原守恒纯挂机、恢复 journal、停止时原子保存/读回和后台五分钟边界仍保留。
- Windows 正式版继续沿用历史数据身份；实验性能版仍隔离，禁止自动搬运稳定版资料。
- 不修改、重算、回滚或上传玩家存档。

## 最终开发门禁

- TypeScript typecheck、Rust fmt、Rust clippy `-D warnings`：通过。
- Vitest：202 文件通过 / 14 条件跳过；1,704 项通过 / 29 跳过 / 0 失败。
- Server：384/2；station：4/4；Ops：56/6；release-switch：29/29。
- Native JavaScript/桌面边界：180/1/0；Rust workspace：222/222。
- Chromium：432/27/0；durable E2E：7/7；production preview：3/3。
- 生产构建：1,984 modules；startup gzip 180,069 B；forbidden startup modules 0。
- 许可证：125 个运行时包；根项目与 server 生产依赖审计：0 漏洞。
- 手机浅色 390×844 完整检查器专项和稳定身份 Windows unpacked 隔离 smoke：通过。

## Release Agent 仍须完成

- 在 clean runtime commit 上重跑制品身份门禁并生成 source/Web/API/Windows/Android 不可变清单、SHA256SUMS、SBOM、gate report 和 provenance。
- Android 必须从批准受保护来源签名，并验证 v2/v3、zipalign、包名、versionCode 和历史证书连续性；不得创建新证书。
- Windows stable 继续按既有策略明确标注 `NotSigned`，核对正式 API/更新地址、stable 身份和覆盖升级边界。
- Android 实体设备以及低配/主流/高配 Windows、覆盖升级和长时门禁若未实测，必须取得只针对最终 Release ID 的明确风险豁免。
- 香港、上海和下载页必须先完成受保护连接、磁盘/指针/健康预检以及各 API 节点新鲜 Backup API evidence，再使用不可变目录和原子切换；不得部分发布。
- 切换后必须独立验收 health/ready、PWA/service worker/cache、完整下载哈希、Range 206、Windows/Android feed 与签名，并记录 previous-stable 和回滚指针。

候选的精确 runtime SHA、Release ID、Build ID、逐文件哈希和 aggregate SHA-256 只以冻结后的候选 manifest 为准；不得复用任何旧 1.2.3 Beta 或性能开发包。
