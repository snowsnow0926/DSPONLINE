# DSP极简网络 1.1.4 完整稳定发布记录

> 发布日期：2026-08-24
> 运行时提交：`7dbc149a016c7f53e5d3648e0fdcb3679309ef73`
> Release ID：`1.1.4-7dbc149a016c`
> Build ID：`1.1.4+7dbc149a016c`
> previous-stable：`1.1.3`

## 发布结论

1.1.4 已完成香港 Web/API、上海 Web/API、上海下载页、Windows stable 和 Android stable 全量发布。香港和上海均以不可变新目录安装并通过受控原子切换；1.1.3 保留为两个节点的直接 Web/API 回滚目标，香港公开 previous-stable 入口也已固定到 `1.1.3-f35d60a22223`。本次没有恢复、覆盖或迁移生产数据库，没有修改玩家存档或排行榜数据。

| 目标 | 最终状态 |
| --- | --- |
| 香港 Web/API | `web-1.1.4-7dbc149a016c` / `api-1.1.4-7dbc149a016c` |
| 上海 Web/API | `web-1.1.4-7dbc149a016c` / `api-1.1.4-7dbc149a016c` |
| 上海下载页 | `download-site-1.1.4-7dbc149a016c` |
| Android stable | `dsp-idle-1.1.4-1001004.apk`，长期证书签名 |
| Windows stable | `dsp-idle-1.1.4-x64-setup.exe`，`NotSigned` |
| 香港备份入口 | `/canary/previous/` → `/canary/1.1.3-f35d60a22223/` |

## 冻结候选与开发门禁

- clean runtime SHA、Release ID、Build ID 与交接一致；发布过程中未重建或覆盖冻结 Web/API。
- candidate manifest 为 8/8，aggregate SHA-256 `57b8a08fe0d0e8e2bb67cffa56fc1edf8205b8f964532766508922541bbf62ee`；provenance 3/3、SHA256SUMS 10/10。
- Web 归档 SHA-256 `e5483fad770933085ff0f2868e882613ecb6b9cc9571cfbc67e413a2f16ea8da`；API 归档 SHA-256 `f084552be981fa3f54e0e3ea10ebae93ab0b6b534fbcb09fa51f1745bf19804c`。
- 完整开发门禁：Vitest 1,446/7 skipped、server 376/2、station 3/3、native 25/25、ops 56/6、Chromium 427/26、release-switch 29/29 连续五轮、source 260/260、candidate 8/8、provenance 3/3，均为 0 失败。
- 67,254,143 字节真实终局档可稀疏化到 62,264,191 字节并通过完整性、重载和保存门禁；云容量合同为 64 MiB 保证、`96 MiB - 1024 B` 单修订硬上限、80 MiB gzip、96 MiB expanded、128 MiB concurrent、208 MiB response、180 秒 timeout、Nginx `112m`。

## 正式 Android 与 Windows

- Android `cn.dsponline.network`：versionName `1.1.4`、versionCode `1001004`。
- APK：5,202,457 B，SHA-256 `6ff4e802a5a5dcd99ccd691a01a6ff8b73ee7dabaa5abd9dd1cadf15e47d978e`；APK Signature Scheme v2/v3、zipalign、包名/版本和批准历史证书连续性均通过。
- AAB：4,991,376 B，SHA-256 `c824881f39281fee564c0a86340690d53a30506d9fd8f2aaf250a27f1c77b13d`；与 APK 证书连续性一致，保留在不可变原生归档中。
- Windows setup：109,784,731 B，SHA-256 `fc5b4d9bcb7c3b2fdba551c560bbf05a89474e80e1264ccc65af5068da072852`；blockmap SHA-256 `1859a98eda5506c44d672d22d03e7358474db45f5d63c6540160225a6a1c3761`。
- Windows 继续采用既有公开策略 `NotSigned`，没有创建新证书或发布自签包。

## 备份与数据门禁

两个节点均在 API/data-affecting mutation 前创建并独立验证新的 SQLite Backup API snapshot/evidence：

| 节点 | 快照字节 | SHA-256 | 检查 |
| --- | ---: | --- | --- |
| 香港 | 3,772,833,792 | `26260ef4e7d8a916b07de4e999a47eab5670485267cf2de7f0a47e08a9fe723b` | quick_check=ok、integrity=ok、schema 8、layout 3 |
| 上海 | 462,848 | `155aad2bfc5dbebd34a7c804a3c8b578d45a5ff63e3101d80d6302f415e482d1` | quick_check=ok、integrity=ok、schema 8、layout 3 |

- backup evidence 与 preflight evidence 的字节和 SHA-256 分别一致。
- 未删除 live DB、WAL/SHM、有效备份、current/previous/rollback/canary 目录或 release-control 状态。
- 为满足香港 90% 磁盘保护线，发布前仅归档了经完整分片哈希、远端逐片读回和整文件串接验证的旧历史快照；至少保留六份本地大快照，超过既定四份保留要求。

## 香港切换结果

- release-control current：`1.1.4-7dbc149a016c`；Web/API current 均为 1.1.4。
- generation **34**，slot **green / 4322**；proxy generation **123**，mode `forward`。
- previous：Web `web-1.1.3-f35d60a22223`，API `api-1.1.3-e59885bee29b`。
- `pending-switch.json` 不存在；API 与 handoff proxy 均 active，`NRestarts=0`；healthcheck、node-health 和 offsite-backup timer 均 active。
- 本地 `/api/health`、`/api/ready` 与公网健康探针均为 200；三轮观察中 generation、proxy generation、版本和指针未变化。
- 根盘最终约 78%。发布后一度因缓存活动升至约 81%，5 秒写入归因复核后仅剩 12 KiB 增量并回落；没有备份进程或服务重启遗留。

第一次常规切换因长期请求未排空而停止，并留下 proxy hold；先恢复 forward 且未重启 API，再按低流量维护窗口临时阻断非 health/ready 请求后完成切换。Nginx、代理和健康 timer 全部恢复，未留下维护态。

## 上海与下载页

- release-control current：`1.1.4-7dbc149a016c`；generation **20**，slot **green / 4322**；proxy generation **56**、mode `forward`。
- previous：Web `web-1.1.3-f35d60a22223`，API `api-1.1.3-e59885bee29b`。
- API/proxy active、`NRestarts=0`，healthcheck 与 node-health timer active；offsite timer 继续按节点既有策略保持 inactive。
- 本地 health/ready、版本文件、Web/service worker 哈希和三轮观察通过；根盘最终约 86%。
- 下载页 current 为 `download-site-1.1.4-7dbc149a016c`，previous-download 为 `download-site-1.1.3-e59885bee29b`；9 文件 manifest aggregate SHA-256 `bb4860d1f9195304c526462ead1720d724a53fd6a6893ed2810f97a94bb18eb0`。

上海 Web 节点既有的服务器名/TLS 基线仍不能通过严格证书验证，本次没有使用 `-k` 或降低验证。该节点的本机版本、哈希、health/ready、进程、指针和切换证据通过；公开下载域名的严格 TLS 验收独立通过。该 TLS 基线需作为单独运维问题修复，不能把本记录解释为上海 Web 证书已修好。

## 公网、PWA 与下载验收

- 香港公网 `/version.json` 为 `1.1.4+7dbc149a016c`，Web/API 健康检查通过。
- `version.json` SHA-256 `daf88a457072ad951eb61889d48f1052fda5e35f25c7c694f14a01ff71687af3`；`sw.js` SHA-256 `eecda7cfdfb3daacd71caa437e20eea5da2545d7c59ab74d241126ca9cdee301`；入口 JS/CSS 与冻结 Web 逐字一致。
- 真实浏览器加载、重载 1.1.4 主站均显示正确版本且控制台 error 为 0；随后打开 previous-stable，页面落到不可变 1.1.3 URL、显示 v1.1.3 且控制台 error 为 0。
- 下载页、Android stable feed、Windows stable feed、APK、setup 和 blockmap 均通过完整文件哈希；APK 与 setup 的 Range 请求均返回 206，更新清单保持 no-cache/no-store，版本制品保持不可变缓存策略。
- 最终公开复验再次完整下载 APK 与 setup，SHA-256 分别仍为 `6ff4e802…978e` 和 `fc5b4d9b…2852`。

## previous-stable 1.1.3

- `/canary/previous/` 返回 302 到 `https://dsponline.cn/canary/1.1.3-f35d60a22223/`。
- 1.1.3 HTML 983 B，SHA-256 `0aa960eedd376b06655a2de9eb34ea67ff484a7e58a0d2317788db6d773cfc5d`；`version.json` SHA-256 `f7676cb86076dbb29ec9a6f3619015748fda989752762fa5102000dfbfed986a`；canary `sw.js` SHA-256 `ff0ec9d4458b10e0dad17ff453a46dfba72fd7cdb9c9b990c05d308d0faa5060`；6 个入口资源逐字验证通过。
- 响应包含 `Cache-Control: no-store, no-cache, must-revalidate`、`Vary: *` 与 `X-Robots-Tag: noindex, nofollow, noarchive`。
- 根 `/sw.js?v=1.1.3+f35d60a22223` 返回 404，防止 previous build 注册根作用域 worker；当前 1.1.4 worker 保持 200。
- Nginx snippet 从 SHA-256 `00488362a1942149351bd8eb09650053531bec26ddd778de3be4b60bd4819f4d` 更新为 `3f86c5c49326c31f420a6bc3dab2a3ac83beaa0af14653ea904125d3e325eb3f`；原配置已保留为可识别的 pre-previous 备份。候选结构校验、`nginx -t`、原子安装、reload 和回滚保护均通过。

## 发布后清理

- 只清理两个节点精确命名的 `1.1.4` 临时上传 staging；删除前逐文件复核大小/SHA、current 指针、正式目录与 health/ready。
- 香港释放 2,471,283 B；上海释放 116,592,645 B。冻结本机制品和远端不可变 release 目录仍完整保留。
- 没有执行广泛递归清理、备份裁剪、数据库/WAL/SHM 删除或 current/previous/rollback 删除。

## 回滚边界

- 香港仅在 current 仍为 generation 34、1.1.4、pending 为空且新 evidence 仍有效时执行 release-control 的 last rollback；目标为 Web 1.1.3 / API 1.1.3。数据库不回滚。
- 上海仅在 current 仍为 generation 20、1.1.4、pending 为空时执行同一受控回滚；目标为 Web 1.1.3 / API 1.1.3。数据库不回滚。
- 下载页回切到 `download-site-1.1.3-e59885bee29b`；Android/Windows stable feed 回切时必须同时恢复对应 1.1.3 feed 与制品引用，不能只改单个 JSON。
- 香港 Nginx previous-stable 路由可用本次 pre-previous 配置备份恢复；恢复后必须重新执行 `nginx -t`、reload、current worker 与 health/ready 验收。

开发证据见 [Release Agent 交接](../RELEASE_HANDOFF_1.1.4.md)、[开发报告](../DEVELOPMENT_REPORT_1.1.4.md) 与 [候选记录](./1.1.4-candidate.md)。
