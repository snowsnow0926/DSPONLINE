# Rust RP1 持续开发记录

> **电力扫描候选本机验证完成（2026-09-09）**：移除完整电源索引缺键和行星燃料汇总中的无用全实体扫描。旧代码两项反例失败，定向 243/243、类型/Web 构建和完整本机单元 3,172/39 skip/0 fail；公开 27,107 实体、60,000 带工厂六次完整状态相同，子步骤观测不冒充新 UI 收益。所有验证无窗口、低优先级、串行，玩家应用未操作。新源码云端和原终局完成入口继续待验，目标保持进行中，详见[本轮证据](./reviews/rust-rp1-power-lookup-2026-09-09.md)。

> **新包终局验证有实质进展（2026-09-09）**：从干净 37630d05 重新编译并冻结包含 7fd 保存优化的 Windows beta，75/78 件制品通过。完整终局 9 秒 Rust/JS 相同，取消保档、取消后两次重开通过；完成入口仍超出原 90 秒。修正诊断收尾空日志覆盖，补验保留完整候选身份；10 次启动/退出均隐藏、静音、正常退出，原档不变。新云端构建、Linux 单元/Server/Ops/原生通过，完整浏览器 403/33 skip/29 unexpected/23 flaky，Windows Rust/Native 通过，游戏单元 3,169/39 skip/1 项矿脉长挂机超时。详见[本轮证据](./reviews/rust-rp1-private-cancel-2026-09-09.md)与[易读报告](./RUST_BATCH_REPORT_2026-09-09_CURRENT.md)。目标继续进行，仍需终局持久采用/成功重进及完整回归。

> **终局保存开销继续收敛**：真实隐藏菜单已两次返回 9 秒 Native 候选，但原 90 秒工厂就绪仍失败；随后无窗口保存专项定位到约 13.8 秒保存成本。复用已验证字节数后，主线程六次大型重复编码降为零，避免累计约 646 MB 临时缓冲区分配，完整主档/备份/快照一致。容量专项等四文件 130/130、类型检查通过，尚非新 Windows 完整入口验收。0ce 旧源码云端 Windows 全量成功、浏览器 401/33 跳过/34 失败/20 flaky，终态已收齐。见[保存阶段记录](./reviews/rust-rp1-save-byte-length-2026-09-09.md)、[终局菜单失败](./reviews/rust-rp1-endgame-entry-wait-2026-09-09.md)与[云端终态](./reviews/rust-rp1-recovery-test-startup-2026-09-09.md)。

> **终局来源前提已纠正**：菜单本来就在 Native 前核对日志并恢复孤立时间加速事务。授权终局档两项 pending 均为零，实际恢复后的一秒真实 Broker / Host 候选与 JS 完整状态一致，原文件和来源不变，正常退出及清理通过。新增 30 / 31 秒合计预算回归，三文件 21/21、类型检查通过。此为正确性诊断，不是实际十天离线或完整 UI 等待通过；下一步转到真实隐藏菜单的采用、取消、持久及重开，不新增重复恢复规则。见[本轮证据](./reviews/rust-rp1-recovered-endgame-source-2026-09-09.md)，目标继续进行。

> **恢复测试前提补齐**：云端实际失败截图/轨迹留存已生效，c6 的 15 份挂机失败上下文均未越过启动阶段。现明确等运行时 ready，再执行原停止/保存/重开断言；原操作时限和数据断言不变，本机真实 Chrome 13/13、项目类型检查通过。c6 Linux Ops 已 60/2 平台跳过通过，完整浏览器仍非全绿，新修复云端待验。见[证据及范围](./reviews/rust-rp1-recovery-test-startup-2026-09-09.md)；目标继续进行，不扩大 Rust 采用资格。

> **线程参与前提已完成本机验证**：只读反例证明选择两线程不保证短任务当次实际占用两线程；专项现以有界同步保证实际第二映射线程进入，原参与数量及完整实体/authority 比较保持。正常优化核心 1,113/5 ignored/0 失败、严格 Clippy 通过，新源码云端待验。c6 浏览器第二组新附件显示 15 份挂机失败上下文均尚在启动阶段，下一步按此定位，不盲目放宽停止断言。见[本轮记录](./reviews/rust-rp1-worker-participation-2026-09-09.md)。目标继续进行，后台约束不变。

> **当前循环收尾（2026-09-09）**：新 `2bb10b43` 包八个后台入口场景通过，实际等待三对中位 20.789→5.959 秒，缩短 71.3%；完整结果/持久/重开及 32 次隐藏静音关闭通过。完整单元与 push Windows 全量通过；PR Windows 核心有线程参与断言失败，完整浏览器本机 27 失败、PR 33 失败并有 19 flaky。main 历史失败只能解释部分同名场景，不能一概排除回归。Ops 状态同步修复本机专项和完整检查通过，云端待验；补充 CI 失败附件留存。原始终局 timeWarp 与历史堆根因仍未收口，**目标保持 active**。见[本批小白版报告](./RUST_BATCH_REPORT_2026-09-09_CURRENT.md)及[完整身份/结果/目标审计](./reviews/rust-rp1-current-batch-validation-2026-09-09.md)。后续仍按后台低负载方式执行。

> 大工厂实际入口已完成本批验证：`67a2ffbe` 六例三对完整等待中位 21.003→5.854 秒，完整状态/持久/两次重开一致；加取消与自然来源，共 32 次隐藏静音正常关闭。云端第三轮已通过 Rust/Native 并上传带范围标注的候选 Host，全量 JS 正在执行。原始终局 timeWarp 准入和历史堆异常仍未收口；目标保持进行中。见[本批报告](./RUST_BATCH_REPORT_2026-09-09_UI_WAIT.md)与[证据](./reviews/rust-rp1-buffered-large-ui-wait-2026-09-09.md)。下方为此前时点状态。

> 最新后台结果：本机正常优化 `6bf9f983` Host 已取得公开文件导入/运行态候选的三对收益，完整状态一致，新本地接口/后台专项 49/49。原始终局来源明确被 timeWarp 准入拒绝，未生成候选，原文件不变。云端 `34274908889` 已结束，Rust/Native/类型通过，但完整单元 3,157/39 跳过/2 失败；独立 JS 诊断 31/0 跳过/2 失败。新 run `34280286855` 仍在 Rust 验证，不作为全绿。私人诊断继续要求 6 GiB 启动余量。见[本批易读报告](./RUST_BATCH_REPORT_2026-09-09_BUFFERED.md)、[读取缓冲进展](./reviews/rust-rp1-v47-read-buffer-2026-09-09.md)及[监控证据](./reviews/rust-rp1-private-source-memory-guard-2026-09-09.md)。

> 旧堆异常补充只读审计：原登记 5 份转储现存 4 份并逐件 SHA 校验，其中 9 月 2 日三份访问/非法指令异常属于另一 PDB GUID，不能用本批归档 PDB 展开或合并成同一回归；9 月 8 日 `b208e79c…` 堆异常仍与归档构建相符。其模块清单仅见该测试 EXE 及 Windows/VC 运行库名称，没有支持第三方注入的模块证据；这不证明破坏来源或排除硬件。四份现存转储另复制到忽略目录并校验，原文件未改；一份旧转储缺失。证据为开发 worktree 的 `artifacts/rust-rp1-loop/heap-crash-identity-audit-v1.json`、`heap-registered-dumps-frozen-v1/receipt.json`。仍未宣称原堆异常根因已解决。

> 后台循环新结果：云端正常优化 Rust **1,363/5 ignored/0 失败**及 Host 编译通过；Native 工具一项 Windows 进程树超时测试失败，原测试本机独立通过，尚待云端启动观察。后续类型及单元未运行，没有取得新 Host 制品，不能宣称终局等待改善。只把公开代码/测试发往云端，本机保持隐藏和低负载。见[读取缓冲记录](./reviews/rust-rp1-v47-read-buffer-2026-09-09.md)。

> 下一循环已有进展：`840ec68d` 低优化的真实 v47 模块 12/12 通过，公开样本底层读取从 524,708 次降至最多 9 次，完整解析及校验一致；不代表玩家等待改善。新增同仓库 Windows 云端验证，接续正常优化构建和完整回归，避免继续挤占用户桌面内存。见[读取缓冲记录](./reviews/rust-rp1-v47-read-buffer-2026-09-09.md)。

> 当前下一阶段为 v47 分块读取待验候选：约 512 KiB 的公开测试已在旧代码复现 52 万多次底层读取，现加入缓冲及错误兼容测试。改后编译因系统可用内存不足 1 GiB 主动停止，保留日志；尚不能称修复通过或终局超时解决。后续在后台且资源允许时继续完整验证，见[开发记录](./reviews/rust-rp1-v47-read-buffer-2026-09-09.md)。

> 2026-09-09 后台策略已实际验收：`33d96597` 包三组流程均通过，12 次启动从未显示/聚焦、保持静音，全部正常关闭；全状态、保存、取消保档及重开通过。78 件目录文件冻结读回一致。后续继续在后台排查终局运行态来源请求 300 秒超时，原玩家文件不变；先验证读取缓冲，不提高超时或放开资格。见[后台记录](./reviews/rust-rp1-background-testing-2026-09-09.md)。

> 2026-09-09 下一循环先落实用户的后台要求：新增隔离 profile 后台策略，窗口/对话框/音频专项与完整 Native 工具 674/1 条件跳过均通过；新包实际隐藏行为待验。完成后继续真实终局完整等待，未改变目标或放开 30 秒资格。见[后台验证记录](./reviews/rust-rp1-background-testing-2026-09-09.md)。

> 2026-09-09 第三批已完成：实现提交 `ee2ea715`，生成清单提交 `dfe0e980`；完整单元 3,159/39、Native 工具 668/1、Chromium 455/33 均零失败。新 beta 实包完成/取消/自然保存来源三例均通过，12 次正常退出 0，77 件制品/80 件冻结文件读回一致。无新增终局完整等待收益，未发布。**用户明确要求后续所有测试后台运行，不得显示游戏窗口、切换前台或抢焦点；旧可见桌面驱动不得再次直接运行，需先实现后台隔离方式。** 见[本批小白版完整报告](./RUST_BATCH_REPORT_2026-09-09_ENTRY.md)与[技术证据](./reviews/rust-rp1-runtime-source-entry-2026-09-09.md)。

> 最新循环（2026-09-09）：公开实包自然保存探针确认检查点来源缺口，已完成不提交 Native 档的临时运行态候选 Host。九组跨语言完整状态一致，完整 Host 252/0、Native 工具 654/1 条件跳过/0；未接新的 UI 传输或发布。首次测试编译、空锁读取、信封夹具及客户端重启失败全部保留，修正测试后通过，不调整产品校验或退出规则。详见[阶段证据](./reviews/rust-rp1-runtime-source-2026-09-09.md)。

Role: develop。开始日期：2026-09-08。用户要求设定目标并持续循环开发；目标已在任务中建立，不含生产部署。

## 目标与循环

先解决或以可复现证据隔离已有验证异常，再测量代表性大工厂的离线拒绝原因与成本，改进有界证明；最终完成完整状态一致、取消不损档、持久提交与重开一致的实际入口闭环，并取得完整离线等待的可复核收益。每轮按定位、最小实现、必要验证、文档与提交继续推进。

保持 GameState v47、envelope v2、玩家主档及原工作区内容；任何未证明的长离线、活动写入者或状态漂移继续拒绝。局部内核测量不替代完整等待，测试 Host 不替代玩家安装包资格。前一阶段见 [进度报告](./RUST_PROGRESS_REPORT_2026-09-08.md)及[历史重建记录](./reviews/rust-rp1-history-append-2026-09-08.md)。

## 循环 1：验证异常隔离

- 起点 `35792aa6`，运行实现 `5c619817`；在 `codex/rust-rp1-after-1.2.7` 继续，原工作区保持不动。
- Host 目录联接失败提取为仅依赖 `tempfile` 和标准库的独立程序，不包含游戏代码。首次约 31 秒返回 Access is denied；原样程序再次运行约 30 秒后成功。未改安全配置、目录保护或测试断言，原因不能仅凭时间归因。
- 在同一旧 Host 测试二进制上复验原来失败的两项，**2/2 通过，0.05 秒**；已实际覆盖预置目录重定向及每次使用时替换目录的拒绝保护。前一轮失败日志保留，成功不覆盖失败事实。
- 核心堆异常增加接手 main `9f4ac5c3` 的独立源码对照。先归档候选的 debug EXE/PDB 及 SHA，再复用构建缓存编译基线；候选发布测试 Host 与其摘要不变。四线程完整基线 **1,096 通过 / 3 ignored**，216.90 秒。此前候选串行及安静四线程也通过；仍不能宣布堆异常已定位。
- release 单元测试中既有的“未标脏写入者”断言失败由编译条件导致：原注释要求 debug/test 审计，实际仅 `debug_assertions`。改为 `any(debug_assertions, test)`，保留 release 单元测试中的完整审计断言，生产 release 仍不编译该审计。修复后的原失败专项 **1/1**；完整 release workspace **1,346 通过 / 4 ignored / 0 失败**（Core 1,099、Host library 244、Host binary 3），fmt 通过。

## 循环 2：大厂诊断及物理证明复制开销

新增显式 opt-in `nativeOfflineScaleProbe.test.ts`，默认不分配临时 profile。用同一冻结 Host `1ca71bd401223071b583da0d0d55d4c87579b674c9ec8dfe7814366666a4d266` 对公开产线做 600 秒诊断：

| 产线数 | 实体 / 带 | 结果 | 完整状态 / 源输入 |
| --- | --- | --- | --- |
| 1 | 110 / 2 | 宏成功，570 秒尾段 | 与 JS 逐秒结果相同，源不变 |
| 128 | 491 / 256 | 宏成功，570 秒尾段 | 与 JS 逐秒结果相同，源不变 |
| 512 | 1,643 / 1,024 | 证明预算拒绝 | 原修订号及状态不变 |
| 2,048 | 6,251 / 4,096 | 证明预算拒绝 | 原修订号及状态不变 |

复验 **4/4**，类型检查通过。最大组 raw 7,623,969 B、估算运行 19,880,255 B，仍在旧内存预算内；总记录步超过上限。当前耗时是在另一构建运行期间取得，只用于分段诊断，不作独立性能收益。首轮驱动误把 600 秒 / 1 秒墙钟的一次调用当作逐秒 JS 参考，造成两个对照失败；修正为已有资格套件的 600 次 1/1 秒推进后全通过，首轮报告保留。

已实现下一项源码优化：物理签名按原 canonical JSON 顺序逐条解析、归一化和写入 SHA；不再先 `materialize()` 全部状态再删除历史和已单独证明的时钟字段。保留所有未知字段和原矿量/计数归一化，只临时持有一条实体/带、有限矿覆盖值及必要基字段。使用原始记录条数，避免尚未重建索引时遗漏原始行；旧实现留作测试参考。当前预算和资格门槛不变。

物理证明模块 **11 通过 / 1 性能 ignored**：有限/无限矿在每个真实 Exact 秒与旧整份展开参考的 canonical SHA 一致；扩展字段、Unicode、浮点表示、未索引原始带及错误计数拒绝均覆盖。首轮新增有限矿夹具错误使用小数余数，改为实际的 0–9 整数单位后复验通过；原失败保留。签名性能项后来单独两次通过，30 对输出相同；0/256/2,048 附加静态行中位缩短 62.5%/32.4%/26.0%。五对独立 Host 的 600 秒 RPC 中，1/128 条公开产线中位缩短 14.1%/4.9%，后者一对更慢且范围有重叠。新 Host 完整 release Rust **1,348/5 ignored/0 失败**、公共目录 **14/2 性能跳过**和严格 Clippy 通过。测量不包含完整玩家等待；[阶段证据](./reviews/rust-rp1-streamed-physical-proof-2026-09-08.md)记录源码、制品和全部范围。

本轮证据保存在本工作区 ignored 的 `artifacts/rust-rp1-loop/`，不包含公开玩家存档或生产数据。

## 循环 3：真实终局档冷开准入复用

循环 3 的显式冷开阶段诊断已提交 `261a140a`。干净源码 Host SHA `eef906b31b433f1d8848f0209d1fae476ae95c0cbb1eb4790ea6786e350e7a39` 的两次独立真实档开档完整校验通过，全部 16 个阶段标记齐全、源不变、内存时间线未截断。开档 9.178 / 10.163 秒，其中准入 4.493 / 5.260 秒；两次采样峰值 2,333,229,056 / 2,334,928,896 B 均落在准入阶段。系统时间仅用于采样关联，短于采样间隔的阶段不推断其真实峰值；这些带诊断耗时不是收益资格。

源码确认本地和星际物流准入各自重新解析完整实体图，与开档持有的原解析结果重叠。修改让已有解析结果的调用借用同一只读切片，保留没有现成解析结果时的入口、原始遍历次序、全部路线/归属/中转校验和资格限制。两项定向测试通过，覆盖 13 个正常/无效路线场景以及拓扑长度拒绝、源实体字节、状态哈希与修订不变；新 Host 测量及完整矩阵继续进行。初版测试误以为物流专用选择器会收集未知建筑，原入口返回 None；改用它实际负责拒绝的 elevator 运行模式复验通过，没有改变规则或断言。证据在 `artifacts/rust-rp1-loop/player-open-profile-{1,2}-summary.json`，只包含固定阶段、计数与哈希。

仅改物流的 `8e5297d2` 首轮真实档完整状态仍一致，但采样峰值仍 2.315 GB，且采样 p95 108 ms 未通过原 100 ms 节奏门槛；该次保持失败，不放宽阈值。继续定位发现戴森准入还有一份完整实体图，现也改为复用。三项定向测试 **3/3**，涵盖 17 个场景，包含发射目标、星系目录、路线归属及拓扑长度保护；新候选收益和完整验证尚待测量，不能把去掉两次重复解析当作已解决峰值。

`061d6e3e` 补齐三处后，完整 debug 核心 **1,104 通过 / 5 ignored**，369.73 秒（与 release 构建并行，非性能样本）。一次独立有效诊断峰值降至 1,364,688,896 B，准入约 1.948 秒、完整状态相同；仍需独立配对及新 release/Host 矩阵。首轮 A/B 的旧 Host 样本再次因采样 p95 109 ms 失败，整个配对未完成；修复采样器后另开批次，不混用失败读数。

堆异常取证取得新进展：本机仍保留原 18:46 崩溃转储，SHA `b208e79c88d87f0dde2ecb586415a5f956e592e75f4053aa6a910d478f303fae`，与归档候选 EXE 的 CodeView GUID/age 相符。使用 Windows SDK 结构和 DbgHelp `StackWalk64`、转储内存与匹配的 PE 展开信息恢复 36 层调用栈：`active_long_research_keeps_4164_rows_sparse_and_matches_force_full_at_1_5_60 → run_research_boundary_advance → prepare_advance_with_runtime_options → Vec<Value>/JSON Map/String 析构 → 堆错误`。这确认发现错误的位置，没有证明破坏发生的位置或根因。只保存异常、模块和代码符号；原转储只读，未导出内存文本。证据见 `heap-minidump-unwound-stack.json`，前面的栈地址扫描仅用于交叉核对，不冒充正式展开。

采样器修复 `b3f3465d` 使用查询句柄读取 `PrivateUsage`，原节奏门槛不变。新批次固定该驱动、关闭阶段日志，三对 AB/BA/AB 独立进程均完整状态一致、原档不变：`coreOpen` 中位 14.032→9.281 秒，Host 峰值私有提交 2,335,293,440→1,370,873,856 B；开档后内存基本持平。六次采样 p95 均 63 ms、max 63–72 ms，全部有效；无同时运行的本任务编译或其他测试。这仅证明原生开档子步骤收益，未包括完整用户等待。样本范围、异常值和制品哈希见[冷开阶段记录](./reviews/rust-rp1-cold-open-admission-2026-09-08.md)。

当前运行 `061d6e3e` 的完整 release workspace **1,351/5 ignored/0 失败**，严格 Clippy 通过；Host 差分 **50/1 条件跳过**、释放生命周期 **1/1**、私有终局档一秒 Exact **1/1** 通过。公共目录长离线首轮 **12 通过 / 2 超时失败 / 2 性能跳过**：有限矿和容量 8 小时 Exact 回退用例均超过原 360 秒测试门槛，当时有并行编译/检查。保留失败，正在独立进程逐项复验，不提前归因为并发负载。旧堆异常对应的原归档二进制用例独立重跑三次均通过，仍未证明根因。

## 循环 4：精确长回退的累积写入记录

冷开候选的有限矿 8 小时独立复验仍触及 300 秒 Host 请求超时，约 323.5 秒完成关闭；冻结旧 Host 同场景也触及该请求门槛，约 331.0 秒完成清理。原 360 秒测试上限未修改，仍缺两项完整长回退验收；不能归因于新冷开改动或并行负载，旧证据中更快的记录也不删除。

源码另确认一个与输入规模和时长相关的累积开销：每秒 `merge` 把同一建筑的重复写入行继续追加，历史边界又 `clone().seal()` 整份累积记录；原收集器的重复行直到结算末尾才去重。新增 `snapshot()` 在生成相同的有序唯一写入清单时，同时压缩候选本地收集器。保留各领域标记、拓扑变化、修订、实体数及累计 `submitted_rows`，不改变真实模拟步数、历史采样或预算。新测试把连续 512 步逐次与原先完整重复行路径对照，另覆盖空集合、仅全局事件和拒绝不同来源的合并。该源文件独立 Rust 测试 **5/5** 通过；完整 workspace 集成与新 Host 验收待运行，尚无新收益结论。

### 验证结果

`e5ca4b2e` 写入快照收口：完整 release Rust **1,353 通过 / 5 ignored / 0 失败**（Core 1,106、Host 244+3），严格 Clippy 通过；新 Host SHA `e288c7bb689c4aede0cd78bb570060e9db6bb6ff78fae60f2fb28ed9f4862368`。公共目录仍 **12 通过 / 2 Host 请求超时 / 2 性能跳过**，有限矿与容量的 8 小时测试分别约 334.1 / 326.1 秒结束。新旧 600 秒诊断基本持平，暂不声称该快照改动已产生整体计时收益。原失败全部保留。

## 循环 5：精确步进中的整份基础状态复制

在冻结 `e5ca4b2e` Host 上，用公开有限矿夹具做 600 / 3,600 秒显式阶段诊断；完整状态与 JS 相同、源不变、采集未截断。3,600 秒带诊断的计算约 30.479 秒，其中 `time-warp-and-dyson-environment` 11.520 秒、`logistics-dispatch-and-routes` 12.057 秒。后一个旧标签实际只包围 `dyson::finalize()`，不能误认为本地物流慢；这是带诊断的热点定位，不是独立收益测量。

源码确认 `dyson::advance_environment()` 和 `finalize()` 每次都 `base.clone()`，把无关生产历史、库存和全局记录一起复制。`load()` 已经独立持有四份戴森记录，`save()` 前基础状态始终只读；改为直接借用基础状态，仍按原顺序吸收、衰减、汇总并最终写入四份记录。新增活动/空闲、0/小数/1/5/60/1,200 秒、阶段间科研变化、未知字段/负零及缺失戴森记录的原路径对照和失败原子性测试。完整验证与新计时尚待完成。

阶段诊断在 `artifacts/rust-rp1-loop/public-exact-profile-finite-reserve-{600,3600}/result.json`。诊断驱动第一版相对模块路径错误，在启动 Host 前失败；修正两级父目录后重新运行通过，原错误日志保留。

新增活动夹具首次漏了 `absorptionProgressBySystem`，原复制路径在吸收时也返回字段缺失；专项首轮 4 通过 / 1 夹具失败。按真实戴森形状补齐逐星系吸收余数，并要求活动场景实际产生帆过期，运行规则不变；原失败日志保留。

补齐后 `borrowed_` release 专项 **5/5** 通过（新增环境/最终汇总全字节对照和缺失记录原子性 2 项，原三处准入 3 项），fmt 与文档检查通过。新冻结 Host 的完整长回退与配对计时继续进行，未修改预算或玩家资格。

### 循环 5 收口

运行提交 `b39131a3`、冻结 Host SHA `a3b1bf2283f5b66f2be9d156a8b9a57509fb8aff558150e2fb9498a2cbf09ba4`：完整 release Rust **1,355/5 ignored/0 失败**，严格 Clippy 通过，公共目录 **14/2 性能跳过/0 失败**，新 Host 差分 **50/1 条件跳过/0 失败**。有限矿与量子容量 8 小时均为真实 Exact 回退，请求约 25.365/25.828 秒，完整状态相同；旧失败保留，原门槛未修改。

三对 AB/BA/AB 独立进程，每个进程三次预热后的公开 3,600 秒 Exact 请求，新旧进程中位的中位 **32.496→3.193 秒（-90.17%）**，所有完整输入、目录、输出 SHA 相同，源不变；无同时运行的本任务编译或其他测试。仅证明该公开工厂请求计时，未覆盖真实终局玩家完整等待。详见[阶段记录](./reviews/rust-rp1-exact-dyson-base-2026-09-08.md)。

## 循环 6：真实档多秒精确诊断

真实档基准增加显式 `DSP_NATIVE_CORE_BENCHMARK_EXACT_SECONDS=1..30`，默认仍为原一秒。超过一秒只允许独立 Exact-only 模式，与原固定亲和性/阶段剖析的一秒合同互斥。JS 参考遵循已有 Core 差分规则：对齐历史时钟逐秒完成公共边界，未对齐保留原批次采样，不重写玩家时钟。完整字段和 canonical SHA、物资守恒、修订号仍验证，报告明确模拟秒数、墙钟秒数及参考边界。

驱动合同 **7 通过 / 1 私有基准 opt-in 跳过**，类型检查通过；真实 5 秒测量尚待运行。仅扩展开发诊断，不修改 Core、普通包或存档格式。

驱动提交 `0cc971d8`、运行 Host `b39131a3` 的真实终局档 **5 秒诊断通过**：完整字段/canonical SHA 相同、修订一致、物资守恒通过，授权原文件 SHA/字节/mtime 不变。该档时钟对齐，JS 参考为五次真实 1/1 秒公共推进；没有归一化时钟。单次原生请求约 10.688 秒、JS 精确引擎 30.310 秒（加守恒扫描 33.453 秒），开档约 8.926 秒；未做该时长的独立配对，不计完整用户等待资格。隐私白名单报告位于 `artifacts/rust-rp1-loop/private-multisecond-5/result.json`，不保留原始 console 差异或玩家负载。

干净 `0cc971d8` 的 beta 性能开发目录包构建通过（类型、启动预算、薄 UI 边界、覆盖清单和包制品校验），Build ID `1.2.7+0cc971d8b451`。仅本地隔离包，尚未通过本轮实际入口与发行矩阵；没有生成公开更新或部署。实际桌面入口验证继续进行。

## 循环 7：继续游戏的恢复结果与主档目录绑定

`0cc971d8` 隔离桌面包的两次真实“继续游戏”均停在“本地存档不可用”，未进入 `coreOpen` 或计算。直接核对冻结包中的主档、原生恢复和 Worker inspection：两份正文均有效、均为 93,034 B，但 payload checksum 分别为 `aea2e7e7` 与 `92d97129`；目录绑定前者。`savePreviewPayload` 却拿恢复后的正文比较原主档目录，因此合法恢复也被拒绝。原失败、界面提示和 RPC 记录保留于 `packaged-offline-complete{,-v2}/`；第二次诊断还保留原始选择器超时和退出清理未完成的事实，不当作通过。

源码修复保留读取时原主档的字符串引用：目录校验原主档，恢复结果独立经 Worker 校验，并保持 journal 的主档 checksum 绑定和原主档→备份→快照顺序。恢复后才发现主档/目录变化仍拒绝，不绕过完整性检查、不写源档或更改版本。新增 6 项回归覆盖同档重组、增量推进、字节数/指纹不匹配、损坏日志、错误模式及检查期间主档替换；相关专项 **29/29**、全量单元 **3,145 通过 / 39 跳过 / 0 失败**，最终类型检查通过。

新增两个真实浏览器继续/写档回归；完整 E2E 首次在收集阶段因独立工作区缺少 server 的 `better-sqlite3` 退出，未执行用例。按现有锁文件安装本工作区 server 依赖后，另开批次复验。新包入口、取消和重开仍待运行，Rust 运行制品仍为 `b39131a3`。

## 后续工作队列

2026-09-09 批次收口：`f9feb88a` 修复 main 旧身份变量，`5274c6e1` 接受严格有界的短精确 DTO 及正常拒绝。最终公开匹配检查点的完成/取消/持久提交/正常重开两例通过，完整 Native 工具 632/1 条件跳过；没有取得真实终局完整等待资格。驱动原文/运行态、autosave、revision/catalog、弹窗及加载零值清理的差异均逐次记录。详见[批次报告](./RUST_BATCH_REPORT_2026-09-09.md)与[技术记录](./reviews/rust-rp1-menu-recovery-2026-09-08.md)。

循环 7 复验收口：完整单元原失败文件独立 137/137；完整浏览器原 105 项失败逐轮复验均已通过。完整原批次为 350/33 跳过/105 失败，不能改记全绿。最后菜单独立当前 p95 277 ms、冻结旧模块 258 ms，均满足原 500 ms；没有性能改动或放宽时限。星球重置测试现在先确认真实离线报告，再走原三次确认。下一步本地新包验证，详见[完整复验证据](./reviews/rust-rp1-menu-recovery-2026-09-08.md)。

循环 7 后续验证又复现两处边界：旧 journal 在状态 checksum 不变时可回退较新主档时间；恢复结果被缓存为保存比较基准则误报跨标签页冲突。两项均保留修复前失败，现已分别加时间先后检查、保留磁盘原文作为保存基准。当前专项 **35/35**、实际菜单/开发直达恢复后界面保存与完整状态重开及保存协调 **22/22**、类型检查通过。开发直达入口仍按原同步离线和回归奖励语义加载。完整单元/浏览器正在补验，不沿用修复前全量通过；旧完整包 78 件逐文件相同归档，新包入口待跑。完整失败顺序和证据见[恢复入口阶段记录](./reviews/rust-rp1-menu-recovery-2026-09-08.md)。

1. 完成目录绑定修复的浏览器与实际包验证，再验证真实大档完整入口；5 秒内核诊断已通过，尚不能代表完整玩家等待。
2. 根据公开四规模和真实档证据设计有界证明，验证边界、漂移、容量、耗尽和完整状态；不直接提高记录或内存预算。
3. 接完整用户入口、取消、持久提交及重开。同档独立 A/B 测量完整离线等待，记录平台、制品身份、样本及完整输出。
4. 达到目标后报告可发布范围；原堆异常、设备矩阵和完整安装包缺口独立保留，不用局部通过替代发行资格。
